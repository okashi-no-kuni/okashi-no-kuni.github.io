# このリポジトリについて

パステルカラーのかわいいタワーディフェンスゲーム。単一のHTMLファイル（`index.html`）で完結しています。

以下は、このシリーズのゲームを作るときの方針です。新しいゲームを追加・改修するときも、この方針に沿ってください。

## 作るもの

女の子向けの見た目をした、2Dのカジュアルゲーム。個人のSNSアカウントで「自分が作ったゲーム」として紹介・拡散することを前提にしています。

ジャンルは女性向けに寄せる必要はありません。タワーディフェンス、シューティング、パズル、放置ゲームなど、定番のジャンルをそのまま使い、**見た目と手ざわりだけを徹底的にかわいく**します。

## アートディレクション

見た目はこのシリーズ共通です。ここを崩さないでください。

- **配色** — パステル。ピンク `#ff8fc4` を主役に、ラベンダー `#c9a7ff`、ミント `#8fe3c4`、クリーム `#fff6e9` を組み合わせる。彩度を上げすぎず、白寄りのグラデーションで柔らかくする
- **背景** — 上から下へのグラデーション（ピンク → 藤色 → 水色）。`radial-gradient` を重ねて奥行きを出す
- **形** — すべて角丸。直角を作らない。ボタンの `border-radius` は 14〜20px
- **キャラクター** — 円をベースに、必ず**目・ほっぺ・口**をつける。目にはハイライトを入れる。ほっぺはピンクの半透明の円
- **動き** — 常に何かが動いている状態にする。待機中もふわふわ上下に揺らす（`Math.sin(time)`）。着地や被弾では伸縮（squash & stretch）させる
- **キラキラ** — 4方向にとがった星形のパーティクルを多用する。背景にもゆっくり流れる粒を置く
- **影** — 黒ではなく紫がかった半透明（`rgba(120,90,120,.18)`）を使う
- **文字** — 丸ゴシック優先（`Hiragino Maru Gothic ProN`）。UIの日本語はひらがなを多めにして、やさしい語感にする（例：「攻撃力」→「こうげき」、「購入」→「かう」）

## 技術方針

- **画像・音声ファイルを使ってよい**。BGMはmp3、絵も必要に応じて画像素材を使う
  - 素材は**商用利用可・再配布可**のものだけを使う。GitHub Pages に置く＝誰でもダウンロードできる状態になるため、規約を必ず確認する
  - クレジット表記が必要な素材を使ったときは、ゲーム内のガイド画面に表記欄を作る
  - iPhoneで確実に鳴らすため、音声は**mp3**にする（Ogg VorbisはSafari 18.4より前で再生できない）
- **絵はコードで描く**。Canvas 2D に直接えがき、丸にラジアルグラデを重ねて つやと立体感を出す
  - Phaser のようなゲームエンジンは入れない。`Graphics` にグラデーションも合成モードもなく、
    このアートが再現できないので、ループと入力しか担当できず、1.1MB ぶんの読みこみが むだになる
  - 画像素材でキャラを作る方針に変えるなら、そのときに エンジンを入れなおすか あらためて考える
- **スマホの縦持ちが基準**。`devicePixelRatio` に対応し、画面サイズに応じてマス目やUIの大きさを自動計算する
- **タッチとマウスの両方**で同じように動くようにする
- 音は必ず**オン・オフできるボタン**を用意する

## 量産のための構造

新しいゲームも同じ骨格で作ると、調整と使い回しが速くなります。

1. ファイル上部に**設定オブジェクトを集約**する（`TOWERS`、`ENEMIES`、`waveList()` のように、バランス調整用の数値を1か所にまとめる）
2. その下に状態、入力、更新、描画の順で並べる
3. 描画関数はパーツごとに小さく分ける（`drawTower`、`drawEnemy` など）
4. HUDはDOM、ゲーム画面はCanvasに分担させる。文字はDOMの方が読みやすく、調整も速い

### うごかないものは 1枚に焼く

じめんと道のように 毎フレーム おなじ絵になるものは、オフスクリーンの
canvas に 1回だけ描いて、あとは貼るだけにします（`bakeGround()`）。
このゲームでは 道のトッピングだけで110個あり、毎フレーム描きなおしていました。

`withCtx()` で `ctx` を差しかえるので、焼くときも 描画関数は
そのまま つかえます。焼きなおすのは ステージ・きせかえ・画面サイズが
かわったときだけです。

## SNSで見せることを意識する

- **最初の3秒**で「かわいい」と伝わる画面にする。起動直後から色とキラキラが見えている状態にする
- 動画やGIFで撮ったときに**動きが分かる**ようにする。エフェクトは大きめ、長めに
- 1プレイが短く、何度も繰り返せるようにする
- 数字が増える瞬間（スコア、報酬）は必ず**浮かび上がる文字**で見せる

## 検証

変更したら Playwright（Chromium）で実際に操作して確認してください。ブラウザは `/opt/pw-browsers` に入っています。

- JavaScriptエラーが0件であること
- ゲームオーバー・クリア・リトライまで一通り到達できること
- スクリーンショットを撮って、見た目が崩れていないこと

### キャラクターの検査は毎回かける（省略しない）

キャラを足したとき、形をいじったとき、描画の共通部分をさわったときは、
報告する前に かならず これを 走らせます。**目視だけで済ませないこと。**
コレクションの200体と、アバターの組み合わせ見本の両方を しらべます。

```
node tools/check-chars.mjs           # 終了コード 0 で合格
```

しらべるのは この6つです。

| | 見つかるもの |
|---|---|
| JSエラー | 描画中の例外 |
| はみ出し | つの・みみ・あし・しっぽが わくで切れている |
| 小さすぎ | わくの中で ぽつんと 見える |
| 中心ずれ | ならべたとき ひとりだけ かたよる |
| 名前かぶり | 同じ名前が2体いる |
| IDかぶり | セーブデータが こわれる |

目視は こういうものを 通してしまいます。実際、200体を目で見て「直った」と
報告したあとに この検査をかけたら、頭やあしが切れている子が **10体** 見つかりました。

**直しかた**：からだの形を いじると、おなじ型（`quad`・`fowl` など）をつかう
別の子まで くずれます。そうではなく、1体ごとの ずらし `dx` / `dy` と
大きさ `sc` で わくに 合わせてください。この3つは 描画のいちばん外がわで
かかるので、ほかの子には ひびきません。

## あつめかたと、いずれの課金について

コレクションの200体は、こうやって 集めます。

| どこで | なにが |
|---|---|
| てきを たおす | その子が ずかんに入り、**なかまとしても つかえる**（48体） |
| ステージクリア | たまごが 1こ。ステージが すすむほど いい たまご |
| ⭐で たまごを 買う | ★べつに 4しゅるい |
| ともだちバトルに かつ | かけら 1こ |

**なかまは「ずかんに いる子」しか おけません。** これが ないと コレクションが
かざりに なってしまいます。はじめての人には ★1を 5人 わたします。

### 課金を 入れるときの きまりごと

いずれ App Store に 出すつもりなので、次を まもってください。

- **買いものは `buyEgg()` の 1か所だけ**。ここの「⭐をはらう」を
  「💎をはらう」に さしかえれば 課金に なる形に してあります。
  買いものを あちこちに ちらすと、あとで 直せなくなります
- **Apple では ゲーム内の デジタル商品は かならず IAP**。
  外部決済（Stripe など）は つかえません
- **ガチャの かくりつ表示は 必須**（Apple 3.1.1／日本の 景品表示法）。
  だから たまごは「おなじ★の子から 均等に 1人」に して、
  「どの子も おなじ かくりつ」と 1行で 言いきれる形に してあります。
  出しかたを 変えるときは、かくりつ表示も かならず 直すこと
- **「◯体そろえたら 特典」は 作らない**。日本では コンプガチャに あたります。
  ずかんの「かんせい」は お祝いの 画面だけで、もらえる ものは ありません
- localStorage は 書きかえ放題なので、**買った ものを そこだけに 置かない**。
  IAP を 入れるときは レシートで もういちど たしかめること

## 通信について

サーバーは ありません。GitHub Pages に 1まいの HTML を置くだけです。
だから **リアルタイムの通信対戦は作れません**。

ともだちと あそぶ しくみが ほしいときは、データを みじかい「あいことば」に
して SNS で おくり、うけとった側の ゲームに とうじょうさせる かたちにします。
アバターは `av.<8もじ>.<なまえ>` の形で、`?f=` つきの リンクでも うけとれます。

## ウェーブに 終わりは ない

ウェーブは無限に続きます。**200ウェーブごとに道そのものが変わり**、
景色の順番も1つずれるので、同じ絵の繰り返しになりません。

道は手で用意しません。無限に続く以上、形も無限に要るからです。
**周番号を種にした乱数でその場で作ります**（`makeWay`）。同じ周なら
いつでも同じ形が出るので、セーブともズレません。1周目だけは手で作った形
（`FIRST_WAY`）にしてあります。はじめての人が見る画面なので。

どの形も次の範囲に収まるまで種をずらして作り直します。**道の形は見た目
だけでなく難易度そのもの**だからです。

| | 範囲 | 外れるとどうなるか |
|---|---|---|
| 長さ | 28〜36マス | 短い＝敵がすぐゴールに着く |
| 置けるマス | 72〜82 | 少ない＝仲間を並べられない |

200周（4万ウェーブ）ぶんで76種類。`tools/sim.mjs` がこの範囲を検査します。

どこかで必ず追いつかれる「かべ」が来て、そこがその日の記録になります。
かべの場所は手で決めず、**モデルで測って**決めてあります。

```
node tools/sim.mjs           # 終了コード 0 で合格（かべが 250〜550）
```

いま効いている数字は3つだけです。

| | 内容 |
|---|---|
| `hpScale` | ウェーブ25をこえたら 1.020 の複利 |
| `goldWave` | ウェーブ30をこえたら 1.004 の複利（HPよりゆっくり＝じわじわ追いつかれる） |
| `upMul` | 強化費は1レベルごと ×1.55。攻撃は ×1.78 なので、育てるほど⭐あたりが良くなる |

**この3つのどれかをいじったら、必ず `tools/sim.mjs` をかけ直すこと。**
レベル上限をなくす前は、ウェーブ60あたりで強化先がなくなって頭打ちでした。

道は曲がり角をつないだ折れ線を太くなぞって描いているので、**斜めの道も引けます**。
置けないマスは、マスをたどるのではなく**中心から折れ線までの距離**で決めます
（`buildPath`）。マスをたどる方式だと、斜めの道の下なのに置けるマスができます。

道を切りかえるのは**敵が1体もいないウェーブの境目だけ**です（`applyLayout`）。
途中で変えると道の長さが変わり、まだ途中の敵が一斉にゴールに入ります。
新しい道の上に乗ってしまった仲間は、こちらの都合なので**全額返します**。

⭐も攻撃力も何十万・何億になるので、表示は `fmtN()` で万・億に丸めます。

## 日本語について

コード内のコメントは日本語で。「何をしているか」ではなく「なぜそうしているか」を書きます。

UIの日本語は**出す場所で書き分けます**。全部ひらがなだと大人には読みにくく、
幼児向けに見えてしまうためです（このゲームは子どもから大人までが対象）。

| どこ | どう書くか | 例 |
|---|---|---|
| メニュー・ボタン・数字・ガイド・ショップ | **漢字を使う** | 「攻撃」「購入」「所持数」「強化」 |
| キャラのセリフ・かけ声・お祝い | **ひらがな多め**（かわいさの核） | 「やったね！」「なかまに なったよ」 |
| キャラ名・図鑑の名前 | これまでどおり | 「ほしのクジラ」「ぷりんぼう」 |
