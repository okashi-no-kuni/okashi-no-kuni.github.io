# このリポジトリについて

パステルカラーのかわいいタワーディフェンスゲーム。単一のHTMLファイル（`index.html`）で完結しています。

以下は、このシリーズのゲームを作るときの方針です。新しいゲームを追加・改修するときも、この方針に沿ってください。

## 作るもの

女の子向けの見た目をした、2Dのカジュアルゲーム。個人のSNSアカウントで「自分が作ったゲーム」として紹介・拡散することを前提にしています。

ジャンルは女性向けに寄せる必要はありません。タワーディフェンス、シューティング、パズル、放置ゲームなど、定番のジャンルをそのまま使い、**見た目と手ざわりだけを徹底的にかわいく**します。

## アートディレクション

見た目はこのシリーズ共通です。ここを崩さないでください。

- **配色** — パステル。ピンク `#ff8fc4` を主役に、ラベンダー `#c9a7ff`、ミント `#8fe3c4`、クリーム `#fff6e9` を組み合わせる。彩度を上げすぎず、白寄りのグラデーションで柔らかくする
- **背景** — 上から下へのグラデーション（ピンク → 藤色 → 水色）。`radial-gradient` を重ねて奥行きを出す
- **形** — すべて角丸。直角を作らない。ボタンの `border-radius` は 14〜20px
- **キャラクター** — 円をベースに、必ず**目・ほっぺ・口**をつける。目にはハイライトを入れる。ほっぺはピンクの半透明の円
  - **白っぽい子は細い線でふちどる**（`opt.line` に色）。うすい背景にとけて形が見えなくなるためです。
    いまはパンダ・ウサギ・シマウマ・ハクチョウ・ヒツジ・おばけの6体。
    1つ1つの形に線を引くと**かならずもれる**ので、`drawOutlined()` が体を1回別のcanvasに
    描いて、そのかげを8方向にずらして下に敷きます（ステッカーの縁取り）。どの型でも効きます。
    線の色はその子のこい色にそろえること。まっ黒だとパステルから浮きます。
    36pxまで小さくしてもつぶれないことを確認済み
  - **正面をむいて二本足で立つ型**（`teddy`）もあります。いまはクマ・パンダ・ヒツジ・ウサギ。
    顔がよく見えるので、真正面の子はこの型にします。**口もとの丸は置かないこと**——
    この顔は目と口のあいだがせまく、丸を置くと目までのみこみます。鼻だけ入れます
    - みみは `opt.ear` で `round`（クマ）／`long`（ウサギ）／`droop`（ヒツジ）。
      **もようではなく、みみとシルエットで見わけます**。小さくするともようは消えるためです
    - `opt.wool` を入れると、頭と胴のふちに丸をならべてシルエットをぎざぎざにします（ヒツジ）
    - `opt.horn:'spiral'` のまきづのは**もこもこを描いたあとに重ねます**。
      うしろへまわすと、まきの内がわが毛にかくれてただのかぎに見えます
    - 左右にならぶ曲線を `s2` でうつすときは、**横だけ**うつすこと。
      角度ごと `s2` 倍すると上下がひっくり返って、片がわだけ下むきのつのになります
  - **ながれるしっぽ（おばけ）は胴とひとつづきの線で描く**。左右対称のすそにすると
    「ふきだし」に見えるので、しっぽは右がわだけに流します。
    太い線を別に引いて胴にくっつける作りにすると、ずんぐりして**別のものを貼ったよう**に
    見えました（実際にそうなって戻しました）。輪郭を1本のパスで回して、
    しっぽの太さはうわがわとしたがわの間かくで決めます
  - **横向きの子だけは例外で、目は1つ、口は描かない**（`GSIDE`）。横から見て目が2つ見えることはないため。
    向きは顔の横ずれ（`GFX`）と同じ。目の色は**その場所の色を実際に読んで**決めます（`eyeInkAt`）。
    こい所なら白い目＋こいひとみ。体の色だけで決めると、ワシのように頭だけ白い子をまちがえます。
    読むのは**顔の中心ではなく目そのものの位置**。中心を読むと、クマのように鼻がこい子でまちがえます
- **動き** — 常に何かが動いている状態にする。待機中もふわふわ上下に揺らす（`Math.sin(time)`）。着地や被弾では伸縮（squash & stretch）させる
- **キラキラ** — 4方向にとがった星形のパーティクルを多用する。背景にもゆっくり流れる粒を置く
- **影** — 黒ではなく紫がかった半透明（`rgba(120,90,120,.18)`）を使う
- **文字** — 丸ゴシック優先（`Hiragino Maru Gothic ProN`）。UIの日本語はひらがなを多めにして、やさしい語感にする（例：「攻撃力」→「こうげき」、「購入」→「かう」）

## 技術方針

- **画像・音声ファイルを使ってよい**。BGMはmp3、絵も必要に応じて画像素材を使う
  - 素材は**商用利用可・再配布可**のものだけを使う。GitHub Pages に置く＝誰でもダウンロードできる状態になるため、規約を必ず確認する
  - クレジット表記が必要な素材を使ったときは、ゲーム内のガイド画面に表記欄を作る
  - iPhoneで確実に鳴らすため、音声は**mp3**にする（Ogg VorbisはSafari 18.4より前で再生できない）
- **絵はコードで描く**。Canvas 2D に直接えがき、丸にラジアルグラデを重ねて つやと立体感を出す
  - 絵だけは Phaser にやらせない。`Graphics` にグラデーションも合成モードもなく、
    このアートが再現できないためです。だからレンダラは `Phaser.CANVAS` にしてあります
  - **`Phaser.AUTO`（WebGL）に「1行で」は変えられません。**むかしここには
    「キャラをPNGにしたら `type:` の1行で変えられる」と書いてありましたが、**誤りでした。**
    盤面をえがいているのは `Painter` という自作の入れもので、そこには
    **`renderCanvas` しかありません**。WebGL にすると Phaser は `renderWebGL` を
    呼びに来るので、**画面が真っ白になります**。
    WebGL にするには 盤面の描画（`render` の中の全部）を Phaser のスプライトに
    移す必要があり、それは作り直しです。**いまは 60〜68fps 出ていて困っていないので、
    やらないこと。**

### ゲームの土台は Phaser 3

いずれ App Store に出す前提なので、ループ・入力・画面サイズは Phaser にまかせています。
審査でエンジンの種類は見られませんが、あとから土台を入れかえるほうが高くつくためです。

- **バージョンは 3 系**（いま 3.90.0）。4 系は Canvas レンダラが非推奨で、
  いまの絵は Canvas 2D なので使えません
- **`vendor/phaser.min.js` に同梱する。CDN は使わない。**
  アプリにつつむと端末の中に入る形になりますし、この開発環境からは cdnjs が塞がれています
- 入れるのは **`phaser-arcade-physics.min.js`** のほう。物理エンジンは1つも使っていないので、
  Matter.js を含まないこのビルドで gzip 316KB → **284KB** になります
- `Painter` の **`renderFlags = 0xF` を立てないと Phaser が描画に来ません**
- じめんの焼きこみ（`bakeGround`）は Phaser とは別のオフスクリーン canvas のままです。
  Phaser のテクスチャに入れなおす必要はありません

**Web版は読みこみが遅くなります。**GitHub Pages で測った実測値（gzip あり）。

| | Phaserなし | Phaserあり |
|---|---|---|
| はやい回線 | 0.23秒 | 0.30秒 |
| 低速4G（400kbps） | 3.65秒 | **9.37秒** |

はやい回線での +0.07秒は JavaScript を読みとく時間で、回線を速くしても消えません。
低速4Gの差はダウンロードそのものなので、**アプリにつつめば まるごと消えます**
（1.1MB が端末に入っているため）。Web版だけが払うコストだと分かった上で入れています。
- **スマホの縦持ちが基準**。`devicePixelRatio` に対応し、画面サイズに応じてマス目やUIの大きさを自動計算する
- **タッチとマウスの両方**で同じように動くようにする
- 音は必ず**オン・オフできるボタン**を用意する

## 量産のための構造

新しいゲームも同じ骨格で作ると、調整と使い回しが速くなります。

1. ファイル上部に**設定オブジェクトを集約**する（`TOWERS`、`ENEMIES`、`waveList()` のように、バランス調整用の数値を1か所にまとめる）
2. その下に状態、入力、更新、描画の順で並べる
3. 描画関数はパーツごとに小さく分ける（`drawTower`、`drawEnemy` など）
4. HUDはDOM、ゲーム画面はCanvasに分担させる。文字はDOMの方が読みやすく、調整も速い

### うごかないものは 1枚に焼く

じめんと道のように 毎フレーム おなじ絵になるものは、オフスクリーンの
canvas に 1回だけ描いて、あとは貼るだけにします（`bakeGround()`）。
このゲームでは 道のトッピングだけで110個あり、毎フレーム描きなおしていました。

`withCtx()` で `ctx` を差しかえるので、焼くときも 描画関数は
そのまま つかえます。焼きなおすのは ステージ・きせかえ・画面サイズが
かわったときだけです。

## 表紙（タイトル画面）——**完成済み・原則 変更禁止**

> 表紙（`#titleOv`）と 導入画面（`#weOv`）は **2026-09-01 に 正式採用**
> しました。どちらも 実機で 見て 決めた ものなので、
> **たのまれて いない かぎり 手を 入れないこと。**
> 数字を 1つ 変えるだけでも、実機の 見た目は 変わります。

> **記録（2026-08-30）**：この日、表紙を 3つ 通りました。
> ① コードで えがく 表紙 → ② `title_full.webp` を 1250x2100 に
> アウトペイントした マスター → ③ **いまの「1まいの 絵を 切らずに 見せる」**。
> ①②の 実装（`paintTitle`・`titleBake*`・`titleCastle*`・`titleLogo` など
> 1249行）と 素材・道具は **消しました**。git の 履歴に あります。
> 昔の やり方に 戻さないこと ——理由は この章の いちばん下に 書いてあります。

起動すると まず **表紙**（`#titleOv`）が 出ます。本でいう 表紙で、
「魔法の おかしの国」の 入り口です。「はじめる」で とじると 盤面に なります。

**中みは 1まいの 完成した 絵だけ**です（`art/title/title_main.webp`・1024×1536）。
空・にじ・城・なかま・題字・「はじめる」ボタンまで ぜんぶ 絵に 入っています。

### コードで えがきなおさない

このシリーズは「絵はコードで描く」が きまりですが、**表紙だけは 例外**です。
絵筆の 質感・おくの ぼけ・手がきの 宝石は Canvas 2D の 図形では 出ません。

だから 表紙では **次を ぜったいに しないこと**。

- 城を CSS で 再現する
- にじを Canvas で えがく
- キャラを 別の 素材で 組みたてる
- 題字を HTML の 文字で 再現する
- ボタンを 別デザインに する

いまの 表紙に canvas は **1つも ありません**。rAF も 走りません
（毎フレーム えがく ものが 無いので、いちばん 軽い 画面です）。
JS は `openTitle()` / `closeTitle()` / ボタンの `onclick` の3つだけ。
大きさと いちは ぜんぶ CSS がわです。

### 「はじめる」の つぎは 今日のプレゼント

`closeTitle()` から `afterTitle()` を よび、そこで `checkLogin()` を 出します。

**起動と 同時に 出しては いけません。**むかしは そうでしたが、
紙ふぶきも ファンファーレも **表紙の うしろで 終わって**しまい、
だれも 見られませんでした。音も 手を ふれる 前なので 鳴りません
（WebAudio は タップまで 鳴らせない）。表紙の タップで ふうが とけるので、
そのあとに 出すと 音も 演出も ちゃんと 出ます。

おとし穴が 1つ。**💗の 回復まちの 画面（`checkLife`）が 先に 出ている
ことが あります。**あの画面は `armLifeButton` が ボタンを 1秒ごとに
書きかえるので、止めてから プレゼントを 出し、受け取った あとに
`checkLife()` で もどします。もどさないと「最初からやり直す」も
「続きから」も 押せなく なります。`ovBtn.disabled` を 戻すのも わすれずに。

| 起動のしかた | 「はじめる」の あと |
|---|---|
| **ほんとうに はじめて** | **ようこそ** → ゲームをはじめる → プレゼント → 盤面 |
| つづきが ないが 1回は あそんだ | 今日のプレゼント → 受け取る → 盤面 |
| その日 はじめて ＋ つづきが ある | プレゼント → 受け取る → **おかえりなさい** |
| その日 2回目 ＋ つづきが ある | **おかえりなさい** |
| その日 2回目 ＋ つづきが ない | そのまま 盤面 |
| 💗の 回復まち ＋ プレゼント あり | プレゼント → 受け取る → **おやすみ中の 画面へ もどる** |
| 💗の 回復まち ＋ プレゼント なし | おやすみ中の 画面（時計も 動いたまま） |

### ようこそ（`#weOv`）——はじめての人だけ・**完成済み・原則 変更禁止**

> **記録（2026-09-01）**：正式採用しました。**ここから 先は さわらないこと。**
> 直したく なったら、**まず 相談してから**。
> 固定した もの：36:64 の あまりの 配分／`max-height:660px` の 44:56／
> 上の 絵（`welcome_castle.webp`）／題・説明・📕の 文／カード／ボタン／
> 区切りの かざり／配色／`$('weGo').onclick` の 中み。

「はじめる」の すぐ あと、**今日のプレゼントより 先**に 出します。
まだ 何も 持っていない人に いきなり プレゼントを 見せても、
何の ゲームか 分からないまま ⭐が ふえるだけに なるためです。

中みは **上の 絵・カード・題・説明・区切りの かざり・ボタン**の
6つの 部品です。canvas も rAF も 走りません
（「おかえりなさい」とは ここが ちがいます）。

#### 画面は 1まいの 絵に しないこと

**参考の 見本を そのまま 1まいの 絵として 貼っては いけません。**
表紙だけが 例外で、ここは ちがいます ——表紙は 文字も 絵の 中に あるので、
切らずに 全部 見せて あまりを ぼかした 同じ絵で うめれば すみます。
この画面は **文字が 主役**なので、1まいに すると 端末の たてよこ比の
ちがいが そのまま **文字が 切れるか、絵が つぶれるか**に なります。

だから 部品に 分けて CSS で 組みます。ならびは 上から
**絵 → 題 → 説明 → 区切り → 📕 → ボタン**。

- 題・説明・📕の 文は **かならず HTML の 文字**。絵に 焼きこまない
- 区切りの かざりは CSS だけ（細い 金の線＋小さな ひし形）。
  **Canvas も 画像も ライブラリも 足さない**
- 色は ピンク 一色に しない。上＝淡い スカイブルー、中＝ブルー〜ラベンダー、
  下＝ラベンダー〜パールピンク。ここは 子どもにも 大人にも、が ねらいです

#### あまった たては 上下に 36:64 で 分ける

まん中（50:50）に すると、たて長の 端末で **上の 空きが 大きく 見えて
カードが 沈んで 見えます**（実機の 報告）。表紙の `--titleUp:.40` と
まったく 同じ話です。いまは 上が 36%（`--weUp`）。

やりかたは **flex の のびしろを 持った からの 箱**を 上下に 置くこと
（`#weOv` の `::before` / `::after`）。

- **`margin:auto` では できません。**上下が かならず 半分ずつに なります
- **`margin-top` に 割合を 書くのも だめ**です ——`%` は margin では
  「高さ」では なく **はば**を 指します（表紙で 一度 やった 失敗）
- **のびしろの 合計を 1未満に しないこと。**`.36` と `.64` に すると
  あまりが ぜんぶ 配られず、カードが ねらいより 上に 行きます。
  だから **36 と 64**（合計100）で 書きます
- 場所が 足りない ときは のびしろが 0の ままなので、カードは
  いちばん上から 始まって **切れません**（`margin:auto` の ときの
  「375x667 で 上が 19px 欠けた」は 起きない）
- **みじかい 画面では 上に 寄せすぎないこと。**あまりが もともと
  少ないので、`@media (max-height:660px)` で 44:56 に もどします

| 画面 | 上のあき | 下のあき |
|---|---|---|
| 393x852 | 102px | 178px |
| 430x932 | 117px | 205px |
| 320x568 | 77px | 105px |

safe-area は `padding-top` に 入っているので、この 重みを どう いじっても
カードが **島や ノッチに かぶることは ありません**（上は かならず
`env(safe-area-inset-top) + 12px` から 始まる）。

#### せまい 画面では 絵から 先に ちぢめる

けずる 順は **すき間 → 絵 → かざり**。
**文字と ボタンを 小さくして 場所を 作っては いけません** ——
読めない 題と 押しにくい ボタンでは、この画面の 用が たりません。

| | どこで |
|---|---|
| 絵の 頭うち | `#weArtWrap` の `--weCap`（ふだん `34dvh`・たてが みじかいと `27dvh`）|
| かざりを 消す | `@media (max-height:660px)` で `.weOrn.sm` と `.weOrn.dot` |

絵は **たてよこ比を たもったまま**、はみ出す ぶんだけ 上下を 切ります
（`object-fit:cover`）。**よこに つぶさないこと。**
320x568 で 23%・375x667 で 4% 切れます。

#### 決めうちの `clamp()` だけでは 実機で おりかえる

**Linux で 測った 幅は iPhone の 丸ゴシックより せまい**ので、
ここで 1行に 収まって 見えても 向こうでは 1行 ふえます。
実測の のこり幅は 題が いちばん せまく、320px で **10%** しか ありません
（図鑑の 札の `fitStat()`・見出しの `fitHead()` と まったく 同じ話）。

`fitWe()` が **出したあとに 行を かぞえて**、おりかえって いたら
その行だけ **0.82倍まで** 下げます。ふだんは `clamp()` の ままです。
これで もつのは **フォントが +30% 広くなるまで**（検査ずみ）。

行を かぞえるのは `weLines()`。**まん中の たかさで まとめる**こと ——
上ばしで かぞえると、📕 のように 大きさや ベースラインの ちがう 字が
おなじ行でも 別の行に 見えます（じっさい 2行と 出ました）。

#### 8つの 画面で 機械に しらべさせる

```
node tools/check-welcome.mjs      # 終了コード 0 で 合格
```

320x568 / 320x690 / 375x667 / 393x852 / 402x874 / 430x932 / 360x800 / 412x915 を、
**ふつうの フォントと わざと 広げた フォントの 2周**で 見ます。
よこスクロール・カードの はみ出し・行数・`object-fit`・ボタンの 大きさ・
ホームバーとの すき間・**タップ判定と 遷移**まで。

タップ判定は `elementFromPoint` で 見ます ——かざりを 足したときに
`pointer-events:none` を 書きわすれると、**見た目は 正しいのに 押せない**
画面に なります。目では 見つけられません。

> ページ全体の `scrollWidth` は 320px だと 2px はみ出しますが、それは
> うしろの ワザバー（横スクロールする）で、**前から そうです**。
> だから しらべるのは `#weOv` の 中だけ。

- **出すのは ほんとうに はじめての人だけ。**旗（`sweetTD.welcomed`）1つでは
  足りません ——**この版より 前から あそんでいる人**には 旗が 立って
  いないので、あの人たちにも 出てしまいます。だから
  `sweetTD.run`（つづき）と `sweetTD.login`（1回でも プレゼントを 受け取った）も
  見て、どれか あれば 出しません
- 旗を 立てるのは **とじたとき**です。出したときに 立てると、
  読みこみ直しで 一生 見られなく なります
- `?dbg=1` では 出しません（全画面を おおうので 検査どうぐが 通れない）

#### 上の 絵は 1まいで 出す（スプライトを ならべない）

画面ぜんたいは 部品に 分けますが、**上の 絵の 中みは 1まい**です。
スプライトを ならべて 作っては いけません ——奥ゆきも 地面の つながりも
出ず、「その国に いる」ように 見えません。

**絵を さしかえる ときに 直すのは 2か所だけ**です。

| | |
|---|---|
| `#weArtWrap` の `--weAR` | 切りぬいた あとの たてよこ比（いま `906 / 635`）|
| `#weArt` の `src` | 絵の ありか（いま `art/screens/welcome_castle.webp`）|

#### 見本から お城の ところ だけを 切り出す

支給されたのは **画面ぜんたいの 見本**（`welcome_castle_source.png`・
1024×1536）で、題も 説明も 📕の 文も「ゲームをはじめる」も
**焼きこまれて います**。そのまま 貼っては いけません
——文は HTML の 文字で 出す、が この画面の きまりです。

```
node tools/make-welcome-art.mjs      # 原画から webp を 作りなおす
```

| | |
|---|---|
| 切りぬき | x=60 y=82 の 906×635（原画の 画素で 実測）|
| 品質 | WebP 0.92（150KB）。**ちぢめない**（1:1 で うつす）|

- **焼きこまれた 金の ふちは 入れないこと。**CSS がわ（`#weArtWrap`）が
  自分で 金の ふちを 描くので、二重に なります
  （アプリアイコンの 角丸で やった 失敗と 同じ形）
- 四すみの 弧は のこりますが、CSS の `border-radius`（表示で 19px＝
  絵の はばの 5%）が おおいます。弧は 2.7% ぶんしか ありません
- **ふちの のこりを「金の 画素の 数」で しらべては いけません。**
  絵の 中にも 金いろの もの（王冠・お菓子の家・キャンディ）が あるので、
  内がわへ 寄せる ほど **ふえます**（47→55→68 と ふえました）。
  焼きこまれた ふちは **一本の 線**なので、
  **いちばん 長い つらなり**で 見ます（いま 最長 16px。ふちが
  のこって いれば 何百に なります）
- 原画（`welcome_castle_source.png`・2.1MB）は **のこすこと**。
  `*_source.*` なので アプリにも オフラインの キャッシュにも 入りません
- キャラでは ないので `art/sprites/` に 置かないこと。
  `art/screens/` なら `check-chars.mjs` は 見にきません

#### `welcome.webp`（王さまたちの 絵）は 消さないこと

この画面では つかわなく なりましたが、**紹介ページ
（`invite.html`）の 主役の 絵**と、SNSに はったときに 出る
**OGP（`ogp.jpg`）の もと**です。消すと あちらが 出なくなります。

- **王さま・王妃さまは 盤面とは ちがう 表情・体勢**です（王さまは 口を
  あけて わらって 手を ふる、王妃さまは 目を とじて 手を 組む）。
  まじょのときと 同じで、**本人を 手本に 入れる**こと（`SCENES.welcome` の
  `refs`）。入れないと 別人に なります
- 出しかたは `node tools/gen-art.mjs --scene welcome`。
  プロンプトは `SCENES` の 中（チャットで 打った 文は のこらないため）
- 原画（`welcome_source.jpg`）は **のこすこと**。出しなおすと 別の絵に なるので、
  あとから 品質だけ 変えたく なったときに 要ります

### おかえりなさい（`#backOv`）

つづきの セーブ（`sweetTD.run`）が ある人にだけ 出して、
**つづきから / さいしょから** を えらんでもらいます。
`openBack()` は セーブが ないと `false` を かえして 何も 出しません
——はじめての人に「つづき」を 見せても 意味が ないためです。
`?dbg=1` でも 出しません（全画面を おおうので 検査どうぐが 通れなくなる）。

- **色を 分けること。**つづきから＝ピンク（すすむ）、さいしょから＝ラベンダー
  （`#backNew`）。おなじ色だと、消える ほうを まちがえて 押せます
- **さいしょから が 何を 消すか カードに 書く**こと
  （「なかまと ⭐は 0に もどります」）。⭐が 何万も たまってから
  押されると とりかえしが つきません
- 💗の 回復まちが 先に 出ているときは **出しません**。あちらにも
  「続きから」が あるので、2枚 かさねると どちらが 本当か 分かりません
- 絵は カードの 中の canvas（`backCv`）に 毎フレーム えがきます。
  ここだけ rAF が まわるので、**とじたら `cancelAnimationFrame` する**こと

#### 絵は 盤面の 子と 同じでは いけない

まじょは 盤面では **立って いて**、ここでは **ほうきで とんで、ウインクして
手を ふって**います。**別の 絵**です（`art/sprites/witch_fly.png`）。

はじめは 盤面の `art/witch.png` を かたむけて 貼っていましたが、
あれは 立ち絵なので **表情が 変えられず**、同じ絵が 2か所に 出るだけで
お祝いの 画面に 見えませんでした。だから 出しなおしました
（`tools/gen-art.mjs` の「とぶまじょ」）。

- **おなじ子だと わかること。**白い髪・黒い ハットバンドと バックル・
  すみ色の ほうきは そのまま。1回目は 手本を 入れずに 出したら
  **髪が ピンクに なって 別人**に なりました。
  おなじ子の 別ポーズを 出すときは、**その子 じしんを 手本に 入れる**こと
  （`ART` の `refs:['witch']`）。ふだんは 自分を 手本から のぞきますが、
  ここは「寄せすぎ」より「別人に なる」ほうが こまります
- **`pose` で まえ向きの しばりを 外すこと。**`STYLE` に
  「Front facing」が 書きこんで あると、あとの 文で「ななめに とんで」と
  たのんでも **立った 絵が かえって きます**。`ART` の エントリに
  `pose` を 持たせて、そこだけ 差しかえます
- **この絵は もう 右上へ 進む 向きに 描いて あります。**
  `scale(-1,1)` で うつしては いけません（手を ふる 側が うしろに 回る）
- 大きさは `sprite:512`。ふだんの 256 だと カードの中（360pxほど）で ぼけます
- 白い髪が あるので、切りぬきの あと **穴が あいていないか 見ること**。
  ふちから つながった 白だけを ぬく 作りなので だいじょうぶ でしたが、
  白っぽい子は ここが いちばん あぶない（実測：占有率31.0%・ふちの残り0）

#### 1つの 画面に 2つの 絵柄を まぜない

スターは **盤面と おなじ 絵素材**（`art/sprites/star.png`）です。
はじめは `GBODY.star` の 形に **顔を えがきなおして** 出していました
（ウインク＋あいた口）。まじょが コードの絵の うちは 合っていましたが、
まじょを **絵素材に した とたん、となりで 描きかたが ちがう**のが
目立ちました ——かたや 塗りと つやの ある 絵、かたや 平らな 図形。

**ちがいは 顔では なく 動きで 出します**（ぴょんぴょん はねる・かしぐ）。
絵柄を そろえる ほうが 先です。

素材には まわりの 余白と きらめきが 焼きこんで あるので、
**見えている 星は わくより ひとまわり 小さく なります。**
その ぶん 大きく とること（いま `H*0.21`。`H*0.155` では 小さすぎた）。

#### オーラは その子の 色で、しかも 焼く

スターには 金いろの オーラが 焼きこまれて います。まじょにも
**むらさきの** オーラを つけて そろえました（`backBakeAura`）。

- **色は その子の 色に そろえること。**スターが 金なのは 体が 金だから で、
  まじょは むらさきの ドレス。おなじ色に すると、2体が 1つの かたまりに 見えます
- **ぼかしは さしわたしの 0.25倍あたり。**スターは わくの 半けいの 0.5倍
  ＝さしわたしの 0.25倍 です。0.10 まで せまく したら、オーラでは なく
  **むらさきの ふちどり**に 見えました
- **ぼかしを 毎フレーム かけないこと。**まじょ 1体を 2回 かさねる だけで
  **60fps → 44fps** に 落ちました（実測）。オーラごと 1枚に 焼いて、
  あとは 貼るだけに します。焼くわくは ぼかしが 外へ 出る ぶん
  大きく とること（切れると 四角い 跡に なる）

#### キャラで ない 絵は `SCREEN_ART` に 書く

`art/sprites/` に 絵を 置くと、`check-chars.mjs` が
「`ART_KEYS` に 無い」と おこります。キャラでは ない 画面用の 絵は
`tools/check-chars.mjs` の **`SCREEN_ART`** に 足してください。
図鑑にも `CREATURES` にも 入れては いけません（キャラ数が ずれます）。

### 「cover して 切る」では なく「contain して 全部 見せる」

絵は **2:3**（1024:1536）、iPhone は もっと たて長（0.46 あたり）。
`object-fit:cover` で 画面いっぱいに すると **にじの 左右と ユニコーンが
外に 出ます**。だから 横はばを 画面はばに 合わせ、たては その 1.5倍。

```
displayWidth  = viewportWidth
displayHeight = viewportWidth * 1.5
```

はばは `min()` で 3つを くらべて 決めます（`#titleArt`）。

| くらべるもの | なんのため |
|---|---|
| `100%` | ふつうの たて持ち |
| `430px` | 大きい 画面で ふくらみすぎない ため |
| `var(--availH) / 1.5` | **画面が ひくいとき**（よこ向き・小さい端末）。たてが はみ出さない |

`--availH` は `100dvh` から 上下の セーフエリアを 引いた もの。
`dvh` が つかえない ブラウザの ために `100vh` の 行も 先に 書いてあります。

### あまった たては ぼかした 同じ絵で うめる

絵より 画面の ほうが たて長なので、すき間が 出ます。
そこを 黒や 白で うめないこと。**おなじ絵を もう1まい**（`#titleBg`）
`object-fit:cover` ＋ `blur(30px)` ＋ `opacity:.40` で 敷きます。
色が つながるので「別の 何か」に 見えません。**うしろの1まいは 切れてよい。**
前がわ（`#titleArt`）は **ぜったいに 切らない**。

### あまりは 上下に わける（下ぞろえに しない）

**下ぞろえ（`align-items:flex-end`）に しては いけません。**たて長の
iPhone では あまりが ぜんぶ 上へ 行き、空だけが 広くなって、
城から 下が 画面の 下に かたまります（393x852 で 上252px / 下0px）。

`#titleArt` の `margin-top` で わけます。上の 取り分が `--titleUp`。

```
margin-top: max(0px, (--availH - --artW * 1.5) * --titleUp)
```

いまは **`--titleUp:.40`**（上40% / 下60%）。393x852 で 上101px / 下161px。
にじと 城が 画面の 上半分に 入り、題字が まん中あたり、
「はじめる」が 78% あたりに 来ます。

数字ではなく **ならびで** 見ること ——空 → にじ → 城 → なかま →
題字 → はじめる → 余白 が、画面ぜんぶに 近い 間かくで 収まっているか。

### 「はじめる」は 絵から 切りぬいて 下へ おろす

「はじめる」は 原画の y=1344..1489 に 焼きこまれています。そこだと
題字の すぐ下に くっついて 見えるので、**絵の 下の ピンクの ところへ
おろして**あります。やった ことは 3つです。**絵の ファイルは さわりません。**

#### ① おなじ絵を もう1まい 出して、ボタンの ところだけ 窓から 見せる

`#titleStart` の 中に `title_main.webp` を もう1まい 置き、
**原画の x=248..800・y=1322..1524 だけ**を 見せます（`overflow:hidden`）。
倍率は 前がわの 絵と まったく 同じなので **画質は 1ミリも 落ちません**。
ファイルも ふえません（同じ 絵の つかいまわし）。

- **切りぬいた PNG を 作らないこと。**+200KB 増えるうえ、
  同じ 画素を もう一度 符号化する だけで 得が ありません
- 窓の 左はし 248 は **道**（右はしは y=1356 で x=243）、右はし 800 は
  **草**（x>806）に かからない ぎりぎりの ところ
- **見た目と 当たり判定は 1つの 親（`#titleStart`）に 入れること。**
  いちを 変えても ずれません。`#titleGo` は 窓に 対する 百分率
  （`left:3.0797% / top:10.8911% / width:89.3116% / height:72.2772%`）で、
  出る 大きさは 絵の中と **完全に 同じ**です
- **ぼかしは 窓（`#titleStartV`）に かけること。**絵じしんに かけると
  `%` が「絵の 高さ」に なり、カプセルが 65% の うすさで 出ていました

#### ② 元の いちの ボタンは、まわりの じめんの 色で おおう

`#titleHide` が **x=246..804・y=1314..1536 だけ**を おおいます。
ここには ボタンと ピンクの じめんしか ないので、**道も 草も キラキラも
のこります**。

- 色は「ボタンの すぐ右の きれいな じめん」（x=764..792）を 行ごとに 実測。
  **ボタンの 下（y=1494..1512）から 拾っては いけません** ——かげが 入って
  こく なり、四角い 跡に なりました
- 上ふちは y=1314..1336 で 消します。**ボタンの 光は y=1335 から**なので、
  そこまでに 不とうめいに しないと 光の 頭が すけて よこ一本の 線に 見えます
- **全幅を ベタ塗りに しないこと。**いちど 試しましたが、題字の 下から
  いきなり 1色に なって「絵が そこで 切れた」ように 見えました

#### ③ 左下の 道は 下へ 行くほど じめんに とかす

道は 絵の いちばん下まで つづいていて、まわりより 黄いろ寄り
（道 `#eecfd1` / じめん `#e2bad0`）なので **たての ベージュの帯**に 見えます。
`#titleRoad` が その行の じめんの色を 下へ 行くほど こく かぶせます。

**絵の 下まで のばすこと**（`bottom:-28.5%`）。うしろの ぼかした 1まいにも
道の ベージュが 写っているので、絵の 中だけ 直すと 絵の 下から また
ベージュが 出ます（実測で よこに 12 の さ）。

#### たての すじの 正体は だいたい この2つ

| 出かた | 正体 |
|---|---|
| おなじ色の 止まりが 2つ ならぶ | そこで 傾きが 変わる。グラデは **連続**させる |
| `radial-gradient` に 大きさを 書かない | いちばん遠い **角**で 0に なるので、よこの まん中の ふちでは まだ 61% のこって **1pxの 段**に なる（実測 5.9）。ぼかした ふちが ほしいなら `filter:blur()` を つかう |

**四角い ふちは 目立ち、ぼかした ふちは 目立たない。**色の ちがいを
0に するのでは なく「**ゆっくり 変える**」のが 直しかたです。切りぬいた
ボタンの 地は ここの 背景より 8ほど こいので、下に おなじ色の
`filter:blur(14px)` の 台（`#titleStart::before`）を 敷いて、
40px かけて 変わるように してあります。

#### 変数に `100%` を つかわない

`--artW` などは `width` だけでなく `top` / `bottom` / `margin` にも 入ります。
`%` は `width` では「はば」・`top` では「高さ」を 指すので、おなじ 変数が
場所によって ちがう 値に なります。**`100vw` を つかうこと。**
`100%` の ままだと 320x690 で ボタンが 18px 画面の 外に 出ていました。

いちは 画面ごとに 計算します（決めうちの px に しない）。

```
top = slotTop + (slotH - btnH) * .62      ただし 画面の 下から はみ出さない
```

`slotTop` は 絵の y=1350（題字の かざりの 下はし y=1308 より 下）。
393x852 で 絵の下から 27px・ボタンの下 78px。

すきとおっているので `translateY` では 押した ことが わかりません。
`:active` で **白く さっと 光らせて** 手ごたえを 出します。

#### 「はじめる」は いつも 光らせる

金の ふちを 出しっぱなしに して、その上に 波紋を くりかえします
（`goGlow`）。**波紋だけに しないこと** ——消えている 一しゅんに 見た人が
気づけません。ガイドの 📕（`hintGuide`）と 同じ しかけですが、
📕 は **はじめての人だけ**（場所を 教えたら 用ずみ）、こちらは
**いつでも**です。表紙で いちばん 押してほしい ところなので。

ふちは カプセルの すぐ外に つくので、絵に 焼きこまれた 金の ふちとは
かさなりません。`prefers-reduced-motion` のときは 波紋を 止めて ふちだけ。

### 上と 下の ふちの 線は「絵の 色を 外へ つづける」ことで 消す

絵の 上はしと ぼかした 背景の あいだに **よこの 線**が 出ます。
「カードを 貼った」ように 見えるので 消します。**絵の ファイルは さわりません。**

3つ 組み合わせます。どれか 1つ 欠けると 線が もどります。

1. **絵の 上はしだけ うすくする**（`#titleArtIm` の `mask-image`・上から 2%）。
   左右と 下には かけないこと。にじの てっぺんは 13.7%（y=210）なので かからない
2. **絵じしんの 空の色を 上へ つづける**（`#titleArt::before`）。
   うしろの ぼかした1まいは 絵と ちがう ところを 写しているので、
   ①だけでは「青い 空が いきなり 始まる」線が のこります。
   絵の いちばん上（y=26..90）を 列ごとに 平均した 横グラデを 上へ のばし、
   上へ 行くほど 消します
3. **その帯を 絵の 下に もぐりこませる**（`bottom:97%` ＋ `z-index:-1`）。
   絵の 上はしで ぴたり 止めると、うすくした ところの うしろが
   ぼかした1まいの ままに なり、そこだけ **明るい すじ**が 出ます

おとし穴が 1つ。**色は よこに 大きく ならしてから 拾うこと**（±110px）。
生の 画素を つかうと、雲や キラキラの 明るい 点が **たての すじ**に なりました
（60% あたりに 白い 帯が 出た）。

**下ふちも まったく 同じ 3つ**です（`::after`・`top:98.5%`）。
ちがうのは 2つ だけ。

- フェードは **1.5%**（`y=1513` から）と 短く する。「はじめる」の
  下はしは `y=1489` なので、**ボタンにも そのまわりの ピンクにも
  かからない**（24px 下から 始まる）
- 色は **ボタンより 下**（`y=1494..1512`）から 拾う。ボタンの こい ピンクを
  拾うと 帯が こく なって、かえって 目立ちます。角丸の 黒（`y>1514` の
  すみ）にも かからない はんい

はかりかた：ふちを またぐ たての 明るさを 1行ずつ 出して、
**となりの行との さ**を 見ます。直す前は 境めで 17、いまは **上2.5・下4.3
以下**（人の目には 見えません）。谷（いったん 暗くなって また 明るくなる）も
出ていないこと。ボタンに フェードが かかっていないかは、点では なく
**わくの 平均**で くらべること（点だと 金の ふちに 当たって ばらつきます。
わくの 平均なら Δ2 以下＝WebP の 圧縮ぶんだけ）。

### 絵の 四すみの 黒は CSS で 切りぬく

いまの 絵は 四すみが 角丸（半けい 22px）で、**その外がわが 黒**です。
そのまま 出すと すみに 黒い つめが 見えます。

`#titleArtIm` の `border-radius` で 切りぬきます。**絵の 画素は
1つも さわりません。**半けいは 絵より すこし 大きい 32px ぶん
（`3.1% / 2.0667%`）。ぴたり 22px に すると、ふちの なめらか処理で
**黒が 数px のこりました**（実測 120画素）。よぶんに 切っても
消えるのは 空だけです。

### 絵を さしかえるときの 手じゅん

1. 原画を `art/title/` に 置く。**source も のこす**（`title_main_source.png`）
2. WebP に する。**大きさは 変えない・強く 圧縮しない**
   （品質 0.92 で 1.76MB → 149KB・平均のちがい 1.3）
3. たてよこ比を CSS に 書きうつす —— `#titleArt` の `aspect-ratio` と
   `min()` の わりざん（いま 1.5）の **2か所**
4. 「はじめる」の いちを 原画の 画素で はかり、百分率に して
   `#titleGo` の `left/top/width/height` に 入れる
5. 四すみに 黒や とうめいが あるか しらべ、あれば `border-radius` を 合わせる

```
node tools/check-title.mjs      # 5つの 画面幅で ボタンの いちと fps を しらべる
```

### 表紙 v2（新しい 原画）——**完成済み・原則 変更禁止**

> **記録（2026-09-01）**：853x1844 の 新しい 原画で **正式採用**しました
> （`261cb73` の 見た目で 固定）。**ここから 先は さわらないこと。**
> 直したく なったら、**まず 相談してから**。
> v1 は 消して いません ——`TITLE_V2` を false に すれば すぐ もどせます。

#### 固定した もの（変えない）

| | |
|---|---|
| 原画 | `art/title/title_v2_source.png`・**SHA-256 `85aec0fde4dd9e413557d334c033b09958926d14dd899de671157bbbe01304e3`** |
| ゲームが 読む もの | `art/title/title_v2.webp`（853x1844・343KB・品質0.92）|
| 見せかた | たてよこ比を たもつ・**crop しない**・にじ／城／題字／ボタンの いちを 変えない |
| 393x852 以上 | いまの 表示で 固定（**画素で 0.000%** を 保つこと）|
| 320x568 / 375x667 | 左右を **鏡写しで のばす** 方式で 固定 |
| ぼかし／フェード | `blur(5px)` ／ フェードは **基本の `0 → 1 を 2.5%` だけ**（深い フェードは 入れない）|
| ボタン | 原画の いちの まま。すきとおった 判定と 百分率の 変換を 固定 |
| safe-area | いまの 処理の まま |

いちばん 大きな ちがいは **たてよこ比**です。

| | 比 | |
|---|---|---|
| v1 `title_main.webp` | 1024x1844 → **0.667** | スマホ（0.46）より ずっと 四角い |
| v2 `title_v2.webp` | 853x1844 → **0.4626** | iPhone（393/852＝0.4613）と ほぼ 同じ |

だから v1 は「はばを 合わせると たてが 大あまり」で、あまりを 上下に
40:60 で 分けて いました。**v2 は ほぼ 画面いっぱいに なり、あまりは
0〜33px しか 出ません。`--titleUp:.40` を そのまま つかっては いけません。**

```
node tools/make-title-art.mjs      # 原画から webp を 作りなおす
node tools/check-title2.mjs        # 8つの 画面で しらべる（終了コード 0 で 合格）
```

| | |
|---|---|
| 原画 | `art/title/title_v2_source.png`（853x1844・2,381,969 バイト・**さわらない**）|
| ゲームが 読む もの | `art/title/title_v2.webp`（同じ 853x1844・343KB・品質0.92）|

#### あまりは 上に 78%

上と 下では「外へ のばしやすさ」が まるで ちがいます（原画で 実測）。

| | よこの ばらつき | 40pxで 明るさが |
|---|---|---|
| 上ふち | **5.5** | +6.6 …空と 星。なめらか |
| 下ふち | **24.0** | +23.5 …お菓子。こまかい |

下へ のばすと お菓子の 形が とぎれて 見えるので、`--titleUp2:.78`。

#### 「はじめる」は 動かさない。上に すきとおった 判定を 置くだけ

v1 は 絵に 焼きこまれた ボタンを **下へ 引っこして** いました
（`titleHide` で 消して `titleStart` で 切りぬいて 出す）。
**v2では やりません。**原画の いちの まま つかいます。

いちは 原画の 画素を 実測して 百分率に した もの
——`x=141..700 / y=1529..1687`。百分率なので 画面の 大きさが 変わっても
ずれません。

**チカチカする 光は v2 用の `goGlow2` を つかいます。**表紙で いちばん
押してほしい ところなので 出しっぱなしに します。金の ふちは
**カプセルの すぐ外**に つくので、絵に 焼きこまれた ふちとは
かさなりません。うごきを へらす せっていの人は 動きが 止まって
ふちだけ のこります（v1 と 同じ あつかい）。

> **v1 の `goGlow` を そのまま つかっては いけません。**いちど そう
> しましたが、実機で「金の縁はできてますが チカチカしていない」と
> 言われました。**動いては いたのです** ——ふちの 明るさの ふれはばが
> **10.1 しか なかった**（実測）。
>
> v1 の ボタンは **無地の ピンクの 地**の 上に あったので、金の ふちが
> 1 → .62 に なるだけで 目立ちました。v2 は まわりが **明るい お菓子**で、
> しかも すぐ内がわに **絵の 金の ふち**が あるので 埋もれます。
>
> だから `goGlow2` は **明るさだけで なく 太さも 変えます**
> （3px ⇔ 7px）。こみいった 背景の 上でも「形が 変わる」ので 見えます。
> いまの ふれはば は **54**（止めずに 撮った 14まいの 実測）。
>
> **明るくなる 向きに すること。**はじめ こい 金（`rgba(255,176,24,.85)`）で
> 組んだら **暗くなる 向き**に なり、光では なく 影に 見えました
> ——まわりの お菓子が 明るい（明るさ 240ほど）ので、それより
> 明るい 白っぽい 金で ないと 光って 見えません。

> はじめ v2 では「原画に 金の ふちが あるので 二重に なる」と 思って
> 切って いましたが、実機で「チカチカが 無い」と 言われて もどしました。
> **ボタンの 地を CSS で 塗る のとは ちがいます** ——塗ると 原画の
> ボタンを 隠して しまうので、そちらは しないこと。

**検査で 光を 止める こと。**`check-title2.mjs` は「いまの 絵」と
「フェードを 外した 絵」の 2まいを くらべますが、光が 動いて いると
2まいで 明るさが ちがい、**ボタンと ローマ字が「食われて いる」と
出ます**（じっさい 差 127 と 出ました）。撮る 前に
`animationPlayState='paused'` で そろえます。

**ボタンの id は 1つの まま に すること。**`titleGo` と `titleGo2` に
分けると、`#titleGo` を おす 検査どうぐ（`check-title`・`check-welcome`）が
ぜんぶ 通らなく なります。だから 起動時に **ボタンそのものを
v2 の 絵の 中へ 移して** います。

#### 継ぎめ ——3つ 直して 61 → 3.9 に なった

はじめは 絵と まわりの 境めに **50〜61 の 段**が 出ていました
（人の目に 見えるのは 5から）。原因は 3つ で、**1つ 直しても のこりが 効きます**。

| 直したところ | なぜ |
|---|---|
| `#titleOv` の 背景 | v1 用の 明るい 空（明るさ 226〜230）の まま だった。v2 の 絵の ふちは 160〜177 なので、それだけで 段に なる。絵から 実測した 色に 塗りなおす |
| うしろの ぼかした 1まい | 画面いっぱいに `cover` で 敷くと、ふちの すぐ外に **絵の 別の ところ**が 来る。**同じ いち・同じ 比のまま 1.3倍**に して、ふちの 色を そのまま 外へ つづける。あまりが 小さいので うすくしない（v1 の `.40` は v1 の 事情）|
| 左右の フェード | v1 の「左右には かけないこと」は **v1 の 話**。v2 は ひくい 端末で よこに あまるので、はしを 2.5% 消す（`#titleArt2W` に 1軸ずつ かける ——1つの mask に まとめると `mask-composite` が いる）|

実測（いま）：上 2.1〜2.7／下 3.3〜4.0／左 1.4／右 2.2〜2.3。**ぜんぶ 5未満。**

#### せまくて ひくい 端末は「全部 見せて、左右へ 世界を つづける」

320x568 と 375x667 は 画面が 絵より **よこ長**なので、はばを
いっぱいに すると たてが 21% はみ出します。切ると にじの てっぺんか
ボタンの 下が 欠けるので、**切らずに 全部 見せて 左右に すき間**を
出しています（29px／33px ずつ・絵は 画面はばの 82%）。

そのすき間は **鏡写しの つづき**（`#titleSide`）で うめます。
きまりは この 2つ です。

> **① 単純な 拡大＋ぼかしでは うめないこと。**
> にじ・山・お菓子の **高さが ずれて**、まん中に 細い ポスターを
> 置いたように 見えます。
>
> **② よこ長の みじかい 画面では、左右反転した 同じ絵を つかって、
> 原画の はしから おなじ 高さの まま 世界を のばすこと。**


**はじめは `#titleBg2` を たてにも 1.3倍 して うめて いました。
これが「まん中に 細い ポスターを 置いた」ように 見える 正体でした。**
すき間の 中の にじ・山・お菓子が、となりの 絵より 最大 85px（320x568）
上に ずれるので、**色は 合って いても 別の 絵に 見えます**
——継ぎめの 数字は 1.4〜2.3 と 出ていたのに、目には 額ぶちに 見えました。
**数字だけで 合格に しないこと。**

鏡写しなら、絵の 左はしの 1px 外は 左はしと **同じ色**で、
帯（にじ・山・お菓子）は **おなじ 高さの まま** 外へ ながれます。
3まい（鏡・本体・鏡）を 1つの 箱に ならべて **箱ごと 1回 ぼかす**こと
——1まいずつ ぼかすと、つなぎめの 内がわで 外の とうめいを ひろって
そこだけ うすい すじに なります。

**ぼかしを 強くするほど 段は 大きく なります**（8とおり 実測）。
ぼかしは まわりの 色を 引きこむので、鏡写しで せっかく そろえた
ふちの 色が ずれる ためです。

| ぼかし／フェード | 段 | |
|---|---|---|
| 0／なし | 10.0 | 鏡写しが くっきり 見えて 別物に なる |
| 4／.6→1・5% | 3.8 | 鏡写しの チョコが それと 分かる |
| 22／.55→1・5.5% | 10.7 | ぼかしすぎ |
| 12／.15→1・12% | 2.5 | **不採用**（下の とおり 金の わくを 食った）|
| **5／基本の 2.5% のみ** | **0.7** | **採用** |

#### **フェードを 絵の 大事な ところまで 食いこませない**

> **記録（2026-09-01）**：実機の 報告「おかしの国の 金の枠が 消えています」。
> **本当でした。**いちど 出した 版に 入って いた 不具合です。

左右の つなぎめを なじませる フェードを **`.15 → 1 を 12%`** と
深く して いました。ところが **題字の 金の わくは、原画の
左はしから 約 3.2% の ところまで のびて います。**
だから わくの 端の 不とうめいが **0.38** まで 下がり、
**6割 以上 消えて 見えました。**

**検査が 見のがした 理由が いちばん だいじ**です。
継ぎめの 段（2.5）しか 測って おらず、
**「絵が のこって いるか」を 一度も 測って いませんでした。**
フェードなしの 絵と くらべると こうです（320x568・題字の 帯）。

| | 継ぎめ 左/右 | 絵が 食われた ぐあい |
|---|---|---|
| `.15→1 / 12%` | 0.9 / 1.6 | **平均 14.6** ←わくが 消える |
| **`0→1 / 2.5%`（基本）** | **0.6 / 0.7** | 平均 2.5（＝393x852 と 同じ）|

**深い フェードは 継ぎめにも 効いて いませんでした**（むしろ 悪い）。
鏡写しで 色が すでに つながって いるので、フェードは
**基本の ぶんで じゅうぶん**です。

##### これから 守る こと

**左右の つなぎを なじませる ための フェードや ぼかしを、
中央の 原画の 大事な かざりまで 深く 食いこませないこと。**

つぎの ものには **フェードも ぼかしも かけない**：

- タイトルロゴ
- 金の わく
- 王冠
- 文字（「魔法の おかしの国」「OKASHI NO KUNI」）
- 「はじめる」ボタン
- そのほか 大事な UI

**足す ときは かならず「フェードなしの 絵」と くらべて 測る**こと
（`node tools/check-title2.mjs` が 部分ごとに しらべます）。
継ぎめの 数字だけ 見て 合格に しては いけません ——
あのときの 段は 2.5 で、**数字の上では 合格に 見えて いました。**

#### しきいは 絵の 比 ちょうど に しない

`#titleSide` を 出す 条件は `min-aspect-ratio: 12/25`（0.48）です。
絵の 比（0.46258）ちょうどに すると **320x690（0.4638）まで 入り**、
あそこは すき間が 0.4px しか ないのに 絵の はしが 12% 透けます。

| | 比 | すき間 | |
|---|---|---|---|
| 320x568 | 0.5634 | 28.6px | 出す |
| 375x667 | 0.5622 | 33.0px | 出す |
| 320x690 | 0.4638 | 0.4px | 出さない |
| 393x852 | 0.4613 | 0px | 出さない（基準画面）|

**もとから ある 2.5% の フェード（`#titleArt2W`）を 消さないこと。**
深い フェードを メディアクエリの 中に 移す ときに 基本の ぶんまで
外して しまい、**393x852 の 画素が 4.6% 変わりました**。
「ここは 変えない」と 言われた 画面は、**前後の 画面を 画素で
くらべて 0.000% を 出す**こと ——目では 気づけません。

#### 当たり判定の 検査は「形」を 切り出そうとしないこと

`check-title2.mjs` は はじめ **カプセルの 形を 画素から 切り出して**
DOM と くらべて いましたが、まん中の 白い「はじめる」で つらなりが 切れ、
下には 大きな ピンクの お菓子が ならんで いるので、**しきいを どう 決めても
どこかで まちがえます**（「70px ずれている」「50px 大きすぎる」が 交互に 出た）。

いまは 形では なく、**この3つ**を 見ます。

1. 当たり判定の **中**の 45点が ボタンの 色か（92%以上）
2. **12px 外**の 26点が もう ボタンで ないか（30%以下）
3. まん中を おして `elementFromPoint` が `titleGo` を かえすか

わざと 8.9%（75px）ずらすと 落ちることを 確認ずみです。

### むかしの 表紙（コードで えがいていた もの）

2026-08-29〜30 に、表紙を **ぜんぶ コードで えがく** 作りと、
1250×2100 の アウトペイント素材で つくる 作りを 試して、やめました。
コミット `4152c09`〜`17ae89e` に のこっています。学んだ ことだけ 書いておきます。

- **絵の 足りない ところを こちらで 作ると、かならず 継ぎ目に なる。**
  伸ばす・かぶせる・スライスする —— 5とおり 試して ぜんぶ 線が 出ました。
  色を どれだけ 合わせても 消えない 正体は **質感**（つるつる → ざらざら）
- **行の 平均で 見ている かぎり、境めを またいで 切れた モノは
  ぜったいに 見つかりません**（色の 段差 0 でも 実機では 見えていた）
- **ぼかしで 直すと 上だけ 曇ります。**正しいのは
  「そもそも 境めの 近くに 大きな モノを 描かせない」こと
- **答えは「絵の 外がわに 余白を 足しておく」**でした。足りない ものを
  こちらで 作るのでは なく、はじめから 余分に 持たせる

いまの 作り（1まいの 絵を 切らずに 全部 見せる）は、この 問題が
**そもそも 起きません**。あまった ところは ぼかした 同じ絵で うめるだけです。

道具（`title-recomposite.mjs`・`title-edge-match.mjs`・`title-sky-decor.mjs`）と
昔の 素材（`title_full.webp`・`title_master.webp`・1250x2100 の マスターPNG）は
**消しました**。いる ときは git の 履歴から 戻せます。
つかわなかった 案だけ `art/title/candidates/` に のこしてあります。

いま のこす 表紙の 画像は この2つ だけです。

| | |
|---|---|
| `art/title/title_main_source.png` | 原画（1024x1536・1,755,488 バイト・さわらない） |
| `art/title/title_main.webp` | ゲームが 読む もの（149KB・品質0.92） |


## 画面は 1まいずつ

「閉じる」を押さないとほかの画面へ行けないのは面倒なので、
**ちがうボタンを押したらそこへ直接飛びます**（`openPanel` / `togglePanel`）。

置くか置かないかは、**その画面で下のボタンが見えるかどうか**で決めます。

| 画面 | どこに乗るか | 閉じるボタン |
|---|---|---|
| 図鑑・ガイド | `#wrap` の中（盤面だけ覆う） | **あり**（見出しの ✕）。下のボタンでも閉じる |
| ショップ・今週のおだい・合言葉 | **図鑑（👑）の中**。見出しの行に 🛍🏅🔑 | — |
| アバター・シェア | 図鑑の **タブの行の 右はし**（🎀📤） | — |
| 合言葉 | `#wrap` の中（盤面だけ覆う） | **なし**。短いので下のボタンをもう一度押す |
| キャラクターの詳細 | `#wrap` の中。図鑑のカードの **i** から | 「戻る」で図鑑へ（入れ子なので） |
| おかしの国 | `#wrap` の中 | 「戻る」で図鑑へ（入れ子なので） |
| 仲間を選ぶ・たまご・アバター・今週のおだい・ショップ | `#wrap` の外（**画面ぜんぶ覆う**） | **必要**。下のボタンが隠れて戻れないため |

開いている画面のボタンは光ります（`syncNav`）。入れ子の画面は
どこから開いたかを `kingFrom` / `eggFrom` / `avFrom` で覚えておきます。
- `openPanel` は**先に表示してから中身を作る**。隠れたままだと幅が0で、
  グラデーションの半径がマイナスになって描けない（おかしの国で実際に起きた）
- **長い画面（図鑑・ガイド）の見出しは `position:sticky` で のこすこと。**
  下までスクロールすると 閉じるボタンも タブも 画面の外に出て、
  「戻れない」と 言われました（実機の報告）。うわに 12px の padding が
  あるので `top:-12px` で ぴたりと はりつきます
- 見出しにボタンを足したら **320/375/393/430px で タイトルが切れないか**
  見ること。「✕ 閉じる」と 書いたら 393px で「コレクション 5...」に
  なったので、記号だけにしました
- 「コレクション N/M」は **かぞえている ならびの 真上**に 置きます。
  数はタブごとに変わるので、見出しのとなりだと 何の数か 分かりません
- **たて方向の flex（`#colOv`）に 何かを 足すときは `flex:0 0 auto` を
  つけること。**ないと 高さ0に つぶれて **文字が まるごと 消えます**。
  `getBoundingClientRect()` の いちは 出るので、目でも 検査でも
  「そこに ある」ように 見えます（じっさい 1回 消しました）

### 横に並ぶ札は、文字の幅をブラウザで測って確かめない

図鑑の記録カード（自己ベスト・倒した数・順位の目安）は、1つでも折り返すと
その札だけ数字が下がって、3つの数字が横にそろわなくなります。

**Playwright（Linux）で測った幅は当てになりません。**iPhoneの丸ゴシックは
代わりに使われるフォントより広いので、393pxで収まって見えても実機では
折り返します（実際にそうなりました）。

だから決めうちのpxではなく、次の3つで守ります。

1. `white-space:nowrap` — 折り返させない（折り返す＝ずれる、なので）
2. `fitStat()` — 出したあとに測って、入らなければ0.55倍まで縮める。
   **札は3つとも同じはば**にしておくこと（1つだけ広げると見た目がそろわない）

たしかめるときは `.cn` に `letter-spacing` を足して**わざと広いフォントを
まねます**。+20%でも1行に収まれば実機でも大丈夫です。

## 文字の大きさは `--fs` ひとつで変える

CSSの `font-size` はぜんぶ `calc(11px * var(--fs))` の形にしてあります。
`:root` の `--fs`（いま **1.10**）を変えれば、UIの文字がまとめて変わります。

かけられないところが2つあります。

| | なぜ |
|---|---|
| **21px以上の絵文字アイコン**（🧸 🎀 🥚 など） | 入れものの幅・行の高さが固定。大きくすると枠から出る |
| **横に5つ並ぶタイルの中の文字**（`--fsT`、いま1） | 幅が画面の1/5で決まっているので、少しでも大きくすると「キャンディ」「図鑑から貼る」が折り返し、5つの高さがそろわない |

**盤面の高さは、下のUIの高さを実際に測って決めています**（`resize()`）。
むかしは `window.innerHeight - 384` の決めうちで、`--fs` を変えるたびに
この数字も手で直す必要がありました（1.10にしたとき 362 → 384）。いまは
`#app` の子のうち**流れの中にあるもの**の高さを足して引いています。

- 測るのは「**下のブロックの高さ**」。アドレスバーの出入りでは変わらないので安定します。
  むかし失敗したのは「**はみ出し量**」を測るやり方で、あれは `innerHeight` が
  ゆれると一緒にゆれて、盤面が縮み続けました
- `position:absolute` の画面（たまご・ショップ・おだい・仲間を選ぶ）は
  **数えないこと**。流れの外なので場所を取りませんが、数えると開いたとたんに
  盤面がつぶれて、閉じても戻りません
- **測るあいだはパネルをふだんの状態（ショップバー）に戻すこと。**タワーを
  えらぶとショップが情報カードに入れかわります。iPhoneの丸ゴシックは広いので
  実機ではカードの文字が折り返してパネルが高くなり（Linuxでは折り返しません）、
  その高さで測るとタワーをえらんだとたんに盤面が縮み、とじると戻って
  びくびく動きます。同期のうちに戻すのでちらつきません
- レイアウトができていない起動直後は 0 になるので、そのときだけ **312** を使います。
  下のUIをつめたら、この数字も一緒に直すこと（説明文を消して 382 → 298 になった）

#### たての余白は 1つけずるごとに 盤面が のびる

盤面は **たて12マス**で、高さは「まどの高さ − 下のUIぜんぶ」です。
だから下のUIから **12px けずるごとに マスが 1px 大きく**なり、
盤面の幅は 9px のびます。

説明文（`#tip`）を消して、`#app` のすきま 7→5・余白 8→5、
HUD・ワザバー・ショップ・ボタンの たてpadding を 2px ずつ つめた結果：

| | 前 | 後 |
|---|---|---|
| 下のUIぜんぶ | 382px | **298px** |
| 盤面（Safari 660） | 216×288 | **261×348**（面積で 1.46倍） |
| 盤面（standalone 852） | 342×456 | **369×492** |

ショップ・今週のおだい・合言葉を 図鑑の中へ 移したのも 効いています
（1行ぶん 37px ＝ マス3つぶん）。**盤面の高さを 1pxも 取らない場所へ
うつすのが、いちばん よく効きます。**

- 見出しの 名前は **出したあとに 測って きめます**（`fitHead`）。
  決めうちの `@media` では 実機で はみ出します ——**Linux で 測った幅は
  iPhone の 丸ゴシックより せまく**、393px では 余裕が 25px しか ないためです。
  はみ出すと ✕が 画面の 外に 出て「戻れない」に なります。
  横に ならぶ札の `fitStat()` と 同じ 考えかたです

  | すき間の のこり | 出す 名前 |
  |---|---|
  | 8px 以上 | 🏅 今週のおだい |
  | それ未満 | 🏅 おだい |
  | さらに 足りない | 🏅 だけ |

  ひらいたときと `resize()` で よびなおすので、よこ向きにも ついてきます
- **見出しの 余裕は `#colHeadSp`（すき間）の のこり幅で 測ること。**
  `scrollWidth` は すき間が 縮んで 吸うので、はみ出す 手前まで
  `clientWidth` と 同じ数字を 返し、**余裕が 0でも 合格に 見えます**
- 合言葉は タブの ならび（横スクロールする）なので 文字を つけられます。
  タブの よこ余白を 9→7px に つめて、393px で ちょうど 1行に 収めました
  （375px 以下は 少し スクロールします）

- **よこの余白（10px）は へらさないこと。**盤面はたて長で、あまるのは
  横のほうです。へらしても盤面は1pxも大きくなりません
- **下の余白は 20px ＋ セーフエリア。5px では足りません**（実機の報告）。
  ボタンの かげ（`box-shadow:0 4px 0` と `0 6px 16px` のぼかし）は
  `getBoundingClientRect()` に入らないので、`resize()` が測る高さより
  20pxほど下にはみ出します。iPhoneのホームバーのぶんも要ります
  - 見た目のすき間は「この余白 ＋ マスの切り上げぶん（0〜11px）」です。
    14pxにしたときは切り上げが吸って**見た目が1pxも変わりませんでした**。
    ここをいじったら数字ではなく **innerHeight − ctrlの下端** で確かめること
  - **`visualViewport.height` に かえないこと。**iOSのバーの出入りで
    盤面が縮みつづけます（`resize()` のコメントにある 過去の失敗と同じ）。
    下が足りないときは **余白で取る**のが正しい直しかたです

#### 実機でしか出ないときは、数字を画面に出す

「写真だと余白があるのに実機だと無い」のように、こちらで再現できない
報告があります。**iPhoneのスクリーンショットにはホームバーが写りません。**

ガイド（📕）のいちばん下に、その場の数字を出してあります（`showDiag`）。

```
はいしん: 08/29/2026 14:06 ／ まど 700 ／ 見えてる 700 ／ ホームバー 0 ／ 下のすき間 20
```

| | 分かること |
|---|---|
| はいしん | 古いキャッシュを見ていないか |
| まど / 見えてる | ちがえば ブラウザのバーが かぶっている |
| ホームバー | 0なら `env(safe-area-inset-bottom)` が効いていない |
| 下のすき間 | ボタンの下に実際に何px あるか |

`env()` はCSSの中でしか使えないので、その高さの見えない箱を1つ作って
測っています（`safeBottom`）。

**「下のすき間」が「ホームバー」より小さいと、ボタンがホームバーの帯の中**
です。⚠ が出ます。実機の報告のときは 34 に対して 31 でした。

##### 起動直後に1回はかるだけでは足りない

**iOS はセーフエリアをすぐには返しません。**0のまま測ると 34px ぶんを
見のがし、そのぶん下が画面の外に出ます。`resize()` は何回よんでも同じ
結果になるので、落ちついてから何度かはかりなおします。

```
window.addEventListener('load', resize);
setTimeout(resize, 300); setTimeout(resize, 1200);
document.fonts.ready.then(resize);
```

起動直後の決めうち（298）にも `safeBottom()` を足してあります。
- **高さが 変わるところは ぜんぶ 決めうちにすること。**下のUIの 高さは
  `resize()` が 1回 測って 盤面を 決めるので、あとから 高くなると
  そのぶんが **画面の 外に 出ます**（スクロールできず 切れる）。
  - `#panel` の `min-height`（91px）… タワーをえらぶと ショップが
    情報カードに 入れかわるため
  - `#ctrl .btn` の `height`（46px）… ウェーブのボタンが
    「次のウェーブ」1行 →「ウェーブN／進行中」2行 に かわるため。
    実機で 42px → 64px に のびて 下が 切れました（報告）。
    2行のときは `waveBtn()` が `.two` を つけて 字を 小さくします
  - `.stat .sub` の `min-height`… 3つの札の 下の行（げんき・ベスト）は
    中みが 出たり 消えたり するので、から でも 1行ぶん 場所を とらせます。
    ないと **はじめての 記録が 出た しゅんかん HUDが 11px 高くなり**、
    そのぶん 下が はみ出します

**げんき（`life`）は HUD の ハートの札に 出します。**むかしは 盤面の うえに
💗＋ピンクの帯で えがいていましたが、ステージ名が 見えにくいので 外しました。
`hearts`（あと何回あそべるか）とは 別ものなので、**どちらか片方だけを
出すのは だめ**です ——げんきが 0で ゲームオーバーなのに、
のこりが 見えないと 何も できません
- 説明文を消したので、**ガイドへの案内が画面から消えました**。かわりに
  **はじめての人にだけ 📕 を光らせます**（`hintGuide`／`sweetTD.guided`）。
  文字ではなく光にしたのは、場所を1pxも取らないためです
  （足したぶんだけ盤面が縮むので、案内に高さを使うのは本末転倒）
  - おぼえかたは `giveStarters` と同じ、localStorage の旗 1つ
  - **金のふちは出しっぱなしにする**こと。波紋だけだと、消えている
    一しゅんに見た人が気づけません
  - `prefers-reduced-motion` のときは波紋を止めて、ふちだけ残します
- ショップの札は `.tw .ic`（絵のわく）が いちばん たかい 40px です。
  中の canvas は 46px で **わざと はみ出させて**いるので、わくを つめても
  絵は 小さくなりません。ここを 40px より つめると 名前に かぶります

### 出しっぱなしのカードは 出しなおす

タワーをえらぶと出る情報カードは、**タップしたときの数字のまま固まって**
いました。`openInfo()` の中でしか書いていなかったためです。

レベルの上限（`maxLv`）は ウェーブで上がるのに、カードを開いたままだと
ボタンは「最大レベル」でおせないまま。**遊ぶ人は もう育てられないと思って
手を止め、⭐だけたまっていきます**（実機の報告：ウェーブ85・Lv9で
上限は 11 だった）。

- 絵をつくる部分（`openInfo`）と、数字を書く部分（`syncInfo`）を分けて、
  **`syncHUD()` から毎回よぶ**。⭐は敵をたおすたびに変わるので、
  ウェーブの変わり目だけでは足りません
- `syncInfo` は カードが閉じていたら すぐ帰るので、ふだんは
  `classList.contains` 1回ぶんです
- **「最大レベル」とは書かないこと。**上限はウェーブでのびつづけるので
  うそになりますし、そこで手が止まります。かわりに
  `W120でLv12` のように **次が開くウェーブ**を出します

同じ形のバグは「開いたまま外の状態が変わる画面」すべてにあります。
出しっぱなしにできる画面を足したら、更新の口も一緒に作ること。

### 「敵だけ小さく左上を歩く」は save/restore の対のくずれ

3回の実機報告の正体はこれでした。`drawTower` に、消したブロックの
`ctx.restore()` だけが残っていて（かざりを消したとき・66d5667）、
**タワーを1体えがくたびに外がわの保存を1つよぶんに食う**状態でした。

- タワー1体 → `render()` 自身の保存（角丸クリップ）を食う。見た目ほぼ変わらない
- **タワー2体 → Phaser（Painter）の保存まで食う。変換が等倍にもどり、
  そのあとの敵・弾・キラキラがぜんぶ 1/DPR（0.4倍）で左上にえがかれる**

「タワーを置くまでは正常、2体置いたとたんに敵が小さく道を外れる」という
症状のときは、まず save/restore の対を数えること。

- `ctx.save()` と `ctx.restore()` は**かならず同じブロックの中で対にする**。
  ブロックを消すときは対ごと消す
- `node tools/check-path.mjs` がタワーを2体以上置いてから、**1フレームの中で
  変換（`getTransform().a`）が変わらないか**を見ます（`?dbg=1` の `__dbg._as`）。
  わざと restore を足すと落ちることを確認済み
- この検査がすり抜けた原因は「タワーを置かずに調べていた」こと。
  **えがくものの組み合わせ（タワー＋敵）まで実戦と同じにして調べること**

### 「キャラが小さく道を外れて歩く」は canvas の消しのこり

いちばん たちの悪いバグでした。**キャラが小さく、左上のほうに かたまって、
道を外れて歩く**——これは キャラの位置の話ではなく、**canvas の消しのこり**です。

`render()` は `clearRect(0, 0, W, H)` で消していました。`W`/`H` は CSSピクセル、
canvas の実寸は `W×DPR`。**この2つがずれると、左うえの一部しか消えません。**
古い大きな絵がそのまま残り、その上に新しいフレームだけが小さく重なるので、
「キャラだけ小さくて位置がおかしい」ように見えます。

- 消すのは **`ctx.canvas.width/height`（実寸）で全部**。CSSピクセルで消さないこと
- canvas の実寸が `W×DPR` とずれていたら**毎フレーム直す**。
  Phaser の `scale.resize()` はすぐ反映されないことがあります
- **`render()` の中で canvas をリサイズしないこと。**あそこは Phaser の
  `save()`/`restore()` の内がわで、canvas を作りなおすと Phaser の状態がこわれます。
  直すのはループの中（`groundKey` を見ているところ）で

`node tools/check-path.mjs` が、わざと canvas を2倍にして赤で塗り、
次のフレームで赤が0になるかを見ます（直す前は 292万px 残りました）。

### 敵が 道の 外を あるいていたら pathPts を うたがう

道のいち（`pathPts`）は `CELL` をつかった**ピクセル**です。`CELL` が変わったのに
`measurePath()` を呼びわすれると、**敵だけ古いマスの大きさで描かれて道から外れます**。
地面（`bakeGround`）は `CELL` を見て焼きなおすので、地面だけ正しくなって
敵が芝生の上を歩きます。

とりでを2つ置いてあります。**どちらも毎フレーム見ています。**

- `measurePath()` は はかったときの `CELL` と道の形をおぼえます。
  ずれていたらループが自分ではかりなおします（敵の `e.d` も比で つけかえる）
- `groundKey()` に**道の形も入れて**あります。国・マス・DPR・周が同じまま
  道だけ変わる道すじがあると、古い道が残るためです

```
node tools/check-path.mjs        # 終了コード 0 で合格
```

### ボタンの 文字と disabled は かならず 対で 動かす

ボーナスウェーブの あと、`endBonus()` が **文字だけ**「次のウェーブ」に
もどして `disabled` を 戻しわすれて いました。うすいまま 押せず、
カウントダウン（`nextT`）が 0の ときは **そこで ゲームが 進められなく
なります**（実機の 報告：ウェーブ59）。

**目では 見つけられません。**「次のウェーブ」と 書いてあるので、
ぱっと見は 正しい 画面に 見えます。

```
node tools/check-btn.mjs         # 終了コード 0 で合格
```

ものさしは 1つだけ ——**「つぎに 進める」と 見せて いるのに 押せない、を 作らない。**
`waveBtn()` を よぶ ところは 11か所 あるので、足すときは
`btnWave.disabled` も その場で 決めること。

**さがすのは 文言では なく `btnWave.dataset.next`** です（`nextWaveBtn()` が
つけ、`waveBtn()` が 消す）。むかしは 「次のウェーブ」の 文字で さがして
いましたが、ボタンの 文言を 変えた とたん **検査が 空ぶりしました**
（合格と 出るのに 何も 見て いない）。2か所に 書くと ずれる、の 一例です。

### ボタンには「つぎに 戦う ウェーブ」を 書く

> **記録（2026-09-02）**：実機の 報告「一旦60ウェーブに なったら
> 負けても 通常59には 戻らないはずなのに 59に 戻ってしまうのは 何？」

`wave` は **「さいごに 戦った ウェーブ」**です。`startWave()` が
先に `wave++` してから 始めるので、`wave = 59` は「つぎは 60」を 意味します。

ゲームオーバーから「つづきから」を おすと `continueRun()` が `wave` を
1つ もどします。**落とした ウェーブを やりなおす**ための ものですが、
HUD の 数字だけ 見ると **60 → 59 に 降格した ように 見えます**。

| | HUD | ほんとうは |
|---|---|---|
| ① W59 を クリア | ウェーブ **59** | つぎは W60 |
| ④ W60 に まけて つづきから | ウェーブ **59** | つぎは W60 |

**①と④は まったく 同じ 状態**なのに、遊ぶ人には「戻された」ように
見えます。だから **ボタンに 行き先を 書いて** 取りちがえを なくしました
（`nextWaveBtn()`）。`wave` にも `startWave()` にも `continueRun()` にも
**さわって いません。表示だけ**です。

#### 文字数に 気をつける ——320px だけで なく **393px も** せまい

`#ctrl .btn` は 高さ **46px の 決めうち**で、2行に おれると 下が 切れます。
`<br>` が 無いと `waveBtn()` は `.two` を つけないので **字も 小さく
なりません**。

**「ウェーブ200へ」では だめでした。**320px と **393px（主力の 画面）**で
2行に おれます。393px が 375px より せまいのは、`max-width:389px` を
こえると まるいボタンが 44 → 50px に なって、その ぶん ここが やせる ため。
ウェーブ200 は ふつうに 通る ところ（かべは W250）なので 使えません。

だから **`W○○`** に して、数は **`fmtN()` で 丸めます**
（情報カードの「W120でLv12」と おなじ 表記。⭐や こうげきと おなじ
「万・億」の 書きかた）。ぴったりの 数は すぐ 上の HUD に 出て います。

#### **決めうちの けた数で 守っては いけません**

`fmtN()` は いちばん 長いと **「9999万」の 6文字**に なり、320px と
393px で やはり 2行に おれます（実測：W1000万 で 2行）。
字の はばは 端末の フォントで 変わるので、ここで「何けたまで」と
決めても 実機では 外れます。

だから **出したあとに 行を かぞえて**、おれて いたら もとの
「次のウェーブ」に もどします（`fitWe()` `fitHead()` `fitStat()` と
まったく 同じ 手）。どの 端末・どの ウェーブでも ひとりでに 収まります。

行を かぞえるのは **まん中の たかさで まとめる**こと ——▶ の SVG は
字とは 別の わくを かえすので、上ばしで かぞえると おなじ行でも
2行に 見えます（`weLines()` の 失敗と 同じ）。まだ 出て いない ときは
わくが 0こ で 0行に なるので、そのまま 数の ほうを のこします。

| 320px | 出る 文字 |
|---|---|
| W59 → | 次は W60 |
| W249 → | 次は W250 |
| W99999 → | 次は W10万 |
| **W9999999 →** | **次のウェーブ**（おれるので もどす）|
| W99999999 → | 次は W1億 |

#### 高さは 1pxも 変えて いない

6画面で 実測：`#hud` 59.17／`#panel` 83・94.19／`#skillBar` 70.44／
`#ctrl` 46／盤面 288・360・492・540・444・516。よこの はみ出しも なし。
ボタンより 上の 画素も、**前↔後の 差が ぜんぶ「同じ版どうしの ゆれ」の
中**に 収まって います（320px：ゆれ 0.577〜0.943 に 対して 信号
0.665〜0.867）。

焼いた地面を1マスずつ読んで、**その国の道の色（p1/p2）**と地面の色（g1/g2）の
どちらに近いかで判定します。**色を決めうちにしないこと** —
こおりのくには道が水色で、クリーム色を前提にすると全部まちがえます。

### 盤面が細くなるのは Safari のバーのせい

9×12 のマス目は**たて長**です。iPhone の Safari はアドレスバーと下バーで
**190pxほど**持っていくので、盤面に使える高さが足りず、幅が余ります。

| iPhone 15 Pro（393px幅） | 盤面 | 幅をつかえた率 |
|---|---|---|
| Safari（バーあり・innerHeight 660） | 261×348 | 66% |
| ホーム画面に追加（standalone・852） | 369×492 | 94% |

**これは不具合ではなく、たて長の盤面と横長のあき場所という形の問題です。**
`manifest.webmanifest` の `display:standalone` があるので、
**ホーム画面に追加すれば解決します**。スクロールでバーが隠れたときも同じです。

たしかめかたは、320 / 375 / 393 / 430px の4つで

- 下の3つのボタン（ショップ・今週のおだい・合言葉）が**1行に収まっているか**
- 仲間バーの名前と「図鑑から貼る」が**折り返していないか**
- ページ全体の高さが `window.innerHeight` をどれだけ超えるか

を測ります。320pxだけは元から収まりません（そこはスクロールする前提）。

## SNSで見せることを意識する

- **最初の3秒**で「かわいい」と伝わる画面にする。起動直後から色とキラキラが見えている状態にする
- 動画やGIFで撮ったときに**動きが分かる**ようにする。エフェクトは大きめ、長めに
- 1プレイが短く、何度も繰り返せるようにする
- 数字が増える瞬間（スコア、報酬）は必ず**浮かび上がる文字**で見せる

## ホーム画面のアイコン

`apple-touch-icon` が無いと、iPhoneは「ホーム画面に追加」でページの縮小画像を
アイコンにしてしまいます。だから次の5つを置いています。

| ファイル | どこで |
|---|---|
| `apple-touch-icon.png`(180) | iPhoneのホーム画面 |
| `icon-192.png` / `icon-512.png` | Android・PWA |
| `icon-maskable-512.png` | Androidが好きな形に切りぬく版 |
| `manifest.webmanifest` | 名前・向き・色 |

**もとは 1まいの 原画だけ**です（`art/icon/app_icon_source.png`・1254x1254）。
`node tools/make-icons.mjs` が そこから 7つ ぜんぶ 切り出します。
**手で 作らないこと** ——絵を 差しかえたときに 作りなおせなくなります。

むかしは アイコンも コードで えがいていましたが、支給された 絵に
なったので やめました（git の 履歴に あります）。原画の 記録と
差しかえの 手じゅんは `art/icon/README.md` に あります。

- **角を自分でまるめない**。iOSもAndroidも自分で切りぬくので、二重になります。
  ふちまで色を塗った四角を出すこと。
  **1回 やりました** ——はじめに もらった 原画は 角丸が 焼きこまれていて、
  iOS の マスクを かけると すみの **8.2%** が 黒く のこりました。
  角丸なし版を もらって 差しかえてあります
- **maskable は中身を すこし小さく**（いま `k=0.88`）。あいた ところは
  **同じ絵を ぼかして** 敷きます（表紙の `#titleBg` と 同じ手）。
  **縮めすぎないこと** ——0.72 だと 内がわの 四角い ふちが 目立って、
  額に 入れた 絵に 見えます。むかし 0.72 だったのは、コードで えがいた
  アイコンの ぼうや みみが 細くて 切れたためで、いまの 原画は
  お城が まん中に あるので そこまで 縮める 必要が ありません
- **アルファを 持たせない**（App Store の 1024・ITMS-90717）。
  canvas の `toDataURL()` は かならず RGBA で 出すので、生の画素を もらって
  カラータイプ2の PNG を 自分で 焼いています

## UIアイコンは `art/ui/`（絵文字を やめていく）

ボタンや 見出しの しるしは、スマホの 標準の 絵文字を やめて
**専用の 絵**に していきます。端末で 形が ちがう・このゲームの
絵柄と 合わない・大きさが `font-size` に しばられる、の 3つが 理由です。

```
node tools/make-ui-icons.mjs      # 原画 1まいから 4つ 切り出す（終了コード 0 で 合格）
```

| | |
|---|---|
| 原画 | `art/ui/_reference/phase1_source.png`（1536x1024・**さわらない**）|
| 見本 | `art/ui/_reference/icons_v1_source.png`（絵柄の きまり）|
| ゲームが 読む もの | `art/ui/*.webp`（**128x128**・品質0.92・透過）|

- **256px は 要りません。**画面で いちばん 大きい UIアイコンは 38px
  （ワザバー）なので、DPR3 でも 114px。128px で じゅうぶんです
- 置き場は **`art/ui/`**。`art/sprites/` に 入れると
  `check-chars.mjs` が「`ART_KEYS` に ない」と 落ちます
- `tools/_files.mjs` の **CORE** に `'art/ui'` の 1行だけ。
  図鑑・ガイド・音の ボタンは 起動した しゅんかん 画面に あるので、
  REST に すると 初回だけ 予備の SVG が 出て、あとから ぱっと 変わります
- 原画は `_source` を つければ `expand()` の `SKIP` が はじくので、
  アプリにも オフラインの キャッシュにも 入りません

### 背景つきで 届いた シートは「ますごとに」抜く

Phase 1 は はじめから 透過でしたが、**Phase 2 は 背景つき**で 届きました
（ピンク〜ラベンダーの グラデ＋ますごとの かざり枠）。
`make-ui-icons.mjs` の `keyOut()` が 抜きます。手順は 3つ。

1. **ますごとに** 背景を 二次曲面で 当てる（はずれ値を すてながら 5回）
2. ふちから つながった 背景を ぬり、**いちばん 大きい かたまり だけ** のこす
   （ますの かざり枠・きらめきが 落ちる）
3. **外がわから もう一度** たどって、うすい モヤ（絵に 焼きこまれた ひかり）を 落とす

**シート全体で 背景を 当てては いけません。**ずれが 21ほど 出ますが、
絵の うすい 白は 背景と **22〜26 しか はなれて いません**（実測：屋根の
白いしま 26・わくの 内がわ 22）。**ますごとなら ずれは 3.0〜3.3**に 下がるので、
しきい値 13 で 分けられます。

**やわらかくするのは「ふちに 接する 画素」だけ。**うすい 所を まとめて
半とうめいに すると、**クリームの 面が すけて 穴に なります**（1回 やりました。
封筒も 紙も 屋根も 中が 市松に すけました）。中は 255 の ままに します。

**外がわの モヤは 外からしか たどらないこと。**輪郭に かこまれた 中の うすい所
（屋根の 白いしま・鏡の 中）は たどりつけないので のこります。しきい値は 42
——26 では 封筒の まわりの モヤが 落ちきりませんでした（8とおり 見くらべ）。

検査は `p90`（背景モデルの ずれ）が 12 を こえたら 落とします。

### かたまりが 2つ ある 絵は `keep` で のこす

ふだんは **いちばん 大きい かたまり だけ**を のこします（ますの かざり枠や
きらめきを 捨てるため）。ところが かけら（`cur_shard`）は **大きい 結晶と
小さい かけら の 2つ**で できているので、そのままだと 小さい ほうが 落ちて
ただの 結晶に なります。

シートに `keep:0.08` を 書くと「いちばん 大きい ものの 8%以上」の かたまりも
のこします。**ふだんは 書かないこと** ——書くと かざり枠や きらめきまで
拾いかねません。2つ以上で 1つの 絵に なっている ときだけ です。

### Phase 5A ——HUD の ⭐💗（**盤面の 高さに 直結する ゆいいつの 場所**）

`art/ui/` の 絵は これまで ぜんぶ `position:absolute` の 画面か
高さが 決めうちの ボタンの 中でした。**HUD だけは ちがいます**
——`#hud` は `#app` の 流れの 中に あるので、1px 高くなれば
`resize()` が その ぶん 盤面を 縮めます。

| | |
|---|---|
| 大きさ | **21px**（`UI_PX.hud`）|
| たての 余白 | **0**（`display:block` で まん中に そろえる）|
| `.stat .v` の 固定 height | **足さない** |

`.stat .v` は font 20.9px・line-height 22.99px なので、**21px までは
行が のびません**。22px で +0.30px、24px で +1.30px（実測）。
**上げないこと。**

#### `vertical-align:middle` の ままだと 0.79px 下がる

`swapSpot()` の ふだんの 書きかた（`display:inline-block` ＋
`vertical-align:middle`）は、絵を **親の `<span>` の 文字の ベースライン**に
そろえます。HUD の 絵の 親は `.v`（`align-items:center` の flex）では なく
その中の `<span>` なので、**数字より 0.79px 下**に つきました（実測）。

`display:block` に すると span の 高さが 絵と おなじ 21px に なり、
`.v` の `align-items:center` が まん中に そろえます（**ずれ 0.00px**）。
`UI_SPOT` の 5つめに 書く CSS で 差しかえます。

#### `UI_KEYS` には **つかう 絵だけ** のせる

`art/ui/` には `cur_gem`（💎）と `cur_shard`（🩷）も ありますが、
5A では どこも つかって いないので **`UI_KEYS` にはのせません**
（読みこむ だけ むだに なる）。5B で つかう ときに 足します。
ファイルは 置いて あるので、`sw.js` の CORE（`art/ui`）には 入ります。

#### 6画面の 基準（さわったら ここを くらべる）

| 画面 | `#hud` | 盤面 `#cv` | `.stat .v` |
|---|---|---|---|
| 320x568 | 59.17 | 288 | 22.98 |
| 375x667 | 59.17 | 360 | 22.98 |
| 393x852 | 59.17 | 492 | 22.98 |
| 430x932 | 59.17 | 540 | 22.98 |
| 360x800 | 59.17 | 444 | 22.98 |
| 412x915 | 59.17 | 516 | 22.98 |

**`#colGrid` の 高さは くらべないこと。**あそこは スクロールの 総丈で、
**おなじ 版を 2回 撮っても ±0.22px ゆれます**（実測。ゆれる 画面も
そのつど ちがう）。ここを 見て「ずれた」と 早とちり しないこと。

### Phase 5B ——DOM の ⭐💎💗🩷 は 表に せず、変化を 見て さしかえる

通貨の 記号は **60か所ほどに ちらばって います**。しかも ショップ・図鑑・
ガイド・お祝いの 文は ひらくたびに 作りなおされるので、`UI_SPOT` のように
**呼ぶ ところを 数えあげる やりかたでは 足しわすれが かならず 出ます**。
だから `paintCur()` を `MutationObserver` に つないで、
**変わった ところ だけ**を さしかえます（呼ぶ ところは 0か所）。

- **`textContent` を `innerHTML` に 変えた ところは 1つも ありません。**
  出たあとの **文字ノード**を さしかえるので、文字列を HTML に する
  危なさが そもそも 生まれません
- **画面ぜんたいを 毎回 たどらない**こと。ウェーブの ボタンは 1秒に 4回
  書きかわりますが、見るのは その ボタンの 中だけ です。
  実測 60fps・DOMの 変化は 3秒で 3件
- 自分が 入れた `<img>` で また 呼ばれない よう、`curBusy` で ふたを する

#### 大きさは その場の 文字から 出す

**1つの px に そろえては いけません。**盤面の ショップバーは 10.5px、
商品の 絵は 26px で、おなじ 大きさでは どちらかが かならず はみ出します。

```
px = min(18, max(14, round(font-size × 1.15)))
```

決めうちは **2つ だけ**（`CUR_PX`）——`#shop`＝12px と `.spIc`＝28px。
この 2つは 文字の 大きさから 分けられません（ショップバー 10.5px と
ショップの 説明文 10.4px が ほぼ 同じ なので）。

| どこ | 出た 大きさ |
|---|---|
| 盤面の ショップバー | 12px |
| ガイド本文・ショップの 説明 | 14px |
| おだい・たまごの ボタン | 15px |
| ねだん・持っている 💎 | 16px |
| たまごの 持ちもの・おだいの ボタン | 18px |
| 商品の 絵（💗ハートぜんかい）| 28px |

#### おなじ 字でも 意味が ちがう ものは `.dic` で よける

⭐ は **3つの 意味**を もっています ——通貨の おほしさま／`TOWERS.star.ic`
（スターの タワー）／`ITEMS.rain.ic`（⭐の雨の アイテム）。
**まとめて さしかえては いけません。**

よけるのは `CUR_SKIP` の 6つ。

| | なぜ |
|---|---|
| `#hud` | Phase 5A が べつの 大きさ（21px・`display:block`）で もっている |
| `#infoIc` | `TOWERS[k].ic`。スターの タワーの 絵 |
| `.itIc` | `ITEMS[].ic`。⭐の雨の アイテムの 絵 |
| `.skN` | ワザの 名前「⭐の雨」 |
| `.dic` | データの ic を そのまま ならべる ところ（下の 5か所）|
| `#weOv` / `#titleOv` | さわらない きまり |

`.dic` を つけたのは この 5か所です。**ここに ⭐ が まざるので、
つけわすれると スターの タワーの ⭐ が 通貨の 絵に なります。**

- ガイドの「お菓子（タワー）の 得意分野」（`TOWERS[k].ic`）
- ガイドの「アイテム」（`it.ic`）
- ステージクリアの ドロップの 文（`it.ic`）
- ショップの おたすけセット／にじいろセットの 説明（`ITEMS` の ic を ならべる）

#### ねだんの はこは 1px ちぢむ。それで 正しい

絵文字は 字より 背が 高いので、`<img>` に すると 行の わくが すこし
低くなります。`.spP` 31→30・`#shopGem` 29→28・`.spIc`（💗の 行）31→30。

**ここを 0 に しようと 余白を 足しては いけません。**-4 から 0 までは
どれも 30 で、+1 に すると 32 に なって **ショップの 総丈が 1px のびます**
（実測）。**行・見出し・総丈は 1pxも 変わって いません**
（`.spIt` 77.63/60.31・`#shopBody` 941・`#helpBody` 3363〜3918・
`#hud` 59.17・盤面 288/360/492/540/444/516）。

### Phase 5C ——canvas の ⭐💎💗🩷 は 段に わけて 1行に する

canvas は `<img>` に できないので `drawImage`。**呼ぶ ところ
（`floatTx` の 11か所）は 1つも 書きかえて いません。**出た 文字列を
その場で「文字」と「絵」の 段（`curSegs`）に わけ、`drawCurLine` が
1行として 置きます。

- **絵が まだ 読めて いない 記号は 文字の まま のこす。**だから
  読みこみ前は これまでの 絵文字が そのまま 出ますし、1つも 読めて
  いなければ **これまでどおり 1回で 書きます**（見た目が 完全に 同じ）
- **総幅を 先に 出してから 置く。**`measureText` の 合計 ＋ 絵の 幅。
  絵が 入っても **行の まん中は 1pxも 動きません**（17ケースで 実測）
- **大きさは 決めうちに しない。**そのときの 字の 大きさ × 1.05
  （盤面の 字は `CELL*0.42` なので、320px で 11px・430px で 20px）
- たては 絵の まん中を 字の まん中に そろえる。だから
  **⭐💗🩷💎 が まざっても 上下に ばらけません**

#### ふちどりは 字を ぜんぶ 先に

うかぶ文字には 白い ふちどり（`strokeText`）が ついています。
**段ごとに stroke → fill に しては いけません** ——つぎの 段の
白い ふちが 前の 段の 字を 上から 消します。
`drawCurLine` は「ふち ぜんぶ → 字 ぜんぶ → 絵 ぜんぶ」の 3周です。

**絵じしんには ふちどりを つけません。**毎フレームの ぼかしは
60fps を 44 まで 落とすため（まじょの オーラで 実測ずみ）。
この 絵は 金の ふちが 焼きこんで あるので、それで 読めます。

#### さわった 状態は かならず もどす

`textAlign` / `textBaseline` / `globalAlpha` / `fillStyle` / `strokeStyle`
を 入ってきた ときの 値に もどします。**ここを もらすと、
そのあとの えがきものが ぜんぶ ずれます**（「敵だけ 小さく 左上を歩く」と
同じ たぐいの こわれかた）。

#### データ由来の ⭐は `floatTx` の 5つめで よける

`it.ic + ' アイテム「…」+1'` の 先頭は `ITEMS.rain.ic` の ⭐で、
**通貨では ありません**。`floatTx(x, y, text, color, true)` の
5つめ（`dataIc`）で よけます。DOM がわの `.dic` と おなじ 考えかたで、
**呼ぶ ところを 変えたのは この 1か所だけ**です。

#### 記録シェアは 👑 と ⭐ を おなじ しくみで

Phase 4 の 👑 だけの 手組みを やめて、`drawCurLine` に まとめました。
先頭の 👑 を 段の 1つめに 置き、⭐ は 文字列から わけて もらいます。
**片方だけ 読めた ときも 正しく 出ます**（👑は 絵・⭐は 絵文字で
1行の まん中は そのまま）。

#### 折りかえしの 数を `getClientRects()` で かぞえない

インラインの `<img>` は 文字とは **別の わく**を 返すので、おなじ行でも
1行 ふえて 見えます。じっさい ガイドと ショップで「折りかえしが ふえた」と
出ましたが、**箱の 高さは 1pxも 変わって いませんでした**。
かぞえるなら **箱の 高さ ÷ 行の 高さ**で 見ること
（`weLines()` の「まん中で まとめる」と 同じ 話の 続きです）。

### Phase 5D-0 ——データ由来の アイコンは `dataIcon()` で 決める

`ITEMS` / `TOWERS` / `SHOP_ITEMS` … の 1件から「何を えがくか」を かえします。
**決める じゅんばんは この 4段。上から 見て、無ければ 下へ 落ちます。**

| | |
|---|---|
| ① `rec.icKey` ＋ しっぽ | 進化（`_s2`）・属性（`_fire`）の 絵 |
| ② `rec.icKey` | その品ものの 絵（`art/ui/`）|
| ③ `rec.art` | いまある スプライト（`art/sprites/`）|
| ④ `rec.ic` | **絵文字。ぜったいに 消さない** |

**なぜ 先に これを 作るのか。**アイコンを 決めている ところは
10か所以上に ちらばって います。このまま 属性（火・砂・水）や 進化を
足すと、足すたびに 全部を 直すことに なります。

**属性・進化の しくみ そのものは まだ ありません。**ここは 受け口だけ。
絵が 無ければ ②③④へ 落ちるので、用意する 前でも こわれません。

- **属性を 絵に 焼きこまないこと。**アイテム7種 × 属性5種 = **35枚**に
  なります。小さな バッジを 別に 作って すみに 重ねれば **7＋5＝12枚**
- **属性は データの がわに 置くこと**（`rec.elem`）。
  `ic:'🔥🧪'` のように 文字列に 混ぜては いけません
- `opt.allow` で どこまで つかってよいかを しぼれます。ガイド本文のように
  小さい ところで スプライトを つかいたくない ときは `'sprite'` を 外します
- `dataImg(rec, px, opt)` は **絵文字の ときは `null` を かえす**ので、
  呼ぶ側は これまでの 絵文字を そのまま のこせます

#### 5D-0 で つないだのは 2か所だけ

もともと「スプライトが あれば 絵、無ければ 絵文字」＝ **③→④ そのもの**
だった ところ だけを 移しました。だから **見た目は 変わりません**。

| どこ | 大きさ |
|---|---|
| ワザバーの アイテム（`.itIc`）| 38px |
| 図鑑の アイテムカード | 64px |

`#infoIc`（`TOWERS[k].ic`）や ガイドの `.dic` は **いま 絵文字だけ**なので、
つなぐと ③に 上がって 見た目が 変わります。**5D-1 の しごと**です。

#### 変わったのは 属性 2つ だけ（見た目では ない）

`<img>` に `alt`（その品ものの 絵文字）と、図鑑カードに `width`/`height` が
つきました。**箱・計算後のCSS・絵の いちは 6画面すべてで 完全に 同じ**です
（`alt` は 絵が 出ない ときにしか 見えません）。

#### 決める じゅんばんは 機械で しらべる

`__dbg.dataIconTest()` が にせの 絵を さしこんで、
①②③④ と `allow` の 落ちかたを 9とおり たしかめます。
**目では 見つけられません** ——絵が 無い いまは どの段を 通っても
同じ絵が 出るためです。

### Phase 5D-1 ——いまある 絵だけで 置きかえる（新規 0枚）

**新しい 絵は 1枚も 作って いません。**Phase 2・3 の `nav_week` /
`nav_egg` と、盤面の スプライトを つかいまわしました。

| どこ | 何に | 大きさ |
|---|---|---|
| 情報カード `#infoIc` | 盤面と おなじ **似顔絵**（`charThumb('tw_'+k)`）| 31px |
| おだいの ボタン `#wkGo` | `nav_week` | 18px |
| 仲間を選ぶ・たいけつ・クリアの文 の 🥚 | `nav_egg` | 14〜30px |
| ボーナスウェーブの 6種 | `candy/donut/cookie/cupcake/cake/purin.png` | `CELL*0.95` |

#### 情報カードは `#panel` の 高さを 1pxも 変えない

`#infoIc` は **`#app` の 流れの 中**です。ところが `#info` の 高さを
決めているのは `#infoTx`（61.08px）なので、**絵を 24〜46px の
どれに しても `#panel` は 変わりません**（6画面で 実測）。
いまは 31px ——もとの `font-size:31px` と 同じ。

**`charThumb` を つかうこと。**`dataIcon()` は `art/sprites/` の
生の 絵を かえしますが、盤面の ショップバーは `charThumb`（似顔絵＋つや）
です。生の絵に すると **同じ子なのに バーと カードで ちがって 見えます**。

#### あとから 作られる 画面は 見はり役に のせる

ショップ・仲間を選ぶ・たいけつは ひらくたびに 作りなおされるので、
`paintUiIcons()` の 1回では 足りません。呼ぶ ところを 数えあげると
足しわすれが 出るので、**5B の `MutationObserver` に `paintSpotIn()` を
のせました**（呼ぶ ところは 0か所）。

> **`textContent = '…'` は 文字ノードが 足された 形で 来ます。**
> 要素だけを 見ていると、たいけつの 題（`#duT`）と ボタン（`#duGo`）を
> **取りこぼします**（じっさい 取りこぼしました）。親の 要素へ 上げること。

#### 小さすぎる ところは 据え置く

図鑑カードの 🥚（`.cTt`・**9.9px**）は のせて いません。
25件 ぜんぶ 絵文字の ままです。**ここだけ つぶれます。**

#### ボーナスは わくでは なく「中みの 大きさ」で そろえる

スプライトは まわりに 余白が あるので、絵文字と おなじ `CELL*0.72` に
すると **8px 小さく 見えます**（実測）。

| 倍率 | 中みの たて | 絵文字（31.8px）との さ |
|---|---|---|
| 0.72 | 23.7px | −8.1 |
| 0.86 | 28.7px | −3.1 |
| **0.95** | **31.0px** | **−0.8** |
| 1.05 | 35.0px | +3.3 |

**🍓🍭 は 絵文字の まま**です。絵が ありません ——**8種を むりに
そろえないこと。**無い ものを こちらで 作ると 画風が ずれます
（`dataIcon()` の ③→④ が 1つずつ 決めるので、まざっても こわれません）。

### Phase 5D-2-0 ——こだいの秘薬（⚗️）に 専用の 絵

**にじいろの秘薬（🧪）と 同じ絵が 出て いました。**どちらも
`art:'it_elixir'` を 指していたためで、ワザバーに **べつの 切りふだが
2つ 同じ絵で ならんで**いました（名前でしか 見わけられない）。
Phase 5 より 前から あった 不具合です。

| | |
|---|---|
| 新しい 絵 | `art/ui/it_elixir3.webp`（128x128・品質0.92・透過）|
| 原画 | `art/ui/_reference/phase5d20_source.png`（1098x1432・**さわらない**）|
| つなぎ | `ITEMS.elixir3` に `icKey:'it_elixir3'` を 1行 |

**特例の えだは 1つも 書いて いません。**`dataIcon()` の ②が 拾い、
`elixir` は `icKey` を 持たないので ③の まま `it_elixir.png` です
（**1pxも 変わりません**）。

#### `art:'it_elixir'` は **消す**こと

のこすと、絵が 読めなかった ときに ③で また 🧪 と 同じ絵に なり、
**直したい ことが そのまま 起きます**。消せば ②→④ で
**⚗️ の 絵文字**に 落ちます（実測ずみ）。

#### 色ちがいでは なく **シルエット**で 分ける

「上位の 秘薬」を 色だけで 表すと、14pxでは 同じ形の 色ちがいに 見えます。
たてに 細い 瓶＋王冠＋星の 頭で、**形そのもの**を 変えました。

| | 中みの 大きさ | たてよこ比 | 地との さ | こい画素% |
|---|---|---|---|---|
| 🧪 `it_elixir`（まるい フラスコ）| 88x104 | **0.846** | 26.2 | 0% |
| ⚗️ `it_elixir3`（たてに 細い 瓶）| 58x122 | **0.475** | **66.0** | 14.2% |

32x32 の 形の かさなり（`check-dup.mjs` と 同じ ものさし）は **IoU 0.551**。
しきい値 0.86 を 大きく 下まわります。

#### たてに 細い 絵は うめ具合の しきい値を さげる

`make-ui-icons.mjs` は「わくの 25%以上」を もとめますが、たてに 細い 絵は
四角い わくを うめられません（この絵は **21.9%**）。**ぜんぶを さげては
いけません** ——ほかの 絵の 見はりが ゆるみます。その絵の `plan` に
`minFill:20` と 書いて、そこだけ さげます。

### Phase 5D-2-A ——ショップの 26px の 行（新規 7枚）

`SHOP_ITEMS` と `SHOP_THEMES` の レコードに `icKey` を 1行ずつ 足しただけです。
**特例の えだは 1つも ありません** ——`dataIcon()` の じゅんばんが 決めます。

| キー | | 絵文字 |
|---|---|---|
| `shop_start` | はじめてパック（リボンの 箱）| 🎁 |
| `shop_pass` | 30日パスポート（王冠と 時計の 券）| 🎫 |
| `shop_help` | おたすけセット（もちもの入りの かばん）| 🎒 |
| `shop_rainbow_set` | にじいろセット（**宝箱**）| 🌈 |
| `theme_dream` | ゆめいろパステル（**にじと 雲**）| 🌈 |
| `theme_lemon` | レモンのおかやま | 🍋 |
| `theme_mermaid` | にんぎょのうみ | 🐚 |

**🌈 の 2つは 別の キー・別の 絵**です。おなじ 絵を 共有しては いけません
——にじいろセット（品もの）と ゆめいろパステル（着せかえ）は べつの もので、
⭐ が 3つの 意味を もっていたのと 同じ形です。

#### `.spIc` は 31 → 30px に ちぢむ。それで 正しい

5B の 💗（ハートぜんかい）と おなじ 1px です。**そろえるために
`margin:-4px 0` を つけること** ——つけないと 35px に のびて、
💗の 行だけ 30px で ちぐはぐに なります。
**行（77.63/60.31）・見出し（17）・ねだん（30）・注記・総丈は
1pxも 変わって いません**（6画面で 実測）。

#### 形だけでは 見わけられない ——それでよい

ご指定の「色を 見なくても 区別できるか」を 測りました。**結論は
「色つきなら はっきり 区別できる。形だけでは できない」**です。

| | 32x32 の 形の かさなり（IoU）|
|---|---|
| 🎁 はじめて ↔ 🎒 おたすけ | **0.890** |
| 🍋 レモン ↔ 🐚 にんぎょ | 0.789 |
| 🌈 セット ↔ 🌈 ゆめいろ | 0.759 |
| 21組 ぜんぶ | **すべて 0.6以上** |

**原因は 絵が 悪いのでは なく、7つとも「四角い ますを うめる 一枚絵」**
だからです。きらめき・さんご・花・地面が まわりを うめるので、外がわの
形は どれも 同じ かたまりに なります。すきとおりの しきい値を 240まで
上げても 0.89 → 0.868 にしか なりません（＝きらめきの せいでは ない）。

**それでも 採用しました。**ショップの 品ものは **26px・1行に 1つ・
名前つき**で ならぶので、横に くらべる 場面が ありません。
色と 中みでは ひとめで 分かります（26/20/16px で 見くらべずみ）。

> **形で 見わけたい ときは、絵を「もの 1つだけ」に する**こと。
> まわりの 景色を 入れると、どんな 題材でも 外がわは 四角い かたまりに
> なります。コードでは 直せません。

### Phase 5D-2-B ——アイテム 6種（新規 6枚）

`ITEMS` の 6件に `icKey` を 1行ずつ 足しただけです。**特例の えだは ありません。**

| キー | | いまの 絵文字 |
|---|---|---|
| `item_ball` | にじいろボール（4方向の 衝撃光つき オーブ）| 🌈 |
| `item_bolt` | いなずま（よこ方向の 電撃波）| ⚡ |
| `item_hammer` | ハンマー（一点衝撃の 星）| 🔨 |
| `item_elixir` | にじいろの秘薬（まるい胴の フラスコ）| 🧪 |
| `item_freeze` | こおりのつぼ（ふたつき＋雪のけっしょう）| 🧊 |
| `item_rain` | おほしさまの雨（雲から 星が 降る）| ⭐ |

**`art:'it_*'` は のこします。**⚗️ こだいの秘薬（5D-2-0）とは ちがい、
③に 落ちても **正しい 絵**（いまの スプライト）が 出るためです。

#### キーは `item_*`。`it_*` に しない

`art/sprites/it_ball.png` と おなじ 名前に すると、1つの レコードに
`icKey:'it_ball'` と `art:'it_ball'` が ならんで **読み分けられなく なります**。

#### いちばん だいじなのは ワザバー（38px）

**7つが よこに ならぶ ゆいいつの 場所**です。ここが そろっていないと
新しい 絵が かえって 浮きます（5D-2-0 の あと、⚗️ だけ 輪郭が あって
のこり 6つが うすい 状態に なっていました）。

| | 前 → 後（26px の 地との さ）|
|---|---|
| `item_freeze` 🧊 | **16.6 → 83.9** |
| `item_hammer` 🔨 | 38.2 → **96.4** |
| `item_elixir` 🧪 | 26.2 → 77.3 |
| `item_ball` 🌈 | 32.1 → 72.8 |
| `item_rain` ⭐ | 25.7 → 64.1 |
| `item_bolt` ⚡ | 23.2 → 43.3 |

輪郭（こい画素）も **0% → 2.9〜36.8%** に なりました。

#### 🌈 と ⭐ の 取りちがえは 形で 解いた

| | 前 → 後 |
|---|---|
| 🌈 ボール ↔ 🌈 ゆめいろパステル | **0.760 → 0.575** |
| 🌈 ボール ↔ 🌈 にじいろセット | 0.655 → 0.542 |
| ⭐ の雨 ↔ ⭐ スターのタワー | 0.683 → 0.658 |
| ⭐ の雨 ↔ ⭐ 通貨 | 0.476 → 0.425 |
| 🧪 ひやく ↔ ⚗️ こだい | 0.551 → **0.476** |

**ボールを にじの アーチや 宝箱に しないこと。**⭐の雨も
**1つの 星に しないこと**（雲から 降る 形に する）。
どちらも「絵文字が 同じ」だけで 中みは 別の ものです。

#### `IoU` が 上がっても 見た目で 決めてよい

🧊 こおり ↔ 🐚 にんぎょのうみ は **0.521 → 0.812 に 上がりました**。
どちらも わくを うめる 密な 絵に なったためです。
けれど 色は こい 青 と うすい 真珠いろで、**26px でも 一目で 別物**です。
5D-2-A と 同じで、**外がわの 形の かさなりは 密な 絵では 飽和します**。
数字だけで 決めず、かならず 並べて 見ること。

### Phase 5D-2-C ——ボーナスの 🍓🍭（新規 2枚）

`BONUS_KEY` の 1行と `UI_KEYS` の 2キーだけ。特例の えだは ありません。

#### ここだけ **`art/ui/` の 画風に しては いけません**

ならぶ 相手が ちがいます。5D-2-A / 5D-2-B は Phase 1〜5C の アイコンと
ならびますが、**ボーナスは 盤面の スプライト**（candy / donut / cookie /
cupcake / cake / purin）と ならんで 降ってきます。

| | 30px・空の 色（`#e9ecf7`）の 上で の 地との さ |
|---|---|
| ボーナスの 6スプライト | **21.1〜29.4**（平均 25.1）|
| `art/ui/` の アイコン | 65.5〜76.0 |
| いままでの 🍓 絵文字 | **101.8**（＝いちばん 浮いていた）|
| 新しい `bonus_ame` | **24.8** ✅ |
| 新しい `bonus_ichigo` | 51.5（いちごは もともと 色が こい。それでも 絵文字の 半分）|

#### `pad:0.78` を わすれない ——これが いちばん こわい

盤面の スプライトは **256pxの わくの 中で 中みが よこ0.76・たて0.78**
しか ありません。`CELL*0.95` の **1つの 倍率で 8種 ぜんぶ**を えがくので、
わくいっぱい（`PAD` 0.955）で 切ると **この 2枚だけ 22% 大きく** 見えます。

`make-ui-icons.mjs` の `plan` に `pad` を 書けるように しました。
実測：新しい 2枚の たては **0.789**、既存6つの 平均は **0.783**。

#### ここでは 形の 見わけは 要りません

ボーナスは **降ってくる ものを ぜんぶ タップする** 遊びなので、
どれが 何かを 見わける 必要が ありません。じっさい 既存6つの 形の
かさなりは **donut ↔ cookie が 1.000**（どちらも わくの 8割を うめる 真円）で、
15組 ぜんぶ 0.525〜1.000 です。

**5D-2-A / 5D-2-B の IoU の ものさしを ここに 持ちこまないこと。**
要るのは「見える」「画風が そろう」の 2つだけ です。

#### 検査どうぐと えがく ところは 同じ 式を 見る

`bonusRec(em)` に まとめて あります。**2か所に 書くと ずれます**
——じっさい 検査どうぐだけ `icKey` を 見ておらず、
「🍓🍭 は まだ 絵文字」と うそを 報告しました。

### Phase 5D-2-D ——「仲間を選ぶ」の 🧸（新規 1枚）

`UI_SPOT` に 2行と `UI_KEYS` に 1キーだけ。特例の えだは ありません。

| どこ | 大きさ |
|---|---|
| 盤面の 札 `#shop .tw[data-k="__pick"] .ic` | **40px** |
| 仲間を選ぶ 画面の 見出し `#pickHead > div` | 20px |

#### **46px に しては いけません** ——320px で ページが 3px はみ出す

`#shop` は `repeat(5,1fr)` の grid で、**`1fr` は `minmax(auto,1fr)`＝
中みより 細くは なりません**（図鑑の `#colGrid` で 一度 やった 失敗と
まったく 同じ）。46px の 絵を 入れると 5つめの 札が
**47.94 → 54px** に ふくらみ、320px の 画面で 右はしが 317.09 → 323.16 に
なって **ページが よこに 3px はみ出します**。

**`check-title2` が 見つけました。**あの検査は 表紙の 話に 見えますが、
ページ全体の `scrollWidth` を 見るので、**うしろの 盤面の はみ出しも 拾います**。
目でも、ショップバーだけ 見ていても 気づけません。

40px なら 札は **48px**＝もとの 47.94 と ほぼ 同じ（+0.06px）。

> **タワーの 似顔絵は 46px なのに なぜ 入るのか。**あちらは `.cs` の
> 「⭐110 ×0」が すでに 札を 53.69〜61.69 に 広げて いるためです。
> 5つめの 札は 「選ぶ」しか 無いので 細く、絵が そのまま 効きます。

#### 絵は よこに 長いと 小さく なる

3体が よこに ならぶ 絵なので、**たてよこ比が そのまま 大きさに ひびきます**。
3回 作りなおして 2.11 → 1.82 → **1.74** に なりました。

| | 中みの 大きさ | 比 | 面積 | 地との さ | こい画素% |
|---|---|---|---|---|---|
| いままでの 🧸 絵文字 | 31x36 | 0.86 | 1116 | **119.9** | 65.1% |
| `nav_pick`（1回目）| 40x20 | 2.00 | 800 | 37.3 | — |
| `nav_pick`（2回目）| 40x22 | 1.82 | 880 | 36.6 | 1.9% |
| **`nav_pick`（いま・3回目）** | **40x23** | **1.74** | **920** | **37.1** | 2.2% |
| tw_candy / ice / star | 36x30〜32 | 1.13〜1.20 | 1080〜1152 | 51.7〜54.9 | 0〜2.1% |

**色の 浮きは 直りました**（119.9 → 37.1。こい画素も 65.1% → 2.2%）が、
**面積は まだ 4つより 15%ほど 小さい**です。

> **よこに 長い 絵は この 箱では 大きく できません。**
> 40px の わくの はばが 上限なので、たては **はば ÷ 比**で 決まります
> ——比 2.11 で 20px、1.82 で 22px、1.74 で 23px。
> 4つと おなじ 大きさに するには **比 1.2 前後**が いります
> （そうすれば 40px の わくで 38x32 に なり ぴたりと そろう）。
> **コードでは 直せません。**左右の 2体を「耳と 頭の てっぺんだけ」まで
> 中央の うしろへ 深く 重ねる ——絵の 作りかたで しか 縮みません。
>
> **4回とも そこまでは 縮みませんでした。**どの版も 左右の 2体が
> 顔ぜんぶ（目・ほっぺ・口）を 見せて わくの 左右の はしまで のびて
> います。**中央の クマを 大きくするだけでは 比は ほとんど 動きません**
> ——利いているのは 左右の 張り出しの ほうだからです。

#### **もう 作りなおさないこと**（2026-09-02 に 決定）

4回目の 素材（比 1.67・40x24・面積 960・地との さ 33.8）も 作って
**ならべて 見くらべた うえで、3回目の いまの 版を 採用しました。**
4回目の 得は **たて 1px** だけで、地との さは かえって 下がりました。

**ねらいの 38x32（比 1.15〜1.25）に するには、左右の 2体を
「耳と 目の 一部だけ」まで 隠す しか ありません。**そこまで すると
**「仲間が 何人も いる」という 意味が 伝わらなく なります。**
数字より そちらの ほうが 大事なので、**ここで 打ちどめ**にします。

いまの 版で **できて いること**は この 2つ。ここが 目的でした。

- 濃い 茶色の 異物感が 消えた（絵文字の 地との さ 119.9・こい画素 65.1%
  → **37.1・2.2%**）
- ショップバーの ほかの 4つと **画風が そろった**（色・つや・パステル感）

**のこる ずれ**は 大きさだけ（面積が 4つより 15%ほど 小さい）。
これは 構図その ものの 話で、**コードでも 切りかたでも 直りません。**

#### 透過は 見た目では 分からない。PNG の カラータイプで 見る

3回目の 途中で 届いた 1枚は、**すきとおりの 市松もようが 絵として
焼きこまれた** ものでした（カラータイプ2＝アルファなし。市松は
`#fefefe` と `#f6f6f6` の 塗り）。プレビューでは 透過に 見えます。

**そこから アルファを 作りなおしては いけません。**なめらか処理の
ふちに 灰いろが 混ざり、半とうめいは もう 戻せず、しかも 市松の 白と
絵の 白い ハイライトが 見わけられません。出しなおして もらうこと。

```
python3 -c "import struct;d=open(F,'rb').read();print(struct.unpack('>IIBB',d[16:26]))"
# 4つめが 6 なら RGBA（透過あり）／2 なら RGB（透過なし）
```

### 絵文字の さしかえは `UI_SPOT` の 表で

**HTMLは 1文字も 変えません。**絵文字は そのまま のこして、絵が 読めた ときに
`swapSpot()` が 上から さしかえます。だから 絵が 読めない ときは
いままでの 絵文字が 出て、ボタンが 空っぽに なりません。

- **`innerHTML` を 丸ごと 書きかえないこと。**中の `<span class="pwTx">` が
  作りなおされて、`fitHead()` が 出し引きした 状態が 消えます。
  **文字ノードだけ**を 分けて 差しこみます
- さしかえた あとは `fitHead()` を 呼びなおす（はばが 変わるため）
- Phase 2 の 置き場所は どれも `position:absolute` の 画面の 中なので、
  大きさを 変えても **盤面の 高さには ひびきません**（`resize()` は 数えない）。
  6画面で `#hud` `#panel` `#skillBar` `#ctrl` と 盤面が 1桁まで 同じ ことを 実測ずみ

### `const` は 巻きあがらない

`paintUiIcons()` は `INLINE`（インラインSVGの style）を つかいます。
`const INLINE` を **あとに** 置いたら、呼ばれた しゅんかんに
`ReferenceError` で ゲームごと 止まりました。**先に 置くこと。**

### 絵が 読めるまでは SVG を 出す（消さないこと）

`bookSvg` / `crownSvg` / `noteSvg` / `keySvg` / `playSvg` は
**予備として のこして あります。**`paintUiIcons()` が

1. まず SVG で ボタンを うめる
2. `art/ui/*.webp` が 読めたら はめなおす

の 順で 動きます。アイテムの 絵（`ART_SPRITE`）と まったく 同じ 段どりです。
**消すと、電波が 無いときや 絵が 読めなかった ときに ボタンが 空っぽに なります。**

### 大きさは かならず px。em に しない

ボタンの 高さ（`#ctrl .btn{height:46px}`）も ワザバーも 決めうちです。
アイコンが 行の 高さを 決められる 書きかたに すると、
**下のUIが 高くなって そのぶん 盤面が 縮みます。**

じっさい HUD の 💗⭐ を `font-size × 1.15` の `<img>` に して ためしたら
**59.2 → 61.8px（+2.6px）**に なりました。高さを 行に 収めても 0.3px のこります。
だから **`UI_PX` の 決めうち（ボタン30px・文の中22px）**で 書いて あります。

**足したら 見た目では なく 高さの 数字を くらべること**
——`#hud` `#panel` `#skillBar` `#ctrl` と 盤面（`#cv`）の 高さが
6画面すべてで 1pxも 変わっていないことを 実測してから 出します。

### 音の ON と OFF は 別の 絵

むかしは 音符 1つを `opacity:.45` に していましたが、
**うすいだけでは「切れて いる」のか「押せない」のか 分かりません。**
いまは `snd_on` / `snd_off` の 2まいを `syncSndIcon()` が 入れかえます。

**2まいは 同じ わくで 切ること**（`make-ui-icons.mjs` の `PAIR_W/PAIR_H`）。
別々の わくで 切ると ベルの いちが ずれて、押すたびに ガタつきます。
検査が わくの ずれを 見て、4px を こえたら 落とします（いま 1px）。

### 絵は 文字より 高い。たての 負の 余白を わすれない

`swapSpot()` と `uiOr()` が 入れる `<img>` には **`margin:-4px 0`** が
ついています。ないと 行の 高さが のびます。

じっさい 忘れた ときは、ガイドの 見出しが **56.0 → 56.4px**（+0.4px）に なり、
本文が まるごと 0.4px ずれて **画素の ちがいが 3.35%**に なりました
（見た目は 同じでも、字の なめらか処理が ぜんぶ 変わるため）。
-2px で もう 足ります。まわりの 高さは padding と line-height で
決まっているので、へこませても つぶれません。

**足したら 画素で くらべること。**「見た目が 同じ」では 見つかりません。

### 閉じるだけ 24px

CLAUDE.md の きまりで「✕ は 見つけやすさ が いちばん だいじ」。
ほかと 同じ 20px に したら、**わくの 中の ✕ が いまの 文字より
見つけにくく なりました**（14〜40px で 見くらべて 決めた）。
はこは 36x32 なので 24px でも 入り、高さは 1pxも 変わりません。

文字の となりに ならぶ ✕（ガイドの「✕ 閉じる」）は 20px の まま
——あちらは 字が 助けて くれるので。

### 図鑑と ガイドは シルエットで 見わける

`#ctrl` では **となり同士**に ならぶので、両方を「本」に しては いけません。

| | 形 |
|---|---|
| 図鑑（`nav_dex`）| **王冠**の 紋章（たてよこ 同じくらいの 四角）|
| ガイド（`nav_guide`）| **ひらいた 本**（よこ長）|

16px まで 縮めて ならべて 確かめて あります。

### おなじ 絵文字でも 意味が ちがう ことが ある

ガイド本文の 👑 を まとめて 図鑑の 絵に しては いけません。
**敵の 見かたの ところ（`<li>👑 ボス：小さい子を呼びます</li>`）は
「ボスの 印」**で、図鑑では ありません。🛡かたい子・💨はやい子と
ならんだ ならびの 1つです。ここは 絵文字の まま のこして あります。

置きかえたのは **図鑑を 指している 👑 だけ**（4か所）。
絵文字は 見た目が おなじでも、**何を 指しているかを 1つずつ 読む**こと。

### ガイドの ▶ は 白地用に 色を 変える

`playSvg(n, st, ink)` の `ink` を つけると こいピンクに なります。

- ふだんの ▶（`PLAY`）は **こいピンクの ボタンの上**に 出るので 白で 塗る
- ガイドの 見出しは `.hSec{background:rgba(255,255,255,.8)}` の **白**。
  白い 三角は 輪郭しか 見えず、**中みと 地の さは 1**しか ありませんでした
  （見出しの 字は 112）。`PLAY_INK` に 変えて **136.6** に なりました

**形は 変えないこと。**「次へ・ひらく」の 意味は 形で 伝わって います。
色を 見出しの 字（`--pink-d`）に そろえ、上半分だけ 明るくして
ほかの アイコンと おなじ つやを 出して います。

### canvas の 絵文字は `drawImage`

記録シェアの 画像（`makeShareCard`）は canvas なので `<img>` に できません。
`g.drawImage()` で 置きます。**まん中ぞろえの ままだと 絵の ぶん ずれる**ので、
「絵＋すき間＋字」の 幅を `measureText` で 出してから 左ぞろえで 置きなおし、
そのあと `textAlign` を もどします。絵が 読めて いなければ
これまでどおり 絵文字で 1行 書きます。

> **検査の 注意。**`file://` で ひらくと キャラの 絵で canvas が よごれて
> `toDataURL` が 例外に なり、シェア画像を 取り出せません。
> `python3 -m http.server` で 出してから 撮ること。

### 導入画面（`#weOv`）の 📕 と ▶ は さわらない

あの画面は「完成済み・原則 変更禁止」で、`check-welcome.mjs` が
`weLines()` で **行数を 数えて** います。📕 を 画像に すると
行の 高さが 変わって、あの 検査の 前提が くずれます。
**1つだけ 絵文字が のこりますが、そのままに すること。**

## 絵を Gemini に 描いてもらう

`tools/gen-art.mjs` が キャラの 絵を 出します。アイコン（`make-icons.mjs`）と
おなじで、**プロンプトは ファイルの中に 置きます**。チャットで打ったプロンプトは
のこらないので、色や作りを変えたときに 同じ絵へ たどりつけなくなるためです。

- 絵がらの きまりは `STYLE` の1か所。ここを変えると全員そろって変わります
- 出す子は `ART`。キーは `CREATURES` の `opt.key` にそろえてあります
- **モデル名は書きこまない。**`--models` で API に きいて決めます。
  モデル名は変わるので、書きこむとある日とつぜん動かなくなります

### キーは ヘッダで おくる（`?key=` では ない）

キーには2つの形式があります。**新しい方はヘッダでしか通りません。**

| 形式 | いつの | どう送るか |
|---|---|---|
| `AIza…` で始まる（Standard key） | 古い | URLの `?key=` でも通った |
| `AQ.` で始まる（Auth key） | 2026年以降 AI Studio が出すのはこちら | **`x-goog-api-key` ヘッダ** |

公式ドキュメントは「すべてのリクエストに `x-goog-api-key` ヘッダを付けること」と
書いています。`?key=` は古い書きかたで、`AQ.` のキーだと 401 になることがあります。
**ヘッダならどちらの形式でも通る**ので、`call()` はヘッダ1本にしてあります。
URLにキーが乗らないぶん、ログにも残りません。

なお Standard key は 2026年9月に使えなくなります。`AIza` を前提にした
書きかたに戻さないこと。

#### 401 が 出たら、まず キーが 古くないか うたがう

切れた キーは こう 返します。

```
401 UNAUTHENTICATED / reason: ACCESS_TOKEN_TYPE_UNSUPPORTED
"Expected OAuth 2 access token, login cookie or other valid authentication credential."
```

**この 文だけ 読むと「形式が ちがう」と 読めますが、ちがいます。**
`AQ.` の キーは ヘッダで ちゃんと 通ります（実測。`models` 一覧も
画像生成も 200）。切れて いるだけです。

**でたらめな キーと 見くらべても 分かりません。**でたらめな `AQ.…` と
切れた `AQ.…` は **1文字も ちがわない 返事**を 返します
（`AIza` は「API key not valid」と 400 を 返すので 見分けが つく）。
だから「形式の せいだ」と 早とちり しないこと ——じっさいに 1回 やりました。

**たしかめかたは 1つだけ。新しい キーで 打ちなおす。**
環境変数は コンテナ起動時に 1回しか 読まれないので、
**古い キーが 残ったまま**に なりがちです。

### APIキーの あつかい

**リポジトリには ぜったいに 置かない。** GitHub Pages は中みをだれでも
ダウンロードできるので、コミットした時点でだれでも使えてしまいます。

置き場所は クラウド環境の 環境変数（`GEMINI_API_KEY`）ですが、**そこは
秘密を置く場所として 作られていません。**公式ドキュメントに
「環境を使う人はだれでも値を読めるので、APIキーや認証情報は入れないで」と
書かれています（専用のシークレット保管庫がまだ無いため）。

それでも使うので、次の3つで しのいでいます。

| | |
|---|---|
| **この用途だけのキーを作る** | 漏れても これだけ 無効にできる。ほかのキーは 生きたまま |
| **予算上限をつける** | 万一のとき 請求が 青天井に ならない。ただし**通知するだけで 止まらない** |
| **絵の作業が終わったら キーを消す** | 出しっぱなしに しない。**確実に止まるのは これだけ** |

環境変数は コンテナ起動時に 1回だけ 読まれるので、設定したら
**新しいセッションを開く**こと（動いているセッションには 入ってきません）。

## App Store に 出す

**手じゅんと 文言は `docs/appstore.md` に ぜんぶ 書いてあります。**
ここには、あとから さわる ときに 引っかかる ところ だけ。

1.0 は **課金なし・iPhone だけ・4+・たて持ち**で 出します。
年齢の 質問の「漫画・ファンタジーの 暴力」は **「なし」**です ——敵は
傷つかず、たおれると キラキラに なって 図鑑に 入り 味方に なるため。
**「軽度」に すると 9+ に なります。**見た目を こわい方向に 直したら、
ここも 見なおすこと（`docs/appstore.md`）。
StoreKit も 銀行口座も 要らないので、審査で 止まる ところが いちばん 少ない形です。

```
node tools/build-www.mjs      # アプリに 入る ぶんを www/ に あつめる
node tools/shots.mjs          # スクリーンショット 6まい（1320x2868）
node tools/make-icons.mjs     # icon-1024.png と assets/（4〜5分 かかる）
```

ビルドは **Mac なしで GitHub Actions の macOS ランナー**（`.github/workflows/ios.yml`）。
手で 走らせる ときだけ 動きます。鍵は Secrets に 4つ 入れます
——**リポジトリには ぜったいに 置かないこと**（公開なので だれでも つかえる）。

### 買えない ねだんを 画面に 出さない

課金を 入れるまで、💎パックの **¥320 などを 出したままには できません。**
買えないのに ねだんが 出ていると Apple の 2.3.1（かくれた 機能）に かかります。

`IAP_ON`（`index.html`）**1つ**で、💎パック・復元ボタン・年齢と上限が
まとめて 引っこみます。中みの コード（`payGems` / `canPayYen` /
`recordYen` / `receiptOwned` / `restorePurchases`）は **消しません**。

ただし **「`true` に するだけ」では ありません。**課金を 入れるには
ほかに **有料Appの 契約（銀行口座・米国の 税務書類）**と、
**WebView から StoreKit を よぶ ネイティブの 橋わたし**（Capacitor の
課金プラグイン）が 要ります。手つづきの ほうが 重いです
——`docs/appstore.md` の 8章に ぜんぶ 書いて あります。

### アプリには 20MB だけ 入れる

`art/` の 直下には 生成したときの 原画が 160まい・**114MB** ありますが、
ゲームが 読むのは `art/sprites/` と `art/screens/` と `title_main.webp` だけです。
まるごと つつむと 130MB を こえて モバイル回線で 落とせなくなります。

`tools/build-www.mjs` の **`KEEP` に 書いた ものだけ**を `www/` に あつめます。
**絵や ページを 足したら KEEP にも 足すこと。**足しわすれると
web では 動くのに **アプリだけ 絵が 出ません**。さいごに
「index.html が 読む ファイルが ぜんぶ あるか」を 機械で たしかめるので、
足しわすれれば そこで 落ちます（`art/screens` を わざと ぬいて 14件 出るのを 確認ずみ）。

### アイコンは アルファを 持てない

App Store の 1024x1024 は **すきとおりの チャンネルが あると はじかれます**
（ITMS-90717）。canvas の `toDataURL()` は かならず RGBA で 出すので、
`make-icons.mjs` は 生の画素を もらって **カラータイプ2（RGB）の PNG を
自分で 焼いて**います。角も まるめません（iOS が 自分で 切りぬくため）。

- **生の画素を 配列で わたさないこと。**1024x1024 は 420万個の 数に なり、
  ブラウザから Node へ うつすだけで 2分 以上 かかりました。base64 の
  文字列なら 一瞬です
- 2732x2732 の 起動画面は、GPUの ない この環境では えがくのに 4〜5分
  かかります。止まったように 見えても 待つこと

### Playwright の ありかを 決めうちにしない

道具は ぜんぶ `tools/_pw.mjs` の `launch()` を とおします。
`/opt/node22/...` や `/opt/pw-browsers/chromium` を 直に 書くと、
**手もとでは 通るのに GitHub Actions だけ 落ちます**（あの パスが 無い）。

## ネイティブへの 橋（NATIVE）と オフライン

App Store 版と web版を **同じ 1つの コードで** 出しています。
そのために 大事なのが この 2つです。

### WebView に 無い ものは `NATIVE` を とおす

`Capacitor` は **アプリの 中にしか いません。**うっかり 直に よぶと
web版で `undefined` に なって そこで 止まります。だから 入口を
`NATIVE`（`index.html`）1つに まとめてあります。

```
NATIVE.on          アプリの 中で 動いているか（ブラウザでは false）
NATIVE.plugin(名)  プラグインを とる。無ければ null
```

**「無い ときは しずかに 何も しない」**が いちばん だいじです。
これから 足す ものも おなじ 形に します。

| 足すもの | どこに つなぐか |
|---|---|
| ふるえ | `vib()`（**もう つないである**・`@capacitor/haptics`） |
| 課金 | `payGems()` の 中みだけ さしかえ |
| 通知 | `LocalNotifications`。💗が たまったら 教える |

ふるえは **リズムを 自分で 組みたてます**。ネイティブの ハプティクスは
「トンッ」の 単発しか 出せないので、`[30,50,30]` の ならびを
鳴らす→待つ→鳴らす に ほどきます。長さで 強さも 変えます
（40ms以上＝HEAVY／20ms以上＝MEDIUM／それ未満＝LIGHT）。
**実機で ためしていません**（この環境に iPhone が 無い）。
鳴らなくても 落ちない 作りに して あります。

### web版の オフラインは Service Worker

アプリ版は 絵も コードも 中に 入っているので はじめから オフラインですが、
**ブラウザ版は 毎回 GitHub Pages から 読んでいました。**
`sw.js` を 置いて、一度 あそべば 電波が 無くても あそべるように しました
（実測：電波を 切って ひらきなおしても 表紙・盤面・ショップの絵まで 出る）。

```
node tools/build-sw.mjs           # sw.js を 作りなおす
node tools/build-sw.mjs --check   # 古いままなら 落とす
```

- **2段がまえ**。ぜんぶ（12MB）を 最初に ためると はじめて 来た人の
  待ち時間が のびるので、**CORE 2.4MB を すぐ**・**のこり 9.9MB は
  ゲームが 動きだしてから うしろで** ためます
- のこりは **1つずつ**。`addAll` で まとめると 1つ こけただけで
  9.9MB が まるごと むだに なります
- 版は **中みの ハッシュ**。日づけに しないこと ——中みが 同じでも
  ビルドの たびに 全員の キャッシュが 捨てられます
- **`sw.js` が 古いまま 出ると、直したのに 遊ぶ人には 古いままが 出ます。**
  しかも エラーが 出ないので 報告されるまで 気づけません。
  だから **デプロイの ワークフローに `--check` を 入れて**あります
- いま どの 版で 動いているかは **ガイド（📕）の いちばん下**に 出ます。
  「直したのに 直っていない」の 切りわけは まず ここ

### ファイルの ならびは `tools/_files.mjs` 1か所

アプリ（`build-www`）も オフライン（`build-sw`）も おなじ ならびを 見ます。
**2か所に 書くと かならず ずれます** ——アプリには 入っているのに
オフラインでは 出ない、その逆、が 起きます。

**`*_source.*` は 入れないこと。**`art/screens` を ディレクトリで
書いていたので、出しなおす ため だけの 原画 13まい・**6.7MB** が
アプリにも キャッシュにも 入っていました（18.9MB → **12.3MB**）。
`expand()` が まとめて はじきます。

## 検証

変更したら Playwright（Chromium）で実際に操作して確認してください。ブラウザは `/opt/pw-browsers` に入っています。

- JavaScriptエラーが0件であること
- ゲームオーバー・クリア・リトライまで一通り到達できること
- スクリーンショットを撮って、見た目が崩れていないこと

### fps を 合否に つかう 検査は **単独で 走らせる**

> **記録（2026-09-09）**：`lg_berryqueen_e1` の 採用検査で、
> `check-title` が **1回だけ** 落ちました。ほかの Playwright の 検査を
> **同時に 何本も 走らせて いた** ときだけ で、**単独で 2回 走らせたら
> 2回とも 合格**（60.0 / 59.3 / 59.9fps）。ボタンの いちも 下の すき間も
> ふだんどおりでした。

**fps・フレーム時間・描画性能などを 合否条件に ふくむ Playwright 検査は、
ほかの Playwright 検査と 並列実行しない。性能判定は 単独実行で 行うこと。**

並列実行中だけ 性能の しきい値を 下まわった 場合、**単純に flake として
無視しては いけません。**まず 単独で 走らせなおして、

| | どう あつかうか |
|---|---|
| 単独でも 再現する | **ほんとうの 性能問題**として あつかう |
| 単独では 再現しない | **並列負荷による 測定汚染**として 区別する |

ただし **機能エラー・JSエラー・レイアウト異常など、性能以外の 失敗は
この 例外で 無視しません。**

> **これは「fps の 失敗を 無視して よい」という ルールでは ありません。
> 性能測定の 条件を 固定する ルールです。**

いま 性能を 合否に つかって いるのは `tools/check-title.mjs` です
（平均 50fps 以上・わるい1フレーム 34ms 以下）。ここを 走らせる ときは、
**ほかの Playwright を 止めてから**にすること。

### キャラクターの検査は毎回かける（省略しない）

キャラを足したとき、形をいじったとき、描画の共通部分をさわったときは、
報告する前に かならず これを 走らせます。**目視だけで済ませないこと。**
コレクションの全キャラと、アバターの組み合わせ見本の両方を しらべます。

```
node tools/check-chars.mjs           # 終了コード 0 で合格
```

しらべるのは この9つです。

| | 見つかるもの |
|---|---|
| JSエラー | 描画中の例外 |
| はみ出し | つの・みみ・あし・しっぽが わくで切れている |
| 小さすぎ | わくの中で ぽつんと 見える |
| 中心ずれ | ならべたとき ひとりだけ かたよる |
| 名前かぶり | 同じ名前が2体いる |
| IDかぶり | セーブデータが こわれる |
| 絵の とりこぼし | 絵は あるのに `drawGen` が つかっていない／`ART_KEYS` に 書きわすれた |
| 出ない てき | どの国にも 入れわすれて 一生 会えない子 |
| 出ない なかま | どの たまごの ★にも 入らず 一生 手に 入らない子 |

目視は こういうものを 通してしまいます。実際、200体を目で見て「直った」と
報告したあとに この検査をかけたら、頭やあしが切れている子が **10体** 見つかりました。

### 形が かさなっていないかも 機械で しらべる

★の見た目や 名前が ちがっても、**32pxに 縮めたときに 同じ形・同じ色**なら
遊ぶ人には 同じキャラです。これも 目では 通してしまいます。実際、
新しい20体を 目で見て「そろった」と 報告したあとに、
「カメがいるから マグマガメは 被らない？」と 指摘されて しらべたら、
**4組**（カメ↔マグマガメ・トカゲ↔オオトカゲ・ゴーレム↔ようがんゴーレム・
ようがんのりゅう↔ドラゴン）が 被っていました。

```
node tools/check-dup.mjs                 # ぜんぶ
node tools/check-dup.mjs camel oni …     # その子が からむ組だけ
```

ものさしは **形と 色**で、どちらも 近いときだけ ほんとうに 見わけられません。

| | |
|---|---|
| 形 | 32×32 に 縮めた すきとおり具合の かさなり（IoU 0.86 以上） |
| 色 | 色あい30°以内・あざやかさ0.12以内・明るさ0.12以内・**ばらつき0.06以内** |

**色を 平均RGBの へだたりだけで 見ては いけません。**灰いろの ネコと
うすい茶いろの クマは RGBでは 近いのに、見れば ひとめで ちがいます
（あざやかさが ちがう）。さらに パンダは 白と すみいろなので、ならすと
灰いろに なって 灰いろの ネコと 同じに 見えます。だから
**明るさの ばらつき**も くらべます。この2つを 足して、
出てくる組は 69 → 22 に へり、動物は ぜんぶ 消えました。

**これは 合否を 出しません。**のこる22組は 丸い おかしどうしで
（クッキー↔ドーナツ、まんじゅう↔ロールなど）、直しようが ないためです。
どれも 36pxで 見れば ちゃんと 見わけられます（ドーナツは あな、クッキーは
つぶつぶ、プリンは カラメル）。**形の ものさしが 32×32の 白黒の 形しか
見ていないので、丸い子どうしは かならず ひっかかります。**
「22組 出た」は「22体が 出ない」ではありません。**キャラは 1体も 消えません。**
キャラを 足したら 走らせて、**足した子が からむ組だけ** 見てください。

直すときは 色ではなく **形** を変えます。マグマガメは 色だけ 変えても
「オレンジの亀」にしか 見えなかったので、**カタツムリ**（うずまきの殻）に
差しかえました。

### よこ向きの子は あるく向きに かがみ写しになる

てきは 進む向きに 体を むけます（`EN_FACE`・`drawEnemy`）。よこ向きの
絵の子だけ、進む向きと 絵の向きが 逆なら 左右を うつします。上下は
絵が 1枚しか ないので 変えられません。

- **絵を 足したり 出しなおしたら、その子の 向きを 目で 見て `EN_FACE` に
  足すこと**（1=右向き・-1=左向き。正面の子は のせない）
- 機械の 推定は 目安に しかなりません。カタツムリは 殻が 重心を
  持っていって「正面」と 出ました
- たてに 進む あいだは さいごの よこの向きを おぼえます（角で ぱたぱた しない）

### 白っぽい子は 36pxで たしかめる

`check-dup.mjs` も `check-chars.mjs` も とおるのに、盤面では 見えないことが
あります。ゆきおんなは 白い着物＋うすい水色の髪で、こおりのくにの
うすい地面に **ほとんど とけていました**（1024pxで 見ると ちゃんと 見える）。

生成した絵は **36pxに 縮めて、その国の 地面の色の 上に 置いて** 見てください。
直すのは この3つです。

- **頭を 大きく**（ちび体型に）。顔が つぶれると 何の子か わからない
- **色を こく**。白に 近い 色は 地面に とける
- **輪かくの 線を はっきり**。うすい子は 線が いのち

**直しかた**：からだの形を いじると、おなじ型（`quad`・`fowl` など）をつかう
別の子まで くずれます。そうではなく、1体ごとの ずらし `dx` / `dy` と
大きさ `sc` で わくに 合わせてください。この3つは 描画のいちばん外がわで
かかるので、ほかの子には ひびきません。

### アバターの服は シルエットから 変える

服は `AV_WEAR_DRAW`（`AV_WEAR` と同じ並び）に 1着ずつ 関数で 置いてあります。

はじめは シルエットが「すそが広がる／広がらない」の2種類しかなく、
上からもようを のせて 見わけていました。並べると **全部 同じ服に見えた**
ので、いまは 1着ずつ **胴の形・そで・すそ** から 別々に えがきます。

- そでは `avArm(g, o, kind)` で 選ぶ（`puff`／`short`／`long`／`cuff`／`wide`／`bare`）。
  そでの形だけで 服の印象は かなり 変わります
- ズボンは `avPants()`。2本に分けると ワンピースとの ちがいが ひとめで わかります
- 上下で色をずらすときは `avShade(色, t)`。`t<0` でこく、`t>0` でうすく
- **足と靴は服より先に**えがきます。あとから えがくと、すその下はしと
  足の上はしのあいだが すき間になって「服と足がはなれている」ように見えます
- **胴にかさなる そで（ぱふそで）に まるごと ふちを引かない**こと。
  かさなった ところに線が残って、そでが宙にういて見えます

服をさわったら `node tools/check-chars.mjs` が必須です（アバター見本42体も
同じものさしで はみ出しを しらべます）。

## あつめかたと、いずれの課金について

コレクションのキャラ（`CREATURES` から作る127体）は、こうやって 集めます。
図鑑の表示は153マス＝この127体に、お菓子タワー・敵・なかま・伝説・
アイテムを足した数です。
数は変わるので、`node tools/check-chars.mjs` の「キャラ数」で確かめてください。

| どこで | なにが |
|---|---|
| てきを たおす | その子が ずかんに入り、**なかまとしても つかえる**（63体） |
| てきの ドロップ | ときどき **アイテム**（8%）。ふだんは ワザの プレゼント |
| ステージクリア | たまごが 1こ。ステージが すすむほど いい たまご |
| ⭐で たまごを 買う | ★べつに 4しゅるい |
| ともだちバトルに かつ | かけら 1こ |

**キャラを消したら、まず `node tools/check-chars.mjs` の「キャラ数」を見ること。**
`RAINBOW_OF` に消した子のキーが残っていても `buildRoster` が読みとばすので、
気づかないまま数だけ合わなくなります。`fbg` の一覧も同じです。
古いセーブは壊れません（`tryResume` が `TOWERS[t.k]` を確かめて落とし、
図鑑の数は生きているリストだけを数えます）。

#### 見出しと タブの 2行

| 行 | 中み |
|---|---|
| 見出し（`#colHead`） | 🛍ショップ・🏅今週のおだい・🔑合言葉 …すき間… ✕ |
| タブ（`#colTabs`） | 仲間・敵・伝説・アイテム・友だち・🎀・📤 |

**どちらの行も 中みを おなじ大きさで ならべます。**幅は `flex:1 1 0` で
等分するので、文字数が ちがっても はこは そろいます（高さは 32px）。

- 入りきらないぶんは **出したあとに 測って つめます**（`fitHead`）。
  タブは `--fsTab` を さげて いちばん長い「アイテム」が 入るまで 文字を 小さく、
  見出しは 名前を 長い順に けずります（今週のおだい → おだい → 🏅だけ）
- **えらんでいるタブは うすいピンク**（`.ctab.sel`）。ショップ・🎀📤 のような
  「おすと 何かが おきる」ボタンは こいピンクの まま。
  **えらぶ ものと 実行する ものは 色を 分けること。**同じ こさだと
  どれを おせば 何が おきるのか 分かりません。
  うすい 地では 白ぬきが 読めないので、字は こいピンク（`--pink-d`）に します
- 等分するのは **名前の タブ5つ（仲間〜友だち）だけ**です。
  🎀📤 は 絵文字ボタンなので 入れません
- **🎀・📤・✕ は おなじ大きさの 四角**（`--icoW`、いま 36px）。
  1か所で 決めうちに してあります。
  **実寸を 測って 合わせては いけません** ——ひらいた 直後は まだ はばが
  決まっておらず、6px ずれました（じっさいに そうなった）
- 幅を そろえると、**402px から** 「今週のおだい」が 入ります。
  393px では はこが 97pxに なるので 「おだい」に 落ちます

- **`#colTabs` に `overflow-x:auto` を つけないこと。**スクロールわく に なると
  たてにも 切られて、🎀📤 の かげ（`box-shadow:0 3px 0`）が 下で
  ちょん切れます（実機の 報告）。タブは `flex:1 1 0` で ちぢむので
  はみ出しません
- **`#colTabs` の クリックは `.ctab` だけに 配ること。**`children` を
  まわすと、おなじ行に ならべた 🎀📤 の はたらきまで 上書きします
- タブに **絵文字を つけないこと**。「👭 友だち」だけ 24px 広くて、
  1行に 収まらなくなりました。ほかの4つに 絵文字が ないので 見た目も 浮きます
- ✕ は 細い字なので、となりの 絵文字と 同じ 大きさに 見えるよう
  少し 大きく してあります（`#colClose{font-size:calc(15px * var(--fs))}`）
- **おかしの国（🏰）の 入り口は 外しました。**キャラが ふえて 1枚の絵に
  収まらなくなったためです。画面（`kingOv`／`buildKingdom`）は のこして
  あり、ずかん完成の お祝いからだけ 見られます

## 将来こうする、という前提（2026-09-02 に決定・**まだ 作らない**）

**ここに 書いてある ものは、まだ 1つも 作りません。**けれど
**これから 何かを 決める ときに、この 10こと ぶつからないか**を
かならず 見てください。あとから 直すのが 高くつく ものだけを 書いています。

| | 何を するつもりか |
|---|---|
| ① 進化 | 秘薬で 強くすると **見た目も 変わる** |
| ② コンセプト | 国ごとに 敵も 味方も その世界の 子にする |
| ③ 季節イベント | 1年の 行事に あわせた 特別ウェーブと 限定キャラ |
| ④ 誕生日 | その人の 誕生日にだけ もらえる 特別な子 |
| ⑤ 47都道府県 | 都道府県を テーマに した ウェーブ |
| ⑥ 交換 | 人と 人で キャラを やりとり する |
| ⑦ SNS紹介 | 紹介すると ごほうび |
| ⑧ 通貨 | ゲーム内の お金を **コインの 絵**で 出す |
| ⑨ 男の子向け | かっこいい・強そう・モンスター的な 子を ふやす |
| ⑩ 2言語 | 日本語と 英語の 両方に する |

### 絵に していい ものの ものさし ——**文字が 入るなら だめ**

> **絵の 中に 文字・数字・記号が 入るなら、画像に しては いけません。**
> 入らない（もの・生きものの 形だけ）なら、画像に して よい。

`art/ui/` の 34枚は ぜんぶ「もの・生きものの 形」で、文字が 1文字も
入って いません。だから **Phase 5 で 作った 素材は、英語版でも
そのまま つかえます。**

逆に「ガイドの 見出しを 絵に する」「セリフを 絵に する」方向へ
進むと、**日本語が 絵に 焼きこまれます。**⑩ の じゃまに なるので
しないこと。

### **通貨は「コインの 絵」と「¥ / $」を 分ける**

いまは すでに 正しい 形に なって います。**くずさないこと。**

| | 何か | 出しかた |
|---|---|---|
| ⭐💎💗🩷 | ゲーム内の 通貨・状態。**言語に よらない** | `art/ui/cur_*.webp` の 絵 |
| **¥320 など** | **ほんとうの お金**。日本語＝円／英語＝$ | `yen()` **1か所・文字の まま** |

- **`yen()` を 画像に しないこと。**ここは 通貨記号 そのもの なので、
  `¥` → `$` の 切りかえが 要ります。1か所に あつまって いるのが
  いちばん 大事で、**ちらさないこと**
- ⑧ で コインの 絵を 足す ときも、**絵と 記号は 別の 要素**に する。
  `<img コインの絵> ¥320` の ように ならべ、
  **絵の 中に 記号や 数字を 焼きこまない**
- **「¥」や「$」の 入った 画像は 1枚も 作らないこと**

### 表紙は 言語ごとに 1枚 要る

`art/title/title_v2.webp` には **「魔法の おかしの国」「OKASHI NO KUNI」
「はじめる」が 絵として 入って います。**表紙は「コードで えがきなおさない」
きまりなので、英語版を 出す ときは **英語の 原画を 別に もらう**しか
ありません。`invite.html` の `128体`（3か所）と `ogp.jpg` も 同じです。

**いま やる ことは ありません。**「表紙は 言語ごとに 1枚」と
おぼえて おけば 足ります。

導入画面（`#weOv`）が「1まいの 絵に しないこと／題・説明の 文は
かならず HTML の 文字」に なって いるのは、**この 話の 先どり**でした。
あそこは 絵を さしかえる ことなく 英語に できます。

### 図鑑データは「種類の 集合」で、「個体の 持ちもの」では ない

```
dex['ch_bear'] = { w:入手ウェーブ, d:入手日, lv:とうたつレベル }
```

**キーが 種類**なので、**おなじ子を 2体 持つことが 表せません。**
③④⑥ は どれも 次を 要ります。

| 要るもの | いまの dex に |
|---|---|
| 個体ID | ない |
| 入手元（誕生日・イベント・交換） | `w`/`d` だけ（弱い 代用）|
| 限定区分 | ない |
| 交換して よいか | ない |
| 進化段階 | ない（`lv` は タワーの レベル）|
| 個数 | **持っているか どうかの 真偽値だけ** |

**足すのは 安全。キーを 変えるのは 非互換。**

- **値の オブジェクトは 開いて いて、`unlock()` は
  `if (dex[id]) return false;` で 守られて います**（`index.html` の
  `unlock`）。だから **フィールドを あとから 足しても、
  前の セーブは こわれません**
- 前例が あります ——`t.rb ? (t.rbx || 2) : 1`（`dmgOf`）。
  古い セーブに 無い ものを `|| 既定値` で 吸収する 書きかたです
- **`dex` の キーを 種類から 個体に 変えるのは、交換機能と 同時に やる
  「引っこし」です。**ついでに やっては いけません

### ★は 強さ。**限定性を ★の 本数に 埋めない**

★ は「つよさ」の ものさしで、`'★'.repeat(rec.lv)` で 本数が 変わります。
「誕生日でもらった」「イベント限定」「交換で来た」は **べつの 軸**なので、
★を 7つに する ような 表しかたを しないこと。

### ウェーブに「何が おきるか」を 場あたりに 足さない

いま「この ウェーブは 何か」を 決めて いる ところが **7つ**に
ちらばって います。

| どこ | 何を 決めて いるか |
|---|---|
| `themeIdxOf` | 20ウェーブごとに 国が かわる |
| `waveMod` | とつぜんの ウェーブ（1周に 10こ）|
| `wave % 10 === 0` | 中ボス |
| `wave % STAGE_WAVES === 0` | ステージクリア |
| ボーナスウェーブ | 20の 倍数の あと |
| `isFriendWave` | `wave % 20 === 10` 友だちバトル |
| `LEGENDS[].wave` | 伝説との たいけつ（12国 × 15ウェーブめ）|

**③ 季節イベントと ⑤ 47都道府県を この まま 足すと、9〜10の 式が
おなじ `wave` を 取りあいます。**「ハロウィンで、かつ 京都で、かつ
ボスで、かつ とつぜんの ウェーブ」の とき 何が 勝つのかを 決める
場所が ありません。

だから **「このウェーブは 何か」を 1つの 関数が かえす 形**に
まとめて から 足すこと。

> **`waveList()` そのものは いじらないこと。**`tools/sim.mjs` が
> 読んで います。上に かぶせる 形に して、難易度の モデルの 入口を
> 変えないで ください。

### 絵を 作る 前に 見る こと

| 足したい もの | 待つ べきか |
|---|---|
| とつぜんの ウェーブの 記号（⚡🛡🌸）| **待つ。**③⑤で 種類が ふえる |
| 敵の 種別（🛡💨👑）| **待つ。**属性バッジと 規則を 共有する |
| お菓子タワーの 絵（🍬🍧🍫⭐）| **待つ。**① で 進化版と 作り分ける |
| 階級バッジ（🌈🥇🥈＋ピンク）| **待つ。**③④⑥で 段が ふえる。ガイドと `#eggRates` の **2画面で 対**に なっている |
| ガイドの 見出し・セリフ | **作らない。**日本語が 絵に 入る |
| `¥` / `$` | **作らない。**通貨記号は 文字 |
| もの・生きものの 形だけの 絵 | **作って よい** |

### 図鑑の 色は Design System v1（2026-09-01）

図鑑（`#colOv`）だけ、**ピンク 一色を やめて アイボリーを 地に**しました。
色は **`#colOv` の 変数 1か所**（`--dxIvory` 〜 `--dxGrayL`）で 決めます。

| | わりあい | つかう ところ |
|---|---|---|
| パールアイボリー／白 | 65% | 画面の 地・カード・タブ |
| パールピンク | 15% | えらんでいる タブ・数字 |
| ラベンダー | 10% | 上の ぼかし・✕・説明の 字 |
| シャンパンゴールド | 7% | 見出しの 線・自己ベスト・金のわく |

ねらいは「魔法の おかしの国の コレクションブック」。**キャラが 主役**なので、
カードの 地は 白に して 絵を いちばん 目立たせます。**黒い かげは つかいません**
（`rgba(150,110,170,.12〜.22)` の 紫がかった かげ）。

#### 共有の クラスは かならず `#colOv` の 中だけに しぼる

ここが いちばん 危ないところです。**図鑑だけの ものだと 思って いた
クラスが、ほかの 画面でも つかわれて います。**

| | どこでも つかっている |
|---|---|
| `.ctab` | **「仲間を選ぶ」（`#pickOv`）の ★1〜★6** |
| `#colHeadH` | **ガイド（📕）の 見出し** ——`#colHead` と 同じ 規則に 書いて ある |
| `.mini` | ショップ・ガイド・盤面 …ほぼ 全画面 |

だから 素の 規則は **1文字も 変えずに のこして**、上に
`#colOv .ctab` / `#colOv #colHead` のように しぼった 上書きを 足します。
`#colHeadH` を 巻きこんだ ときは **ガイドの 画素が 1.46% 変わりました**
（目では 気づけない。前後で 画素を くらべて 見つけた）。

#### タブの ふちは `border` では なく `inset` の かげ

タブは `flex:1 1 0` で 等分するので、`border:1px` を 足すと
**中に つかえる はばが 2px へって、字が よけいに 切れます**
（320px で 18 → 16px。「アイテム」が さらに 欠けた）。
`box-shadow:inset 0 0 0 1px` なら 見た目は 同じで はばを 取りません。

#### `repeat(3,1fr)` は 320px で 3列目が 切れる

`1fr` は `minmax(auto,1fr)` なので **中みより 細くは なりません**。
320px では ならびの 幅 242px に 対して わくが 192px しか なく、
**3列目が 右で 切れて いました**（実測：はみ出し 38px・6要素）。

`repeat(auto-fill,minmax(92px,1fr))` に すると こうなります。

| 画面 | わく | 列 | 1マス | |
|---|---|---|---|---|
| 320 | 192 | **2** | 92.5px | 切れない |
| 375 | 246 | **2** | 119.5px | |
| 393 | 345 | 3 | 110px | **前と 同じ** |
| 430 | 381 | 3 | 122px | **前と 同じ** |

**92px を えらんだのは、393 と 430 が 1pxも 変わらない いちばん 大きい 値**
だからです。ここを 動かすと 主力の 画面まで 変わります。

#### 320px の タブは 字の 大きさの 話では ない

`--fsTab` を いくら さげても、320px では 「アイテム」「友だち」が 切れます。
タブの はこが **18px しか ない**ためです（行の 37% を 🎀📤 の
`--icoW:36px` × 2 が 取っている）。4文字を 18px に 入れるには
`--fsTab` 0.42＝**5px の 字**が 要り、読めません。

直すには `--icoW` を せまくするか、🎀📤 を タブの行から 出すしか なく、
**どちらも いまの きまりの 外**です。375px は `FSTAB_MIN` を
0.6 → 0.56 に して 入るように しました（実測 29 に 対して 31 だった）。

### 図鑑は 5タブ・わくは 強さで 4段階

タブは **仲間・敵・伝説・アイテム・友だち** の5つです（「すべて」と ★別は
やめました。多すぎて さがせなかったため）。

**ならびは わくの つよい順**（にじ → 金 → 銀 → ピンク）、おなじ わくなら
★の 大きい順です。ほしい子ほど 上に 来ます。

- **精霊（ステージクリアの子・ながれぼし）は 銀**です。★は 強化のレベルで
  ものさしに なりませんが、12国を 1つずつ クリアしないと そろわないので、
  たまごから 均等に 出る子より 手に入りにくいためです
- **お菓子タワーとアイテムは わくを つけません。**タワーは 毎回 ⭐で
  買えるので めずらしくなく、ならびも いちばん下です。
  むかしは 精霊ごと 先頭に 置いていましたが、17枚も 先に あって
  金の子まで 6行 スクロールしないと 見えませんでした
- ワザを もつ子は ならびの 中に ちらばるので、みちしるべは ばしょでは なく
  **見た目（`✨` の行）**で さがしてもらう 書きかたに してあります
- 敵か 仲間かの ふりわけは `gen.side` ですが、**見た目だけの 引っこしは
  `DEX_SIDE` と `DEX_LEGEND`** に 書きます。side を 変えると 出る国・
  たまごが 変わってしまうので、見た目の 整理に side を つかわないこと
  - `DEX_LEGEND`（ユニコーン・ゆきのじょおう・にんぎょ・てんし・ひのとり・ゆきおんな）は
    伝説タブに ならび、にじの わくが つきます。ものさしは
    **神話・幻想の 存在か**。まほうつかい・まじょは 人間なので 仲間のまま
  - ゆきのじょおうは 3つの国の ボスの まま、図鑑だけ 伝説です
- わくの 4段階：アイテムと `DEX_GOLD` の子＝**金**・★5〜6＝**銀**・ふつう＝ピンク・伝説＝**にじ**。
  どの わくかは **`dexTier()` 1か所**が 決めます。ならび（`rank`）も
  見た目（`tierOf`）も そこを 見るので、色と ならびが ちぐはぐに なりません。
  `.tGold`/`.tSilv`/`.tRain`。角丸と グラデのふちの 両立は
  border を 透明にして background 2枚がさね（padding-box / border-box）で
- 銀とピンクは ★で 自動に 決まりますが、はずれる子は
  **`DEX_SILVER`（★4だが銀）／`DEX_PINK`（★5だがピンク）** に 書きます。
  **この2つの あいだの 引っこしは 見た目だけで すみます** ——手に入りかたが
  おなじ（たまごは 均等・たおせば 図鑑に入る）ためです。
  金・にじへ うつすときは 出かたも しぶくすること
- まだ居ない子の 白ぬき（`.yet`/`.no`）が わくの色まで 消すので、
  金・銀・にじの カードだけ 色ぬきを よわめてあります

### 手に入りにくさは わくの色と そろえる

| わく | 手に入りかた |
|---|---|
| ふつう・銀 | いままでどおり（たまごは 均等、たおせば 図鑑に入る） |
| 金（アイテム） | ドロップ8%・クリア1こ・セットは **1日1回ずつ** |
| 金（キャラ・`DEX_GOLD`） | たまごは にじを のぞいた のこりの 20%（`GOLD_RATE`）、たおしても **60%**（`GOLD_KEEP`） |
| にじ（でんせつの子） | **運まかせ**。たまごでは あわせて5%（`LEG_RATE`）、たおしても **25%**（`LEG_KEEP`）でしか 図鑑に 入らない（「にげてしまった…」） |

**わくの色だけ 変えては いけません。**金の わくに するなら、たまごの
出かたと たおしたときの 図鑑入りも いっしょに しぶくすること。
見た目だけ 金に すると、遊ぶ人には ただの「色ちがい」に なります。

- `LEG_RATE` / `GOLD_RATE` を さわったら **かくりつ表示（eggRates）も
  かならず 見なおすこと**。いまは `eggOdds()` 1か所を えらぶ側（`eggPick`）と
  表示（`eggRates`）の 両方が 見ているので、数字が ずれようが ありません。
  **ここを 2か所に 分けないこと**（表示が ずれるのが いちばん まずい）
- はじめてパックからは にじの子は 出ません（かんたんな 近道に しない）
- かけら交換は のこします。ゆっくり ためれば 確実、という 救済の道
- ウェーブの伝説（くじゃく・りゅう・スフィンクス）は とうたつ制の まま

#### 金の わくの子は「その国の いちばん おく」

`DEX_GOLD` は 仲間10体・敵9体です。

| | だれ |
|---|---|
| 仲間 | クジラ・ウエディングケーキ・まほうつかい（★6）／シャチ・ようせい・よくばりパフェ・プリンパフェ・マカロンタワー・王さま・王妃さま（★5） |
| 敵 | ライオン・ツタンカーメン・オニ・ゴーレム（★6）／サメ・クレオパトラ・ファラオ（★5）／ようがんゴーレム・あくま（★4） |

**金は「つよさ」では なく「めずらしさ」です。**★4の ようがんゴーレム・あくまが
金で、★5の クマ・トラが 銀なのは まちがいでは ありません。だから
`tierOf()` の ならびも ★より わくが 先に 来ます。

**ふつうの子が 1人も いない たまごに 気をつけること。**にじのたまご（★6）は
金3人＋にじ1人で、ふつうの子が いません。ここで にじに のこりを あげると
にじが 25%に なって「運まかせ」で なくなります。`eggOdds()` は
**にじの 5%を さきに 取って、のこりを 金が ぜんぶ 取る**ように してあります。

#### 金を ふやしたら GOLD_RATE を 見なおす

`GOLD_RATE` は **金の 人数で 変わります**。1人あたりが
**ふつう > 金 > にじ** の 順に ならんでいないと、わくの色が うそに なります。

金を 3体から 9体に ふやしたとき、15% の ままだと きらきら たまご
（にじ2人・金6人）で **にじ 2.50% ＜ 金 2.37%** と ひっくり返りました。
20% に 上げました。いま（金19体）は ふつう4.5% > 金2.7% > にじ2.5% で、
**にじとの さは あと 0.2ポイントしか ありません**。金を もう1〜2体 ふやすと
また ひっくり返るので、そのときは 25% あたりまで 上げること。

```
node tools/check-chars.mjs      # 「たまごの ならび」が 毎回 たしかめる
```

目では 気づけません（表の 小数点 2けたの ちがい）。**金を 足したら
かならず この検査を 走らせること。**

**なかまは「ずかんに いる子」しか おけません。** これが ないと コレクションが
かざりに なってしまいます。はじめての人には ★1を 5人 わたします。

### かべの 先は「⭐で ない 力」で のばす

かべの 正体は **⭐が 足りなくなる**ことです（W250で Lv22／上限28）。
だから ⭐を つかわずに 火力を 足せる ものだけが かべを 動かせます。
モデルで 測ると こうでした。

| W200から 火力 | かべ |
|---|---|
| ×1.0（いま） | 250 |
| ×1.5 | 290 |
| **×2.0** | **320** |
| ×3.0 | 370 |
| ×4.0 | 380 |

×4に しても 380で 止まるのは、敵の HPが 複利で のびる ためです
（倍に しても 35ウェーブぶんしか 買えない）。

**中盤に 出しては いけません。**秘薬の ×2が はじめから 効くと、
W50の よゆうが 1.9 → 3.8 に なって、締めた ぶんが 消えます。
だから **こだいの秘薬（⚗️・攻撃3倍）は `late:200`** に して、
ウェーブ200からの ドロップでしか 出ません。**ショップにも 置きません**
——強さそのものは 売らない（この ファイルの きまり）。

坂では なく **階段**に するのが ねらいです。同じ速さで 両方 のびると
数字が 大きくなるだけで 手ざわりは 変わらず、それも 飽きます。
「詰まる → 何かが 開く → 一気に 越える」を 作ります。

- `late` を もつ アイテムは ドロップの pool から はずれます。
  **しきりは `dropPoolAt()` 1か所**で、ステージクリアと 戦いの ドロップの
  両方が そこを 通ります（むかしは 2か所に 書いて あり、戦いの ほうだけ
  見て いませんでした ——下の「ドロップの late は 1か所」）
- 秘薬は **1人に 1回だけ**。かさねると 一気に こわれます
- ばい率は `t.rbx`。**古いセーブには 無い**ので
  `t.rb ? (t.rbx || 2) : 1` と 書いて、前の セーブは ×2の まま 動かします

### まけた とき「何が 足りなかったか」を 出す

ただ まけると「なんか むりだった」で 終わります。**言葉に できると、
人は つぎを 始めます。**`overHint()` が のばせる ところを 1つだけ
名ざしで 出します。

**3つ とも 出さないこと。**いっぺんに 言うと どれが いまの 課題か
分かりません。1つだけ 出すから 目標に なります。

#### **順番では なく「どちらが 火力を のばすか」で えらぶ**

> **記録（2026-09-02）**：実機の 報告「59ウェーブと 60ウェーブを
> 行ったり来たり、ずっと 次に 進めない」。**ヒントが うそを
> ついて いました。**

むかしは この順で 上から 見て いました。

```
1. おけるマスが 3つ以上 あまっている → 「あと◯マス おけるよ」
2. 強化の 上限まで 育っていない       → 「Lv◯〜◯、上限は Lv◯」
3. 上限まで 育っている                → 「もっと ★の 大きい なかまに」
```

ところが 盤面は 108ます、道が 30ほどなので **おけるのは 78ます**。
ふつうの 人は 20〜40体しか 置かないので、**1が ほとんど いつも 真**に
なり、**2と3は 一生 出ません**でした。

じっさい ウェーブ60は **レベルの かべ**です（実測）。

| おいた なかま | 結果 | てきの 最大数 |
|---|---|---|
| Lv5 × 26体 | **まける**（13秒）| **30体まで ふえつづける** |
| Lv9 × 30体 | クリア（9秒）| 12体 |

なのに 画面は **上限まで 育てた 人にも**「あと38マス おけるよ」と
言いつづけて いました。遊ぶ人は 直す ところが 分からないまま、
おなじ ウェーブを くりかえします。

いまは **2つの のびしろを 実際に 計算して、大きい ほうだけ**を 出します。

| | 何倍に なるか |
|---|---|
| 育てる | いちばん 弱い子を 上限まで → `dps(cap) / dps(いま)` |
| ならべる | あいた ますを ぜんぶ うめる → `(いま + あき) / いま` |

ますの ほうは **ぜんぶ 買える 前提の 多めの 見つもり**なので、
それでも 育てる ほうが 勝つ ときは まちがいなく 育てる ほうが よい。

**倍率を 書き写さないこと。**`dmgOf` / `rateOf` を そのまま 呼びます
（DPS ＝ こうげき ÷ うちかんかく）。1.6 や 0.9 を 2か所に 書くと、
balance を いじった ときに **ヒントだけ 古く なります**。

| なかま | W | 出る文 |
|---|---|---|
| 3体 Lv1 | 10 | ならべて |
| 10体 Lv5 | 30 | ならべて |
| **26体 Lv5** | **60** | **育てて** ←直った |
| 30体 Lv8 | 60 | ならべて（1レベル差なので ますの ほうが 効く）|
| 40体 Lv9 | 60 | ならべて |
| 76体 Lv9 | 60 | ★の 大きい なかまに ←3つめが はじめて 出る |

#### ウェーブ60が かべに なりやすい わけ（直して いない）

**ボスは 4秒ごとに 2体 しょうかんします**（`enemies.length < 60` まで）。
W60 は **ボスが 2体**（`n%10===0` と `n%10===0 && n>=40`）なので
毎秒 1体。**倒しきれない ほど 敵が ふえる**、正の フィードバックです。

**`tools/sim.mjs` は この ぶんを 1体も 数えて いません**
（`waveList` の 合計HPしか 見ない）。モデルの よゆうは W59が 2.49、
**W60が 1.75**、W61が 2.56 で、W60は もともと 谷です。

**ここは まだ さわって いません。**召喚に 上限を つけると 難易度が
直に 下がりますが、**モデルには 何も 見えないまま** 下がるので
（祝福で 一度 やった 失敗と 同じ形）、別に 判断すること。
かべは いま W250 で 合格はんい（250〜550）の 下限ぎりぎりです。

### アイテムは「たおす」、ワザは「けずる」

アイテム（`ITEMS`）は 1プレイに 数回の 切りふだです。ワザ（`SKILLS`）と
**効きめの しゅるいを 変えて**役わりを 分けています。

| | 効きめ | なぜ |
|---|---|---|
| ワザ | HPを **けずる** | 何回も つかう。後半は 相対的に 弱くなる |
| アイテム | **たおす** | めずらしい。後半の かたい敵にも おなじだけ 効く |

- 効きめは **`useItem()` の1か所**。ふやすときは `ITEMS` に 1行 足すだけ
- **ワザの持ち数は `skStock`、アイテムは `gear`。**混ぜないこと。
  混ぜると「ワザが へった／アイテムが へった」が わからなくなります
  （もとは どちらも `items` で、アイテムを足すときに 改名しました）
- 入手は **ドロップ8%・ステージクリア1こ・ショップ（💎20と💎55・1日1回ずつ）**。
  消耗品なので あそんでいれば たまります。**「待てば無料」を くずさないこと。**
  強さそのもの（攻撃力・⭐）は 売らない、という きまりは そのままです
- にじいろの秘薬は 盤面の 仲間を タップして かけます（`aimItem`）。
  `t.rb` を セーブにも のこすので「つづきから」で 消えません。
  絵は 色相だけ かけて **焼いておく**こと（毎フレーム 合成すると おもい）

**にじいろ版のキャラ（`RAINBOW_OF`）は やめました。**元の子の 色ちがいでしか
なく、ずかんの 半分近くを うめていたためです。にじいろの 見た目は
秘薬（`rbSprite`）で つかっています。`drawGenFx` と `LV[7]` は そのために
のこしてあります。

### 光・グラデーション・ふるえ

#### 音と ふるえは ITEMS / SKILLS の `snd`・`vib` に まとめる

アイテムの 音は **`ITEMS` の中**、なかまのワザは **`SKILLS` の中**に
`snd`・`vib` として 置きます。むかしは 音が `useItem()` の 6つ、
`useSkill()` の 13の えだに ちらばっていて、**ならべて 聞きくらべることが
できません**でした（ワザには ふるえが 1つも ありませんでした）。
`TOWERS` や `ENEMIES` と おなじ考えです。

```
snd:[[高さ, ながさ, 波の形, 大きさ, すべり, 何びょう後], …]
```

**形を 変える**こと。目をつぶっても どれを つかったか 分かるのが ねらいです
（ボール＝4つ上がる／いなずま＝のこぎり波が落ちる／ハンマー＝四角波の低い2発／
ひやく＝三角波が3つ上がる／こおり＝サイン波の長い尾／⭐の雨＝5つ速く上がる）。
ワザも 役わりで 分けます（止める＝下がる／ダメージ＝低い一発／回復＝あたたかく
上がる／バフ＝明るく上がる）。ならすのは `playSnd()` 1か所。

**ワザを 足したら `snd` と `vib` も 足すこと。**忘れても 落ちませんが、
その子だけ 音も ふるえも 無い 子に なります。

#### えらぶ ボタンにも 音と ふるえ

「おす」ボタンだけでなく、**えらぶ**ボタン（ショップの札・盤面のなかま・
仲間を選ぶ・ワザ）にも 手ごたえを 出します。`tapSel()` 1か所です。

- **音と ふるえは かならず 対で 出すこと。**音を 切っている人にも つたわり、
  ふるえの ない 端末（iPhoneの Safari）には 音で つたわります
- `tapSel(false)` は「いま つかえない」の 合図（低い音＋みじかい2回ふるえ）。
  てきが いないのに 切りふだを おした、クールダウン中、などに つかいます
- 秘薬は **えらぶ音と つかう音を 分ける**こと。えらんだ だけでは まだ
  へっていないので、おなじ音だと「つかってしまった」と 思われます

- `iflash` は **色つき**の ひかり、`iSweep` は まん中から ひろがる わっか。
  白フラッシュ（`wflash`）とは べつです。**かさねると 白く とんで 色が 消えます**
- ふるえは `vib()` を とおします。**音と 同じで かならず オンオフ**（ガイドの中）。

  **iPhone では 鳴りません。**iOS の Safari にも WKWebView にも
  Vibration API（`navigator.vibrate`）が ありません。Android は 鳴ります。
  だから **音は かならず ふるえと 対で 出すこと** ——iPhone では
  音だけが たよりです。

  iOS 17.4 の **スイッチ型チェックボックス**（`<input type="checkbox" switch>`）を
  切りかえる 手も ためしましたが、**実機で なにも おきませんでした**。
  効かないコードを のこすと つぎに さわる人が 勘ちがいするので 外しました。
  同じ手を もう一度 足さないこと。

  ちゃんと 鳴らすには **ネイティブの 橋わたし**が いります。
  **アプリに つつむだけでは 足りません**（中みは 同じ WKWebView）。
  - iOS は **Core Haptics**（`CHHapticPattern`）。`vib()` の ミリびょうの
    ならびを そのまま 鳴らせます
  - `UIFeedbackGenerator`（Capacitor の 標準）は 単発の トンッ だけなので、
    せっかくの リズムが つぶれます
  - **さしかえるのは `vib()` 1つだけ。**ゲーム側は 1行も さわりません

### 💎ジュエルとショップ

⭐とかけらは「あそんでためる」もの。**💎だけが買うもの**です。
ここを混ぜると、あとで課金を入れるときに直せなくなります。

- 💎を使うのは `payGems()` の1か所だけ。売りものは `SHOP_ITEMS` に足す
- **⭐は単品で売らない**。後半は1ウェーブで何万も手に入るので
  （W60で293、W380で6,228）、買った⭐がすぐ値うちをなくして
  「損した」になります。⭐ははじめてパックの中身としてだけ出す
- 💗（あそべる回数）は売ってよいが**安く**。スタミナ課金は
  子どもも遊ぶゲームで一番批判されやすいので、「待てば無料」を守る
- いまは💎を買えないので、ログインボーナスで毎日+2わたしています。
  課金をつないだらこの数は見なおすこと

#### たまごは ⭐と💎の どちらでも開ける

`EGGS` の `cost`（⭐）と `gem`（💎）の2本立てで、**出かたはまったく同じ**です。
`buyEgg(e, pay)` の中だけで払いを分けています。

- **どちらか片方にしない**こと。💎だけにすると遊んで集められなくなり、
  ⭐だけにすると始めたばかりの人がいつまでも1人も増やせません
- 確率表示に「⭐でも💎でも出かたは同じ」の1行を必ず残すこと
- **今週のおだい中は開けません**。おだいで使った⭐は出るときにふだんの分へ
  戻るので、そこで使うとタダになって仲間だけ残ります（実際に起きていました）
- ⭐を払ったら **`saveRun()` を呼ぶ**こと。⭐は `saveRun` でしか残らないので、
  開いたあと読みこみ直すだけで⭐が戻ってきます（これも起きていました）

#### はじめてパック（1回だけ）

💎30で ⭐3,000 ＋ ★4のなかま1人。買ったことは `sweetTD.own` に
`pack_start` として残します（着せかえと同じ持ちもの）。

- **⭐を出すのはここだけ**。単品では売りません
- なかまはランダムなので、**確率表示が必須**です（`note()`）。
  たまごと同じ「★4の◯人から均等に1人」にして、1行で言いきれる形にしてあります
- **今週のおだい中は買えません**。おだい中の`saveRun()`は何もしないので、
  買った⭐が戻るときに消えてしまいます
- ✓を出すのは `done()` が真のときだけ。買えないだけのときに出すと、
  持っていないのに持っているように見えます

#### 着せかえは 2つの入り口から入る

着せかえ（`SP_THEMES`）は、合言葉でもらう分と💎で買う分の両方があります。
**持ちものは別々に持ちます。**

| | どこに入るか | 出しかた |
|---|---|---|
| 合言葉でもらう | `sweetTD.codes`（`redeemed`） | `CODES` に合言葉を足す |
| 💎で買う | `sweetTD.own`（`bought`） | `SHOP_THEMES` に id を足す |

混ぜると、買ったテーマが「その合言葉はもう使いました」の判定に
ひっかかって、あとから合言葉を配れなくなります。
持っているかを見るときは、かならず `hasTheme(id)` を通してください。

**💎で売るテーマは `CODES` に のせない**こと。のせると、合言葉さえ
知っていれば ただで手に入ってしまいます。

着せかえたあとの焼きなおしは要りません。`groundKey()` にテーマ名が
入っているので、ループが気づいて `bakeGround()` します。BGMも
`bgmTick()` が毎回 `themeOverride` を見るので、次の小節から変わります。

#### 30日パスポート（💎90）

毎日 かけら🩷3 と 💗満タン。**⭐や攻撃力は上げません。**強さを売ると
「課金しないと勝てない」になり、⭐をふやすと `tools/sim.mjs` ではかった
かべの位置もずれます。

持ちかたは「いつまで有効か」の時こく1つだけ（`sweetTD.pass`）。日数で持つと、
遊ばない日があったときに減るのか減らないのかがあいまいになります。
買いなおすとのこりに30日たされます。

#### お金の入り口は canPayYen → recordYen の2つではさむ

未成年のひと月の上限は JOGA／CESA の目やすに合わせています。

| | ひと月 |
|---|---|
| 16歳未満 | ¥5,000 |
| 16〜19歳 | ¥20,000 |
| 20歳以上 | 上限なし |

しばるのは**「💎をお金で買うとき」だけ**。持っている💎をつかうのは何回でも
できます（買ったあとにつかえなくなるほうが混乱しますし、返金の話にもなります）。

```
const r = canPayYen(n); if (!r.ok) return …    ← 買う前にしらべる
…Apple の買いもの…
recordYen(n);                                  ← はらえてから記録
```

**1つの関数にまとめて先に記録しないこと。**買いものをとちゅうでやめたり
失敗したときに、はらっていないお金が上限から引かれます。

localStorage は書きかえ放題なので、これは画面がわの歯どめでしかありません。

#### もちものは receiptOwned() が正、なければ端末の記録

`owns(id)` と `passUntil()` は、まず `receiptOwned()` を見ます。

| | どこから決まるか |
|---|---|
| レシートあり | **レシートだけ**。そこにないものは持っていない扱い |
| レシートなし（いま） | 端末の記録（`sweetTD.own` / `sweetTD.pass`） |

ここが書きかえ対策の本体です。セーブを書きかえて着せかえを3つ持ったことに
しても、レシートが入れば買った分だけに戻ります（`?dbg=1` の
`__dbg.setReceipt()` で確かめられます）。

**自分で 書いた 判定を 信じないこと。**セーブも、そこから 読んだ こたえも
書きかえられるので、「持っているか」を JavaScript の 中だけで 決めては
いけません。

よりどころに するのは **Apple が 署名した もの**です。**StoreKit 2 なら
端末の 中で 署名を 検証ずみの トランザクションが 返る**ので、
**1.1 は サーバーなしで 始めて かまいません。**

もっと 堅くしたく なったら App Store Server API
（`/inApps/v1/transactions/{id}`、`verifyReceipt` は古い）で、署名の鎖・
bundleId・environment・払いもどし・パスポートの `expiresDate` を見ます。
**売上が ふえて 不正が 気に なってから で じゅうぶん**です
——先に サーバーを 建てると、無いはずの サーバーが 1つ 増えます。

**復元ボタンは必須**です（非消費型を売るアプリに Apple が求めています）。
`restorePurchases()` の中を `restoreCompletedTransactions()` の結果に
さしかえれば本番になります。ふだんの読みこみも同じ `loadOwned()` を
とおしてあるので、直す場所は1か所です。

### 課金を 入れるときの きまりごと

いずれ App Store に 出すつもりなので、次を まもってください。

- **買いものは `buyEgg()` の 1か所だけ**。ここの「⭐をはらう」を
  「💎をはらう」に さしかえれば 課金に なる形に してあります。
  買いものを あちこちに ちらすと、あとで 直せなくなります
- **Apple では ゲーム内の デジタル商品は かならず IAP**。
  外部決済（Stripe など）は つかえません
- **ガチャの かくりつ表示は 必須**（Apple 3.1.1／日本の 景品表示法）。
  だから たまごは「おなじ★の子から 均等に 1人」に して、
  「どの子も おなじ かくりつ」と 1行で 言いきれる形に してあります。
  出しかたを 変えるときは、かくりつ表示も かならず 直すこと
- **「◯体そろえたら 特典」は 作らない**。日本では コンプガチャに あたります。
  ずかんの「かんせい」は お祝いの 画面だけで、もらえる ものは ありません
- localStorage は 書きかえ放題なので、**買った ものを そこだけに 置かない**。
  IAP を 入れるときは レシートで もういちど たしかめること

## 国ごとに キャラを そろえる

ステージ（＝国）は 20ウェーブごとに かわります。**その国に いそうな子だけを
出す**ことで、景色と キャラが つながって 見えます（氷の国 → ゆきのじょおう）。

むすびつけている ところは 2つです。

| | どう決まるか |
|---|---|
| ステージクリアでもらう なかま | `CHARS` の **ならび順**。`THEMES` と 同じ順にしてある |
| そのウェーブに 出る てき | `EN_THEME`（`THEMES` と 同じ順の 配列） |

いまは12国あり、ならびは旅のじゅんばんです。

| | 国 | なかま | てきの ならび |
|---|---|---|---|
| 1 | シュガーガーデン | ふたばのこ | のはらの 小さい子 → キリン・ライオン |
| 2 | いちごのおかしばたけ | よつばのこ | はたけの 子 → ウマ |
| 3 | マカロンのもり | つむじかぜ | もりの 子 → クマ |
| 4 | チョコレートのたに | かみなりのこ | 茶いろの 子 → トラ・ゴーレム |
| 5 | こおりのくに | こおりのつぶ | 白い子 → ゆきおんな・ゆきのじょおう |
| 6 | ゆきのケーキやま | わたぐも | 雪の子 → クマ・ワシ・ゆきおんな |
| 7 | ソーダのうみべ | いずみのしずく | うみの子 → サメ・マンタ |
| 8 | キャラメルのさばく | すなどけい | サバンナの子 → ライオン |
| 9 | さばくのくに | かげろう | 砂の子・は虫るい → 王家（ミイラ・クレオパトラ・ファラオ・ツタンカーメン） |
| 10 | ひのくに | たいようのこ | 火の子 → ひのとり・オニ |
| 11 | よぞらのくに | はなびのこ | 夜の子 → ゆきのじょおう |
| 12 | にじのてんくう | ひかりのわ | 空をとぶ子 → マンタ |

さばくが2つ つづくのは わざとです。**同じ あめ色にすると 見わけられない**ので、
キャラメルのさばくは あめ色＋サバンナの大きい子、さばくのくには
白っぽいクッキーの砂＋は虫るいと 小さい子、で分けています。

### なかまは 並べ替えるだけ

`nextChar()` は `CHARS` を あたまから 見て、まだ いない子を わたします。
だから **並べ替えるだけ**で「どの国で だれが 来るか」が 決まります。
`id` は 変えないこと。セーブは `id` で つながっています。

クリア画面に出す 国の名前は、**ステージ番号ではなく その子の 並び順**から
出します。まけて やりなおすと ステージ1のクリアで5人目が来ることがあり、
ステージ番号で出すと ちがう国の名前を 言ってしまいます。

### てきは「国の中で ★が 上がる」

見た目の★は **国の中の どこまで きたか**で上げます（`(wave-1) % 20`）。

ウェーブそのもので上げると、**ウェーブ72で 上限に はりつきます**。
そこから先は 何百ウェーブ あそんでも ★6の4体しか 出てこず、
コレクションの残りに 一生 会えません（実際にそうなっていました）。
国ごとに 上げなおすと、どの国でも「小さい子 → その国のボス」の
ながれになり、44体ぜんぶに 会えます。

- 1体を いくつの国に 入れてもよい（ウサギは のはらにも 雪山にも いる）
- ★の見た目は **強さを 変えません**。強さは `ENEMIES` のまま
- **どの国にも 入れわすれた子は 一生 出てきません。**
  ずかんに いるのに 会えない＝コレクションが 完成しなくなるので、
  `tools/check-chars.mjs` の「出ない てき」が 1周ぶん たどって しらべます

## 伝説との たいけつ（#duOv）

「なにかが おきる」ウェーブ。むかしは 出会うだけで 自動的に 仲間に
なっていましたが、**1対1で たたかって かちとる**形に しました。

### 特別ウェーブは 国ごとに 1つ。その国の 15ウェーブめ

いち は **`ステージ番号 × 20 − 5`**。

| | | | |
|---|---|---|---|
| 15 シュガーガーデン | 75 チョコレートのたに | 135 ソーダのうみべ | 195 ひのくに |
| 35 いちごのおかしばたけ | 95 こおりのくに | 155 キャラメルのさばく | 215 よぞらのくに |
| **55 マカロンのもり** | **115 ゆきのケーキやま** | **175 さばくのくに** | 235 にじのてんくう |

**なぜ 15ウェーブめか。**20の倍数は ステージクリア＋ボーナスウェーブで
すでに こんでいます。5の倍数には 中ボスが 出ます。15なら その国の
おわりかけで、お祝いと ぶつかりません。国が かわるたびに 1回 来るので
リズムも そろいます。

**12国ぜんぶ 立っています。**国と ボスは かならず 合わせること。

| W | 国 | 伝説 | しゅくふく |
|---|---|---|---|
| 15 | シュガーガーデン | あめのきのせい | はじまりの ⭐+100 |
| 35 | いちごのおかしばたけ | いちごのじょおう | たまごが 5%やすい |
| 55 | マカロンのもり | ぎんがのくじゃく | 今日のプレゼントの 💎+1 |
| 75 | チョコレートのたに | ショコラのきし | かけら交換が 20%やすい |
| 95 | こおりのくに | ひょうがのマンモス | ダブりの かけら+1 |
| 115 | ゆきのケーキやま | ようがんのりゅう | たまごから にじの子が 出やすい |
| 135 | ソーダのうみべ | あわのクラーケン | アイテムの ドロップ+2% |
| 155 | キャラメルのさばく | キャラメルのぞう | 💗の 回復が 1分 はやい |
| 175 | さばくのくに | えいえんのスフィンクス | 早よびの 待ち時間+3秒 |
| 195 | ひのくに | ほむらのきゅうび | 早よびの ⭐+10% |
| 215 | よぞらのくに | つきのうさぎ | 今日のプレゼントの ⭐+50 |
| 235 | にじのてんくう | にじのペガサス | ボーナスの おかしが 3こ 少ない |

### しゅくふくは **難易度に さわらない もの だけ**

12こも つくので、1つでも 強いと 永久に かんたんに なります。
だから **「たおされにくさ」に さわる ものは 入れません。**

| やめた もの | なぜ |
|---|---|
| はじまりの げんき+5 | 20→25。通してよい数が **+25%**。負けにくさが 直に 上がる |
| はじまりのハート+2 | げんきも +2 上がる |
| ハート0で ふみとどまる | 実質 **無料の 復活が1回** |

**この3つが いちばん あぶない のは、`tools/sim.mjs` から 見えないから**です。
あのモデルが 見ているのは「時間内に たおしきれるか」だけで、
**「何体まで 通してよいか」は 一切 見ていません。**げんき+5 は
たおす速さを 1ミリも 変えないので、数字の上では 何も 起きていないのに
実際は 負けにくさが 25%上がります。**測れない ものを 入れない**のが きまりです。

のこした ものは ⭐・たまご・かけら・アイテム・💗・💎 だけ。
**12こ ぜんぶ 持った ときでも よゆうは +4〜7%、かべは W280 のまま**でした（実測）。
だから 数の 制限（何こまで つけられる）は **balanceの ためには 要りません**。

それでも 気をつける ことが 2つ。

- **かならず 関数に すること。**`ITEM_DROP`・`REGEN_MS`・`eggCost` は
  もとは 決めうちの 数でした。しゅくふくで 変わるのに 決めうちの まま だと、
  **ショップの 説明や 図鑑の 表示だけが 古く なります**（2か所に 書くと ずれる）
- **⭐に さわる ものは `tools/sim.mjs` を かけ直すこと。**
  きゅうびの「早よびの ⭐+10%」は `earlyBonus()` を とおるので モデルにも 効きます
- **かくりつを 変える ものは 表示も ついてくる ことを たしかめる。**
  りゅうの「にじの子が 出やすい」は `LEG_RATE()` を 上げます。えらぶ側
  （`eggOdds`）も かくりつ表示も おなじ 関数を 見るので ずれません。
  ここを 2か所に 分けると、表示だけ うそに なります（Apple 3.1.1／景表法）

### つよさは ウェーブから 式で 出す

```
hp = 900 + w*14 ／ atk = round(6 + w*0.055) ／ ivl = 1.6 - w*0.0015
```

**1体ずつ 手で 書かないこと** ——ならべたとき 前後で 逆転していても
気づけません。ねらいは「その国に とどく ころの ★の子で ぎりぎり 勝てる」
（W15は ★1、W55は ★2、W175は ★4、W235は ★6あたり）。

伝説は `GEN_LIST` に 入らないので、**キャラ数（128）は 変わりません**。
図鑑の 合計だけ 増えます（`lg_` の ずかん行）。

`tease`（「ウェーブ○○で なにかが おきる…」）は **`wave` から 作ること**。
数字を 2か所に 書くと かならず ずれます。

なかまを 1人 えらぶ → 画面を れんだして こうげき → かてば ずかんに 入り、
**にじの たまご 3こ**（`EGGS[2]`）が もらえます。

★6の たまごは むかし「伝説の たまご」でしたが、**伝説は いま
たいけつの ボスの 名前**なので、同じ 言葉が 2つの ものを 指して
いました。**にじの わくの子が 出る ゆいいつの たまご**なので
「にじの たまご」に 改名して あります（`id` は `e6` のまま
——セーブも かくりつ表示も id で つながっている）。

**近道には なりません。**にじの たまごでも にじの子が 出るのは
5%（`LEG_RATE`）で、のこり 95% は 金の わくの ★6です。
名前を 出すのは **`EGGS[2].name` から**（勝利画面と ガイドの 2か所）。
文字で 書くと、たまごを 変えたときに そこだけ 古くなります。

### まけても 取り上げない

**ここが いちばん だいじです。**3回 まけたら その周は 終わりですが、
つぎに あそんで また その ウェーブまで 行けば **また 3回**。
のこり回数（`duelSt`）は `reset()` で 消します ——**ここを 消し忘れると
1回 まけただけで 一生 会えなく なります。**

| | |
|---|---|
| 1回目 | 無料 |
| 2・3回目 | ⭐（`DUEL_STAR` ＝ 100 + ウェーブ×6） |
| 4回目〜 | 💎3 で「いますぐ もう一度」**だけ** |

**「💎を 払わないと 一生 手に 入らない」に しては いけません。**
コレクションを 課金で 閉じる形は、この リポジトリの
「強さそのものは 売らない／待てば無料」と 正面から ぶつかります。
💎は あくまで 時短です。

### まもり神——かちとった 伝説は 1体 つれていける

伝説は ながいあいだ **図鑑に かざるだけ**でした。祝福（永久パッシブ）も
空を とぶ 影も、見えているのに **手で さわれない**ので、集める はりあいが
出ません。だから **1体を つれていって、1プレイに 1回だけ よべる**ように
しました（`GUARD_ULT`・`useGuard`）。

- 入口は **ワザバーの いちばん左**（金のふち）。バーは 横スクロールなので
  **高さは 1pxも ふえません**（下のUIを 1px 足すと 盤面が 縮む）
- **タップでは 発動しません。**1プレイに 1回きりなので、うっかり さわって
  消えると とりかえしが つきません。ボタンは `#gdOv` を ひらくだけで、
  発動は 中の「よぶ ✨」
- **敵が いない ときは 画面を とじない**こと。とじると「押したのに 何も
  おきなかった」に 見えて、つかい切ったと 思われます。カードに
  「敵が いる ときに よんでね」と 出して ひらいた ままに します
- はじめて 勝った 1体は **こちらで えらんで おく**（`duWin`）。空っぽの
  スロットを 見せても、つかい方に 気づいてもらえません。勝利画面にも
  「ワザバーの いちばん左から よべる」を 1行 出します
- よんだ かどうか（`guardUsed`）は **`saveRun` に 入れる**こと。入れないと
  読みこみ直すだけで 何回でも よべます。えらんだ子（`sweetTD.guard`）は
  好みなので プレイを またいで のこします
- 説明文は **`ult` から 組みたてる**（`guardDesc`）。手で 書くと、数字を
  変えたときに 説明だけ 古くなります
- **スクロールするのは 中みだけ**（`#gdBody`）。画面ぜんたいを スクロールわくに
  すると、12体 そろった とき「よぶ」も ✕も 画面の 外へ 出て 押せなく
  なります（じっさい そうなって 直しました）。flex の 子には
  `min-height:0` を つけること ——ないと 中みの 高さで つっぱって
  スクロールせず、下が 切れたままに なります

#### 効きめは 12体とも 別のもの

同じ 効きめで 名前だけ ちがうと、どれを つれていっても おなじで、
**集める 意味が なくなります。**ことばは `GUARD_ULT` の中に そろえてあり、
足すときは この中で 組みます（`kill` / `cut` / `stun` / `push` / `star` /
`buff` / `cdReset`）。

| | 効き |
|---|---|
| あめのきのせい | 10秒 止める＋35%けずる |
| いちごのじょおう | ⭐2倍 20秒＋⭐どっさり |
| ぎんがのくじゃく | 画面の敵を ぜんぶ たおす |
| ショコラのきし | 先頭6体を たおす＋30%けずる |
| ひょうがのマンモス | 14秒 止める＋25%けずる |
| ようがんのりゅう | 75%けずる |
| あわのクラーケン | 9秒 止める＋3マス もどす |
| キャラメルのぞう | 7マス もどす＋20%けずる |
| えいえんのスフィンクス | 12秒 止める＋ワザの まちじかん回復 |
| ほむらのきゅうび | かたい9体を たおす |
| つきのうさぎ | 50%けずる＋⭐どっさり |
| にじのペガサス | ぜんぶ たおす＋攻撃2倍 30秒 |

#### バランス——1スロット・1プレイ1回に しばる

かべ（W250）は「そのウェーブを たおしきれるか」＝**つづく 火力**で
決まります。一発の 必殺が まるごと 1ウェーブを 肩がわりしても、敵HPの
複利は 1ウェーブで 2%しか のびないので、**かべは 1〜2ウェーブしか
動きません。**

**ここに「げんき」「ハート」「復活」に さわる ものを 入れないこと。**
`tools/sim.mjs` は たおす速さしか 見ておらず、通してよい 数が 変わっても
数字の上では 何も 起きません（祝福で 一度 やった 失敗と 同じ）。

いちばん 強い ペガサス（ぜんぶ たおす＋攻撃2倍）は **W235の 伝説に
勝たないと 手に 入りません**。強い 手ほど おくに 置く、が ものさしです。

#### 演出は 大きめ・長めに。ただし ぼかさない

よぶと その子が 盤面を よこぎります（`gdFly`）。**shadowBlur は かけない**
こと ——毎フレームの ぼかしは 60fps を 44 まで 落とします（まじょの
オーラで 実測）。かわりに 大きな 絵（`CELL*2.6`）と キラキラの 尾で 見せます。
似顔絵は **128pxで 焼いた もの**を つかうこと（`portraitOf` の 44px を
2倍に すると ぼける）。実測 68fps・JSエラー 0。

音は **役わりで 形を 分けます**（`gdSnd`）。たおす＝上へ かけ上がる／
止める＝下がる／けずる＝低い一発／バフ＝明るく のびる。SKILLS と 同じ 考えです。

### 予告の 黒い影は「来る 直前」だけ

まだ 会っていない 伝説は、すがたの 見えない **黒い影**で よぎります
（`flyer.sil`）。**ウェーブ0から とばしては いけません。**

条件は もともと「`wave >= つぎの伝説.wave - 15`」でした。1体目が
ウェーブ60の ころは W45からで よかったのですが、12国に そろえて
**1体目が W15に なった とたん W0 から** 条件が 立ち、ゲームを 始めた
とたん 黒い影が よこぎるように なりました（実機の 報告：3回 出た）。

いまは 2つ しばって あります（`silNear`）。

| | なぜ |
|---|---|
| ウェーブ6より前は 出さない | 始めた とたんに 出さない |
| その伝説の **6ウェーブ前から その ウェーブまで** | 来る 直前の 期待だけ 作る。通りすぎた あとに 出さない（3回 まけて つかい切ったら その周では もう 出てこないのに、影だけ 一生 とびつづける） |

**条件は `silNear()` 1か所に まとめること。**検査の `__dbg.flyerTest` も
そこを 見ます（2か所に 書くと ずれて、検査だけ 合格します）。

```
node tools/check-flyer.mjs      # 終了コード 0 で 合格
```

**目では 見つけられません** ——影は「そういう 演出」に 見えるので、
早すぎるのか どうかが 分かりません。直す前の コードで 走らせると
「ウェーブ 0,1,2,3,4,5 で 影が とぶ」と 落ちることを 確認ずみです。

### 出す条件は「いまの ウェーブ」。自己ベストでは ない

`checkLegends()` は **`wave >= L.wave`** を 見ます。
自己ベスト（`rec.bw`）で 見ると、記録が 60ある人には
**新しい周の ウェーブ1で** 出てしまい、背景（ウェーブ60の 国）と
ちぐはぐに なります。予告の「ウェーブ60で なにかが おきる」とも 合いません。

### 背景は その国の 絵。キャラは 上に えがく

`art/screens/duel_*.webp`（3:2）。えらぶのは **その とき いる 国**
（`DUEL_BG`）で、伝説の ウェーブでは ありません ——ウェーブを 動かしても
絵が ずれない ようにする ため。まだ 絵の ない 国は 予備（`LEGENDS` の `bg`）。
**絵を 足したら `DUEL_BG` に 1行 書くこと。**

**絵には キャラを 入れない**こと ——2体は canvas で 上に えがくので、
まん中と 下半分は 空けて 出します（`duelBg()` が その 文を 作ります）。

#### その国だと ひとめで わかる 目じるしを 1つ 決める

「砂漠」とだけ 書くと、キャラメルのさばくも さばくのくにも
**同じ すな色**に なって 見わけが つきません（`THEMES` の コメントと 同じ問題）。
だから 国ごとに 主役を 1つ 決めて 書きます。

| 国 | 主役 |
|---|---|
| さばくのくに | **ピラミッド・スフィンクス・オベリスク**（エジプト） |
| キャラメルのさばく | あめ色の 岩と サバンナの 木 |
| マカロンのもり | マカロンの 木 |
| ゆきのケーキやま | ケーキの 山に クリームの 雪 |
| ひのくに | クリームの 火山と キャラメルの 溶岩 |

12国ぶんの 文は `tools/gen-art.mjs` の `SCENES` に 書いてあります
（`duel_sugar` 〜 `duel_rainbow`）。出すのは
`node tools/gen-art.mjs --scene duel_egypt` のように 1つずつ。
**12国ぶん そろっています。**

#### 抜けは 目で 気づけないので 検査に 入れた

1国 抜けると、その国では **よその国の 絵**（`LEGENDS` の `bg`）が 出ます。
絵は ちゃんと 出るので、見ても おかしいと 分かりません。
`check-chars.mjs` の「たいけつの背景」が `DUEL_BG` と ファイルの 両方を
見て、12国 そろっているか しらべます（1国 消して 落ちることを 確認ずみ）。

### 2体とも 1枚に 焼く

毎フレーム `drawGen` / `drawPortrait` を よぶと **45fps** に 落ちました（実測）。
まじょの オーラと 同じ 失敗です。たたかいの はじめに 1回 焼いて
（`duBake`）、あとは 貼るだけに します。伝説の 金の オーラも 焼きこみます。

**入場の 紙ふぶきが 出ている あいだは 50fps あたりまで 下がります。**
これは 演出ぶんなので 直しません ——測るときは 2秒ほど 待つこと
（待たずに 測って「まだ おそい」と 早とちり しました）。

### つよさは ★で 決まる

`duAllyHp = 70 + ★×30` / `duAllyDmg = 7 + ★×5`。
伝説は `hp`・`atk`・`ivl` を `LEGENDS` に 持たせています。
**`ENEMIES` から 引かないこと** ——あちらは「何十体で けずる」前提の
数字なので、1対1では けたが 合いません。

ねらいは「★2で くじゃく／★4で りゅう／★6で スフィンクス」。
ゲージ（タップ20回で 6倍の 一発）を 入れて、+25%ほど 上のせして あります。

## 今週のおだい（週替わりステージ）

サーバーがないので全国ランキングは作れません。かわりに
**その週は世界じゅうで同じおだい**にして、記録をSNSで見せ合う形にしています。
週番号（月曜はじまり）からおだいと道を決めるので、誰がいつ開いても同じです。

| | |
|---|---|
| おだい | `WEEK_RULES` の6種類（かたい敵・はやい敵・げんき5・大群・ボスだらけ・はやい大群） |
| 道 | `makeWay(1000 + 週番号)`。ふだんの周とかぶらない種 |
| 記録 | `sweetTD.week`。週がかわると自動でまっさらに |
| ごほうび | ウェーブ10/20/30/40 で🩷かけら。その週に1回ずつ |

**ふだんのプレイとは別もの**です。入るとき `saveRun()` で進行を残し、
終わったら `tryResume()` で戻します。`saveRun` はおだい中は何もしません。

- 自己ベスト（`rec.bw`）は動かさない
- ステージクリアのごほうびとボーナスウェーブは出さない
  （短いプレイでたまごや💗をためる抜け道になるため）
- **💗（あと何回あそべるか）だけは共通**。ずるくためられないように

おだいを足すときは、**敵と盤面がわだけ**をいじってください。値段やUIに
手を入れると直すところが増えてこわれます。`?dbg=1` で `__dbg.setWeek(n)`
を使うと、どの週のおだいでも見た目を確かめられます。

## ともだちに おしえる（紹介）

図鑑の **📤** から ひらきます（`#invOv`）。おくるのは **紹介ページ**
（`invite.html`）で、ゲーム本体では ありません ——いきなり 盤面が 出ても
何の ゲームか わからないし、「ホーム画面に ついか」の やりかたも
つたえられないためです。

**App Store に 出す前でも ちゃんと はたらきます。**web版が あるので、
リンクを 開いた人は その場で あそべます。ストアに 出たら
`invite.html` の「じゅんび中」の `div` を バッジに さしかえるだけ
（**さしかえるのは そこ 1か所**）。

### おくり先ごとに できることが ちがう

| | リンクを 送る web の 口 | どう するか |
|---|---|---|
| LINE | **ある** | 1タップで 送れる |
| X | ある | 記録シェアの 予備で つかっている |
| **Instagram** | **無い** | コピー → アプリを ひらく |
| **TikTok** | **無い** | コピー → アプリを ひらく |

**インスタと TikTok に「送る」ボタンは 作れません。**リンクを わたす
公開の 口が ないためです。押しても 何も おきないボタンは 不具合に
見えるので、**「コピーしてから アプリを ひらく」**に して、
貼るのは 本人（ストーリーの リンクスタンプ・プロフィール・コメント）に
してもらいます。文でも そう 書くこと。

いちばん 楽なのは **けいたいの 共有シート**（`navigator.share`）で、
入れてある アプリが ぜんぶ 出ます。だから これを いちばん上に 置きます。
**ただし パソコンの ブラウザには 無いので、`navigator.share` が
無いときは ボタンごと 隠すこと**（押しても 何も おきないため）。

- コピーは **2とおり** 用意すること。`navigator.clipboard` は https と
  localhost でしか うごかず、`file://` で ひらくと 落ちます。
  古い `execCommand('copy')` を 予備に します
- **カードは `margin:auto` で まん中に すること。**`align-items:center`
  だけだと、カードが 画面より 高いとき **上が 切れて スクロールしても
  出てきません**（375x667 で 上が 19px 欠けた）

### OGP が 無いと ただの URL に なる

**紹介の いのちです。**むかしは `og:` タグが 1つも なく、LINEに はっても
絵の 出ない ただの URLでした。`index.html` と `invite.html` の 両方に
入れてあります。

- **絵は 1200x630 に こちらで 切っておく**（`art/screens/ogp.jpg`）。
  むこうに 切らせると どこが 残るか 分かりません。中みは
  「ようこそ」の 絵の まん中あたり（y=165..795）で、なかまの 顔が
  まん中に 来ます
- **JPEG に すること。**WebP を 読めない SNSが まだ あります
- URLは かならず 絵ぜんぶ 書く（相対パスだと 出ません）
- 「128体」は `invite.html` の **3か所**（`og:description`・
  `description`・本文）。キャラを 足したら ここも 直すこと。
  ゲーム本体の ガイドは 変数から 出しますが、あの ページは
  1まいの HTMLなので 手書きです

## 通信について

サーバーは ありません。GitHub Pages に 1まいの HTML を置くだけです。
だから **リアルタイムの通信対戦は作れません**。

ともだちと あそぶ しくみが ほしいときは、データを みじかい「あいことば」に
して SNS で おくり、うけとった側の ゲームに とうじょうさせる かたちにします。
アバターは `av.<8もじ>.<なまえ>` の形で、`?f=` つきの リンクでも うけとれます。

## ウェーブに 終わりは ない

ウェーブは無限に続きます。**1周（＝国をひとめぐり）ごとに道そのものが変わり**、
景色の順番も1つずれるので、同じ絵の繰り返しになりません。

1周の長さは `LAP_WAVES = STAGE_WAVES × THEME_COUNT`。いま20×12＝**240ウェーブ**です。
**ここを国の数とそろえないと、1周で通らない国ができて
ステージ番号と国がずれます**（なかまも敵もずれる）。`THEMES` を増やしたら
`THEME_COUNT` も増やすこと。ずれたら読みこみで落ちるようにしてあります。

道は手で用意しません。無限に続く以上、形も無限に要るからです。
**周番号を種にした乱数でその場で作ります**（`makeWay`）。同じ周なら
いつでも同じ形が出るので、セーブともズレません。1周目だけは手で作った形
（`FIRST_WAY`）にしてあります。はじめての人が見る画面なので。

どの形も次の範囲に収まるまで種をずらして作り直します。**道の形は見た目
だけでなく難易度そのもの**だからです。

| | 範囲 | 外れるとどうなるか |
|---|---|---|
| 長さ | 28〜36マス | 短い＝敵がすぐゴールに着く |
| 置けるマス | 72〜82 | 少ない＝仲間を並べられない |

200周（4万ウェーブ）ぶんで76種類。`tools/sim.mjs` がこの範囲を検査します。

どこかで必ず追いつかれる「かべ」が来て、そこがその日の記録になります。
かべの場所は手で決めず、**モデルで測って**決めてあります。

```
node tools/sim.mjs           # 終了コード 0 で合格（かべが 250〜550）
```

#### 「早よび」の ⭐を 数に 入れること

`sim.mjs` は ながいあいだ **早よびの ボーナス⭐を 数えて いませんでした。**
ウェーブ50の 早よびは +220、ウェーブ報酬も 220 ——つまり
**いつも 早よびする人の 収入を ちょうど 半分に 見あやまって**いました。

そのせいで「50ウェーブまでが かんたんすぎる・⭐が たまりすぎる」に
気づけませんでした。モデルは 2.0〜2.8倍と 出していたのに、
早よびを 入れて 測りなおすと **3.2〜5.6倍**でした。

**⭐が 入る 口を 足したら、かならず モデルにも 足すこと。**
式は `earlyBonus()` 1か所に まとめて、`__chk.bal` から 検査に わたしています。

いま効いている数字は5つだけです。

| | 内容 |
|---|---|
| `hpScale` | ウェーブ25をこえたら 1.020 の複利 |
| `MID_MAX` | **ウェーブ20〜100だけ 坂を きつくする（×2.5）**。中盤の「よゆう」を ならすため |
| `LAP_HP` | **1周（240ウェーブ）ごとに ×1.35 の段差**。複利の坂だけだと、ならべておけば自動ですすむ時間が長すぎた |
| `goldWave` | ウェーブ30をこえたら 1.004 の複利（HPよりゆっくり＝じわじわ追いつかれる） |
| `upMul` | 強化費は1レベルごと ×1.55。攻撃は ×1.78 なので、育てるほど⭐あたりが良くなる |

**この5つのどれかをいじったら、必ず `tools/sim.mjs` をかけ直すこと。**
レベル上限をなくす前は、ウェーブ60あたりで強化先がなくなって頭打ちでした。

### かべだけ見てもダメ。「よゆう」も見る

`tools/sim.mjs` は**かべの場所しか**見ません。かべが 330 でも、そこまでの
300ウェーブが 手ごたえなしなら ゲームとして こわれています。実際に
そうなっていました（実機の報告：「ウェーブ85で 敵が入り口で全部たおれる、
⭐もたまりすぎる」）。

**よゆう** ＝ そのウェーブを たおしきるのに 必要な火力に対して、実際に
どれだけ出ているか。1.0倍が ぎりぎり、2倍なら 半分の時間で たおせます。

| | 直す前 | 直したあと |
|---|---|---|
| W10 | 2.0倍 | 2.0倍 |
| W80 | **5.4倍** | 2.7倍 |
| W100 | **6.0倍** | 2.4倍 |
| W240 | 4.3倍 | 1.7倍 |
| かべ | W330 | W280 |

**この表は 早よびを 数えていない ときの ものです。**早よびを 入れて
測りなおすと、上の「直したあと」でも 50ウェーブまでは 3.2〜5.6倍 ありました。
いまの 数字は 下の 節を 見てください。

#### 50ウェーブまでの ゆるみは 3つ かさなっていた

「50ウェーブまでが かんたんすぎる・⭐が たまりすぎる」の 報告（実機）を
測ったら、原因が 3つ ありました。**1つ 直しても のこりが 効きます。**

| | 何が おきていたか | 直しかた |
|---|---|---|
| 坂が おそい | `MID_FROM 20 / SPAN 80` だと **ウェーブ50で まだ ×1.41**（ぜんぶ かかるのは 100） | `12 / 45` に つめる（出そろうのは W57） |
| 新しい敵が 来ない | ウェーブ16〜27に **新しい てきが 1体も 入らない**（つぎは ゴーレムの 28）。そこへ W20の レベル上限 解放が 重なる | ゴーレムを **18から** |
| 5の倍数だけ ゆるい | ボスが 10の倍数だけ なので、W15・25・35 が がくんと ゆるむ（W25 は **5.6倍**、W10 は 2.6倍） | 5の倍数にも 中ボスを 1体 |

なおした あとの よゆう（早よび こみ・かべは W280）。

| W10 | W15 | W20 | W25 | W30 | W40 | W50 | W60 | W80 | W100 |
|---|---|---|---|---|---|---|---|---|---|
| 2.5 | 3.0 | 2.5 | 3.0 | 2.9 | 2.4 | 2.2 | 2.1 | 2.4 | 2.6 |

#### それでも ゆるかったので、もう一段 締めた

「まだ かんたん」との 報告で、**HPと ⭐の 両方**を 動かしました。

| | 前 | 後 |
|---|---|---|
| `MID_MAX`（中盤の HP） | 2.5 | **3.0** |
| `goldWave`（⭐の のび） | 1.004 | **1.0037** |

| W20 | W30 | W40 | W50 | W60 | W80 | W100 | W150 | かべ |
|---|---|---|---|---|---|---|---|---|
| 2.4 | 2.7 | 2.1 | **1.9** | **1.8** | 2.0 | 2.2 | 2.4 | **W250** |

**かべが W280 → W250 に 来ました。**合格はんい（250〜550）の いちばん下です。
つぎに ⭐や HPを さわるときは、**ここから 先は ほとんど 余裕が ない**ことを
おぼえておいてください。

#### かべは 周の 段差（`LAP_HP`）に 引きよせられる

かべが どの 組み合わせでも 250 に なるので しらべたら、
**ウェーブ241の ×1.35 の 段差**が 効いていました。240で よゆうが
1.4ほど だと、段差で 1.05 に なって 10ウェーブほどで 力つきます。

`LAP_HP` を ゆるめれば かべは 遠のきます（2.5 の ころ 1.35→1.15 で
W280 → W310）。ただし 中盤を 締めた あとは 効かなく なりました
——そこまでで すでに ぎりぎり だからです。

**ウェーブ12より前は 1つも さわっていません。**あそこは もともと
よゆうが 2.5倍しか なく、いじると すぐ かべに なります（過去に W10で かべ）。

原因は **タワーと敵ののびかたの差**です。タワーは レベル上限が
10ウェーブごとに 1つ上がり、1レベルで こうげきが ×1.78。敵は ×1.02 の
複利だけ。ほうっておくと 差がひらきつづけます。

だから `MID_MAX`（ウェーブ20〜100で ×2.5）で うめます。**100 で 止めるのが
大事**です。そこから先は レベル上限ではなく **⭐が足りなくなる**ので、
止めないと かべが 手前に来すぎます。

**ウェーブ20より前は さわらないこと。**はじめの20ウェーブは もともと
よゆうが 2〜3倍しかなく、`hpScale` の linear（いま 0.30）を 0.8 に
上げてみたら **ウェーブ10で かべ**になりました。

はかりかたは `tools/sim.mjs` と同じモデルです。かべだけでなく
中みちの よゆうも 見たいときは、あのファイルの計算を そのまま使って
1ウェーブずつ `dps*walk*0.55/hp` を 出してください。

### 段差と とつぜんのウェーブ

複利の坂は なめらかすぎて、**⭐をならべておけば ウェーブ400あたりまで
自動で すすめて**しまいました。だから2つ足しています。

- **`LAP_HP`（周ごとの段差）** — 1周のかわりめで 敵のHPが ×1.6。
  「この周は もう こしがくだけた」が はっきり分かる区切りになります
- **`WAVE_MODS`（とつぜんのウェーブ）** — 1周に10こ、むずかしい回と
  やさしい回が まざります。ならべて待つだけの時間を こわすのが役目です

| | 効きめ |
|---|---|
| ⚡ とつげきラッシュ！ | 速さ×1.35・数×1.5 |
| 🛡 がんこな大群！ | HP×1.9 |
| 🌸 そよかぜタイム | HP×0.5・⭐×1.6（休ませる回） |

どのウェーブに 出るかは **ウェーブ番号から決めます**。乱数にすると
セーブや ともだちとの 話が 合わなくなるためです。

- **1周に かならず10こ。**周を `WMOD_N`＝10 の わくに割って、
  わくごとに1つ えらびます。番号を そのまま ハッシュしていたときは
  周1が8こ・周3が15こと かたよりました
- **しゅるいは `WMOD_PAT` の 決めうちの ならび**（むずかしい7・やさしい3）を、
  周ごとに1つ ずらして つかいます。ハッシュで えらんでいたときは
  **周2だけ やさしい回が7つ**になって、その周が まるごと 楽になりました
- ステージクリア（`%20=0`）・ともだちバトル（`%20=10`）・はじめの14ウェーブは
  さけます。おぼえてもらう回と かさねると、なにが起きたか 分からなくなるため
- **今週のおだい中は 出しません**（おだいは その週 全員が 同じ条件のため）
- `waveList()` そのものは いじらないこと。`tools/sim.mjs` が読んでいます。
  数をふやすのは `startWave` の中で、`spawnQ` に足す形で

`tools/sim.mjs` も `waveMod` を折りこんで かべを はかります。
**`WAVE_MODS` や `WMOD_PAT` をさわったら、これも かけ直すこと。**

道は曲がり角をつないだ折れ線を太くなぞって描いているので、**斜めの道も引けます**。
置けないマスは、マスをたどるのではなく**中心から折れ線までの距離**で決めます
（`buildPath`）。マスをたどる方式だと、斜めの道の下なのに置けるマスができます。

道を切りかえるのは**敵が1体もいないウェーブの境目だけ**です（`applyLayout`）。
途中で変えると道の長さが変わり、まだ途中の敵が一斉にゴールに入ります。
新しい道の上に乗ってしまった仲間は、こちらの都合なので**全額返します**。

⭐も攻撃力も何十万・何億になるので、表示は `fmtN()` で万・億に丸めます。

### Phase 6-0 ——「この ウェーブは 何者か」の あつめ先（`waveDef`）

**呼ぶ ところは 1か所も 変えて いません。**見た目も 音も 1pxも
変わりません。`waveList` / `waveMod` / `hpScale` / `goldWave` /
`earlyBonus` / `makeWay` / `wayMeasure` / `LAP_WAVES` の かたちも
そのままです（`tools/sim.mjs` が 読むため）。

#### なぜ 先に あつめ先だけ 作るのか

いま「ウェーブ番号から 何かを 決める」ところが **15系統**に
ちらばって います。このまま 季節イベントや 47都道府県を 足すと、
**どれが どれを 上書きするのか 決める 場所が ありません。**

じっさい `startWave()` は いま **上書き**で 処理して います。

```
2  spawnQ = waveList(wave)      基本の てき
3  wmod.count                → (count-1)倍を 水まし
4  weekly.many               → (many-1)倍を 水まし   ← 3と ほぼ 同じコード
5  isFriendWave(wave)        → spawnQ = [] で **1〜4を ぜんぶ 捨てる**
```

3つ目・4つ目を 足せば、おなじ ループが 4回に なります。

**まとめて 移しては いけません。**6-0 は あつめ先を 作るだけ、
移すのは 6-1 から 1つずつ です ——まとめると、どこで こわれたかが
追えなく なります。

#### かならず 純関数に する

次を 見ては いけません。

| 見ないもの | なぜ |
|---|---|
| **`new Date()`** | `tools/sim.mjs` は `file://` で n=1..5000 を まわします。日付を 見ると **走らせた日で かべの いちが 変わります** |
| セーブ・`weekly`・きせかえ・持っている キャラ | 同じ ウェーブが 人によって ちがう ものに なる |

だから 国は **`themeIdxOf()`** を 読みます。`themeOf()` は
**きせかえ中に 別の 名前を かえす**ので つかいません。

#### `special` は 旗の あつまり。1つの 文字列に しない

**同時に いくつも 立ちます。**W1〜480 を しらべると **52回**
2つ以上 立って いました。

| ウェーブ | 立つ 旗 |
|---|---|
| W10・30・50… | `midBoss` ＋ `friendSlot` |
| W20・40・60… | `stageClear` ＋ `bonus` |

#### かえす もの

```
n / stage / inStage / lap / inLap / countryIdx / enemyTypes / mod
special: { countryStart, midBoss, stageClear, bonus,
           friendSlot, duelSlot, mod }
```

- `friendSlot` と `duelSlot` は **枠だけ**です。ほんとうに 出るかは
  `avFriends.length` や 図鑑・のこり回数（＝ゲームの 状態）なので
  ここでは 見ません
- `enemyTypes` は **`waveList()` を そのまま 呼ぶだけ**。
  ここに 式を 写しては いけません（2か所に なると かならず ずれます）
- `countryIdx` は **番号の まま**。安定IDに するのは 6-2 です。
  **ここで 国の 名前を 恒久キーに しないこと**
  （`DUEL_BG` と `groundKey` が 名前を キーに して いて、名前を 変えると
    だまって こわれる ——それを 直すのが 6-2）
- `element`（属性）は 6-3、`origin`（入手元）は 6-6。**先まわりしない**

#### 1つ おぼえて、こおらせる

`groundKey()` の ように **毎フレーム 呼ばれる ところ**へ 6-1 で
移しても おもく ならない よう、直前の 1つを おぼえます（`wdN` / `wdDef`）。
純関数なので 同じ ものを かえして よく、`Object.freeze` で
呼び出し側が こわせない ように します。

#### 検査は「呼び出し側の 式を 書きうつして」くらべる

`__chk.bal.waveDef` から n=1..5000 を まわし、
**`waveDef` の 中と 同じ 変数を つかわずに** 式を もう一度 書いて
つきあわせます。おなじ 変数を つかうと、うつしまちがいを 見のがします。

#### 画素は「おなじ版を 2回 撮って」から くらべる

盤面は キラキラが いつも 動いて いるので、**md5 は かならず ちがいます**
（18まい ぜんぶ ちがう と 出ます）。ゆれの 下限を 先に 出すこと。

| | ちがう画素（平均 / 最大）|
|---|---|
| おなじ版を 2回 | 0.656% / 0.949% |
| **変更前 ↔ 変更後** | **0.696% / 1.041%** |
| 変更前 ↔ 変更後（もう1組）| **0.628% / 0.906%** |

**2組目は おなじ版どうしより 小さい**ので、差は ぜんぶ 動いている
粒子です。片方だけ 撮って「1%も ちがう」と 早とちり しないこと。

### Phase 6-1 ——ちらばった 判定を `waveDef` から 読む（6か所）

**`waveDef` の 中みは 1文字も 変えて いません。**読む がわだけを 移しました。
見た目も 音も 変わりません（下の 画素の 節を 見ること）。

| どこ | 前 | 後 |
|---|---|---|
| `startWave` とつぜんのウェーブ | `waveMod(wave)` | `waveDef(wave).mod` |
| `startWave` 国名の おひろめ | `(wave-1) % STAGE_WAVES === 0` | `.special.countryStart` |
| ウェーブ終了 中ボス祝い | `wave % 10 === 0 && wave % STAGE_WAVES !== 0` | `.special.midBoss` |
| ウェーブ終了 ステージクリア | `wave % STAGE_WAVES === 0` | `.special.stageClear` |
| ウェーブ終了 ボーナス予約 | `wave % 20 === 0` | `.special.bonus` |
| `isFriendWave` | `n >= 10 && n % STAGE_WAVES === 10` | `.special.friendSlot` |

#### **材料の がわは さわらない**（ぐるぐる回りに しない）

```
もとの 判定（waveList / waveMod / themeIdxOf）
        ↓
     waveDef
        ↓
     つかう がわ          ← 6-1 で 移すのは **ここだけ**
```

だから 次は **変えて いません**。

- `waveMod()` の 中の `i % STAGE_WAVES === 0 || i % STAGE_WAVES === 10`
- `waveList()` の 中の `n % 10 === 0`
- `themeIdxOf()`（国の 計算そのもの。6-2）

#### `duelSlot` は 移せない ——**対応する 呼び出し側が ない**

`waveDef` の `duelSlot` は「その ウェーブに 伝説が **立って いる**」
（`l.wave === n`）ですが、`checkLegends()` は
**`wave >= l.wave`（以上）**で さがします。**別の 意味**なので、
おなじだと 証明できません。むりに 置きかえないこと。

`silNear()`（予告の 黒い影）も `next.wave - 6 〜 next.wave` の
**はんい**で、`duelSlot` とは ちがいます。

#### `isFriendWave` は **枠と 状態を 分ける**

```
isFriendWave = n => avFriends.length > 0 && waveDef(n).special.friendSlot;
                    ~~~~~~~~~~~~~~~~~~~~~   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
                    ゲームの 状態             ウェーブの 枠（純関数）
```

`waveDef` は 純関数なので「ともだちが いるか」を 見られません。
**状態の ほうを 呼び出し側に のこす**のが 正しい 分けかたです。
`duelSlot` も 同じ 考えで 枠だけ です。

#### `special` は **複数booleanの まま**でよい

`stageClear: true` と `bonus: true` を 同時に 持てて いるので、
Set や 配列に する ひつようは まだ ありません。
**変更の ための 変更を しないこと。**

#### wave=0 に 気をつける（`waveDef` は 1に まるめる）

`waveDef(0)` は `Math.max(1, n|0)` で **1に なる**ので、
`stageClear` と `bonus` が **旧式と 逆に なります**（旧 true／新 false）。

| n=0 | 旧式 | waveDef |
|---|---|---|
| `countryStart` | false | **true** |
| `stageClear` | **true** | false |
| `bonus` | **true** | false |

移した 6か所は ぜんぶ **wave が かならず 1以上**です。

- `startWave()` の 中は `wave++` の あと
- ウェーブ終了の えだは `running` が 立って いる 中。
  `reset()` は **`wave = 0` と `running = false` を おなじ行で** する

**移す ところを ふやす ときは、そこが wave=0 を 通らないかを
かならず たしかめること。**

#### 検査は「変更前の 式を 書きうつして」50周ぶん

`__chk.bal.waveDef` から **n=1..12000（＝50周）**をまわし、
変更前の 式を **もう一度 書いて** 1ウェーブずつ くらべます。
ずれ 0 でした（それぞれ 600回・とつぜんのウェーブは 500回 立つ）。

さらに `__dbg.setWave(n)` ＋ `__dbg.run()` で
**「ウェーブ終了」の えだ そのもの**を W1〜44 で 走らせ、
ステージクリアと ボーナス予約が 出る ウェーブが
変更前後で 同じ（どちらも 20 と 40）ことを 確かめます。

> **ボタンの 文字で 判定する ときは、似た 文に 気をつけること。**
> 予約は「🎁 このあと ボーナスタイム！」、じっさいの 開始は
> 「🎁 ボーナスタイム！ お菓子をタップ！」で、**どちらにも
> 「ボーナスタイム」が 入って います。**「ボーナスタイム」で
> 見ると、W20 の 2.4秒後に 始まった ボーナスを **W34 の 予約**と
> 読みまちがえます（じっさい 1回 だまされた）。「このあと」で 見ること。

#### 画素は「両がわの ゆれ」で はさむ

片がわ（変更後どうし）だけだと、たまたま 静かな 2まいを 引いて
**下限を 小さく 見つもります**。変更前どうしも 撮って はさむこと。

| 組 | ちがう画素（平均 / 最大）|
|---|---|
| **変更前 ↔ 変更前**（ゆれ）| **0.657% / 1.055%** |
| 変更後 ↔ 変更後（ゆれ）| 0.587% / 0.867% |
| 変更前 ↔ 変更後 ①| 0.619% / 1.030% |
| 変更前 ↔ 変更後 ②| 0.609% / 0.777% |
| 変更前 ↔ 変更後 ③| 0.602% / 0.808% |

**変更前後の 3組が ぜんぶ「変更前どうしの ゆれ」より 下**なので、
差は ぜんぶ 動いている 粒子です。

### Phase 6-2 ——国に **言語に よらない 恒久キー**を 持たせる

`themeIdxOf()` の 計算そのもの・国の ならび・ウェーブの わりあて・
`LAP_WAVES`・`waveList`・難易度・表示される 国名は **1つも 変えて いません。**

#### 4つを 分ける

| | 何か | どこ |
|---|---|---|
| **国** | ゲームの すすみ方が 決める **恒久の identity** | `countryIdOf(w)` |
| テーマ | その国を どう 見せるか | `THEMES[i]` |
| きせかえ | プレイヤーが 一時的に えらんだ 見た目 | `themeOverride` |
| BGM | 音の 見せかた | 6-4。**さわらない** |

```
const countryIdOf = w => THEMES[themeIdxOf(w)].id;          // きせかえに 影響されない
const themeKeyOf  = w => themeOverride ? 'sp:' + themeSel : countryIdOf(w);
```

#### 12国の 恒久キー

| idx | id | 表示名 |
|---|---|---|
| 0 | `sugar` | シュガーガーデン |
| 1 | `berry` | いちごのおかしばたけ |
| 2 | `forest` | マカロンのもり |
| 3 | `choco` | チョコレートのたに |
| 4 | `ice` | こおりのくに |
| 5 | `snow` | ゆきのケーキやま |
| 6 | `sea` | ソーダのうみべ |
| 7 | `caramel` | キャラメルのさばく |
| 8 | `egypt` | さばくのくに |
| 9 | `fire` | ひのくに |
| 10 | `night` | よぞらのくに |
| 11 | `rainbow` | にじのてんくう |

- **日本語の 名前を 内部キーに しないこと。**英語版で 変わります
- **絵の ファイル名を 正本に しないこと。**`DUEL_BG` は
  `id → 絵` の 表を べつに 持ちます（`'duel_' + id` で 作らない）
- id が かぶって いたら 起動時に 落とします（`THEME_COUNT` の
  見はりの となり）——だまって こわれると 気づけないため

#### たいけつの 背景は **国**から 引く（きせかえで 落ちていた）

```
むかし  DUEL_BG[themeOf(wave).name]     ← きせかえ中は 表示名が SP_THEMES の 名前に なる
いま    DUEL_BG[countryIdOf(wave)]      ← 国は きせかえに 影響されない
```

**きせかえ中は 表が 空ぶりして、12国 ぜんぶが 予備（`l.bg` ＝
`duel_forest`）に 落ちて いました。**絵は ちゃんと 出るので
目では 気づけません（実測で 確かめた）。

| | 変更前 | 変更後 |
|---|---|---|
| きせかえ なし | duel_sugar / ice / egypt / rainbow | **同じ** |
| きせかえ 中 | **ぜんぶ duel_forest** | duel_sugar / ice / egypt / rainbow |

**これが 6-2 で ゆいいつ 目に 見える ちがい**です。

#### `groundKey` は「見せている もの」。国IDでは ない

じめんの 焼きなおしは **見た目が 変わったか**を 見る ところなので、
`countryIdOf` では なく `themeKeyOf` です。国IDに すると
**きせかえても 焼きなおされなく なります。**

きせかえには `sp:` を つけて、国の id と ぜったいに かぶらない
ようにします。

**分けかたが 前と 同じ ことを 機械で たしかめること。**
9きせかえ × 36ステージ ＝ **324とおり**の 総あたりで、
「旧で 同じ組は 新でも 同じ・旧で ちがう組は 新でも ちがう」を
見ます（旧 20しゅるい／新 20しゅるい・ずれ 0 でした）。

#### 表示に つかう `.name` は のこす

置きかえたのは **内部キー**だけです。次の 3か所は 人に 見せる
文字なので `themeOf().name` / `themeOfStage().name` の ままです。

- ステージ開始の 国名の おひろめ
- ステージクリアの「『こおりのくに』の子が やってきた！」
- 図鑑の ヒント「ステージN『…』クリア」

#### BGM は 6-4。ここでは さわらない

`BGMS` が 10曲しか なく 12国と 合っていない ことと、
周が かわると 曲と 国が ずれる ことは **見つけた まま のこして** あります。
音が 変わる 変更なので、別に 判断します。

### Phase 6-3 ——国は「土地」。**RPGの 属性を 割りあてない**

12国に `env`（その国が どんな 場所か）を 足しました。
**戦闘・演出・UI・絵には 1つも つないで いません。**データと
読み口だけ です。プレイヤーから 見た ちがいは ありません。

#### なぜ `element`（火・水・風…）に しなかったか

**国の 中みを 見たら、1国＝1属性が 成り立って いませんでした。**

| | 場所 | なかま | 伝説 |
|---|---|---|---|
| マカロンのもり | もり | つむじかぜ（**風**）| ぎんがのくじゃく（**星空**）|
| ゆきのケーキやま | ゆき | わたぐも（**雲**）| ようがんのりゅう（**炎**）|
| チョコレートのたに | たに | かみなりのこ（**雷**）| たに |
| よぞらのくに | よぞら | はなびのこ（**花火**）| つき |

**ゆきのケーキやまの 伝説は「炎の伝説」**です。雪山の 国に
炎の 伝説が いる ——1国に 属性を 1つ 決めた 時点で うそに なります。
場所と なかまが ずれて いる 国も **5つ**（もり→風・たに→雷・
ゆき→雲・よぞら→花火・はたけ→四つ葉）。

さらに **シュガーガーデン・いちごのおかしばたけ・マカロンのもり・
チョコレートのたに**には 自然な 属性が ありません。ここに むりに
`earth` や `wood` を 当てると、**世界観の ほうが 属性表に
引っぱられます。**

そして **こおり↔ゆき、キャラメルのさばく↔さばくのくに**は
場所としては 同種です（CLAUDE.md の「同じ あめ色に すると
2つが 見わけられない」＝ わざと 見た目で 分けた）。
**12国は 12属性に なりません。**

> **結論：国 ＝ 土地。属性は 国とは べつの 軸で、しかも
> 1国1属性では ない。**だから `element` の フィールドは
> **作りません**（`null` を ならべる ことも しません
> ——空の 欄が あると、あとで 機械的に 埋められます）。

#### 12国の `env`

| id | env | 表示名 |
|---|---|---|
| `sugar` | `garden` | シュガーガーデン |
| `berry` | `field` | いちごのおかしばたけ |
| `forest` | `forest` | マカロンのもり |
| `choco` | `valley` | チョコレートのたに |
| `ice` | `ice` | こおりのくに |
| `snow` | `snow` | ゆきのケーキやま |
| `sea` | `sea` | ソーダのうみべ |
| `caramel` | `desert` | キャラメルのさばく |
| `egypt` | `desert` | さばくのくに |
| `fire` | `volcano` | ひのくに |
| `night` | `night` | よぞらのくに |
| `rainbow` | `sky` | にじのてんくう |

**11しゅるい／12国。`desert` が 2つ かぶるのは 正しい**
——土地の 種類なので、同じ 土地が 2つ あって かまいません
（id で 見わけます）。

- **言語に よらない 安定ID。**日本語を 入れないこと
- `countryEnvOf(w)` と `waveDef(n).env` で 読めます。
  `countryIdOf` と 同じで **きせかえに 影響されません**
- 見はり：`env` の ない 国が あると 起動時に 落ちます

#### 属性を 入れたく なったら

**先に「何と 何の 相性表を 作るか」を 決めてから**にすること。
相性表が 無いまま 値だけ 決めると、あとで かならず 変わります。
入れる ところも 国とは かぎりません ——敵1体・アイテム・
とつぜんの ウェーブの ほうが 自然かも しれません。

`env` は そのとき も **そのまま つかえます**。「こおりのくには
氷の 場所」は 相性表が どうなろうと 変わらない 事実だからです。

### Phase 6-4a ——BGM は 国IDの 表から えらぶ

**これまでで はじめて「耳で 分かる」変更**です。10国で 流れる 曲が
変わりました。新しい 曲は 1つも 作って いません。

#### ならびの ぐうぜんの 一致に たよって いた

```
むかし  BGMS[ceil(w/20)-1 % BGMS.length]     ← ステージ番号を そのまま index に
いま    BGMS[BGM_OF[countryIdOf(w)]]         ← 国IDの 表から
```

**`BGMS` の ならびは `THEMES` の ならびと ちがいます。**曲の コメントは
国名を 名のって いるのに、配列の 順が 対応して いませんでした。
だから **周0の ステージ3（W41）から** ずれて いました
——12国で 合って いたのは **3つだけ**（`sugar` / `berry` / `sea`）。

原因は 3つ あって、**1つ 直しても のこりが 効きます**。

| | 何が おきて いたか | 直しかた |
|---|---|---|
| ① ならびの ずれ | `BGMS` の 順 ≠ `THEMES` の 順 | 国IDの 表（`BGM_OF`）で 引く |
| ② 曲の ない 国 | `egypt` と `fire` は **曲そのものが 無い** | いちばん 近い 曲を あてる（暫定）|
| ③ 周で さらに ずれる | 式に `lapOf` が 無い | `countryIdOf` が ふくむので **自動で 直る** |

#### いまの 表

| 国ID | 曲# | 曲 |
|---|---|---|
| `sugar` | 0 | シュガーガーデン |
| `berry` | 1 | いちごのおか |
| `forest` | 7 | マカロンのもり |
| `choco` | 2 | チョコレートのおか |
| `ice` | 3 | こおりのしろ |
| `snow` | 8 | ゆきのケーキやま |
| `sea` | 6 | ソーダのうみべ |
| `caramel` | 5 | キャラメルのさばく |
| **`egypt`** | **5** | **暫定**（専用曲なし。土地が 近い）|
| **`fire`** | **6** | **暫定**（専用曲なし。10曲で いちばん 勢いが ある bpm134）|
| `night` | 4 | よぞらのテラス |
| `rainbow` | 9 | にじのてんくう |

**曲の 名前は 遊ぶ人には 見えません**（コードの コメントだけ）。
だから `fire` に「ソーダのうみべ」を あてても 名前は 問題に なりません
——**耳で 合うかが すべて**です。

**専用の 曲を 作ったら、この 2行を 書きかえる だけ**で すみます（6-4b）。

#### きせかえの 上書きは **ぜったいに 変えないこと**

```
const bgmFor = w => themeOverride ? themeOverride.bgm : BGMS[BGM_OF[countryIdOf(w)]];
```

きせかえの BGM は **有料の 機能**です。

- ショップの 商品説明（💎60）に「ステージの見た目と**BGM**が変わります」
- 合言葉の 画面に「限定テーマ（見た目と**BGM**の着せかえ）」
- `SP_THEMES` の **8種すべてに 専用の `bgm`** が 書いてある

「きせかえで BGM を 変えない」に すると、**売って いる 商品の 説明が
うそに なり、8曲が つかわれなく なります。**

#### 国を ふやしたら 曲も 足す

`BGM_OF` に その国が 無いと **音が 出なく なります**。目では
気づけないので、起動時に 落とします（`THEMES` の id の 見はりと 同じ）。
はんい外の 番号も 見ます。

#### 何が 鳴って いるかを 見る

```
__dbg.bgmNow()
→ { wave, countryId, normalBgmIndex, override, finalBgm, bpm }
```

`__chk.bal.normalBgmIdx(w)` は **音の 準備なしで** 表だけ 引くので、
`file://` の 検査からも つかえます。

#### 耳だけは 機械で 測れない

見た目は 変わらないので **画素では 何も 出ません**（実測：ゆれ 0.504%
に 対して 変更前後 0.479% と、**むしろ 小さい**）。
「その国に 合って いるか」は 実機で 聴いて 決めること。

（2026-09-02：W41 マカロンのもり／W161 さばくのくに／W181 ひのくに を
実機で 聴いて **採用**。`egypt:5` / `fire:6` の 暫定も そのまま 確定）

### Phase 6-5 ——① base → ② modifier → ③ encounter

`startWave()` は `spawnQ` を 上から 順に **上書き**して いました。
`waveList` → とつぜんのウェーブ → おだい → ともだちバトル の 4段が
おなじ 配列を 取りあう 形です。ここに 季節イベントや 47都道府県を
足すと、**どれが どれを 上書きするのか 決める 場所が ありません。**

だから 3段に 分けて 名前を つけました（`wavePlan()`）。
**中みは 1つも 変えて いません。**

```
① base       waveList(n)                              1つだけ
② modifier   wmod.count → weekly.many → weekly.boss   0こ以上・順序に 意味が ある
③ encounter  ともだちバトル                           1つだけ・**前段を すてる**
```

#### `spawnQ` に 入らない ものを 混ぜない

ここが 6-5 の いちばん 大事な 線引きです。数えて 分けました。

| | どこに 効くか |
|---|---|
| **ambient** HP・速さ・⭐・げんき | `spawnQ` に **焼きこまれない**。`wmod` / `weekly` を **つかう しゅんかんに 読む**。だから **ボスの しょうかんで わく スライムにも かかります**（`e.sum`）|
| **overlay** midBoss・stageClear・bonus・duel・countryStart | `spawnQ` に さわらない。ごほうびと 演出だけ |

**この2つを `wavePlan()` に 入れないこと。**入れると
「キューを 作る 関数」が HPも 演出も 決める ことに なり、
分けた 意味が なくなります。

#### これから 足す ときの ものさし

| 足したい もの | どこへ | 前段を すてるか |
|---|---|---|
| 47都道府県モード | ① base の 差しかえ | ベースごと 入れかえ |
| 季節イベント（難易度に さわらない）| overlay | すてない |
| 季節イベントの 特別ウェーブ | ③ encounter | **すてる** |
| 誕生日の 特別キャラ | ③ encounter | すてる |
| 新しい とつぜんのウェーブ | ② modifier | すてない |
| 属性バッジ・国の 見た目 | overlay | さわらない |

**③が 2つ以上に なった ときに はじめて 優先順位の 表が いります。**
いまは ともだちバトル 1つだけ なので 作りません
（`waveDef().special` も **複数booleanの まま**）。

#### 決める ところを 2か所に しない

- `wmod` を 決めるのは **`wavePlan()` の 中 1か所**。
  `startWave()` で `weekly ? null : waveDef(wave).mod` を
  もう一度 計算しないこと
- `isFriendWave()` は **消しました**。`wavePlan()` の ③に 移した ので、
  のこすと「ともだちバトルか どうか」を 決める ところが 2か所に なります
  ——6-5 が なくそうと している 形 その ものです

#### 演出は 1つも 動かさない

うかぶ文字は **配列の 順に えがかれます**。キューの 計算を
まとめた ついでに `floatTx` を うしろへ 寄せると、かさなり方が
変わって 画素が 動きます。だから ならびは いまの まま
——**とつぜんのウェーブ → ともだちバトル → 国名**。

#### 水ましの 式を「自然だから」で 変えない

```
add = Math.round(base.length * (mul - 1));    ← ceil / floor に しない
q.push(base[i % base.length]);                ← もとは 足す前の 長さ
```

**`Math.round` は ほんとうに 効いて います**（ceil に すると 検査が 落ちる）。

いっぽう **`base[i % base.length]` と、のびていく `q[i % q.length]` は
数学的に 同じ もの**です（600とおりで 実測）。`i < base.length + i` なので
あまりは いつも `i` で、`q[i]` は 何周しても `base[i % base.length]` に
なります。**だから ここを まちがえても 検査では 見つかりません。**
`base` を のこしているのは 読む人の ための 用心で、ふるまいでは ありません。

#### weekly は 通常モードとは **別に** 測る

`waveDef` からは 見えない ことが 4つ あります。**ぜんぶ 現行仕様として
そのまま のこしました。バグと 決めないこと** ——おだい中にも ともだちが
出るのは、ゲームとして その ほうが 楽しい かもしれません。
weekly そのものを 設計する ときに 判断します。

| | 実測 |
|---|---|
| `weekly` 中の `wmod` | **つねに null** |
| **`weekly` + ともだちバトル** | **重なる。**W10 で spawnQ が **全置換**され、おだいの 水ましも ボスも すてられる |
| **`weekly` + 伝説の たいけつ** | **重なる。**`checkLegends()` に weekly の ガードが 無い |
| `weekly` + 国名の おひろめ | 重なる。道は `weekWay()` なのに **国名は 出る** |
| `weekly` + 敵の 見た目 | そのウェーブの 国の まま |
| `weekly` + ボーナス | 起きない（`&& !weekly`）|
| `weekly` + ステージクリア | 重なるが お祝いだけ（ごほうび なし）|

#### 同時に 立つ 旗（W1〜5000 で 実測）

| 組 | 回数 | |
|---|---|---|
| `midBoss + friendSlot` | 250 | 実際に 重なる |
| `stageClear + bonus` | 250 | 実際に 重なる |
| `countryStart + mod` | **21** | 実際に 重なる |
| `friendSlot + mod` ほか | **0** | **構造的に 起きない**（下記）|
| `duelSlot + mod` | **0** | **理論上は 起きうる。いまは 偶然 起きていない** ⚠️ |

`waveMod()` は `i % STAGE_WAVES` が 0 か 10 の とき null を かえします。
`LAP_WAVES`(240) は 20 の 倍数なので **`i % 20 === n % 20`** が 成りたち、
stageClear / bonus / friendSlot / midBoss とは **証明つきで 排他**です。

> **`duelSlot` だけは ちがいます。**伝説は 15,35,55…235（`i % 20 === 15`）で、
> `waveMod` の ガードを **12本 すべて 通過します。**重なって いないのは
> 周0の たね（`lapOf(n) * 7919 = 0`）が たまたま そこを 外して いるだけ
> （周0の mod は 22,51,66,101,116,128,166,179,211,219）。
> **`WMOD_PAT` や `WMOD_N` を さわると 重なりえます。**そのときは
> 「伝説の ウェーブに とつぜんの ウェーブが 乗ってよいか」を
> 決めて から 変えること。

#### `reset()` は `wmod` / `friendWave` を 消していない

つぎの `startWave()` が かならず 上書きするので **実害は 出ません**が、
`__dbg.fill()` の ように 直接 `spawnEnemy` を 呼ぶ 道では 古い 倍率を
拾います。**6-5 では 直して いません**（記録だけ）。

#### 検査 ——220,000ケースの 総あたり

`__chk.bal.wavePlan` は 純関数なので、**旧ロジックを 検査がわに
独立して 書きうつして** つきあわせられます。
**`wavePlan` の 中の 変数を つかい回さないこと**（うつしまちがいを 見のがす）。

- `n = 1..5000` × `weekly` 11通り × `friends` 0/1/3/12
- くらべるのは `spawnQ.join(',')` ／ `mod.id` ／ `encounter`

`weekly` の 11通りには **にせルール 4つ**（`many` が 2.5〜6）が
入って います。**いまの 倍率は ぜんぶ 2ばい 以下**（1.5 / 1.6 / 1.3）なので、
それだけでは 水ましの 経路が 浅くしか 踏まれません。ゲームには 足しません。

**わざと こわして、検査が 落ちる ことを たしかめる こと。**
6とおりで 確認ずみ ——`Math.round`→`ceil` ／ modifier の 順いれかえ ／
`weekly.boss` の 条件ずらし ／ ともだちの ならびかえ ／
encounter が 前段を すてない ／ weekly でも mod を 出す。

実走行の くらべ方は **前↔前・後↔後の ゆれを 先に 出す**こと。
`早よび +⭐` の 額は `nextT`（実時間）で 決まるので、
**同じ版を 2回 走らせても 3〜6件 ちがいます**。ここを 見て
「ずれた」と 早とちり しないこと（`spawnQ` と 演出順は 全ペアで 0）。

### Phase 6-6 ——「どこで 手に 入れたか」（origin）の 受け口

`dex` に **フィールドを 1つ 足した だけ**です。個体ID・複数所持・
交換・限定フラグ・進化段階は 1つも 作って いません（Phase 7）。

```
dex[id] = { w:入手ウェーブ, d:入手日, lv:とうたつレベル, origin:入手元 }
```

#### 意味は「その **種** を **はじめて** 手に 入れた 経路」

いま 個体モデルが 無いので、「いま 持って いる 個体の 出自」は
正しく 表せません。だから **種単位の 初回入手元**として あつかいます。
Phase 7 で 個体モデルを 入れたら `instance.origin` へ そのまま
のばせる 名前と 値に して あります。

**`dex` を 個体ごとの 配列や インスタンスに 変えないこと。**
セーブ・交換・複数所持・進化・限定キャラまで 一気に 連鎖します。

#### 上書きの 見はりは **もともと あった**

`unlock()` は いちばん上で `if (dex[id]) return false;` と 返すので、
2回目からは 何も 書きません。**初回の origin は 構造的に 守られます**
——新しい 判定を 足す 必要は ありませんでした
（`fillDex` で 全件 unlock しなおしても 変わらない ことを 実測）。

#### 呼び出し元から 明示的に わたす

`unlock()` の 中で グローバルから 推測しないこと。いまなら `weekly` や
`duS` を 見て 当てられますが、イベントが ふえた ときに **だまって
まちがえます**（推測の 条件が 1つ ふえるたびに 全経路を 見なおす）。

**`eggDraw()` と `giveItem()` は 1つの ヘルパを 3〜4か所から 呼んで
います。**ここは 引数で 通すこと ——中で 決めうちに すると、
ステージクリアの たまごも 伝説の ごほうびの たまごも おなじ `egg` に
なって、**あとから 区別できなく なります**。

#### 値は 言語に よらない 安定ID（`ORIGIN`）

| | どこから |
|---|---|
| `starter` | はじめての 5人（`giveStarters`）|
| `egg` | ⭐/💎で 買った たまご |
| `stage` | ステージクリア（なかま・たまご・アイテム）|
| `duel` | 伝説に かった（本人・ごほうびの たまご）|
| `legend` | 到達制の 伝説（`legendArrival`。**いまは 通らない**）|
| `battle` | 敵を たおした |
| `drop` | 戦いの ドロップ（アイテム）|
| `place` | 盤面に おいた（お菓子タワー・なかま）|
| `shard` | かけら交換 |
| `login` | ログインボーナス（ながれぼし）|
| `shop` | ショップで 買った（はじめてパック・アイテムセット）|
| `debug` | 検査どうぐ |
| `unknown` | **既定値**（下記）|

**日本語の 表示文字列を 保存しないこと**（英語版で 変わる）。
`event` / `birthday` / `prefecture` / `referral` / `code` / `trade` は
**まだ 足しません** ——その しくみが 無いので、書いても うそに なります。

> **合言葉（`CODES`）は いま 図鑑に 何も 足しません。**
> 着せかえ（`sweetTD.codes`）だけ です。だから `code` は まだ 要りません。

#### 既定値を `normal` に しない

古い セーブの エントリは **どの 経路で 来たのか ほんとうに 分かりません**。
`normal` に すると「ふつうに 手に 入れた」と 決めつける ことに なり、
あとから 本物の `normal` と 区別できません。だから `unknown` です。

**読むのは かならず `originOf(id)` を とおす**こと
（`(dex[id] && dex[id].origin) || ORIGIN.unknown`）。
**ロード時に 一括で 書きかえない**こと ——実測で、古い セーブの
`localStorage` に `origin` が 1つも 書かれない ことを 確かめて います。

> いちばん 古い セーブには `dex['ch_xxx'] = {}` という
> **フィールドが 1つも 無い** 形が あります（`sweetTD.col` からの
> 引っこしが 作る）。これも `unknown` で 読めます。

#### わたし忘れは 目でも 実行しても 見つからない

`unlock(id)` と 2つめを 書かずに 呼ぶと、その子は だまって `unknown` に
なります。**図鑑には ふつうに 入るので、画面は 何も おかしく なりません。**

だから `check-chars.mjs` が **ソースを そのまま 読んで** 数えます。

```
node tools/check-chars.mjs      # 「unlockの origin」の 行
```

4とおりで 落ちる ことを 確認ずみ ——origin を わたし忘れ ／
`ORIGIN` に 無い 値 ／ 日本語の 文字列を 直に わたす ／
ヘルパ（`eggDraw`・`giveItem`）が origin を 受けとらない。

**コメントを 先に 消してから 数えること。**「行あたまが `*` か `//`」で
よけると、ブロックコメントの 途中の 行が すりぬけて「origin無し」と
出ます（じっさい 出ました）。

### Phase 7-1 ——`dex` を 読む 口を 1つに する

`dex[id]` の 直読みが **39か所**ありました。その うち
**31か所は「持って いるか」の 真偽値**だけ を 見て います。
そこを `hasDex()` / `dexRec()` に 寄せました。
**所有モデルは 1ミリも 変えて いません。**

| | |
|---|---|
| `hasDex(id)` | 持って いるか。**`!!dex[id]` と まったく 同じ 意味** |
| `dexRec(id)` | レコードそのもの（`dex[id] \|\| null`）|

#### なぜ 先に 読み口を 閉じるのか

`dex[id] = [...]` の ように 形を 変えた とき、**`!![]` は true** です。
つまり「**0体 持って いる**」が「持って いる」と 読まれて、
31か所が **だまって こわれます**（図鑑の 数も カードも）。
目でも 見つけられません。

だから 7-2 以降で 個体テーブルを 足す 前に、切りかえ点を 1つに して
おきます。**7-1 は「所有モデルを 変える」のでは なく
「変えられる ように する」だけ**です。

#### `const` では なく `function` で 書く

`hasDex` を いちばん はじめに 呼ぶのは `hasLg`（ファイルの ずっと 上）です。
`const` に すると 場所に よっては TDZ で 落ちます
（「`const` は 巻きあがらない」と 同じ 話）。

#### `dexRec()` は **その場の オブジェクトを そのまま かえす**

写しを かえしては いけません ——`rc.lv = t.lv`（到達レベルの 記録）が
dex に とどかなく なります。無い ときに **かわりの レコードを
作らない**ことも 大事です。作ると「持って いない」が
「持って いる」に 化けます。

#### 寄せない ものが 4つ ある

目的が ちがうので、むりに 同じ 関数へ 隠さない こと。

| のこす もの | なぜ |
|---|---|
| `unlock()` の 書きこみ | 書きこみ。読み口では ない |
| 引っこし（`sweetTD.col` / `tw_c_` / 伝説の `w` そろえ）| 1回きりの 補正 |
| `dex['tw_'+k].lv` の 更新 | 到達レベルの **記録**（種単位。個体状態では ない）|
| `dex['lg_'+id].w` の 補正 | 出会った ウェーブの そろえ |

#### `dex[id] = {}` を 所持と 見なす 現行仕様を こわさない

いちばん 古い セーブ（`sweetTD.col` からの 引っこし）は
**フィールドが 1つも 無い** エントリを 作ります。
`origin` や `w` / `d` / `lv` の あるなしを 所持判定に 足しては いけません
——足した とたん、あの人たちの 図鑑が 空に なります。

#### 直読みが ふえて いないかを 機械で 見る

```
node tools/check-chars.mjs      # 「dexの 直読み」の 行
```

のこして よいのは **4つ だけ** ——accessor じしん／書きこみ／
引っこし／検査どうぐ。それ以外は 落とします。3とおりで 落ちる ことを
確認ずみ（membership を 1つ 戻す ／ record を 1つ 戻す ／ 新しく 足す）。

**accessor を わざと 逆に して、検査が 落ちる ことも たしかめる こと。**
`hasDex` を `!dex[id]` に すると、図鑑が「仲間 74/74」に なって
`check-chars` も 落ちます（＝この 口が ほんとうに 効いて いる 証拠）。

#### はじめての 5人は **顔ぶれが 毎回 ちがう**

`giveStarters()` は `Math.random()` で えらぶので、**同じ版を 2回
走らせても 5人の 顔ぶれは 変わります**。ここを 見て「ずれた」と
早とちり しないこと。くらべるのは **人数（5）と origin（starter）と
ショップバーの 中み**です。

### Phase 7-2 ——個体を 置く **からの 箱**だけ 作る

`sweetTD.inst` を 足しました。**中みを 作る しくみは 1つも ありません。**
mint・いまの 所持からの 生成・複数所持・進化・交換・UI は 7-3 以降です。

#### `dex` とは **別の キー**に する（7-0 の A案）

`dex` は 種／図鑑単位の 所持として **凍結**します。別キーに するのが
大事なのは、**古い 版に もどしても `dex` が 無傷**だから です
——ロールバックできる ゆいいつの 形です。`dex` の 中に 個体の 配列を
持たせると `!![]` が true なので「0体 持って いる」が
「持って いる」と 読まれて こわれます。

#### ふつうに あそんで 1件も できない こと

これが 7-2 の いちばん 大事な 条件です。**ゲームの ロジックからは
1か所も 見て いません。**`hasDex()` / `dexRec()` も `dex` だけを 見る まま。
呼ぶのは 検査どうぐ（`__chk.inst`）だけ です。

```
node tools/check-chars.mjs      # 「instの 参照」の 行
```

`inst` / `instPfx` / `instSeq` / `saveInst` / `instDevice` / `newInstId` /
`INST_V` を、**箱の 定義と 検査どうぐ の 外**で 使うと 落とします。
3とおりで 確認ずみ ——`unlock` から さわる ／ 図鑑が 読む ／
ゲームから `newInstId()` を 呼ぶ。

> `inst` は みじかい 名前なので、**語の 切れめで さがす**こと
> （`instance` や `instagram` を ひろわない ように）。

#### 採番も **同じ キーに 入れる**

`{ v, pfx, seq, items }` を 1つの キーに 入れます。
**採番（`pfx`/`seq`）を 別キーに しないこと** ——書きこみが かたっぽだけ
通ると 連番が ずれ、**IDを 使いまわす**ことに なります。

#### 読んだだけでは 書かない（遅延生成）

古い セーブには この キーが ありません。**無い ときは `{}` として
読むだけ**で、`localStorage` には 何も 書きません。端末の しるしも
連番も、**はじめて IDを 出す ときに 作ります**。だから 7-2 を 入れても、
あそんで いない人の セーブは **1バイトも 変わりません**（実測）。

#### IDは `<端末の しるし6文字>.<連番36進>`

| | |
|---|---|
| 端末の しるし | 一度 作ったら **変えない**。作った その場で 保存する |
| 連番 | **もどらない・使いまわさない。**出した しゅんかんに 保存するので、読みこみ直しても つぎの IDが かぶらない |
| 交換で もらった 個体 | **IDを 変えないこと**（来歴が 切れ、二重に 受けとった ことも 分からなく なる）|

**「世界で ぜったいに 一意」とは あつかわないこと。**サーバーが ないので
保証できません（「通信について」）。**実用上の 衝突よけ**です。

**壊れた 値を 読んでも 連番を 下げないこと**（`raw.seq > instSeq` の ときだけ 採る）。
下げると IDを 使いまわします。

#### 「IDを 出す」と「個体を 作る」は 別の しごと

`newInstId()` は IDを かえすだけで、`inst` には 何も 入れません。
500こ 出しても **個体は 0件**の ままです（実測）。
出した IDが あまっても かまいません ——**使いまわさない**ほうが 大事です。

#### 画素の はみ出しは「どの 1まいか」まで 見る

320px で 信号が ゆれの 帯を こえた ように 見えました。全組合せで
測りなおすと、こえた 2本は **どちらも `b2`（変更前の 1まい）**が
からんで いて、あの 1まいは 12枚中 いちばん 平均が 高い
（0.773）——パーティクルが 多い しゅんかんに 撮れた だけ でした。

| | |
|---|---|
| ゆれ（同じ版どうし・30本）| 0.439〜0.806　平均 **0.612** |
| 信号（前↔後・36本）| 0.445〜0.877　平均 **0.617** |

**平均の さは +0.0055。**本数を そろえて 平均で 見ること
——最大値だけ 見ると、本数の 多い ほうが かならず 大きく 出ます。

### Phase 7-3 ——いまの 所持から 個体を **必要な ときだけ** 起こす

`ensureInst(sp)` を 足しました。**所有の 正本は まだ `dex` です。**

#### `hasDex()` の 意味を 変えない

「`inst` に 無いから 持って いない」には **ぜったいに しないこと**。
7-3 の `inst` は「`dex` に すでに ある 所持を、将来の 個体モデルへ
写す **補助データ**」でしか ありません。

機械で たしかめて あります ——個体が 0件でも 図鑑は「5 / 74」と 出る／
個体を 作っても 図鑑の 数は 変わらない／**`dex` から 消すと、個体が
`inst` に のこって いても 未所持に なる**（5 → 4）。

#### ロード時に 全部 変換しない

その種の 個体が ほんとうに 要る ときに 1件だけ 作ります。
いまの 画面も 戦闘も 個体を 必要と して いないので、
**呼び口は 1つも ふやして いません** ——検査どうぐから 遅延生成を
証明できれば じゅうぶんです。ふつうに あそぶだけでは **0件のまま**、
`localStorage` にも 1バイトも 書きません（実測）。

#### 1種＝1個体（上限1）・idempotent

何回 呼んでも 同じ 個体が かえります（100回で 1通り）。
**すでに ある ときは 書きこまない**こと ——連番も 動かしません。
複数所持は 7-5 で ゲーム仕様を 決めて から。

#### 中みは 2つ だけ

```
inst[個体ID] = { sp: 種のid }
```

> **`dex[id].origin` を 個体に 写しては いけません。**あれは
> 「その **種** を はじめて 手に 入れた 経路」です。写すと
> 「種の 初回入手元」と「個体の 出自」を おなじ ものとして
> 固定して しまいます。個体の origin は 7-6 で 別に 持ちます。

#### `dex` は 1文字も 書きかえない

個体を 作る ときに `dex` を さわりません。`dex[id].instId = …` の
ような **逆リンクも 足しません**。`dex` の かたちは 凍結の ままです。

#### 未所持の 種には 何も 作らない

`dex` に 無い 種を `ensureInst` しても、**`dex` も 個体も 作らず
`null`** を かえします。書きこみも 連番も 動きません。

#### 索引を 作らない

さがすのは `inst` を ただ なめるだけ です。169件で **1回 0.0075ms**
（実測）なので、索引を 足す 理由が ありません。
**性能の ために 二重管理を 1つ ふやさないこと。**

#### ゆるす 参照は 3つ だけ

```
node tools/check-chars.mjs      # 「instの 参照」の 行
```

基盤（7-2）・`ensureInst`／`instOfSpecies`（7-3）・検査どうぐ。
それ以外から さわると 落とします。3とおりで 確認ずみ
——`unlock` から 呼ぶ ／ 図鑑が 読む ／ 配置で さわる。

### Phase 7-4 ——新しく 手に 入れた ときに 1個体 作る

責任を **3つに 分けた まま**、入手経路に つなぎました。

| | |
|---|---|
| `unlock(sp, origin)` | その **種** を はじめて 図鑑・所持に 登録する |
| `mintInst(sp, origin)` | **1個体を 作る**（新しく 手に 入れた とき）|
| `ensureInst(sp)` | いまの 所持から **必要な ときだけ** 1個体へ 橋わたし |

#### **`unlock()` の 中に mint を 入れない**

入れると「新規登録の ときしか mint できない」形に 固まります。
将来 こう なるからです。

- すでに 図鑑に いる 種を もう 1個体 もらう
- 交換で 既存の 種の 個体だけ ふえる

どちらも **`unlock()` は false なのに mint は 要る**場面です。

だから 入口を `gainSpecies(sp, origin)` に して、
**`unlock` の 戻り値で しばらずに** かならず 両方 呼びます。
かえすのは `unlock` の 戻り値 そのままなので、呼ぶ 側の
書きかたも 意味も 1つも 変わりません（12か所とも 語の 入れかえ だけ）。

> **12か所に `mintInst` を 書き足す やりかたは えらびませんでした。**
> 3か所は `if (unlock(…))` の 形で、mint を `if` の 外に 出す 書きかえが
> 必要に なります。`gainSpecies` なら **1語の 入れかえ**で すみ、
> 7-5 で 上限を ひろげる ときも **呼ぶ 側は 1か所も 直りません**。

**検査どうぐ（`__dbg.fillDex` / `guardGive`）は ここを 通しません**
——ほんとうの 入手経路では ないので、個体を ふやしません。

#### 7-4 は 上限1。変えるのは 3行だけ

`mintInst` の 中の「すでに あれば それを かえす」3行が 上限です。
7-5 で 複数所持を 決めた ときは **ここだけ** ひろげます。

#### `origin` は 受けとるだけ・まだ 保存しない

`dex[id].origin` は **種の 初回入手元**、個体の 出自は **別の もの**です。
`mintInst` は 引数で 受けとりますが **保存しません**（7-6）。
ここで 保存すると 2つを 同じ ものとして 固定して しまいます。

#### ソースでは 見つけられない ものが 1つ ある

「mint を `unlock` の 戻り値で しばって いないか」は **参照では なく
ふるまい**なので、ソース検査では 見つかりません。
だから **実行して たしかめます** ——個体だけ 消して（`dex` は のこす）
同じ 種を もう一度 手に 入れ、個体が また できるか。
`if (isNew) mintInst(…)` に 変えると ここが 落ちます（確認ずみ）。

#### 「0件」を あてにした 検査は 7-4 で 意味を うしなう

7-2・7-3 の 検査には「ふつうに あそんでも 個体は 0件」が 入って
いました。**7-4 が わざと それを 変えます。**落ちたら 回帰では なく、
検査の ほうを 新しい 前提（「さわった 種の ぶんだけ」）に 書きかえること。
採番の 検査も **絶対値（500）では なく 前後の さ**で 見る ように しました
——starter の ぶんで 連番が すでに 進んで いるためです。

### Phase 7-5 ——複数所持を ゆるす ところを **1か所に しぼる**

`MINT_MULTI` を 足しました。**7-5 では から のまま**で、
ふるまいは 7-4 と 完全に 同じ です。

```
const MINT_MULTI = new Set([]);      // ← 複数所持の 唯一の 切りかえ点
```

`mintInst(sp, origin)` は `origin` が この 中に **無ければ 上限1**、
**あれば 2個体目 以降を 作れます**。

#### いまの 12経路には 2個体目を 正当化できる ものが **1つも ない**

実測して 分かった ことです。**上限を 外すだけ では すみません** ——
外すと こう なります。

**① 4経路は 重複で 毎回 呼ばれ、大量に ふえる**（12ウェーブの 実測）

| 経路 | 呼び出し | うち 重複 | 重複率 | いちばん 多い |
|---|---|---|---|---|
| `battle` | **480** | 466 | **97.1%** | `en_slime` ×160 |
| `place` | 37 | 33 | 89.2% | `tw_choco` ×12 |
| `drop`（`it_`）| 毎回 | — | — | `gear[id]` が すでに 個数を もつ（上限99）|

`waveList` から 出した 敵の 総数 ——**W250（かべ）までで 8,889体**、
W500 で 17,964体（ボスの しょうかんは 数えて いない ので もっと 多い）。
`battle` を 入れると 個体が それだけ できます。

**② のこり 8経路は、重複が 構造的に 起きない**（外しても 何も 変わらない）

| | しぼり |
|---|---|
| `egg` | `pool.filter(o => !hasGen(o))`。**ダブりは かけらに なる**（既存の しくみ）|
| `shard` | `filter(… && !hasGen(o))` |
| `starter` | 1回きり ＋ pool から 除去 |
| `stage`（`ch_`）| `CHARS.find(c => !hasDex(…))` |
| `duel`（`lg_`）| `checkLegends` が `!hasDex` で しぼる |
| `login`（`sp_rpurin`）| `if (!hasDex('sp_rpurin'))` |
| `shop`（パック）| `packPool().filter(!hasGen)` ＋ 1回きり |
| `legend` | **デッドコード**（`preview` でしか 呼ばれない）|

> **`egg` を 複数mint に しては いけません。**ダブりを かけらに 変える
> `SHARD_GET` の しくみが 意味を なくします。あれは 個体の 話では なく
> **ゲームの しくみ**です。

**だから 現行 12経路は ぜんぶ `MINT_MULTI` に 入れません。**
足して よいのは **個体を くばる ことが 目的の 新しい 経路**
（`event` / `birthday` / `trade`）だけ です。

#### 数の 上限は **まだ 決めない**

1人 何体までか は、ほんとうに 1経路 開ける ときに
「要るのか・経路ごとか・ぜんたいか」を 決めます。
**先に 数字を 仕様に しないこと。**

#### `MINT_MULTI` は `ORIGIN` の **あと**に 置く

はじめ 個体の 箱の となり（`ORIGIN` より 前）に 書いたら、
`ORIGIN.battle` と 書いた とたん
**`ReferenceError: Cannot access 'ORIGIN' before initialization` で
画面が 真っ白**に なりました（「`const` は 巻きあがらない」）。
`mintInst` は 呼ばれるのが ずっと あとなので、`ORIGIN` の あとで かまいません。

**検査どうぐで 見つけました。**わざと `ORIGIN.battle` を 入れて みたら、
検査が 何も 出さずに 終わりました ——落ちて いたのです。
**「検査が 無言で 通った」は「合格」では ありません。**

#### 検査

```
node tools/check-chars.mjs      # 「MINT_MULTI」の 行
```

いまの 12経路（`starter` / `egg` / `stage` / `duel` / `legend` / `battle` /
`drop` / `place` / `shard` / `login` / `shop` / `debug`）が 入って いたら
落とします。`event` / `birthday` / `trade` は 通します。
4とおりで 確認ずみ。

**架空の origin を その場で 入れて ためすこと。**`__chk.inst.multi` は
生の Set なので、検査の 中で `add` → 2個体目が できる ことを 見て →
`delete` で もどせます。**ソースは から の まま**です。

### Phase 7-6 ——**個体ごとの** origin

`inst[id] = { sp, origin }` に しました。**種の origin とは 別もの**です。

| | 何の 経路か | 読む口 |
|---|---|---|
| `dex[sp].origin` | その **種** を **はじめて** 手に 入れた 経路（6-6）| `originOf(種のid)` |
| `inst[id].origin` | その **個体そのもの** を 手に 入れた 経路（7-6）| `instOriginOf(個体id)` |

**1つの 関数に まとめては いけません。**複数所持が 開いたら、
おなじ 種でも 個体ごとに ちがう 値に なります（たまごで 来た子と、
イベントで もらった子）。

#### 新しく 作る ときだけ 保存する

`mintInst` は **個体を 作る しゅんかん**の 経路を のこします。
すでに ある 個体を かえす ときは **書きかえません** ——origin は
「その 個体を 手に 入れた しゅんかんの 値」なので、あとで 別の 経路で
また 手に 入れても 上書きしては いけません（実測：別の origin で
10回 再入手しても `place` の まま）。

#### `ensureInst` は **移行上の 推定値**

古い セーブの 救済なので、個体の 出自の 記録が そもそも ありません。
だから 種の 初回入手元（`dexRec(sp).origin`）を
**best-effort の 推定値**として ひきつぎます。無ければ `unknown`。

**`dex` を 書きかえたり、origin を 埋めもどしたり しないこと。**
読んで うつすだけ です。

#### 古い 個体（`{ sp }` だけ）は そのまま 読める

7-3〜7-5 で できた 個体は origin を 持ちません。それは
**正常な 古いデータ**です ——読むときに `unknown` で おぎなうだけで、
**ロードで 埋めない**こと（`dex` の origin と まったく 同じ 考えかた）。

#### 「2つが 別もの」は **この 形でしか 見わけられない**

`instOriginOf` を `originOf` に すりかえても、ふつうの 場面では
**同じ 値が かえります**（1種1個体の あいだは どちらも 同じ 経路だから）。
見わけが つくのは **dex には origin が あって、個体には 無い**とき だけ。

```
dex['c_dog'] = { origin:'shard' }      個体 = { sp:'c_dog' }（origin 無し）
  正しい … instOriginOf → 'unknown'
  すりかえ… instOriginOf → 'shard'    ← ここで はじめて 落ちる
```

検査には この 形を 1つ 入れて あります。**これが 無いと、
`originOf` に まとめる 改変も、ロードで dex から 埋める 改変も
すりぬけます**（じっさい 3とおりで 落ちる ことを 確認）。

> **「メモリの 中だけ 埋める」は 無害な ことも あります。**
> `unknown` で 埋めるのは 読み口と 同じ こたえなので、検査は
> 落ちません（落ちないのが 正しい）。**dex から 埋める**のと
> **保存まで する**のが 有害で、そちらは 落ちます。

#### 「{ sp } だけ」を あてにした 検査は 7-6 で 意味を うしなう

7-3・7-4 の 検査には「中みは `{ sp }` だけ」「個体に origin を
保存して いない」が 入って いました。**7-6 が わざと それを 変えます。**
落ちたら 回帰では なく、検査の ほうを 書きかえること。

> **`addInitScript` は 読みなおしでも 走ります。**古い セーブを
> 仕こむ 検査で reload を すると、**セーブが もどって しまいます**
> （じっさい「読みなおしても 同じ」が 落ちました）。旗を 1つ 置いて
> 1回だけ 仕こむ こと。

### Phase 7-7 ——恒久進化は 秘薬とは **別システム**

#### `t.rb` / `t.rbx`（秘薬）は これからも run-local

調べた ことを のこします。**恒久進化と 接続しないこと。**

| | |
|---|---|
| 書く ところ | `applyElixir()` と `tryResume()` の **2つだけ** |
| 読む ところ | 6つ（`dmgOf` ／ 二重がけ防止 ／ かけられる相手 ／ `saveRun` ／ 描画2 ／ 検査）|
| 寿命 | **tower の 寿命＝1ラン**。`reset()` で 消え、道が 変わると 返金されて 消え、売ると 消える。`sweetTD.run` に 入るので 同じラン内の 読みなおしだけ 生きのこる |
| 能力への 作用 | **攻撃力だけ**（×2 / ×3）。速さ・射程・ねだん・特殊能力は 見ない |
| `tools/sim.mjs` | **1文字も 見て いない** |

> **かべ W250 は「秘薬なし」で 測られて います。**恒久化すると
> **モデルの 数字は 1ミリも 動かないのに 実機だけ 易しく なります**
> ——祝福で 一度 やった 失敗と まったく 同じ形です。

#### `evo` に 限定を 入れない

`inst[id].evo` に 入れて よいのは **進化の 段階と 分かれみち だけ**
（いずれ `e1` / `e2` / `e2a` / `e2b` の ような 安定ID）。
`halloween` / `bday` / `event` / `limited` は **別の 軸**なので、
別の フィールドに します。「★は 強さ。限定性を ★の 本数に 埋めない」と
まったく 同じ 話です。

#### Phase 7-7-1 ——読む口だけ

`instEvoOf(id)` を 足しました。**書く 口は まだ ありません**
——進化の しかた・もとに もどせるか・ねだんが 決まって いないためです（7-7-3）。

- `evo` が **無い ＝ 基本形**。だから **`null`** を かえします
  ——`'base'` の ような 文字列に すると、ほんとうに 保存された 値と
  見わけが つかなく なります
- 古いデータに `'base'` を **書きこまない**（bulk migration を しない）
- `mintInst` / `ensureInst` は evo を **つけません**。新しい 個体も
  `{ sp, origin }` の まま
- `dex` に evo を 足さない。`artKeyOf` / `drawGen` / 各キャッシュも
  まだ さわらない（画像は 7-7-2）

#### **`dex` を 見ないことは、この 形でしか 証明できない**

`instOriginOf` の ときと まったく 同じ 話です。ふつうの 場面では
`dex` を のぞいても 同じ 値に 見えます。見わけが つくのは
**`dex` には 進化らしい 値が あって、個体には 無い**とき だけ。

```
dex['c_bear'] = { evo:'DEX_E9' }      個体 = { sp:'c_bear', origin:'egg' }（evo 無し）
  正しい … instEvoOf → null
  のぞく … instEvoOf → 'DEX_E9'      ← ここで はじめて 落ちる
```

5とおりで 落ちる ことを 確認ずみ ——dex を のぞく ／ 無い ときに
`'base'` を かえす ／ ロードで 補う ／ mint が つける ／
ensure が dex から ひきつぐ。

#### 保存を 走らせるには「ほんとうに 1件 作る」しか ない

7-7-1 には 書く 口が 無く、`mint` / `ensure` は **すでに ある 個体には
書きこみません**（7-4/7-5 の 性質）。だから 既存の 種で 呼んでも
保存は 走りません。検査で 保存を 見たい ときは
**まだ 個体の ない 種**を 1つ 足すこと（じっさい 1回 だまされました）。

#### tower に 個体IDが 要るのは 複数所持を 開ける とき

いまは `towers[i].k` から `T.ally ? k : 'tw_' + k` で dexId が 出て、
1種1個体なので `instOfSpecies()` が **一意**に 引けます。
同じ 種の 個体A と 個体B が ちがう 進化状態を 持てる ように なった
しゅんかんに、`towers[i].iid` が 要ります。**それまでは 足さないこと。**

#### 画像の キャッシュキーは 3系統ある（7-7-2 の 注意）

絵の 解決は `drawGen` の `ART_SPRITE[artKeyOf(rc)]` 1か所に
まとまって いますが、**キャッシュキーが 3つ あり、どれも evo を
ふくんで いません**。ここを なおさないと 進化の 前後で 同じ絵が 出ます。

| | キー |
|---|---|
| `genSpriteCache` | `o.id + '@' + px` |
| `rbCache` | `'g_' + gen.id + '@' + px` |
| `pcache` / `gdPcache` | `pid` のみ |

#### Phase 7-7-2 ——絵の receiver は **純関数**に する

`artKeyFor(rc, evo)` を 足しました。**種の 絵 ＋ わたされた 進化状態 →
絵の キー**。

```
artKeyFor(rc, evo){
  const base = artKeyOf(rc);
  if (!evo) return base;                 // ← いまと 完全に 同じ
  const k = base + '_' + evo;
  return ART_SPRITE[k] ? k : base;       // ← 絵が 無ければ base へ
}
```

**`inst` を のぞかないこと。**個体の 状態を 知って いる **呼ぶ側が
evo を わたす**形に します。えがく 関数が こっそり 個体を さがしに 行くと、
「どの 個体の 絵か」を 決める ところが えがく 関数の 中に できて、
複数所持が 開いた ときに **どちらの 個体か 分からなく なります**。
そう して おけば 図鑑・似顔絵・盤面・将来の 個体えらび画面の
**どこからでも 同じ resolver**が つかえます。

#### 存在確認は `ART_SPRITE`。`ART_KEYS` では ない

`ART_KEYS` は「読みに 行った」だけ の ならびで、404 でも のこります。
しかも 読みこみは あと から 終わるので、「いま つかえるか」を 見るには
**実物（`ART_SPRITE`）**を 見る しか ありません
（`drawGen` が もともと `ART_SPRITE[…] || null` で 見ているのと 同じ）。

#### `artKeyOf` は さわらない

あちらは 絵の キーで あると 同時に、`DEX_LEGEND` / `DEX_GOLD` /
`DEX_SIDE` の **見わけの キー**にも なって います。evo を 混ぜると
わくの 色や 敵味方の ふりわけまで 変わります。だから **別の 関数**に します。

#### キャッシュキーは「evo が あるときだけ」のばす

```
base  id@px            ← これまでと 完全に 同じ
evo   id@px@e1
```

`genSprite(o, px, evo)` も `rbSprite(src, px, tag, evo)` も この形。
**base まで 別キーに しないこと**（`id@px@base` の ように すると、
何も 変えて いないのに キャッシュが 総入れかえに なります）。

`drawPortrait` → `charThumb` → `pcache` / `gdPcache` は **`DEX_ART[id]` を
ひく 別系統**で、`rc` を 持ちません。evo を わたせる 呼び出し元が
1つも 無いので **7-7-2 では さわりません**。図鑑の UI（7-7-5）で
進化した 似顔絵が 要る ときに、あちら用の receiver を 別に 作ります。

#### 検査

```
node tools/check-chars.mjs      # 「絵の resolver」の 行
```

- resolver の 中で `inst` / `dex` を さわると 落とす
- **ゲームの ほうから evo を わたして いない**ことも 見る
  （7-7-2 では 本番プレイは 全員 base art）

> **さいごの 引数が ただの `evo` なら 通すこと。**それは 定義そのものか、
> resolver どうしの 受けわたし（`genSprite` → `drawGen`）です。
> ここを 見おとすと **まっさらな コードで 落ちます**（じっさい 落ちた）。
> ゲームが ほんとうに わたす ときは 値か 呼び出しに なるので つかまります。

#### 粒子の ゆれでは 足りない ときは 絵そのものを くらべる

7-7-2 の 画素くらべで、393px だけ 平均の さが **+0.0989** と
桁ちがいに 出ました（ほかの フェーズは ±0.001）。本数を 30/36 に
ふやすと +0.0164 まで 下がり、1まいごとの 平均も 前後で 入りまじって
いましたが、**それでも 言い切れません**。

こういう ときは **えがいた 絵を そのまま くらべる**こと。
`__chk.drawGen` で 128体 × 46px/64px ＝ **256枚を 画素で つきあわせて
完全一致**でした。変えたのは 絵の 作りかたなので、これが 本命の 測りかたです。

> **両方の 版に ある 関数で えがくこと。**`genSprite` は 7-7-2 で
> はじめて `__chk` に 出したので、変更前の 版には ありません
> （`__chk.genSprite is not a function` で 止まりました）。

#### Phase 7-7-3-1 ——「戦闘中に つかえる アイテム」を 切り出す

`ITEMS` は「戦闘の 消耗品」の 顔を して いますが、じっさいには
**9つの 入口**を かねて います。

| | 何が 拾うか |
|---|---|
| `gear` の 初期化・保存／`ITEM_BY_ID` | 持ちもの |
| 図鑑の アイテムタブ／ガイド本文 | 見せる |
| ステージクリアの ドロップ／戦いの ドロップ | 出す |
| **ワザバー／`useItem`** | **戦闘で つかう** |
| **ショップの おたすけ・にじいろセット** | **💎の 商品** |

**このまま 戦闘で つかえない アイテムを 足すと、💎20 と 💎55 の
既存商品の 中みと 説明文が だまって 変わります。**有料の 商品なので
事故に なります。だから しきりを 1つ 作ります。

```
const isBattleItem = it => !!it && it.battle !== false;
const BATTLE_ITEMS = ITEMS.filter(isBattleItem);
```

#### `evo` で 判定しないこと

`evo` は「進化の アイテム」、`battle` は「戦闘で つかえるか」で
**意味が ちがいます**。進化では ないけれど 戦闘で つかえない ものが
出て きた とき、`!it.evo` では 表せなく なります。

#### **未指定＝これまでどおり 戦闘用**

いまの 7つには `battle:true` を **1行も 足しません**。あとから 足す
ものだけ `battle:false` と 書きます。

#### 置きかえるのは「戦闘で つかえるか」を 意味する ところだけ

**ドロップの pool を 機械的に 置きかえないこと** ——「戦闘で つかえるか」と
「ドロップして よいか」は 別の 仕様です。`gear` の 保存・`ITEM_BY_ID`・
図鑑・ガイドも `ITEMS` の ままです。

#### `BATTLE_ITEMS` は **読みこみの ときの 写し**

`const` の 配列なので、あとから `ITEMS` に さしこんでも 変わりません。
検査で「入らない」ことを 見る ときは、**しきりで 数えなおす**こと
（`ITEMS.filter(isBattleItem)`）——写しを 見るだけでは、しきりを
逆に しても 気づけません（じっさい すりぬけました）。

#### セットの 種類数は `BATTLE_ITEMS.length` **だけ**から 出す

むかしは「6しゅるい」と **4か所に 数字を 書いて**ありました
（商品の 説明 2つ・買ったあとの メッセージ 2つ）。`elixir3`
（こだいの秘薬）を 足した ときに 文言だけ のこり、**じっさいには
7種 配って いるのに「6しゅるい」と 言って いました**
（絵文字は 7つ ならんで いたので、見くらべれば 分かる 形でした）。

いまは `setKinds()` 1つ から 出します。

```
const setKinds = () => BATTLE_ITEMS.length + 'しゅるい';
```

**数を 2か所に 書かないこと。**足すたびに かならず ずれます。
`check-chars` の「戦闘用の 集合」が、`Nしゅるい` の 決めうちが
のこって いないか・`setKinds()` が 4か所で つかわれて いるか・
`BATTLE_ITEMS.length` から 出て いるかを 見ます。

> **画素で 追わない ほうが 早い ことが あります。**この 直しは
> ショップの 画面の 中だけ なので、盤面の 画素を くらべても
> 粒子の ゆれしか 出ません。`canvas` と `img` を のぞいた **DOM を
> そのまま くらべる**と、`#app`・図鑑・ガイドは 完全一致で、
> ショップだけ `6しゅるい → 7しゅるい` の 2行が 出ます
> （ガイドの「はいしん」の 時こくは ビルドの たびに 変わるので
> 中みの ちがいでは ありません）。

#### 検査

```
node tools/check-chars.mjs      # 「戦闘用の 集合」の 行
```

**ぜんぶ 禁止には しません。**「戦闘で つかえるか」を 意味する
5か所（ワザバー・ショップ2セット・その 説明2つ）に 生の `ITEMS` が
のこって いたら 落とします。`useItem` の 入口の しきり、
`battle:true` の ばらまき、**しきりの 決めかた そのもの**（`evo` で
決めて いないか・未指定が 戦闘用か）も 見ます。7とおりで 確認ずみ。

#### Phase 7-7-3-2 ——「存在」と「公開」を 分ける

```
const isPublicItem = it => !!it && it.visible !== false;
const PUBLIC_ITEMS = ITEMS.filter(isPublicItem);
```

`visible` の 意味は **「遊ぶ人に そのアイテムの 存在を 公開するか」だけ**です。
**未指定＝公開。**いまの 7件には `visible:true` を 1つも 足して いません
（`battle` と まったく 同じ かたち）。

| | 決めるもの | 別の 軸 |
|---|---|---|
| `visible` | **存在を 知らせるか** | — |
| `battle` | 戦闘中に つかえるか | 7-7-3-1 |
| `late` ほか | ドロップして よいか | 未整理 |
| `hasDex` / `gear` | 手に 入れて いるか | — |
| （未実装）| イベント限定か | — |

#### つかうのは 公開UIの **2か所だけ**

| | |
|---|---|
| 図鑑の アイテム行 | `dexList()` の `...PUBLIC_ITEMS.map(...)` |
| ガイド本文の アイテム一覧 | `buildHelp()` の `PUBLIC_ITEMS.map(...)` |

**`colCount`・`checkComplete()`・国の にじ は `dexList()` の 結果に
したがいます。下流を 個別に 直さないこと**（検査が 落とします）。
`ITEM_BY_ID`・`gear` の 初期化・`giveItem`・贈りものの 表示・
ドロップの pool 2か所は **`ITEMS` の ままが 正しい**。

#### 「未所持」で 表しては いけない

未所持の アイテムは 消えるのでは なく `.cCard.no.yet` の 札に なり、
**名前も 説明も 効果も 見えて います**（絵だけ `opacity:.45`）。

さらに 決定的なのが これです。

> **`dexList()` に 入って いて 一生 手に 入らない アイテムは、
> 「🏰 おかしの国 完成！！」を 永久に 止めます**
> （`checkComplete()` は `dexList().every(o => o.gen || hasDex(o.id))`）。
> かんせいの国の **にじ**も 一生 かかりません。

だから 未公開は **`dexList()` に 入れる前に 除く**しか ありません。

#### 図鑑と ガイドを 同じ 旗で ふるって よい。ただし 派生させない

意味は ちがいます ——図鑑は「集める 対象である」、ガイドは「そういう ものが
ある と 説明する」。いまは どちらも「存在を 知らせる」なので 1つの 旗で
足りますが、**`GUIDE_ITEMS = DEX_ITEMS` の ように 一方を もう一方から
作らないこと。**2か所が それぞれ `isPublicItem` を 呼ぶ 形に して おけば、
「ガイドには 書くが 図鑑には 入れない」が 出ても 1行で 分かれます。

#### 検査 ——たすきがけの 架空アイテム 2件

```
node tools/check-chars.mjs      # 「公開の 集合」の 行（ソース）
node tools/check-public.mjs     # 終了コード 0 で 合格（実行）
```

`check-public.mjs` は **検査の 中でだけ** `ITEMS` に 2件 さしこんだ
写しを 作って しらべます（本物の `index.html` は 1文字も 変えません）。

| | |
|---|---|
| `__cn_hidden` | `visible:false` ／ `battle` は 未指定 |
| `__cn_nobattle` | `battle:false` ／ `visible` は 未指定 |

**たすきがけに するのが 大事**です。1件だけだと、しきりを まちがえて
`battle !== false` で 決めても 素通りします。2件なら **両方 落ちます。**

見るのは 9つ ——`ITEM_BY_ID` には いる／`gear` の 対象に できる／
`PUBLIC_ITEMS` に 入らない・入る／図鑑に 出ない・出る／ガイドに 出ない・出る／
`dexList()` の 分母が +1 だけ／完成判定を 止めない／にじを 止めない／
`BATTLE_ITEMS` が `visible` では 変わらない。

- **写しでは なく 数えなおす**こと（`ITEMS.filter(isPublicItem)`）。
  `PUBLIC_ITEMS` は 読みこみの ときの 写しなので、しきりを 逆に しても
  写しを 見るだけでは 気づけません（7-7-3-1 で 実際に すりぬけた わな）
- **写しの HTML は リポジトリの 中に 置く**こと。よそに 置くと
  `vendor/phaser.min.js` などの 相対パスが 解けず、ページごと 動きません
  （`__chk` が undefined に なるだけで、理由は 画面に 出ません）
- 前の版と くらべる ときは **新しい accessor を あてに しない**こと。
  図鑑の 中みは **タブ5つの「N / M」と 札の 数**、完成判定は
  **`sweetTD.done` の 旗**で 見ます（どちらも 前の版にも あります）
- **はじめての 5人は `Math.random()` で えらぶ**ので、そのままだと
  版ごとに 顔ぶれが 変わります。`sweetTD.starter` の 旗を 先に 立てて
  決めうちの セーブを 入れてから くらべること

7とおりで 落ちる ことを 確認ずみ ——`battle` で 判定 ／ `hasDex` で 判定 ／
`gear` で 判定 ／ 下流（`colCount`）を 個別に 直す ／ 図鑑を 生の `ITEMS` に
もどす ／ ガイドを 生の `ITEMS` に もどす ／ `visible:true` を ばらまく。

### 進化の 絵の 名前は **`<種のID>_<evo>`**（Phase 7-7-3-8-2）

> **記録（2026-09-04）**：`base + '_' + evo` を やめました。
> **2つの 規則を ならべないこと** ——どちらで 名づけるか 迷い、いつか ずれます。

```
candy.png          base。**共有の まま。1枚も 名前を 変えない**
c_purin_e1.png     GEN の プリン
c_candy_e1.png     GEN の キャンディ
tw_candy_e1.png    お菓子タワーの キャンディ
```

#### base の キーは **4つが 分けあって いる**

| 画像キー | GEN | お菓子タワー |
|---|---|---|
| `candy` | `c_candy`（キャンディ★1）| `tw_candy`（🍬）|
| `star` | `c_star`（ほしクッキー★2）| `tw_star`（⭐）|
| `icecream` | `c_icecream`（アイス★3）| `tw_ice`（🍧）|
| `choco` | `c_choco`（チョコ★3）| `tw_choco`（🍫）|

`base + '_' + evo` だと この 4つは **1枚の `candy_e1` を 取りあいます**。

**原因は「base を 分けあって いる こと」では ありません。**
種の ID（`c_candy` / `tw_candy`）は **セーブでも 個体でも もともと 別もの**で、
足りなかったのは **resolver に それを わたして いなかった こと**だけ でした。
だから **base を 1枚も 名前を 変えずに** 直せます。

#### resolver は 3つ 受けとる

```
function evoArtKey(base, evo, sp){
  if (!base || !evo) return base;
  if (sp){ const k = sp + '_' + evo; if (ART_SPRITE[k]) return k; }
  return base;                       // 進化の 絵が 無ければ base
}
function artKeyFor(rc, evo){         // ← **呼ぶ 側は 何も 変わらない**
  return evoArtKey(artKeyOf(rc), evo, rc && rc.id);
}
```

- **`rc.id` が そのまま 種の ID**（`c_purin` など）なので、`drawGen` /
  `genThumb` の 呼び出しは **1文字も 変わりません**
- `drawPortrait` / `charThumb` は `DEX_ART` と `TOWERS[k].art` の 2つの 道で、
  **画像の キーだけでは 種が わかりません**。だから **呼ぶ 側が わたします**
  （詳細画面は `o.id` を 持って いる ので `charThumb(o, 128, evo, o.id)`）
- **`drawPortrait` の 中で 種を 逆引きしない**こと ——`id`（`purin` は 精霊の
  こおりのつぶ、`tw_candy` は タワー）は 種の ID とは 別ものです
- resolver は `inst` / `dex` を 1つも 見ません（7-7-2 の 責任分離の まま）

#### ally の `T.art` を 信じない

`TOWERS['c_*']` は 原型（`ARCHETYPE`）を `{...A}` で 広げて 作るので、
**`art` が 原型から 継がれて います**（`TOWERS['c_candy'].art === 'icecream'`）。
盤面は `T.gen` を 先に 見るので 実害は ありませんが、
**GEN の なかまの 絵の identity に `T.art` を つかっては いけません。**
正しい 道は `T.gen` → `artKeyFor(rc, evo)` です。

#### 検査

```
node tools/check-evo-key.mjs      # 終了コード 0 で 合格（実行）
node tools/check-chars.mjs        # 「進化の 絵の caller」（ソース）
```

`check-evo-key.mjs` は **検査の 中だけで** 架空の 絵を 登録して、

- 共有の 4つ ぜんぶで `c_*_e1` と `tw_*_e1` が **同時に 別々に** 解決する
- **古い `candy_e1` だけ 登録しても、GEN も TOWER も 拾わない**
  （＝`base + '_' + evo` の きまりが のこって いない 証拠）
- evo なしは base の まま（`c_candy` も `tw_candy` も `candy`）
- 本物の `c_purin_e1` は のこって いて、古い `purin_e1` は 無い

を 見ます。架空の 絵は **`finally` で かならず もどします**。

`check-chars` の ソース検査は **名前の ならびを 持ちません**。

| | |
|---|---|
| ① | しっぽが `EVO_IDS`（いま `e1`）に ある 進化IDか |
| ② | あたまが **ほんとうに ある 種の ID**か（`dexList` の id）|
| ③ | ほんとうに ファイルが あるか |
| ④ | 古い `<base>_<evo>` の 名前が まぎれて いないか（②で つかまる）|

**共有キーの きんしは もう 要りません** ——種の ID で 分かれる ためです。

5とおりで 落ちる ことを 確認ずみ ——`base_evo` の fallback を もどす ／
`artKeyFor` が 種のIDを わたさない ／ `evoArtKey` が `inst` を のぞく ／
`drawPortrait` が 種を 逆引き ／ 古い 名前が `ART_KEYS` に ある。

> **これで「1つの 種 × 1つの 進化段階 ＝ 1つの 絵の キー」に なりました。**
> 共有の base 画像を 気に せず 量産できます。

### 進化の 絵（`_e1`）の きまり ——**本体の 大きさを そろえる**

> **記録（2026-09-04）**：`purin_e1` を 作る ときに 決めました。
> 8-0 で 書いた「中みぜんたいの 長辺 ≒206px」は **絶対条件から 外します。**

#### 優先順位（上から）

| | |
|---|---|
| ① | **base と 同じ「キャラクター本体」の 見た目の 大きさを たもつ。**本体の bbox は base の **はば 90〜100% / たかさ 85〜100%** が 目安 |
| ② | 足された かざりも 入れた ぜんたいが **256×256 の すきとおる わくに 収まる**。**長辺206px は 目安であって 絶対では ない** |
| ③ | 中央あわせは **本体の 視覚的な 中心・重心**。細い 茎・つの・かざりの 先っぽに 引っぱられない |
| ④ | 収まらない ときは **かざりの がわ**を 縮める・圧縮する・寄せる。**本体を 縮めて 余白を 作るのは 原則 禁止** |
| ⑤ | さいごの 判断は **128px と 小さい サイズで ならべて**「同じ子が 育った」と 見える か |

#### なぜ 変えたか ——1回 やって わかった

はじめは 8-0 の きまり どおり「中みぜんたいの 長辺を 206px」に そろえました。
ところが `purin_e1` は **さくらんぼの 茎と 王冠が たてを 大きく つかう**ので、
その 206px の ほとんどを かざりが 食べて しまい、

| | base | 長辺206 に そろえた e1 |
|---|---|---|
| 本体の はば | 205px | **162px（79%）** |
| 本体の たかさ | 200px | **101px（51%）** |

**「育った」では なく「小さくなって 帽子を かぶった」**に 見えました。
王冠・つばさ・つの・リボンが ふえる 進化では **かならず** こう なるので、
そろえる 相手を **ぜんたい**から **本体**に 変えます。

#### 基準に した 例 ——`purin_e1`

| | base `purin` | `purin_e1` |
|---|---|---|
| 本体 | 205x200 | **200x176（98% / 88%）** |
| かざり | 30px | 69px |
| 中みぜんたい | 205x200 | **200x246**（長辺206を こえるが よい）|
| 中心ずれ | ±0.002 | **±0.002**（本体の 中心で 合わせる）|

#### 作りかたは `tools/make-evo-art.mjs`

```
node tools/make-evo-art.mjs            # ぜんぶ
node tools/make-evo-art.mjs c_purin    # その子だけ
```

**手で 作らないこと**（`make-icons` / `make-ui-icons` と 同じ きまり）。
原画ぜんたいを ただ 縮めるのでは なく、

- **本体（`body.y0` より 下）は 等倍。1ミリも 縮めない**
- **かざり（`body.y0` より 上）だけ** たて `kv` 倍・よこ `hx` 倍
  （`smoothstep` で なめらかに ——上へ 行くほど 効くので、
  さくらんぼが 小さく・茎が 短く・王冠が 本体側へ 寄る）
- 切り貼りでは なく **1回の リサンプリング**。だから 本体と かざりの
  つなぎめに 段差が 出ません（2倍に 拡大して 確認ずみ）

原画の 実測値（`body` の 4すみと 中心 x）は `PLAN` に 書きます。
**目分量で 書かないこと。**道具じしんが 出したあとに はかって、
①②③に 外れたら 落とします。

#### 上の かざりの 上ばしは **実測する**

`y=0` から 数えると、原画の 上の からっぽな 行まで かざりに 数えて
しまい、**絵が 下へ ずれます**（じっさい 4px ずれました）。

### 原画が 無い ときは かざりを **うしろに 敷く**（`deco`）

`make-evo-art.mjs` の 作りかたは 2つ です。**きまり（①〜⑤）は 同じ**で、
ちがうのは 材料だけ。

| `kind` | 材料 | やること |
|---|---|---|
| `warp` | 進化を えがいた **原画** | 本体は 等倍、かざりだけ ちぢめて 256 に 収める |
| `deco` | **base の 絵 ＋ コード** | base を 1:1 で おいて、かざりだけ えがいて **うしろに 敷く** |

`deco` は **本体の 画素を 1つも さわりません**。だから きまりの
①（大きさ）③（中心）⑤（同じ子に 見える）が **作りから して**
守られます ——にせの 見た目では なく、base そのものが 出て いるためです。

**かざりは かならず うしろ**（`sharp(deco).composite([base])`）。
前に 出すと 本体の 画素が 変わります（canary で 4210件・2308件 出ました）。

検査も それを そのまま 見ます。

```
node tools/make-evo-art.mjs      # 終了コード 0 で 合格
```

| 見るもの | |
|---|---|
| **本体の 画素** | base が 不とうめいな ところが **1件も ちがわない**こと |
| 本体の 大きさ | base の 90〜100% / 85〜100%（`deco` は かならず 100%）|
| ぜんたい | 256 に 収まる |
| **わくの ふち** | bbox が **ふちに 当たって いない**こと |
| 中心 | よこの ずれ 0.02 以内 |

**ふちの 見はりが 効くのは ぼかしです。**グロー（`feGaussianBlur`）の
すそが 256 の わくを こえると **そこで 切れて 四角い 跡**に なります。
広げすぎた canary で `0..255` と 出て 落ちる ことを 確認ずみ。

かざりは CLAUDE.md の「絵は コードで えがく」の とおり、
グラデ＋つや＋**4方向に とがった きらめき**で えがきます。
**base の 画像（`kaminari.png` / `icecream.png`）は 読むだけ**で、
1バイトも 書きかえません。

### Batch 1 の 確定仕様 ——`ch_queen_e1` / `ch_donut_e1`

どちらも **A方式（deco）**。base は 1:1、**本体の 画素の ちがい 0件**、
**body shift 0px**。`ART_KEYS` に 1語ずつ 足した ほかは コードを
1文字も さわって いません。

#### `ch_queen_e1`（すなどけい）——**砂**

> **2026-09-05 に 確定。**A方式の 成功例として ここに 固定します。

| | |
|---|---|
| 本体 | `sunadokei` を 1:1・画素の ちがい **0** |
| 主役 | 左右を **S字に 流れる、粒として 読める 黄金の 砂** |
| かたち | **太い オーラ状の 帯に しない。**かならず 粒の 連なりとして 読める こと |
| 流れ | **S字**（下で 外へ ふくらみ → まん中で 内へ → 上で また 外へ）|
| 下部 | 砂の **まとまり**（足もとに たまる）|
| つぶ | **大つぶを ときどき まぜる** |
| きらめき | **4方向の 金の 星** |
| 色 | わく（`#f0c090`）より **明るい 黄金色 ＋ 白い ハイライト**（かざりの 平均 `#f7d796`）|
| 小サイズ | **36px でも 左右の 流れが 読める** こと |

**作りかたの きも**は 3つ。どれも 1回ずつ 失敗して 分かった ことです。

| やって しまった こと | どう 見えたか |
|---|---|
| 同じ大きさの 粒を 等間かくで 一周 | **真珠の 首かざり** |
| 粒を 大きく して すき間を つめた | **あわ** |
| まっすぐな 柱に した | **湧いて いる あわ** |

いまは **すじを 3本 かさね**（中央 / 内 −8.5px / 外 +8.0px）、粒は
半径 2.9〜7.2・大きさ ±30%・まん丸に せず 傾きも 1粒ずつ、
**となりと ぜったいに 接しない**（`gap 1.20`）。細かい 砂ぼこりを 88粒。

> **B方式なら できる 別案（記録だけ・いまの asset には 入れない）**
> ガラスを 虹色に する／砂に ラメを 入れる／輪郭を 太くする。
> どれも **本体を 描きかえる**ので 原画の 支給が いります。
> **いまの `ch_queen_e1` には 入れません。**

#### `ch_donut_e1`（いずみのしずく）——**水**

| | |
|---|---|
| 本体 | `shizuku` を 1:1・画素の ちがい **0** |
| 主役 | **水しぶき ＋ 波紋** |
| かたち | **結晶・雪片・とがった 形を 1つも つかわない**（`tw_ice` と 混同しない ため）|
| しぶき | 左右に **ひとつづきの うねり**（両はしが 細く、まん中が ふくらむ）。先は まるく 止める |
| 波紋 | 足もとに **よこに 広い 楕円 3重**。**下へ のばさない**（下の 余白は 26px しか ない）|
| つぶ | まるい 水のつぶ を 2つずつ。**ばらまかない** |
| 色 | `#f2fffc → #a9eddf → #6fd0c6`（base から 実測）／ふち `#4fa9a0` |

**すなどけいと わざと 逆に します** ——あちらは *粒を ちらす*、
こちらは **ひとつづきの 流れ**で シルエットを 作る。
これで Batch 1 の 中の 個性が 分かれます。

ここでも 3回 失敗しました。**同じ 道を 通らない ように**書いておきます。

| やって しまった こと | どう 見えたか |
|---|---|
| 先ぼそりの 帯を 立てた | **タコの あし** |
| 細い 棒の 先に 玉を のせた | **綿棒** |
| 帯を 本体の すぐ よこに 置いた | 内がわ半分が body の うしろに 隠れて **細い つの** |

だいじなのは **中心線を 本体から じゅうぶん 外へ**出す こと。
体の ふちが x47〜55 の ところで、中心線を x30 前後・はば 34px に すると
**まるごと 見えます**。

> **`ribbon()` の `wMid` は 通過点では なく 制御点**です。まん中の
> 実の はばは およそ `(w0 + 2*wMid + w1) / 4`。ここを まちがえて
> 「思ったより 半分 細い」を 1回 やりました。

#### `ch_prince_e1`（よつばのこ）——**四つ葉が 増える**

| | |
|---|---|
| 本体 | `yotsuba` を 1:1・画素の ちがい **0** |
| 主役 | **大きな 四つ葉 2枚**（左上・右下の 対角）＋小さい 四つ葉 2枚 |
| かたち | **同じ かたちの くりかえし**。ハートの 葉 4枚＋茎 |
| きんし | **花・つぼみを 1つも 使わない**（`ch_apple` が「花が 咲く」役なので）|
| きんし | **葉の 輪・オーラで ぐるりと 囲まない**。左上と 右下だけに 置き、**右上と 左下は 空ける** |
| 色 | **base から 実測した 緑だけ**（`#d6f7dc → #94e1c0 → #66b497`・ふち `#5eaf92`）。うすい所は base の 明るい葉、こい所は base の こい方 5%・ふちは こい方 1%。**金いろも ピンクも 0画素**（`ch_queen` の 金の きらめきと 視覚言語を 分ける）|
| 小サイズ | 36px で **左（上）と 右（下）に 四つ葉が 増えた**と 読める。24px は 中の すじが 消えても 外形が のこれば 合格 |
| いち | 大きい 2枚は **本体の たてが せまい ところ**に 置く（y70 は x84..177・y196 は x65..196）。いちばん 太い y88..176（x42..219）に 置くと 7割 かくれます |

**小さな 葉を たくさん 散らさないこと。**5か所に ちらしたら
**葉の 輪**に 見えはじめました（1回 やりました）。いまは
**2つの かたまり**（大きい 1枚＋その そばに 小さい 1枚）を
対角に 置くだけ です。

**ぼかしの わくに 気をつけること。**四つ葉を 外へ 出しすぎて
`bbox x2..251` に なったとき、うしろの ぼかしが わくの 外で 切れて
**四角い 跡**に なりかけました。いまは `x14..241 / y25..232`。

#### `ch_apple_e1`（ふたばのこ）——**双葉から つるが のびて 花が 咲く**

| | |
|---|---|
| 本体 | `futaba` を 1:1・画素の ちがい **0** |
| 主役 | **大きな 花 1輪**（右上）。小花を ばらまかない |
| つながり | **双葉 → つる → つぼみ → 花**の ひとつづき。花だけを 浮かせない |
| いち | つるは **右の葉の うしろ**（x126,y64 あたり）から 出て 右上へ。実測：双葉は x76..180 / y25..77、茎は y73 で x123..132、顔は y78 から 下 |
| 数 | 花 **1**・つぼみ **1**・つるの 葉 **1**・つるの 巻きひげ **1** |
| 緑 | **base から 実測**（`#caf0d5 → #a9e7c8 → #8bc4a8`・ふち `#84c3a7`＝base の 緑の こい方 1%）|
| 花（補助色）| `#fff4f8 → #ffcbe0 → #ffb0cc`・ふち `#e79ab6`・芯 `#ffeeb4`。**高彩度の ピンクに しない** |
| 金の spark | **0個**（`ch_queen` と 分ける）|
| 36px | 双葉 → 緑の のび → **ピンクの 花**の 順で 成長の 向きが 読める |
| 24px | つるは 消えるが、**右上の ピンクの 点**と 緑の のびは のこる |

`ch_prince` と わざと 逆に します。

| | |
|---|---|
| `ch_prince` | **同じ 形の くりかえし**（四つ葉が 増える）・緑だけ・両がわの 対角 |
| `ch_apple` | **1本の 成長の 流れ**（つる → つぼみ → 花）・**ピンクが 主役**・右上だけ |

かざりの 2,723画素の うち **ピンクが 1,815・みどりが 696**。
`ch_prince` は **ピンク 0画素**なので、36px でも 色だけで 分かれます。

#### わざと 片がわに よせる ときは `asym:true` と 書く

`make-evo-art` は **よこの 中心ずれが 0.02 を こえたら 落とします**。
`ch_apple` は 成長の 向きを 見せる ために 右上だけへ のばすので
**0.057** に なります。PLAN に `asym:true` と 書いた ものだけ 通します
——**書かない かぎり 落ちる**ので、うっかりの かたよりは 見のがしません。

> **本体の 中心を まもる ことが、かざりも 入れた bbox の 中心より 先**です。
> `deco` は 本体が 1:1 の まま なので、本体の 中心は 定義上 base と 同じ。
> `ch_donut` の たての 0.031 も 同じ 理由で 問題に しません。

#### `sp_rpurin_e1`（ながれぼし）——**尾が のびる**

| | |
|---|---|
| 本体 | `nagareboshi` を 1:1・画素の ちがい **0** |
| 主役 | **1方向へ のびる 帯**（星の うしろから 左へ 扇に 広がる 3本＋左下へ 1本）|
| きんし | **左右から 本体を つつまない**（`ch_donut` の 水流と 分ける）|
| きんし | **粒の 連なりを 主役に しない**（`ch_queen` の 砂と 分ける）|
| 星くず | **補助。3つだけ**・パステル（金では ない）|
| 色 | base の 尾から 実測（ピンク `#fc9fcb`／ラベンダー `#cfa7f6`／ミント `#a0f0d0`。ふちは それぞれ こく）|
| 36px | **左へ のびた 尾**の シルエットで e1 と 分かる |

**のばす 場所は 実測で 決めます。**base は 左下が ほぼ 埋まって いて
（尾が x26..139 / y100..223）、いちばん 空いて いるのは **尾の 上がわ**
（x12..109 / y60..115）です。だから 扇は **そこへ** 広げます。

> **`sp_` prefix の 実証**も かねます（GEN の `c_` ／ DEX_ART の `ch_` ／
> TOWER の `tw_` に つづく 4つめ）。`check-evo-key` に 1件 足しました。

#### `ch_gumgum_e1`（わたぐも）——**天気が 豊かに なる**

| | |
|---|---|
| 本体 | `watagumo` を 1:1・画素の ちがい **0** |
| 主役 | **雲が 天気を 生みだす** ——上の 角に **わき雲**、下に **雨の すだれ** |
| 補助 | 左下に **細い 虹**（4本の 弧）。**半円の 記号に しない** |
| きんし | 大きな 水流（`ch_donut`）／円い 輪（`ch_fairy`）／粒の 連なり（`ch_queen`）を 主役に しない |
| 色 | わき雲 `#fbffff → #eef9fb → #d3e7f2`・ふち **`#9db9cc`**（base の 輪郭の いちばん こい 色 `#96b4ca` そのもの）／雨 `#8ec8e6`（base の かげ `#90b4cc` を すこし 水色へ）／虹 `#ffd0dc・#fbe6b0・#bfe9d4・#d5c8f2`（低彩度）|
| 36px | **雲が 横に ふくらみ、下に 雨が 落ちる**——外形で 読める |
| 24px | 虹の 色分けは 消えるが、ふくらんだ 雲と 雨は のこる |

**わき雲は 1つずつ ふちを 引かないこと。**円の ふちが それぞれ 見えて
**「あわ」**に なります（1回 やりました）。**ふち色の 大きい 円を 先に 敷いて
から 中を 塗る**と、かさなりが 1つの 雲に なります。

いちは 実測で 決めます ——本体は y118..142 で `x25..230` と いちばん 太く、
**左右には 13px しか ありません**。空いて いるのは **上下の 角**
（NE/NW 77・SW/SE 84〜87）なので、わき雲は 上の 角、雨は 下の 角へ。

#### `ch_fairy_e1`（ひかりのわ）——**二重の 光の 輪 ＋ 放射光**

| | |
|---|---|
| 本体 | `hikari` を 1:1・画素の ちがい **0** |
| 主役 | **二段の 輪**（うすい 内の 輪 rx97 ／ はっきりした 外の 輪 rx107・ry111）|
| 補助 | **放射光 6本**（ななめ 4本＋たて 2本）|
| きんし | 雲の かたまり・雨すじ・虹（`ch_gumgum` と 分ける）／太い オーラの 円盤 |
| 色 | 輪は 白〜淡いクリーム（`#fff8e4` ＋ `#ffdf9c`）。放射光は **base の すじの 実測色**（藤 `#d5bcfe` ／ 桃 `#fcaace` ／ ミント `#aee8d7`）。**金いろ主体に しない**（`ch_queen` と 分ける）|
| 36px | **外周に もう一段 大きい 輪**が 見える |
| 24px | すじの 細部は 消えるが、外周の 大きさと 二重の 輪は のこる |

**よこの 余白は 19px しか ありません。**だから 外の 輪は
**たてを すこし 長い 楕円**（rx107 / ry111）にして たての 余白（46〜51px）を つかい、
**放射光は ななめ**（余白 91〜97px）へ 長く のばします。

**放射光の いちは 実測の「すき間」に 入れる**こと。base の すじは
0/25/60/115/150/175/205/245/290/335° あたりで 半径 100〜104 まで のびて
いるので、その あいだ（38 / 133 / 224 / 312 / 86 / 268°）へ 置きます。
長さも そろえない（150 / 143 / 148 / 153 / 113 / 112）——**十字の 記号に
見えない ため**です。

**光に ふちを 引かないこと。**丸い キャップを つけて ふちを 引いたら
**カプセル（薬）**に 見えました（1回 やりました）。いまは
**太い うす明かり（不とうめい .34）＋細い 芯（.78）の 2枚**で
「にじむ 光」に して、外へ 行くほど 細くして います。

#### 量産の 優先順位（**外形の 大きさを そろえない**）

> **記録（2026-09-05）**：`ch_apple` の 幅の のびが +19% と いちばん
> 小さい ことを 報告したら、**「そろえる 必要は ない」**と 決まりました。

| | |
|---|---|
| 1 | **同じ キャラクターに 見える** |
| 2 | **36px で base → e1 の 変化が 読める** |
| 3 | その キャラクター固有の **「成長の 意味」**が 読める |
| 4 | ほかの e1 と **シルエット言語が 重ならない** |
| 5 | その 結果として **必要な ぶんだけ** 外形を 広げる |

**「進化＝かならず 大きく 豪華に なる」に しないこと。**
いま 6体の のびかたは ばらばらです（幅 +19%〜+69%）が、
それは 欠点では なく、154体を 量産した ときに
**「ぜんぶ 左右に かざりが 付いただけ」に なるのを 防ぐ 基準**です。

| | 成長の しかた | 幅の のび |
|---|---|---|
| `ch_queen` | 砂が 大きく 広がる | +69% |
| `tw_ice` | 氷結晶が 左右へ 張り出す | +65% |
| `ch_donut` | 水流で つつむ | +39% |
| `ch_prince` | 四つ葉が 増殖する | +25% |
| `sp_rpurin` | 尾が 1方向へ のびる | +6%（たては +9%）|
| `ch_gumgum` | 雲が ふくらんで 雨を ふらす | +12%（たては +14%）|
| `ch_fairy` | 光の 輪が 二重に なる | +12%（たては +26%）|
| `ch_apple` | 一方向へ 開花する | +19% |

**`ch_apple` に 足しては いけない もの**（1回 提案して 却下に なった）——
花を もっと 大きく する／左下にも つるを 足す／足もとに 若葉。
どれも「**右上だけへ のびる 非対称性**＝ふたばのこの 進化記号」を 弱めます。

**`ch_prince`＝四つ葉が 増える ／ `ch_apple`＝一方向へ 成長して 花が 咲く**
の 差は、これからの 植物系 e1 でも たもつこと。

#### Batch 1 は **凍結**（2026-09-05）

正式採用した e1 は **10枚**（Batch 1 の 7体＋先に 通した
`c_purin` / `ch_choco` / `tw_ice`）。これを **量産の 基準画像**と します。

```
node tools/check-chars.mjs      # 「e1の 凍結」の 行
```

`EVO_LOCK` に SHA-256 を 書いて あり、**1バイトでも 変わったら 落ちます**。
道具（`make-evo-art.mjs`）の 部品を 直した ときに、気づかず もう 通った 絵まで
動いて しまう のを 防ぐ ため です。**直すのは「作りなおして よい」と
決まった ときだけ**——その ときは `EVO_LOCK` の 値も いっしょに 書きかえます。

#### Batch 1 の シルエット言語（**量産の 基準**）

A方式でも、キャラクターごとに **形の ことば**を 変えます。
同じ「外がわに かざりを 足す」でも、これで 36px でも 別ものに 見えます。

| | シルエット言語 |
|---|---|
| `ch_queen` | **粒の 連なり**（細かく ばらける）|
| `ch_donut` | **連続した 丸い 水流**（ひとつづきの 曲線）|
| `ch_prince` | **同じ かたちの くりかえし**（四つ葉が 増える）|
| `ch_apple` | **1本の 成長の 流れ**（つる → つぼみ → 花。ピンクが 主役）|
| `sp_rpurin` | **1方向へ のびる 帯**（速さ。左だけ）|
| `ch_gumgum` | **ふくらむ かたまり＋落ちる すじ**（天気）|
| `ch_fairy` | **同心の 輪＋放射**（光）|
| `tw_ice` | **角張った 結晶**（直線と 角）|

**A方式が 安全だから デザインとして 上、では ありません。**
最終判定は かならず **128 / 36 / 24px の 目視**で 行い、
弱ければ B（本体を 育てる）へ 送りかえします。

### e1 の 量産基準（**正式ルール**・2026-09-07）

> **記録（2026-09-07）**：Batch 1・2 の **18枚**で 再現性が とれたので、
> ここまでの 5つを **これから 先の e1 制作 ぜんぶに かかる 正式ルール**に
> 昇格しました。1体ずつの メモでは なく、**新しい e1 を 作る ときは
> かならず ここを 先に 読むこと。**

#### ルール① 判断の 優先順位（**この 順を 入れかえない**）

```
① 同じ キャラクターに 見える
      ↓
② 36px で 進化差が 読める
      ↓
③ 何が 成長・変化したのか **意味**が ある
      ↓
④ シルエット言語が **予約ずみの ものと 重ならない**
      ↓
⑤ その 結果として **必要な ぶんだけ** 外形を 広げる
```

**数値的に 大きく 変える ことを 目的に しないこと。**⑤は 目的では なく
①〜④の **結果**です。

#### ルール② 外形の 拡大量は **そろえない**

Batch 2 の 実績は **+7.8%（`c_lavasnail`）〜 +39.4%（`c_devil`）**。

> **「進化後は ○% 大きく する」という 共通目標は きんしです。**
> 判定は **「36px で 何が 成長したのか 読めるか」** ——それだけ。

bbox・面積・重心・IoU・row profile は **診断値**であって、
**単独の 合否基準では ありません**。げんに `c_ghost_e1` は
36px の bbox幅が ±0、`c_hinotama_e1` は はばが ±0.0%、
`c_devil_e1` は たかさが ±0.0% で、それぞれ 別の 指標で 通って います。

> **外形面積を ふやす ことと、小サイズで 進化差を 読ませる ことは
> 別の 問題です。**`c_mummy` の 設計で、帯を 左右へ 広げる 案（C2）は
> **面積を +20.7% ふやしても 36px の 破れが 2/30 の まま 1行も
> ふえません**でした ——横へ 出した 帯は 本体と 同じ 行に つくので、
> 輪郭を「広げる」だけで「破らない」ためです。
>
> **これは「全キャラで トポロジーを 変える」という 新しい ルールでは
> ありません。**キャラ固有の 成長言語に 応じて **何を 変化の 指標に するか**
> を えらぶ、という 実例です ——`c_octopus` は 長さと 曲率、
> `c_mummy` は 外形の トポロジー、`c_devil` は 幅、`c_hinotama` は 高さ。

#### ルール③ A方式の 基本条件（**浮遊装飾だけでは ない**）

```
base 1:1 ／ 本体の 画素の ちがい 0 ／ body shift 0 を 第一候補
かざりは base の **うしろ**に 敷く（`sharp(deco).composite([base])`）
```

**ただし A方式＝「離れた かざりを 浮かべる」だけでは ありません。**
`c_firebird_e1` の ように **既存部位の 延長として 外側へ 新しい
シルエットを 成立させる**のも 正式な A方式です
（`c_salamander_e1` の 稜線・`c_devil_e1` の 魔影も 同じ 型）。

#### アンカーは **ほんとうに つながって いる 本体**へ 重ねる

> **記録（2026-09-07・`c_octopus_e1` で 発見）**

**bbox の 中／base の 輪かくの 中、というだけで アンカーを 決めては
いけません。**そこが **ほんとうに 連続した 不とうめいの 本体**かを
実測して たしかめること。**内部の とうめいな 穴や、部位と 部位の
すき間に つないでは いけません。**

`c_octopus` の 1回目は 付け根を **(98,146)** に 置きました。base の
輪かくの 中に 見えますが、あの子は **y138〜151 で x57..67 が 穴**
（外がわの 巻き触手と 胴の あいだ）です。そこから 新しい 腕が
**131px ぶん すけて、宙に 浮いた 継ぎはぎ**に 見えました。

付け根を **(102,158)** ——base が y158 で **x28..228 の ひとつづき**に
なる ところ——まで 下げて 消えました（131px の 成分が 0に なる ことを 実測）。

**見つけかたは 行ごとの run**です。`runs` を 出して、アンカーの y で
**1つの run の 中に 入って いるか**を 見ます。run が 2つ以上に
分かれて いる 行は、その あいだが 穴です。

#### ルール④ 1つの 形は できるかぎり **union した 1パス**で 作る

同じ 構造を 小さな パスに 分けると、

- 三角を ならべただけ
- パーツを 貼っただけ
- かさなりに **内側の いらない 線**が のこる

に 見える ことが **5体で 再現しました**（`c_salamander` の 棘・
`c_onibi` の 子・`ch_gumgum` の わき雲・`c_firebird` の 羽根・
`c_hinotama` の 舌）。連続した 1つの 部位・流れ・影として
読ませたい ときは **1つの 閉じた union パス**を 第一候補に します。

**例外は「独立して いる こと じたいが 進化言語」の とき**だけ
（`c_onibi_e1` の 3つの 独立炎・`c_lavagolem_e1` の 浮遊岩）。

#### ルール⑤ 最終判定は **128 / 36 / 24px の 目視**

| | 何を 見るか |
|---|---|
| 128px | 元キャラらしさ・造形の 品質 |
| **36px** | **ゲーム中の 主判定（いちばん 重い）** |
| 24px | 最小サイズで のこる シルエット |

白 / クリーム / ミント / ラベンダーの 地でも 見ること。
IoU・bbox・面積・row profile は **測って よい**が、
**数値だけで 採否を 決めない**。

#### ルール⑥ **バッチの 中でも 言語を 予約する**

重複を 見るのは **予約ずみの 18言語だけでは ありません。**
おなじ バッチで 新しく わりあてる 言語どうしも、36px で 重ならない こと。

> **1体目で 言語を 決めたら、その しゅんかんから 2体目 以降に
> 対して「予約ずみ」として あつかいます。**

primitive や 設定の 説明が ちがっても、**36px で おなじ 形に 読めるなら
重複**です。だから 計画の 段階で「向き」と「質量の 置きかた」を
バッチぜんたいで 見わたして おきます。

#### 予約ずみの シルエット言語（**28枚 ＝ 28言語**）

新しい e1 を 決める まえに、ここと ぶつかって いないかを 見ます。
**大事なのは primitive の 重複では なく、36px で 読める
最終シルエットの 重複**です ——おなじ `catmull()` を つかっても、
読みが ちがえば 問題 ありません。

| | シルエット言語 |
|---|---|
| `c_purin_e1` | 頭上に のる **立体の トッピング**（さくらんぼ＋王冠。ゆいいつの warp 方式）|
| `ch_choco_e1` | 頭上の **角張った 電光の 冠**＋左右の いなずま |
| `tw_ice_e1` | 左右へ 張り出す **角張った 氷の 結晶** |
| `ch_queen_e1` | **粒の 連なり**（S字に 流れる 砂）|
| `ch_donut_e1` | **連続した 水流**＋波紋 |
| `ch_prince_e1` | **同形モチーフの 反復**（四つ葉が 増える）|
| `ch_apple_e1` | **一方向へ のびて 開花**（つる → つぼみ → 花）|
| `sp_rpurin_e1` | **一方向へ のびる 帯**（尾）|
| `ch_gumgum_e1` | **ふくらむ 雲＋落ちる 雨すじ** |
| `ch_fairy_e1` | **同心の 輪＋放射** |
| `c_lavagolem_e1` | **離れた 浮遊岩** |
| `c_salamander_e1` | **身体に つながって 育つ 稜線** |
| `c_onibi_e1` | **3つの 独立した 炎** |
| `c_lavasnail_e1` | **巻いて 中心へ もどる 1本の うず** |
| `c_ghost_e1` | **重なった 薄い ヴェール** |
| `c_firebird_e1` | **既存部位から 育った 実体の 翼** |
| `c_hinotama_e1` | **上へ 育つ 連続した 炎冠** |
| `c_devil_e1` | **左右へ 開く 1枚の 大きな 影** |
| `c_octopus_e1` | **元からある 太い 触手が それぞれ 独立に 長く うねって 育つ** |
| `c_mummy_e1` | **ほどけた 包帯が 不規則な 方向へ 垂れ、外形が「ふさ」に ほぐれる** |
| `c_cobra_e1` | **頭・首を つつむ、外縁の なめらかな 一枚の よこ長の 盾** |
| `c_witch_e1` | **肩から 片がわ 斜め後方へ 流れる 一枚の 不とうめいの cape** |
| `lg_chocoknight_e1` | **持ちものの 閉じた 面の 外がわが 別素材に なる（局所の 素材強化）** |
| `c_snowqueen_e1` | **左右を たてに 流れる 不とうめいの 長髪が のびる** |
| `c_redpanda_e1` | **片がわ 1本の、太い 縞の 尾が たてに のびる** |
| `c_tower_e1` | **積まれた 反復ユニットが 1つ ふえて、たてに 1段 高く なる** |
| `c_ostrich_e1` | **後方へ ながれる、かさなった やわらかい 尾羽の 房が 長く 育つ** |
| `lg_berryqueen_e1` | **衣装の 裾の フリルが、同じ 素材・同じ 波形の まま 外側へ もう 1段 重なる**（左右対称・不とうめい・水平）|

> **「16言語」では なく 28言語**です。`c_purin_e1` と `ch_choco_e1` /
> `tw_ice_e1` も それぞれ 別の 言語を 持って います
> （頭上の トッピング／電光の 冠／氷の 結晶）。

### e1 の のこり ——分類（2026-09-07・**`lg_chocoknight_e1` 採用後に 数えなおし**）

**対象は 157体**（GEN 128・DEX_ART 13・TOWER 4・LEGEND 12）。
アイテム 7件は キャラクターでは ないので 数えません。
完成 **28体**、のこり **129体**（`lg_berryqueen_e1` 採用ずみ）。

| | 総数 | 完成 | のこり |
|---|---|---|---|
| GEN | 128 | **17** | **111** |
| DEX_ART | 13 | 8 | 5 |
| TOWER | 4 | 1 | 3 |
| LEGEND | 12 | **2** | **10** |

のこり 129体の 分類は **下の 再スクリーニング**（2026-09-07）で 更新しました
（`lg_berryqueen` は そこでは A要検証でしたが、2026-09-09 に **A成立**しました）。

| | ものさし |
|---|---|
| **A確実** | base に **外へ のばせる 付属物・元素**が 見えて いて、しかも その 言語が **まだ 予約されて いない** |
| **A要検証** | 付属物は ある が、自然な 言語が **予約ずみと ぶつかる**か、36px で 細すぎる |
| **B候補** | 外部の 付属物が なく、進化の 意味が **本体そのものの 変化**に なる |

> **A の 数を ふやす ことを 目的に しないこと。**B候補を むりに A へ
> 寄せると、「ぜんぶ 左右に かざりが 付いただけ」に なります。

**正確な 実数**（機械で 数えなおし）——

| | 総数 | 完成 | のこり |
|---|---|---|---|
| GEN（`c_*`）| 128 | **17** | **111** |
| CHARS（`ch_*`）| 12 | **7** | **5** |
| SP（`sp_*`）| 1 | **1** | **0** |
| **LEGEND（`lg_*`）** | 12 | **2** | **10** |
| TOWER（`tw_*`）| 4 | **1** | **3** |
| **合計** | **157** | **28** | **129** |

> **2026-09-09 に 数えなおしました**（`c_ostrich_e1` と `lg_berryqueen_e1` の ぶん）。
> かぞえるのは `ART_KEYS` の `_e1` の 数（28件）です ——**手で かぞえない**こと。

#### **むかしの「A確実 4体（LEGEND）」は 訂正します**（2026-09-07）

あの 分類は **Batch 3 で 6つの 失敗則が できる 前**の ものです。
測りなおすと こう なりました。

| species | あき L/R/T/B | 訂正 |
|---|---|---|
| **`lg_candytree`** | 33/33/14/14 ／ **SE・SW 133** | **A有力の まま** |
| `lg_whale`（kujaku）| **14/14**/27/27 | A確実 → **A要検証**。左右 14px・扇が すでに 大きい・`ch_fairy_e1` の 放射系と 近い |
| `lg_kraken` | **17/17**/14/14 | A確実 → **A要検証・保留**（⑤余白不足）|
| `lg_kyubi` | **14/14**/16/16 | A確実 → **A要検証・保留**（⑤余白不足）|

#### 新しい LEGEND の 上位候補 ——**「入れかえ」では ありません**

余白と にぎりが よい だけ では A確実に **上げません**。
**強い 専用の 進化言語が 見つかって から** 昇格させます。

| species | あき | ⑥使用関係 | いまの 区分 |
|---|---|---|---|
| `lg_chocoknight` | **48/48** | **合格**（盾を 保持）| **高優先 A候補 / A要検証** ——大きな 円盤・ハローに 見える 危険が のこる |
| ~~`lg_berryqueen`~~ | **38/37** | **合格**（しゃくを にぎる）| **A成立・採用ずみ**（2026-09-09）——王冠でも しゃくでも 髪でも なく **裾の フリル**でした |
| `lg_moonrabbit` | **32/32** | **合格**（きねを 抱える）| **高優先 A候補 / A要検証** ——持ちもの進化か 身体進化か 未確定 |

#### TOWER の のこり 3体は **次の Batch の A候補から 外します**

| species | |
|---|---|
| `tw_choco` | **fill .929**。ほぼ すきまが ない |
| `tw_star` | いちばん 広い あきが **約17px** |
| `tw_candy` | 約26px だが **既存の 成長軸が ふさがって いる**（記録ずみ）|

**むりに A量産の 対象に しないこと。**B や 別の 言語の 可能性は
将来 また 見ます。

#### そのほかの 訂正

- **`c_cobra` を A要検証 → A確実へ**（フードの 左右に 39px ずつ あり、
  36px で +11列 ふえる ことを 実測）→ **完成**
- **`tw_candy` を A確実 → A要検証へ**（包み紙の 軸が ふさがって いる）
- **`c_deer` `c_scorpion` を A確実 → A要検証へ**（Batch 3・下の 記録を 見ること）

> **のこり 5体の DEX_ART は ぜんぶ A要検証**でした。かげろう・
> たいようのこ・こおりのつぶ・つむじかぜ・はなびのこは **全員が
> 元素の子**で、自然な 言語（立ちのぼる／放射／結晶／うず）が
> `c_hinotama` `ch_fairy` `tw_ice` `c_lavasnail` に **すでに
> 予約ずみ**だからです。

#### 再スクリーニング（2026-09-07・**`lg_chocoknight_e1` 採用の あと**）

Batch 4 の 当初候補が 3体とも 落ちた ので、のこり **134体**に
**新しい 失敗則・成功則**を あてて 選びなおしました。**新しい e1 画像は 0枚**です。

| 段 | のこり |
|---|---|
| のこり ぜんぶ | **134** |
| **粗い 一次フィルタ**（いちばん 広い 安全余白 30px 以上）| **58** |
| すでに 判定ずみを のぞく（`c_crab` `c_deer` `c_giraffe` `c_meerkat` `c_oni` `c_rabbit` `c_scorpion` `c_turtle` `lg_candytree`）| **49** |
| **細い limb が 主役**（細い部分の 割合 23%以上）を のぞく<br>`c_bat` `c_bigparfait` `c_dango` `c_kangaroo` `c_lizard` `c_ostrich` `c_parfait` `c_prince` `c_taffy` `ch_kagero` | **39** |
| **つまった 閉じた 形**（fill 0.74 以上）を のぞく<br>`c_churro` `c_daifuku` `c_eclair` `c_ohagi` `c_popcorn` `c_pretzel` `c_roll` `c_sakuramochi` `c_sandcat` `c_youkan` | **27** |
| **⑨ 素材境界で 拾いなおし**（`c_cupcake` `c_tutankhamun`）| **＋2** |

> **「30px 未満 ＝ A不可」とは しないこと。**30px は **粗い 一次探索**用です。
> 正式な 判定は **「対象部位の 先端 → 実際の 成長軸方向の 余白」**で 行います
> （`c_rabbit` は 中心から 110px・軸に そって 16px でした）。

##### 予約 23言語と ぶつかって **いまの A優先度を 下げた** もの

**永久不採用では ありません。**別の 進化言語が 見つかれば 戻せます。

| 言語 | 下げた もの |
|---|---|
| 翼・ひれ | `c_manta` `c_butterfly` `lg_pegasus` |
| うず | `c_soft` `c_seahorse` |
| 頭上の トッピング | `c_wedding` `c_icecream` |
| もこもこの かたまり | `c_watame` |
| 同形の くりかえし | `c_gummy` |
| 閉じた 甲羅・殻 | `c_armadillo` |

##### 上位候補（**この 順**）

いちばん 効いたのは **⑨「別素材を 境界として 読みかえられるか」**でした。

| | ⑤ 実余白 | ⑥ 使用関係 | ⑨ 素材の 関係 | 候補の 言語 |
|---|---|---|---|---|
| ~~1. `c_pharaoh`~~ | L39 / R38 | にぎり あり | 金＋青緑の しま | **A方式 不採用**（2026-09-07・胸元の かざりが **外形から 17px 内包**）|
| ~~2. `lg_moonrabbit`~~ | L32 / R32 | にぎり あり（**合格**）| うす（陶）＋毛＋金の 月 | **第一仮説（うすの たが）は A不可**（2026-09-07・**面の 上の 帯**）。キャラは A要検証の まま |
| **3. `c_tutankhamun`** | **L67 / R67**（のこりで 最大）| — | 金＋青緑＋ラピス | 外側の 棺（**入れ子**）|
| **4. `c_wizard`** | L41 / R41 | **にぎり あり**（つえを 持つ 手）| 木＋金＋緑の 玉 | つえの 玉に 大きな 金の 台座 |

> **`c_tutankhamun` を 2番手に しない**理由 ——余白は いちばん 大きい（67px）
> けれど、**全身を かこう 閉じた 外殻**に なるので、`c_turtle` で 見つけた
> 「元の 面が まるごと 内がわに のこる」危険が いちばん 高い ためです。
> `lg_moonrabbit` の ほうが **局所の 素材強化**で すむ 見こみが あります。

> **`c_wizard` の 玉は 21x21px しか ありません**（実測）。36px での 効きが
> 小さいので 4番手です。

> **2026-09-07 の 追記**：この あと `c_pharaoh`（完全内包）と
> `lg_moonrabbit` の 第一仮説（面の 上の 帯）が 落ち、rim 以外の 軸で
> しらべなおして **`c_snowqueen`（太い 長髪の 延長）が 通りました**。
> **rim 型だけに 寄らず、空いて いる シルエット言語も 並行して さがす**こと。
>
> さらに **`c_king` の 王冠 rim も 落ちました**（とがりが identity）。
> つづけて **`c_purinala` の 皿 rim も 落ちました** ——外周は なめらかで
> 一周できるのに、**口縁（rim）が すでに 完成して いた**ためです。
>
> **rim 型は 成功条件より 失敗条件の ほうが はっきりして きました。**
> なめらかで 一周できる こと（king の 否定）と、rim を まだ 持って いない こと
> （purinala の 否定）の **両方**が いります。`c_wedding`（台）・`c_cupcake` /
> `c_popcorn` も 同じ 目で 見ること ——どれも **すでに 段や 帯を 持って
> います**。`lg_berryqueen` は `c_king` が 落ちた ので **別に 検証**しました
> ——結果、**王冠 rim では なく 裾の フリル**で A成立（2026-09-09）。
> いまは rim を 掘るより、`c_snowqueen` で 通った
> **「太い 既存部位の 自然な 延長」**を 横に ひろげる ほうが 早い。
>
> そこで 133体を その 系統で 再スクリーニングし、1番手の
> **`c_squirrel` も 落ちました**（尾先が 巻いた フックで、
> 終端から の 実軸余白が ±30° ぜんぶ 0px）。抽出の 条件に
> **④ 終端から の 実軸余白**を 足して あります（下の 節）。

#### 共有 base は **絵が 同じ ＝ 進化後も 同じ**では ない

`candy` / `choco` / `star` / `icecream` の 4枚は GEN と お菓子タワーで
base を 分けあって います。resolver は **種の ID**（`c_candy_e1` /
`tw_candy_e1`）で 分けられる（Phase 7-7-3-8-2）ので、
**別の 進化を 与えて かまいません**。ただし **おなじ バッチには
入れない**こと ——同じ base の 上で 2つの 言語を 同時に 決めると、
どちらも 中途半端に なります。

### Batch 3 ——**4体 正式完成で クローズ**（2026-09-07）

制作順は **A確実 → A要検証**。LEGEND は **今回も 外します**
（aura・silhouette・flyer と 干渉するので、通常キャラの 基準が
もっと 固まって から）。

| # | species | 区分 | 分類 | 進化言語（**向き / 質量の 置きかた**）|
|---|---|---|---|---|
| 1 | `c_octopus` | GEN | A確実 | **既存の 触手が 長く うねって 育つ**（下〜斜め下・多数の 細長い 可動体）**…完成**|
| 2 | `c_mummy` | GEN | A確実 | **ほどけた 包帯が 不規則な 向きへ 垂れる**（左右非対称・重力に したがう 細片）**…完成**|
| 3 | `c_cobra` | GEN | A確実 | **首まわりの 面が 左右へ 大きく 張り出す**（横・1枚の なめらかな 盾）**…完成**|
| 4 | `c_witch` | GEN | A要検証 | **肩から 斜め後方へ 1枚の 不とうめいの マント**（斜め後方・1枚）**…完成**|

**8候補の うち 4体 採用・4体 保留**（`c_deer` `c_scorpion` `c_crab` `c_oni`）。
打率は 5割ですが、**値うちは 4体 ふえた ことでは なく、
A方式が 落ちる 条件を こまかく 切りわけられた こと**です。

> **この ならびは「かならず 最後まで 作る」固定の リストでは ありませんでした。**
> 制作前の 実測で A として 弱いと 分かった 個体は **差しかえて よい**。
> ただし **こちらの 判断だけで 差しかえない こと** ——候補を 変える 必要が
> 出た その 時点で 報告して 止まります。じっさい 4体が この やりかたで
> 保留に なりました。

#### `c_octopus` は **本数を 先に 決めない**

base には すでに **脚が 8本** あります。**どの 脚を のばすと 36px で
いちばん 読みやすいか**は 制作前の 実測で 決めます。
計画の 段階では「複数の 既存触手が 長く うねって 成長する」までに して、
**数を ふやす こと じたいを 言語に しない**こと。

#### `tw_candy` は Batch 3 から 外した（**成長する 軸が ふさがって いる**）

`candy` の 包み紙は **左右では なく 右上と 左下の ななめ**に あります
（実測：右上の 包みは y38..y102 で 先端 (229,86)、左下は y150..y214 で
先端 (25,166)。丸い 本体は x66..189 / y110..142）。

その 軸に そって のばせるのは **右上へ 16px・左下へ 15px だけ**
（安全わく x11..244 まで）。**36px では 2.2列 ぶんが 先端に つくだけ**で、
進化差に なりません。

のばすには 軸と **直角**（空いて いる 左上・右下）へ 扇に 広げるしか
なく、そうすると **ななめの 大きな 面が 2つ**に なります。これは

- `c_devil_e1`（左右へ 開く 1枚の 大きな 面）を 45°まわした もの
- おなじ バッチの `c_witch`（斜め後方の 1枚の 面）

の **2つと ぶつかります**（ルール⑥）。だから A確実 → **A要検証**に
落として、Batch 3 からは 外しました。

> **`c_mummy` との 見わけは「本数」では 決めません。**
> mummy は **不とうめいの 細い 帯（base の 包帯の 間かくは 12〜14px）が
> 不規則な 向きへ 垂れる**もの、candy は **幅の ある ねじれた 面**。
> 最終シルエットが「ぎざぎざの ふさ」か「大きな 面」かで 分かれます。

#### `c_hedgehog` は 第一候補から 外した

base に **すでに 全周へ 針の シルエットが 強く あります**。そこへ
さらに 全周の 長い 針を 足すと、

- `c_salamander_e1`（身体に 沿って 育つ 突起）
- `ch_fairy_e1`（放射）

の **両方に 近づく**うえ、「ハリネズミ → もっと とげとげした ハリネズミ」
に しか ならず、進化言語として 弱く なります。
**分類は A要検証の まま のこし、Batch 3 からだけ 外します。**

同じ 理由で **`c_lion`（たてがみを 大きく する）も 採りません**
——「ライオン → たてがみの 大きい ライオン」に なり、
ハリネズミと まったく 同じ 弱さです。

#### `c_cobra` の 実測（`c_hedgehog` と 入れかえ）

| | |
|---|---|
| base bbox | x43..212 / y25..230（**170x206**）・fill .744 |
| いまの フードの はば | **156px**（y80〜y92 で x50..205）|
| 左右の あき | フードの 行で **左右 39px ずつ**（安全わく x11..244 まで）|
| 顔を 隠さず ひろげられる 面 | かざりは **base の うしろ**なので、顔・目・口は **1画素も 変わらない** |
| 36px で ふえる 列 | フードの 行で **22列 → 33列（+11列）**。y52〜y128 の 12行で +6〜+11列 |
| A方式 | **成立**（base 1:1・本体画素差 0・body shift 0）|

いまは **フード 156px ＜ とぐろ 170px**（y196 で x43..212）なので、
胴の ほうが 広い シルエットです。フードを 234px まで 広げると
**フードが いちばん 広く なり**、上下の 重心が はっきり 入れかわります。

**ほかとの ちがい。**

| | |
|---|---|
| `c_devil_e1` | 半とうめいの **影が 2枚**・下ふちに 切れ込み |
| `c_firebird_e1` | 羽根の **すじが 入った 実体の 翼が 2枚** |
| `tw_candy` | ななめの **ねじれた 面が 2つ** |
| **`c_cobra`** | **首まわりの 1枚**・なめらかな 盾・切れ込みなし・**横** |

**2つに 割れて いない こと**が この子の 言語です。

#### `c_witch` の マントは 三角形に しない

`c_ghost_e1` との ちがいを **とうめい度だけに たよらない**こと。

| | 付け根 | 向き | 枚数 |
|---|---|---|---|
| `c_ghost_e1` | **胴の 下** | **下**へ 波打つ | **3層** |
| `c_witch` | **肩** | **斜め後方**へ 1方向 | **1枚** |

#### Batch 3 の 向きと 質量（**ルール⑥の 見わたし**）

| | 向き | 質量の 置きかた |
|---|---|---|
| `c_octopus` | 下〜斜め下 | 細長い ものが 多数 |
| `c_mummy` | 不規則（重力）| 細い 帯が ばらばらに |
| `c_cobra` | **横** | **1枚の なめらかな 盾** |
| `c_witch` | 斜め後方 | 1枚の 面 |

**上向きは 1つも のこって いません**（`c_scorpion` `c_crab` とも 保留）。
**上向きを 足す なら 次の バッチで**（`ch_kagero` など）。

#### `c_deer` は 制作して **不採用**に した（2026-09-07）——A確実 → A要検証

> **数値指標を ぜんぶ 満たしても、進化言語そのものが 目で 成立して
> いなければ 不採用**という、はじめての 例です。

仮の 言語は「元からある 左右の 小さな 枝角が、**Y字に 分岐しながら**
上・外へ 育つ」。A方式（deco）で 1版 作りましたが、**128px で
「主枝から 枝分かれした」に 読めません**でした。

#### 数値は すべて 合格して いた

| | |
|---|---|
| 36px の 2run 連続 | 左 **2行** / 右 **3行**（base は 左0 / 右0）|
| 1列run | **0** |
| 中央の 空列 | base 4行 → **5行**（左右が つながらない）|
| 24px 上10行の 占有列 | 80 → **104** |
| 24px 角領域の 面積 | 6,670 → **8,468px²** |
| 本体の 画素の ちがい | **0** ／ body shift **0** |
| bbox | x37..217 / y18..230（安全わくの 内がわ）|

#### それでも 落とした 理由

かざりが **見えはじめる** y ——**左 y63 / 右 y71**。
みみの てっぺんは **左 y51 / 右 y67** なので、**Y字の 接続部が
まるごと みみに かくれます**。目で 見ると
**「みみの 外がわから 別の 角が 生えた」**に しか なりません。

#### 幾何で 両立しない ことを 総あたりで 確かめた

分岐点 y（50〜66）× 肘 x（56〜74）× 先端 x（44〜52）の **64通り**。

| 分岐が 見えはじめる y | みみより 上か | 36px の 左の 分離行数 |
|---|---|---|
| **y49〜51** | **見える** | **0〜1行** |
| y53〜59 | かくれる | 1〜2行 |
| y64〜73 | かくれる | 1〜3行 |

**「128px で 分岐点が みみより 上」かつ「36px で 2行以上 2run」の
組み合わせは 0件**でした。理由は 3つが 同時に かかる ためです。

1. 36px で 枝と 主枝を 分離させるには **横に 11px 以上**（x70 では 融合すると 実測）
2. 左は 角の 上端 y25 〜 みみの てっぺん y51 の **26px しか 自由な 高さが ない**
3. その 高さで 11px 横へ 動かすと、**Y字の 分岐点が かならず みみより 下に 落ちる**

**いまの base の 形の まま、A方式の deco だけで この Y字言語を
成立させるのは むずかしい**、が 結論です。

#### 「主枝を 太く 高く する」弱い A案へ すぐ 置きかえない

それでは **「進化 ≠ ただの 大型化」**という いまの 基準が ゆるみます。
`c_deer` は **保留**。B確定にも しません ——本体がわを 変える
（既存の 角そのものを 育てる／みみと 角の 前後関係を 変える）か、
別の 強い 言語が 見つかれば 再検討できます。

#### 一般化して のこす こと

> **小サイズの 数値は 合否の 補助**であって、
> **意味的な つながり・成長の 読みを 上書きしません。**
> `c_mummy` の C2（面積 +20.7% でも 破れ 0行）は「数値が 出ない」例、
> `c_deer` の F1+ は **「数値は 全部 出たのに 意味が 成立しない」**例です。
> ルール⑤の「最終判定は 128 / 36 / 24px の 目視」は、この 順で 効きます。

#### `c_witch_e1`（まじょ）——**肩から 片がわ 斜め後方へ 流れる 一枚の cape**

Batch 3 の 4体目。**A方式は「既存部位の 延長」だけでは ない**ことを
はっきりさせた 例です。

| | |
|---|---|
| 本体 | `witch` を 1:1・画素の ちがい **0**・body shift **0px** |
| 変更しない | 顔・帽子・髪・ドレス・**ほうき**の base 画素 **0** |
| 主役 | **進化した まじょが、肩から 片がわ 斜め後方へ 流れる 一枚の 魔法の cape を まとう** |
| 枚数 | **1枚**（1つの 閉じた path）|
| 向き | **片がわ・斜め後方**（右の 質量は 1pxも 変わらない）|
| secondary | **なし**（spark・dust・halo・星 0）|
| primitive | **`catmull()` だけ**。新規 0 |
| 色 | `#d7b7fb` → `#caa8f2` → `#a77cba`・ふち `#8c5d9b`（ぜんぶ base 実測）|
| **きんし** | 暗黒の 影・半とうめいの 幽霊布・切れこみ・羽根の すじ・多層の すそ |

##### **新要素型 A** ——base に cape は 無かった

さがした ところ、base には **cape も 肩布も 背面布も 留め具も
ありません**でした。ふつうの 一枚の ワンピースです。
だから これは `c_cobra`（フード）や `c_octopus`（触手）の ような
**既存部位の 延長では なく、新しい 進化衣装が 出てくる** 型です。

> **A方式は 既存部位の 延長だけに 限定しません。**
> すでに `c_lavagolem_e1`（浮遊岩）・`c_onibi_e1`（子の 炎）が
> 「無かった ものが 出る」型で 成立して います。
>
> **ただし 新要素を 自由に 足して よい、という きまりには しないこと。**
> 新要素型 A は つぎの 5つを **ぜんぶ** みたす ときだけ です。
>
> 1. その species の identity と **強く むすびつく**（まじょ ↔ マント）
> 2. 128px で **自然**（貼りものに 見えない）
> 3. base の **主要 identity を こわさない**（ほうき）
> 4. 既存の 予約言語と **重複しない**
> 5. **36px / 24px でも 読める**

##### 接続は 帽子の つばの うしろへ 完全に かくす

つば（**y89..117 / x45..210**）は 肩（x92..174）より 広く、その 真上に
あります。cape の 上ばしを そこへ 入れるので **付け根は 1pxも 見えません**。

| y | 見えて いる cape |
|---|---|
| 110 / 114 / 117（つば）| **なし** |
| 118 / 120 / 122 / 124（顔）| **なし** |
| **125** | **1px** |
| 126 / 128 / 130 / 134 | 2px / 6px / 10px / 23px |

**よこ一直線の 切断面を 作らないこと。**1 → 2 → 6 → 10 → 23px と
少しずつ 出るので、継ぎめが 見えません。

> `c_deer` と ちがい、ここでは **成長の 接続点を 見せる 必要が ありません**
> ——のびるのでは なく「着る」ためです。

##### **ほうきの 負の 空間を 1pxも うめない**（固定条件）

ほうきは まじょの identity です。`c_crab` で 得た 注意則を そのまま
当てはめました。

| | すきま | **α>128** | **α>8** |
|---|---|---|---|
| 穂（x36..104 / y204..232）| 1,139px | **0px** | **0px** |
| 柄（x168..206 / y126..206）| 2,551px | **0px** | **0px** |

**右へ 流す 案（W2）は 柄の まわりを 99% うめ、36px の 2run行が
6 → 4 に 落ちました。**だから 左・みじかめの W5 系に して います。

##### 確定の 数値

| | |
|---|---|
| base bbox | x45..210 / y25..230（166x206）|
| **e1 bbox** | **x35..210 / y25..230（176x206）** |
| **安全余白** | **L24 / R34 / T14 / B14** |
| **visible cape bbox** | **x35..105 / y125..202** |
| **cape 面積** | **3,185px²** |
| **cape 重心** | **(67.5, 166.5)** |

##### 小サイズの 固定実測

| | 36px | 24px |
|---|---|---|
| cape が 見える 行数 | **11行** | **7行** |
| 最大厚み | **8列** | **6列** |
| base との さ | — | **29px** |
| **左（後方）の 質量** | **+33.9%** | **+35.4%** |
| 右の 質量 | **不変** | **不変** |
| 2run行 | **6 → 6** | **3 → 3** |

> **合格の 理由は「24px で 色を のぞいた シルエットだけでも
> 片がわ後方の cape が 読める」こと**です。
> **bbox の 最大はばは 指標に しません** ——帽子の つばが すでに
> いちばん 広い ためです（`c_ghost_e1` と 同じ 話）。

##### W5 → W5b ——**形の 種類を 変えず、張り出し量だけ**で 直した

| | W5 | **W5b（採用）** |
|---|---|---|
| bbox | x45..210 | **x35..210** |
| cape 面積 | 2,529px² | **3,185px²** |
| 36px 左質量 | +26.9% | **+33.9%** |
| 24px base差 | 22px | **29px** |
| 24px 左質量 | +26.8% | **+35.4%** |
| ほうき侵入 | 0px | **0px** |

W5 も 成立して いましたが **24px が 控えめ**でした。W5b は
**片がわ 1枚・斜め後方・不とうめい・なめらかな 外縁・1つの path・
つばの うしろの 接続・ほうき侵入 0 を ぜんぶ そのまま**に、
**外への 張り出し量だけ**を 増やして 小サイズの 可読性を 上げた 例です。

**`W5c`（x27..210・左質量 +40.2%）へは 広げません** ——片がわの 面が
大きく なりすぎて「うしろに 大きな 板を 足した」に 寄り、
`c_cobra_e1` の 一枚盾とも 近く なります。

##### 予約言語との ちがい（**最終の 読みで**）

| | |
|---|---|
| **`c_witch_e1`** | **片がわ 斜め後方・一枚・不とうめいの cape** |
| `c_ghost_e1` | 下方向・左右・**3層**・半とうめい |
| `c_cobra_e1` | 頭・首の **左右対称**な 一枚の 盾 |
| `c_devil_e1` | 左右・暗い bat shadow・**切れこみ** |
| `c_firebird_e1` | **左右 2枚**の 実体の 翼 |

`c_cobra_e1` との ちがいが いちばん 大事です ——cobra は
**左右対称の よこ張り**（よこ +32.9% / たて +0.5%）、witch は
**右が 1pxも 変わらない 一方向の 後方流れ**。6体の 中で
**witch e1 だけが 非対称**です。

#### `c_oni` は 設計だけで **不採用**に した（2026-09-07）——A要検証の まま 保留

> **4例目。**しかも いちばんの 原因は これまでの 5則の どれとも ちがう
> **新しい もの**でした。

仮の 言語は「手に 持つ 一本の 太い club が、斜め方向へ 長く 大きく
成長する」。**`c_oni_e1.png` は 1枚も 作って いません**（7案の 下絵を
くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| bbox | **x54..201 / y25..230（148x206）**・面積 22,978px²・fill .754 |
| 安全余白 | **L43 / R43 / T14 / B14** |
| **金棒の 軸** | **(153,217) → (196,172)**・長さ **62.2px**・**46°** |
| 上の 端（こぶ）| x189..197 / y168..178（9x11・**71px²**）|
| 下の 端（シャフト）| x149..181 / y186..221（33x36・591px²）|
| 太さ | 17 → 19.5 → 12 → **0（かくれる）** → 12.5px。**最大 19px** |
| **かくれて いる わりあい** | **軸の 約42%**（うで・ふんどしの うしろ）|
| **見えて いる 金棒** | **1,085px² ＝ base ぜんたいの 4.7%** |
| 色 | `#f8e1c7` → `#f8d4b6` → `#dabd8e`・ふち `#b98d69` |
| **つのとの 色の へだたり** | **RGB 22 しか ない**（つの `#f9deae` ／ 金棒 `#f0cba9`）|
| 36px | 帯 19px → **2.7列**。下半分の ベージュは 25px（7行）|
| 24px | 帯 19px → **1.8列**。下半分の ベージュは 9px（4行）|
| のばせる 場所 | **上右（細い側）45〜75px** ／ **下左（太い側）約15px** |

##### 7案 とも 128px で 落ちた

| 案 | どう 見えたか |
|---|---|
| O1 上右へ 長く＋こぶ | **体を 横切る 棒**。わくから はみ出す（R −8）|
| O2 下左へ | **つえ／ぺろぺろキャンディ**。B −9 |
| O3 太らせる だけ | base と ほとんど 同じ。進化に 見えない |
| O4 上右へ 棒だけ | **ただの 板**。R −1 |
| O5 上右へ 中くらい | **木の しゃもじ** |
| O6 上右へ 短く 太く | **マラカス／ハンマー** |
| O8 太い 丸太 | **板が 肩に 立てかけて ある**だけ |

##### **使用関係が 見えて いない 持ちものの 注意則**

> **base で キャラクターが 道具を「持つ・にぎる・つかう」関係が
> はっきり 描かれて いない 場合、その 道具だけを A方式で 大型化しても、
> キャラクターの 装備では なく **独立した 別物**に 見える 危険が
> 高い です。**
>
> たしかめる こと。
>
> 1. **手・指・にぎりの いち**が 見えるか
> 2. 道具が base で **じゅうぶんな 面積 見えて いるか**
> 3. **姿勢を 変えずに** 大型化しても 使用関係が たもてるか

**「持ちものは A不可」とは しないこと。**`c_witch` の ほうきの ように、
使用関係が はっきり して いる 持ちものなら A で 成立しえます。

##### `c_witch` との 対照

| | |
|---|---|
| **`c_witch` の ほうき** | 柄が **体の 前面を 横切り**、にぎって いる いちが 見える → cape を 足しても ほうきは ほうきの まま |
| **`c_oni` の 金棒** | **体の うしろへ 差しこまれて いる**。**手も 指も こぶしも 無く**、見えて いるのは **4.7%** だけ |

> **新しい 装備・既存の 道具を A で ひろげるかは、「その ものが あるか」
> だけでは なく「キャラとの 関係が 読めるか」で 決める こと。**

##### 複数の 失敗条件が 重なった 例

`c_oni` は 新しい 要因 だけでは ありません。

| | |
|---|---|
| **使用関係が 見えない** | 手・指・こぶしが 無い（**主因**）|
| 遮蔽 | 金棒の 軸の **約42%** が うで・ふんどしの うしろ |
| 終端器官 | **両はしとも 丸い こぶ**。上右へ のばすと こぶが 途中に のこる |
| 比率変更不能 | base の 太さを **細く できない**（足せば 太く なるだけ）|
| 余白不足 | **太い側へ のばせるのは 約15px**。広いのは 細い側だけ |

##### 色の 近さは 補助リスク

金棒と つのの **RGB の へだたりが 22 しか ない**ので、36px で ななめに
のばした 棒が **別の つの・明るい 線**に 見える おそれも ありました。
**主因では ありませんが、小サイズの 部位混同リスク**として のこします。

##### いまは 保留。弱い 代替案へ 逃がさない

**B確定にも しません。**将来 B方式で **うで・手・にぎり・金棒を 一体で
描きなおし**、「太い 金棒を ほんとうに かついで いる／ふるって いる 姿」に
できる ときに 再検討して ください。**いまは B制作に 進まないこと。**

| 逃がさない もの | なぜ |
|---|---|
| 角を 育てる | **「角」の 言語は まだ どこにも 割りあてて いない**（`c_deer` も 保留）|
| 髪を 大きく | **`ch_gumgum_e1` の ふくらむ かたまりと 衝突** |
| 炎を 足す | **`c_hinotama_e1` `c_onibi_e1` と 衝突** |
| ふんどしを 大きく | **`c_witch_e1` の 一枚 cape と 近い** |

### Batch 3 クローズ（2026-09-07）——**8候補中 4体 正式完成**

| | species | 進化言語 | SHA-256 |
|---|---|---|---|
| ✅ | `c_octopus_e1` | 元からある 太い 触手が それぞれ 独立に 長く うねって 育つ | `8cea9188…` |
| ✅ | `c_mummy_e1` | ほどけた 包帯が 不規則な 方向へ 垂れ、外形が「ふさ」に ほぐれる | `b69f700f…` |
| ✅ | `c_cobra_e1` | 頭・首を つつむ、外縁の なめらかな 一枚の よこ長の 盾 | `f9446cbd…` |
| ✅ | `c_witch_e1` | 肩から 片がわ 斜め後方へ 流れる 一枚の 不とうめいの cape | `4ddc38e6…` |
| 保留 | `c_deer` | （Y字の 分岐）——**遮蔽**で 接続点が 見えない |
| 保留 | `c_scorpion` | （高く 反り返る 弧）——**終端器官**を こえられない |
| 保留 | `c_crab` | （細い 関節＋2質量）——**負の 空間 ＋ 比率 ＋ 余白** |
| 保留 | `c_oni` | （太い 金棒が のびる）——**使用関係が 見えない ＋ 他4つ** |

**打率は 5割。**けれど この Batch の 値うちは **4体 ふえた ことでは なく、
A方式が 落ちる 条件を 6つに 切りわけられた こと**です（次章）。
おかげで 次の Batch からは **制作前に かなり 弾けます**。

#### この Batch で 決まった こと

| | |
|---|---|
| **A方式は 既存部位の 延長だけでは ない** | `c_witch_e1` が **新要素型 A**（新しい 進化衣装）の 成立例。ただし 5条件を ぜんぶ みたす ときだけ |
| **数値の 成功で 採らない** | `c_scorpion` S5 は 面積 +11.0%・36px 上ばし y6→y2 でも 不採用 |
| **数値が 出なくても 落ちない** | `c_mummy` C2 は 面積 +20.7% でも 小サイズの 差が 0行 ——**面積と 小サイズの 読みは 別の 問題** |
| **アンカーは 連続した 不とうめいの 本体へ** | `c_octopus` で 内部の 穴に つないで 131px 浮いた |
| **対称軸は base の 実画素から** | `c_cobra` で `255-x` → `256-x`。**全asset 共通の 式に しない** |
| **形の 種類を 変えず 張り出し量だけ**で 直せる | `c_witch` W5 → W5b（24px の base差 22 → 29px）|

#### 次の Batch へ 持ちこす こと

1. **制作前に 6つの スクリーニング**を かける（次章）
2. **完成ずみ 24言語との 重複は 36px / 24px の 最終シルエット**で 判定する
   （primitive の 重複では ない）
3. **候補を こちらの 判断だけで 差しかえない** ——弱いと 分かった 時点で
   報告して 止まる

#### `lg_chocoknight_e1`（ショコラのきし）——**持って いる 盾に チョコの 外周が つく**

> **2026-09-07 に 正式採用。**Batch 4 で **はじめて 通った「持ちもの」の 進化**であり、
> `c_turtle` で 見つかった「完成した 閉じた 面は 二重に なる」問題への
> **はじめての 逃げ道**です。

進化言語は **「持って いる ビスケット盾に、濃い チョコの 外周と クリームの
縁どりが つき、一枚の 補強された 盾に なる」**。

##### 固定した もの（**変えない**）

| | |
|---|---|
| 方式 | **A方式（deco）**・base 1:1・**本体の 画素の ちがい 0**・**body shift 0** |
| SHA-256 | **`246b6cf1a3b25c924baff85660c43149b09f9c2d13d2901c9c39dc8d1665be86`**（EVO_LOCK 23枚目）|
| base の 盾 | **中心 (170, 181)・rx 26 / ry 31**（外周 100点の 最小二乗・**rms 0.0315**）|
| チョコ外周 | **+13px を 基本と した 均等な 同心フレーム**（＋線 1.5px → 外周 rx 40.5 / ry 45.5）|
| e1 bbox | **x59..210 / y25..230**・面積 **20,427px²（base 比 +9.5%）** |
| `asym` | **true**（盾は 右がわ だけ。ただし **盾の 局所は 中心 (170,181) に 均等**）|
| secondary | **なし** ——びょう・spark・halo・dust・粒を **足さない** |
| 変えない base | ビスケットの 面・格子もよう・もとの クリームの ふち・よろい |
| **右足との run 接続** | **正式に 許容。この 理由だけで 左下を 削らない** |

色は **ぜんぶ base の 実測**です ——ぬり `#c08168 → #a76953 → #90604a`（よろい）／
ふち `#663220`／縁どり `#fbe7c4`。primitive は **`<ellipse>` 2つ だけ**（新規 0）。

##### 同素材では 落ちる ——制作前に 10案 くらべた

| | 128px |
|---|---|
| **K1 同心・同じ ビスケット +11px** | **二重盾**（`c_turtle` と 同じ 落ちかた）✗ |
| **K4 片方向（右）・同じ ビスケット +13px** | **うしろから のぞく 2枚目の 盾** ✗ |
| **K7 チョコの 外周 +13px ＋ クリームの 縁どり** | **一枚の 補強された 盾** ✓ |

> **向きの 問題では ありませんでした。**片方向（K4）でも 同素材なら 落ちます。
> 効いたのは **拡張の 向きでは なく 素材の 境め**です。

金・クリーム主体の 輪（K9）は **`ch_fairy_e1` に 近づく**ので 採りません。
びょう（K5 / K10）も 今回は 入れず、**外周そのもの だけで 成立**させました。

##### 実測

| | |
|---|---|
| 新しい 外周 bbox | **x153..210 / y135..226（58x92）**・面積 **1,775px²** |
| 不とうめい | **1,614px² ＝ 90.9%**（のこり 161 は ふちの なめらか処理だけ）|
| 局所の 均等さ | 8方向の 外周 実測 40.3〜45.3 に 対して 理論 40.5〜45.5 ——**ずれ 最大 1.3px** |
| クリームの 縁どり | 見える 範囲の **189/201 点で 連続**・最長の 切れ **2°** |
| **36px** | 面積 414 → 450（+8.7%）／**右半分 181 → 217（+19.9%）** |
| **24px** | 面積 197 → 213（+8.1%）／**右半分 85 → 101（+18.8%）** |
| 32px の IoU | base **0.918**（同じ キャラ）／`c_cobra_e1` 0.570 ／`c_witch_e1` 0.567 ／`ch_fairy_e1` 0.487 |
| 連結成分 | **1つ**（体と ひとつづき ＝ 独立した 円盤では ない）|

##### `ch_fairy_e1` の 輪と 分ける 4つ

| | |
|---|---|
| ① **不とうめい** | 90.9% が 完全不とうめい |
| ② **暗い チョコ** | 金・発光に しない |
| ③ **盾の 上だけ** | キャラ中心 x127.5 に 対して **盾の 中心 x170**（ずれ 42.5px）|
| ④ **放射なし** | 放射線 0本 |

`c_cobra_e1` とも 別ものです ——あちらは **体の 一部**（フード）が **横へ 一方向に +50%**、
こちらは **持ちもの**の **局所の 素材強化**。

##### 前後関係は base の まま

盾は 胴の 前／上左は 肩あての うしろ（外周が 見えはじめるのは **y138 から**。
y134 では まだ 肩あての うしろ）／右ブーツは **外周の 前**（base 画素 0件 変更）。

#### `c_snowqueen_e1`（ゆきのじょおう）——**元からある 長髪が さらに 長く・豊かに 育つ**

> **2026-09-07 に 正式採用。**Batch 4 で はじめて 通った
> **rim では ない 別型**の A成功例です。`lg_chocoknight_e1`（持ちものの
> ふちの 素材強化）とは 別系統 ——**既存の 太い 部位その ものを 育てる**型。

進化言語は **「元からある 左右の 太い 長髪が、同じ 髪の まま さらに
長く・豊かに 育つ」**。**cape・veil・翼を 足す 進化では ありません。**

##### 固定した もの（**変えない**）

| | |
|---|---|
| 方式 | **A方式（deco）**・base 1:1・**本体の 画素の ちがい 0**・**body shift 0** |
| SHA-256 | **`6eb88042773fe4ae2dc28685d01c6238548e8c2d01d35d5a42f08a19e4ae6e57`**（EVO_LOCK 24枚目）|
| 変えない base | **顔・ティアラ・ドレス・元の 髪** —— 20,559画素 すべて 無変更 |
| e1 bbox | **x48..207 / y25..230**・安全余白 L37 / R37 / T14 / B14 |
| 面積 | **23,446px²（base 比 +14.0%）** |
| 追加した 髪 | bbox **x48..207 / y159..229**・面積 **2,887px²** |
| 本数 | **左右 2束ずつ**（外・内）|
| 左右対称性 | 左 1,449px / 右 1,438px（さ 11px）。e1 の 左右差 44px は **base 自身の 59px より 小さい** |
| secondary | **なし** ——snow・spark・halo・dust・宝石・星を 1つも 足さない |
| primitive | **`catmull()` ＋ `flowPath()` のみ。新規 0** |
| 色 | ぬり `#f4fafe → #e2f4fb → #d6e2f2 → #c8cfe4`／ふち **`#aaafce`**（base の 髪の 輪郭 そのもの）|

##### 小サイズの 確定値

| | 左 | 右 | 行数 |
|---|---|---|---|
| **36px** | 23 → **50** | 25 → **50** | 9 → **13** |
| **24px** | 10 → **24** | 10 → **24** | 7 → **9** |

##### 通り道は 実測で 決める

ドレスの いちばん 広い ところが **x66..189（y212）**、毛先は x56〜62 なので、
**x45..66 / x189..210 の たてに 長い 通路**が y186 から 下に あいて います。そこを 流します。

##### 3つの きまり

1. **先を 巻く**（base の 毛先と 同じ 終わりかた）。まっすぐな 帯は **リボン**に 見えます
2. **幅を 一定に しない。**中ほどを ふくらませて 先を すぼめる
3. **中ほどに ゆるい うねりを 1つ。**なめらか すぎると 帯に 見え、強すぎると
   **ダイヤ形の 穴**が あいて リボンの 折り返しに なります

##### 継ぎめ ——**横一直線の 付け根を 作らない**

新しく 見えはじめるのは **y159 で 2px**、そこから 5 → 9 → 14 → 20 → 30 → 36 → 37 …
と ふえます。**突然の 太い 出現は ありません。**毛先は **y229**、連結成分は **1つ**。

##### **二重髪に ならない 理由**

髪は もともと「**束の 前後が 重なる**」描きかたなので、元の 輪郭が
**束と 束の 境め**に そのまま 読めます。`c_turtle`（完成した 閉じた 面）や
`c_rabbit`（1つの 強い 終端）とは ちがいます。

##### S7 からの 正式変更点（4つ）

| | |
|---|---|
| 下端 | **y234 → y229**（安全余白 B10 → B14）|
| 色 | base の 髪色へ **さらに 寄せた**（下半分の 灰むらさきを 解消）|
| うねり | 外側の 束に **弱い S字を 1回**だけ 追加 |
| 穴の 手当て | うねりで できた **ダイヤ形の 穴**を、内側の 束を **6px 外へ 寄せて** 解消 |

##### 小さな すき間は **正式に 許容**

| | |
|---|---|
| 左 | **x81..88 / y182..195** |
| 右 | **x168..174 / y181..194** |

**「髪束と ドレスの あいだの 自然な 負の 空間」**として 許容します
——128px で 不自然な 穴に 見えず、左右 ほぼ 対称、36px では 1px 未満。
**埋めると 髪と ドレスが 接触して cape / veil 的な 一枚面へ 寄ります。**

> **今回を 一般許可に しないこと。**つぎは **別あつかい**です
> ——ダイヤ形／人工的な 穴／大きな 切りぬき／**24px でも はっきり 穴として
> のこる 空間**。

##### 予約言語との 差

| | |
|---|---|
| `c_witch_e1` | **片がわ 1枚の cape**（右は 1pxも 変わらない）|
| `c_ghost_e1` | **下へ 3層の veil**（半とうめい・波打つ）|
| `c_octopus_e1` | **下がわの 4本の 太い 触手** |
| **`c_snowqueen_e1`** | **左右を たてに 流れる 不とうめいの 長髪** |

32px の IoU：base **0.878**（同じ キャラ）／`c_witch_e1` 0.728 ／
`c_ghost_e1` 0.682 ／`c_octopus_e1` 0.671。

> **`c_queen` / `c_princess` へ 同じ 言語を 自動で 移さないこと。**
> ただし **「太い 髪束の 延長」が A方式で 成立しうる**ことは 実証できました。

### **完成した 閉じた 面でも、素材の 境めに 読みかえられれば A は 成立する**

> **完成感の 強い 閉じた 面でも、元の 輪郭を 別素材との 境界として
> 自然に 読みかえられる 場合は、A方式の 同心拡張が 成立する ことが あります。**

`c_turtle` との 対照が いちばん 分かりやすい ところです。

| | やった こと | 元の 輪郭は | 結果 |
|---|---|---|---|
| **`c_turtle`** | **同素材**を 外がわに 足す | **もう一枚の 甲羅の 外縁**として のこる | **不成立** |
| **`lg_chocoknight`** | ビスケットの 面の 外を **チョコ素材**に する | **ビスケット／チョコの 素材境界**に なる | **成立** |

#### **「別素材なら かならず A成立」とは しないこと**

一般則に しては いけません。つぎを **1体ずつ** たしかめます。

1. その 素材の 関係が **既存の キャラの 中に 自然に ある**こと
   （騎士は **チョコの よろい＋クリームの 縁どり**を もともと 持って いた。
    カメには 甲羅の 外に 置ける 自然な 別素材が **無かった**）
2. **128px で 一枚の 物体**として 読めること
3. **小サイズでも 意味が のこる**こと

### run が つながっても、それだけで 落とさない

> **A方式の 新しい 背後の かざりが 既存の 部位と 数px 接続しても、
> run の 接続だけを 機械的な 不採用条件に しないこと。**
> 前後関係・局所の 輪郭・素材の ちがいで **別の 部位として 自然に 読める**なら
> 許容できます。

ただし **顔・手足・主要な シルエットとの 融合に 見える 場合は 不可**です。

`lg_chocoknight_e1` は **許容の 例**として のこします。

| y | base | e1 |
|---|---|---|
| 209 | `128-154` ／ `157-182` | **`128-201`**（つながった）|
| 212 | `128-154` | **`128-199`** |
| 215 | `129-153` | **`129-196`** |

右足と 盾の あいだの すき間は **2px（x155..156）**しか なく、同心の 外周だと
かならず 埋まります。それでも

- 128px の 拡大でも **ブーツと 盾の 前後関係は 自然に 読める**
- **base の 画素は 0件 変更**で、ブーツは 外周の 前に のこって いる
- チョコ外周＋クリームの 縁どりで **盾じたいの 境界も じゅうぶん 読める**
- **「足と 盾の 融合」には 見えない**（局所の 接触だけ）
- 左下を 削る ほうが、**せっかく 成立して いる 均等な 楕円フレームを こわす**

ので、**このまま 採用**しました。

#### `c_redpanda_e1`（レッサーパンダ）——**太い 縞の 尾が 上へ さらに 長く のびる**

> **2026-09-07 に 正式採用（EVO_LOCK 25枚目）。**`c_snowqueen_e1` に つづく
> **「太い 既存部位の 自然な 延長」型の 2例目**で、`c_squirrel` が 落ちた あとに
> **終端から の 実軸余白**（新しい ④）で 拾いなおした 個体です。

進化言語は **「元からある 太い 縞の 尾が、同じ 尾の まま 上へ さらに 長く のびる」**。

##### 固定した もの（**変えない**）

| | |
|---|---|
| 方式 | **A方式（deco）**・base 1:1・**本体の 画素の ちがい 0**・**body shift 0** |
| SHA-256 | **`91c015c55be99d1dcb2e1305f51cd9e2df33c601aa25260fd497251f0fb21907`**（EVO_LOCK 25枚目）|
| 変えない base | 顔・耳・体・足・**元の 尾**——27,331画素 すべて 無変更 |
| e1 bbox | **x24..222 / y25..230（199x206）**・面積 **30,732px²（base 比 +12.4%）** |
| 安全余白 | L13 / R22 / T14 / B14 |
| かたち | base の **左右の ふちを そのまま 上へ つづける 筒**（左 x38→26 ／ 右 x88→63）。先は 半円 |
| しま | **base と 同じ 16px ピッチ・−15/260 の かたむき**。クリームは y90..74 と y58..42 |
| secondary | **なし** ——房・粒・spark・halo を 1つも 足さない |
| `asym` | **true**（尾は 左がわ だけ。よこの 中心ずれ −0.020）|
| 色 | ぬり `#d96d4c → #e8855e → #f2a077`／クリーム `#fdf3e1`／ふち **`#a94c2f`**（ぜんぶ base の 実測）|

##### **いちばん 大事な こと ——ドームの すぐ上は「オレンジ」に する**

base の 尾先は **閉じた 丸い ドーム**（y107〜126・オレンジ）です。
そこへ **クリームの 帯**を かさねると 明度差で 輪郭が 完全に 立ち、
**閉じた ドームが クリームの 中に のこって「古い 尾先が すけて 見える」**
——`c_turtle` / `c_squirrel` と まったく 同じ 二重に なりました（下絵 R8b・R10b で 実証）。

**同じ オレンジで つなぐと ぬりが つながり、のこるのは 細い 弧だけ**に なって
「毛の 折れ」に 読めます。さらに 弧の すぐ上に やわらかい かげ（`rpSh`）を
敷いて 段に します。

> **`lg_chocoknight_e1` の「元輪郭を 別素材の 境界に 読みかえる」とは 別の 手です。**
> こちらは **同色で つないで、元輪郭を かげに 落とす**。
> 「閉じた 終端が のこる」問題への **2つめの 逃げ道**として のこします。

##### ふちは base の ふちを つづける（中心線で 引かない）

中心線＋はばで 帯を 引くと base と はばが 合わず、
**別の 縞の 筒が うしろに 立ちます**（R1〜R7 で 実証）。

##### 上へ 行くほど 左へ 寄せる

耳の 左ふちが y36〜104 で **x78〜87** なので、まっすぐ 上へ のばすと
頭と くっつきます。右ふちを y112:83 → y54:63 と しぼって、
すき間を **2px（y83）**のこします。

##### 小サイズの 固定実測

| | base | e1 |
|---|---|---|
| **36px 面積** | 573 | **645（+12.6%）** |
| **36px 尾の 面積** | 148 | **220（+48.6%）** |
| 36px 2run行 | 11 | **16** |
| **24px 面積** | 268 | **299（+11.6%）** |
| **24px 尾の 面積** | 77 | **108（+40.3%）** |
| 24px 2run行 | 5 | **7** |
| しまの 交番（36 / 24px）| 4 / 6 | **6 / 6** |

**24px でも 縞の 反復が のこる**こと が 合格条件です。
**2run行が ふえて いる**ので、尾は 頭と **融合して いません**。

##### 予約言語との 差（32px IoU）

| base | `c_lavasnail_e1` | `sp_rpurin_e1` | `c_snowqueen_e1` | `c_octopus_e1` | `c_ghost_e1` |
|---|---|---|---|---|---|
| **0.889**（同じ キャラ）| 0.719 | 0.675 | 0.631 | 0.626 | 0.619 |

**しきい値 0.86 を こえるのは base だけ。**最近傍の `c_lavasnail_e1` とは
**巻いて いない・中心へ もどって いない**ことで 読みが 分かれ、
`sp_rpurin_e1`（細い 1方向の 帯）とは **52px の 太さ と 縞**で 分かれます。

##### 予約言語 #25

**片がわ 1本の、太い 縞の 尾が たてに のびる。**

##### 採用前の 確認（4つの 地・左右反転）

| | |
|---|---|
| 白 / クリーム / ミント / ラベンダーの 地 | 128 / 36 / 24px の 12とおり すべてで **尾の のびと 縞が 読める** |
| クリーム地との さ | base 平均 92.8 → e1 **97.2**（下がって いない）。最小は どちらも 2 ＝ **クリームの 縞そのもの**で、base と 同じ 性質 |
| **左右反転** | `redpanda` は `EN_FACE` に **のせて いない**（正面の子）ので 盤面では うつりませんが、反転させても 向きの ある モチーフ・文字・非対称の かげは 1つも なく こわれません |
| base の 画素 | 道具とは **別に 数えなおして** 27,331画素 ぜんぶ 一致・**ちがい 0件（最大差 0）** |
| body shift | 27,331 / 27,331 が **同じ 座標**で 一致 ＝ **0px** |

### 「反復もよう・層・房・素材境界」で 132体を 再抽出（2026-09-08）

`c_redpanda_e1` で「**反復もようが ある 部位では、元の 終端の 輪郭を
もようの 途中の 境めとして 吸収できる**」ことが 分かったので、
その 目で 残り 132体を しらべなおしました。

**全132体 × 144方向**を 機械で まわし、突出部ごとに
①外シルエット接触 → ②**終端から の 実軸余白** → ③太さ 18〜70px →
④軸に そった 明暗の 交番（＝反復もよう）を 測って、
条件を みたした **67species / 194部位**を 目で 確かめて います。

> **機械だけでは 決められません。**上位の 多くは **足・ひづめ・体の 角**を
> 先端と して 拾い、うしろの もようで 交番が 高く 出て いました
> （`c_pharaoh` は 杖の フック、`lg_pegasus` は ひづめ、`c_armadillo` と
> `lg_chronos` は 前足）。⑤「別の 物体に 見えないか」は 目でしか 分かりません。

#### 反復もようは あるのに ②で 落ちた もの

| species / 部位 | もよう | 実軸余白 |
|---|---|---|
| **`c_hermit` 貝の うずまき** | 巻きの 反復（交番 5）| **±30° ぜんぶ 0px** ——先が **自分の 巻きへ もどる**（`c_squirrel` と 同型）|
| **`c_armadillo` 帯の 尾** | 帯の 反復（交番 6）| **±30° ぜんぶ 0px** ——尾先が **体に 向いて いる** |
| `c_mille` 層／`c_cake` 層／`c_pancake` 段／`c_wedding` 段／`c_baum` 年輪／`c_dango` 玉／`c_konpeito` とげ | どれも 強い 反復 | **積む 軸に 14〜15px しか ない** |
| `c_mont` しぼりの すじ | 強い 反復 | **自由な 端が ない**（①で 落ちる）|
| `c_tutankhamun` nemes の しま | しま＋素材境界 | たれは 下向きで **B14px** |

> **「反復もようが ある」だけで 拾うと この 9件が 上位に 来ますが、
> ②で ぜんぶ 落ちます。**

#### 上位候補

| # | species | 部位 | ②実軸余白 | ③終端 | ④反復 | ⑤太さ | ⑥25言語 | |
|---|---|---|---|---|---|---|---|---|
| 1 | **`c_tower`** | 積んだ マカロン（上へ）| **34〜39**（上）/ 41〜65（上左）| 弱い | 交番 7 | 56px | 衝突なし | **A有力** |
| 2 | `c_bee` | しまの 腹 | 40〜68 | 弱い | 交番 4 | 56px | **#25 と 近い** | A要検証 |
| 3 | `c_ostrich` | 尾羽の 房 | 64〜103 | 弱い | 交番 3（弱い）| 44px | `ch_gumgum` / `c_firebird` に 近い | A要検証 |
| 4 | `c_horse` | 尾 | 19〜64 | 中 | 少 | 30px | #25 と 同系 | A要検証 |
| 5 | `lg_mammoth` | 長毛の 層 | 9〜27 | 弱い | 交番 10 | 74px | 無し | A要検証（②が 弱い）|
| 6〜9 | `c_queen` / `c_princess` / `c_mermaid` / ~~`lg_berryqueen`~~ | 長髪 | 37〜80 | 弱い | 交番 7〜12 | 44〜107px | **#24 と 同言語** | 技術○・言語✗ |

> **`lg_berryqueen` は この あと 別の 軸で 通りました**（2026-09-09・裾の フリル段）。
> **「#24 と 同言語だから この species は 落ち」では ありません** ——落ちたのは
> **その 部位の 仮説**だけ です。ほかの 部位に 目を うつすこと。

### **離散した 反復ユニット**は、連続した 部位とは 別ルール

> **「連続した 1つの 部位」と「離散した 反復ユニット」を 分けて 考える こと。**

| | 元輪郭は どうなるか |
|---|---|
| **連続した 部位**（尾・髪・鼻・甲羅）| 足した ぶんの 中に **元の 終端が のこる** → `c_turtle` / `c_squirrel` の 二重。`c_redpanda` は **同色で つないで かげに 落とす**ことで やっと 回避した |
| **離散した 反復ユニット**（積んだ マカロン）| **元の ユニットの 輪郭が 見えて いるのが 正しい。**ユニットどうしは もともと 別の ものだから、二重に 見えようが ない |

**ただし この 型は 数が すくない**（積む 軸に 余白が 要る）。
`c_pancake` `c_dango` `c_baum` `c_konpeito` は ぜんぶ その 軸が 14〜15px で 落ち、
**`c_tower` だけが T36 を 持って いました。**

#### `c_tower_e1`（マカロンタワー）——**積まれた マカロンが もう 1つ ふえる**

> **2026-09-08 に 正式採用（EVO_LOCK 26枚目）。**離散した 反復ユニット型の 1例目。

##### 固定した もの（**変えない**）

| | |
|---|---|
| 方式 | **A方式（deco）**・base 1:1・**本体の 画素の ちがい 0**・**body shift 0** |
| SHA-256 | **`924e97d620035ce29efd0498bf51ba6528966afafd0c823f092a7380fa1f0fd4`** |
| 変えない base | 4段 ぜんぶ——20,352画素 すべて 無変更 |
| e1 bbox | **x25..230 / y14..208（206x195）**・面積 **21,712px²（base 比 +6.7%）** |
| 安全余白 | L14 / R14 / **T3** / B36（`ch_prince_e1` `tw_ice_e1` `c_cobra_e1` と 同じ 帯）|
| 追加する もの | **マカロン 1つだけ**（44 x 31px・**中心 x=127・下ばし y=47**）|
| いち | **base の 上に ぴたり。重ねない・浮かせない・ずらさない** |
| 色 | **ミント**（`#c6f2e0 → #a6e6c8 → #7fd0ac`・ふち `#4e9f7d`・フィリング `#eafbee`）|
| secondary | **なし** ——リボン・王冠・きらめきを 1つも 足さない |

##### base の 実測

| 段 | y | 高さ | はば | 面積 |
|---|---|---|---|---|
| 1（上）| 47..87 | **41** | 69 | 2,147 |
| 2 | 88..125 | 38 | 116 | 3,916 |
| 3 | 126..163 | 38 | 160 | 5,764 |
| 4 | 164..208 | 45 | 206 | 8,525 |

はばは **206 → 160 → 116 → 69 ＝ 1段ごとに ほぼ −45px。**
マカロン 1つの 中みは 上の 殻 24px ／ **フィリング 9px（高さの 58.5〜80%）** ／ 下の 殻 6px。

##### **幾何的に 選べる 道は 上だけ**

2段目を 3個に すると 3段目より 広く なり、4段目を 5個に すると 258px で
わくを こえます。上の あきは **35px**、base の マカロンは **41px** ——
だから **同じ 大きさは 入りません。**44x31（base の 75%）に します。
タワーは 上ほど 細いので 自然です。

##### **フィリングの ところで くびれる。フレアさせない**

2回 作りなおしました。

| やって しまった こと | どう 見えたか |
|---|---|
| あしを 外へ フレアさせた | **ボウラーハット**（つばに 見える）|
| 横を まっすぐに した | **カプセル／薬** |

正解は base の いちばん上の マカロンを **1pxきざみで 測った 半はば**
（`MC_PR`）——フィリング（t 0.585〜0.805）で **0.948 に くびれる**のが
マカロンの 形です。つや（`mcG`）も 入れる こと。

##### 色は base の 最上段と **同じに しない**

ピンク（下絵 T8）に すると **段の 切れめが 見えにくく なります**。
タワーに すでに ある 色から えらぶ こと ——**新しい 色を 足さない**。

##### 小サイズの 固定実測

| | base | e1 |
|---|---|---|
| **36px 面積** | 434 | **461（+6.2%）** |
| **36px 上部（y<12）面積** | 44 | **71（+61.4%）** |
| 36px 上部の 行 | 6 | **10** |
| 36px 上ばし | y6 | **y2** |
| **24px 面積** | 202 | **213（+5.4%）** |
| **24px 上部（y<8）面積** | 22 | **33（+50.0%）** |
| 24px 上部の 行 | 4 | **7** |

**よこはばは 206 の まま。**「ただ 大きく なった」では ありません。

##### 予約言語との 差（32px IoU）

| base | `c_snowqueen_e1` | `c_purin_e1` | `ch_prince_e1` | `c_redpanda_e1` | `ch_gumgum_e1` |
|---|---|---|---|---|---|
| **0.936**（同じ キャラ）| 0.706 | 0.616 | 0.561 | 0.509 | 0.502 |

##### 予約言語 #26

**積まれた 反復ユニットが 1つ ふえて、たてに 1段 高く なる。**

##### 採用前の 確認

| | |
|---|---|
| **128px** | **「帽子」より「1段 増えた」が 優勢**（マカロンの フィリングの 帯が はっきり 見え、まわりの 段と 同じ ものと 読める）|
| 36 / 24px | 上部の 質量 +61% / +50%、行 6→10 / 4→7 で **1段 増えたと 読める** |
| 白 / クリーム / ミント / ラベンダー | 12とおり すべて 合格。**ミントの 地でも base 自身の ミントの マカロンより 見える**（追加分 平均46.6・最小7 に 対して base の ミント 平均43.8・最小5）|
| 左右反転 | こわれない（`tower` は `EN_FACE` に のせて いない）|
| base の 画素 | 道具とは 別に 数えなおして 20,352 ぜんぶ 一致・**ちがい 0件（最大差 0）** |
| body shift | 20,352 / 20,352 が 同じ 座標で 一致 ＝ **0px** |

> **安全余白 T3 は 既存の 帯の 中です。**26枚の 最小余白は −6（`c_purin_e1`）〜14 で、
> **0〜3 が 5枚**（`ch_prince_e1` `tw_ice_e1` `c_cobra_e1` `ch_apple_e1`）。
> 「既存は T14〜15」は 私の 見まちがいでした。

### **のばす 前に「その 部位は 視覚的に 独立して いるか」を 見る**

> **A方式で 既存部位を 延長する 前に、その 成長対象が base の 中で
> 視覚的に 独立した 部位として 読めるかを 確認する。**
>
> 確認材料 ——
>
> - **内部の 輪郭**で 部位が 区切られて いる
> - **素材・色面の 境界**が 部位の 境界として 読める
> - **他部位との 前後関係**が ある
> - 根もとから 先端まで「尾・髪・盾・触手」等として **追える**
>
> これらが なく、**顔や 本体と 同じ 一枚の 面に もようが 乗って いるだけ**なら、
> その もようの 範囲を「部位」と みなして 延長しない。

**「連結成分が 1つなら A不可」とは 一般化しないこと。**見るのは 連結成分の 数では なく、
**絵として 部位に 分かれて 見えるか**です。

`c_pharaoh` の「**部位は 存在するが 完全に 内包されて いる**」とは **別の 問題**です
——あちらは 部位が あって 隠れて いる、こちらは **部位が そもそも 無い**。

#### `c_bee` は 設計だけで **不採用**に した（2026-09-08）——A要検証の まま 保留

仮の 言語は「しまの ある 腹部が、同じ 腹の まま ななめ下へ すこし 大きく／長く 育つ」。
**`c_bee_e1.png` は 1枚も 作って いません**（11案の 下絵を くらべて 落としました）。

##### base の 実測 ——②③④は 合格して いた

| | |
|---|---|
| bbox / 面積 | **x26..230 / y39..217（205x179）**・27,313px²・あき L15 / R14 / T28 / B27 |
| **連結成分** | **1つ**（27,313px。はねも 含めて ぜんぶ ひとつづき）|
| 腹の 終端 | **(52,188)**（重心から 102px）。局所の 二等分線は **154°** |
| **実軸余白** | **0°:52 / +15:45 / −15:69 / +30:41 / −30:60**（自分に 当たる 方向は 0）⭕️ |
| 終端の 強さ | **弱い**（とがりでは なく まるい 角）⭕️ |
| 太さ | **56px** ⭕️ |
| 反復もよう | 黒帯 **4本**・交番 **8**・ピッチ 約25px ⭕️ |
| 色 | きいろ `#fdd274`／黒帯 `#6b6b6b`／ふち `#c99740` |

##### 落ちた 理由 ——**独立した 腹部が 無い**

拡大して 確かめました。**顔（目・口・ほっぺ）と 縞は 同じ 1つの 玉**の 上に あり、

- 腹部を 区切る **内部の 輪郭線が 1本も ない**
- 独立した 腹部パーツとしての **前後関係が ない**
- もようは 本体の 面に 乗って いるだけ で、**根もとから 先端まで 追えない**

だから のばす ことは「腹が 育つ」では なく
**「玉に とがった 別の ものが 生える」**に なります。

##### 11案 ——**128px で 通ったのは 0件**

| 系統 | 中み | 128px |
|---|---|---|
| **E系（7案）** | つけ根に 弦を 張った blade（連続延長・同色接続・反復追加・25〜48px）| ✗ **プラグ／別の 縞棒**。base の 玉の 輪郭が 完全な 曲線の まま のこり、**別の 物体を 差した**ように 見える |
| **F系（4案）** | base の ふちの **接線を 実測して なめらかに つづける**（24〜44px）| ✗ **とがった しっぽ／ハチの 針**。長い 版は **スズメバチ**に 見える |

##### 数値も 足りない ——**質量と 読みが 両立しない**

| | 面積（36px）| 左下の 質量 | 32px IoU（base）|
|---|---|---|---|
| base | 565 | 73 | — |
| **F2 / F3**（なめらか）| **578 / 580（+2.3〜2.7%）** | 86 / 88 | **0.976** |
| E2（blade）| 609（+7.8%）| 117 | — |

**32px の IoU 0.976 は「同じ絵」に 近く、進化として 読めません。**
採用ずみで いちばん ひかえめな `c_lavasnail_e1`（+7.8%）・`c_tower_e1`（+6.7%）に 対して +2.3%。
質量の ある blade（+7.8%）は 逆に「別の 物体」に 見えます ——**両立しません。**

##### 分類と 逃がさない もの

`c_bee` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**B確定に しません。**羽・触角・顔・別部位へ **自動で 進化言語を 差しかえない**こと。

##### この ものさしで 落ちた もの

**`lg_mammoth` の 長毛**も 同じでした ——毛は **本体の 面の 質感**として 描かれて いて、
毛の 部位を 区切る 輪郭が ありません。いっぽう **`c_horse` の 尾**（自分の こい輪郭・
別の 茶色・あしの 前）と **`c_ostrich` の 尾羽**（自分の 波の 輪郭・胴の 上）は
**独立した 部位**として 読めます。

> ただし **`c_horse` は この あと 別の 理由で 落ちました**（すぐ下）。
> 「独立した 部位である」は 必要条件で あって、十分条件では ありません。

#### `c_ostrich_e1`（ダチョウ）——**後方へ ながれる、かさなった やわらかい 尾羽の 房**

> **2026-09-08 に 正式採用（EVO_LOCK 27枚目）。**`c_redpanda_e1` の「同色つなぎで
> 元の 終端を かげに 落とす」型、`c_tower_e1` の「離散した ユニットを 1つ 足す」型に
> つづく、**3つめの 逃げ道 ——「元の 輪郭を 羽の かさなり境界に 読みかえる」型**です。

##### 固定した もの（**変えない**）

| | |
|---|---|
| 方式 | **A方式（deco）**・base 1:1・**本体の 画素の ちがい 0**・**body shift 0** |
| SHA-256 | **`679cdacb2ad584c03e50f1d0219799965605347123774d3af5e060d36beec99a`** |
| 変えない base | 顔・首・胴・翼・あし・**元の 尾羽**——11,468画素 すべて 無変更 |
| e1 bbox | **x72..222 / y25..230（151x206）**・面積 **14,325px²（base 比 +24.9%）** |
| 安全余白 | L61 / **R22** / T14 / B14 ／ わく当たり なし |
| 足した もの | **羽 4枚だけ**（付け根 (148,118)・角度 −46〜+44°）|
| 長さ | **44 / 52 / 50 / 40px ＝ ふぞろい**（そろえない）|
| たれ | 下の 羽ほど 大きい（4 / 7 / 10 / 10px）|
| 毛先 | **base と おなじ ぎざぎざ（scallop）3つ**で 終わる |
| `asym` | **true**（尾羽は 後ろがわ だけ。左右対称に すると 翼に なる）|
| secondary | **なし** ——羽軸・羽枝・spark・dust・halo・粒を 1つも 足さない |
| primitive | **`catmull()` ＋ 自前の オフセットだけ。新規 0** |
| 色 | `#fdfaf0 → #fbefd5 → #eac8ab`・ふち **`#a96e60`**（ぜんぶ base の 実測）|
| 連結成分 | **1つ**（体と ひとつづき）|

##### ① 独立した 部位で ある ことが 出発点

| | |
|---|---|
| 尾羽の bbox | **x182..224 / y119..189**・2,631px² |
| 色の ちがい | 胴 `#e4a673`（明るい tan）↔ 尾羽 **`#faf1d1`〜`#fbefd5`（クリーム）** |
| 内部の 輪郭 | 自分の ふち `#a96e60` を もつ |
| 前後関係 | 翼（tan の ローブ）が **尾羽の 上を 横切る** |
| 追えるか | 上の 房（y88..128・scallop 5つ）と 下の 房（y128..150・3つ）に 分けて 追える |

`c_bee`（顔と 縞が 同じ 一枚の 玉）で 決めた ものさしを、はじめて **合格で** 通った 例です。

##### ② 実シルエット空間 ——`c_horse` と 正反対

`c_horse` は 真下に 65px の 透明が あっても、**36px では そこは あしの 場所**でした。
ダチョウは ちがいます。

| | |
|---|---|
| 幾何学的な 透明余白 | 後方 **61〜78px**（±15° 74 / ±45° 101 / ±60° 122〜142）|
| 自分に 当たる 方向 | **0本**（真上と 左だけ 自分に 当たる）|
| 成長軸上の 別部位 | **なし** ——sprite ぜんたいが x72..183（**112px**）しか なく、あし（x90..142 / y170..230）は ずっと 下・左 |
| **36px の bbox** | はば **16 → 21（+31.3%）** ／ 24px は **10 → 14（+40.0%）** |

##### ③ 元の 輪郭を **羽の かさなり境界**に 読みかえる

base の 尾羽の 外周は **閉じた 1つの キャップでは なく、繰りかえす scallop の 弧**です。
だから うしろに 1層 足すと、元の 弧は **「手前の 羽の ふち」**として そのまま 読めます。

| | 元の 輪郭は どうなるか |
|---|---|
| `c_turtle` / `c_squirrel` / `c_horse` | **閉じた 終端**なので「古い 面の 外縁」として のこる → **二重** |
| `c_redpanda_e1` | 縞が あるので **同色で つないで かげに 落とす**（16pxピッチの 反復）|
| **`c_ostrich_e1`** | **scallop の 反復**が あるので、**そのまま 羽の かさなり境界**に なる |

> **読みかえが 成りたつ 条件**は 3つ です。
>
> 1. 外周が **閉じた 1つの キャップでは なく、繰りかえす 弧・段・境界**で できて いる
> 2. その 反復の ピッチが **小サイズでも 1〜2列 のこる**（ここは scallop 8つ）
> 3. 足す ものが **同じ 素材・同じ 終わりかた**（ここは 同じ クリーム＋同じ ぎざぎざ）

##### ④ 放射させると **かならず 扇か 花に なる**

16案 くらべて いちばん はっきり 出た ことです。**この 3つは くりかえさないこと。**

| やって しまった こと | 128px |
|---|---|
| 等間かくの 細い 羽を 放射（O6 / O7 / O9 / O11）| **花・花びら** |
| 等間かくの 羽を 6枚 放射（O8）| **孔雀の 扇** |
| 長さを そろえて 放射（O3 / O4 / O13 / O15）| **扇** |

**まるい こぶで 終わらせるのも だめ**です（O1 / O5 / O12 → **雲**＝`ch_gumgum_e1`）。

##### ⑤ 通ったのは **ふぞろいの 長さ ＋ かさなり ＋ たれ**の 3つ そろった とき だけ

| | なぜ 要るか |
|---|---|
| **長さを ふぞろいに する** | そろえると 放射で なくても **扇**に 見える |
| **かさねる**（放射させない）| すきまを あけて 出すと **花びら** |
| **すこし 下へ たれさせる** | 重力が 入ると 羽に、入らないと 記号（扇）に なる |
| **毛先は base と 同じ ぎざぎざ** | まるい 先は **雲**、とがった 先は **翼／炎** |

##### 小サイズの 固定実測

| | base | e1 |
|---|---|---|
| **36px 面積** | 239 | **293（+22.6%）** |
| **36px 尾羽側の 面積** | 41 | **88（+114.6%）** |
| 36px bbox はば | 16 | **21** |
| **36px 2run行** | 10 | **12** |
| **24px 面積** | 114 | **140（+22.8%）** |
| **24px 尾羽側の 面積** | 17 | **36（+111.8%）** |
| 24px bbox はば | 10 | **14** |
| 24px 2run行 | 6 | **7** |

**2run行が ふえて います**（`c_horse` は 9 → 7 と へって 落ちました）。

**房の 数は 36px では 数えられません** ——尾羽帯は 1つの かたまりに なります。
読めるのは **「後方の 房が 4列 → 9列に のびた」**ことで、それで じゅうぶんです。
128px では 4枚が はっきり 分かれます。

##### 予約言語との 差（32px IoU）

| base | `c_witch_e1` | `c_snowqueen_e1` | `c_mummy_e1` | `c_tower_e1` | `c_devil_e1` | `ch_gumgum_e1` | `c_firebird_e1` | `c_octopus_e1` | `c_redpanda_e1` | `ch_fairy_e1` |
|---|---|---|---|---|---|---|---|---|---|---|
| **0.817** | 0.455 | 0.454 | 0.423 | 0.419 | 0.413 | **0.394** | **0.390** | **0.383** | 0.373 | 0.344 |

**base とも 0.817 で しきい値 0.86 を 下まわります**（`c_horse` 0.967・`c_bee` 0.976 とは 別物）。

| | ちがい |
|---|---|
| `c_firebird_e1` | **左右 2枚**の 実体翼・**羽軸と 羽枝**・暖色オレンジ・**下外**へ。こちらは **片がわ 1か所・すじ 0本・クリーム・後方** |
| `ch_gumgum_e1` | 上の 角の **まるい 雲**＋下の 雨すじ・**寒色 白青**。こちらは **よこに 長い 房**・暖色・雨なし |
| `c_snowqueen_e1` | **左右対称に たてへ**。こちらは 片がわ・よこ |
| `c_octopus_e1` | **下がわの 太い 曲線が 複数**。こちらは **後方・上下に かさなる 平たい 房** |
| `ch_fairy_e1` | 同心の 輪＋**放射**。こちらは 放射させて いない（放射させた 案は 扇に なって 落とした）|

##### 採用前の 確認

| | |
|---|---|
| **128px** | 「扇」「翼」「花」「雲」に **見えない**。「尾羽が 長い 房に 育って 後方へ ながれた」が 優勢 |
| 36px / 24px | 尾羽側 +115% / +112%、はば +31% / +40%。24px でも **白い かたまりに ならない**（ふち `#a96e60` が 形を たもつ）|
| 白 / クリーム / ミント / ラベンダー | 12とおり すべて 合格。足した ところの 明るさの さは **49.3 / 41.5 / 44.6 / 49.2** |
| **左右反転** | `EN_FACE.ostrich = -1` で うつりますが、向きの ある モチーフ・文字・非対称の かげが 0なので こわれません（128 / 36 / 24px で 確認）|
| base の 画素 | 道具とは 別に 数えなおして 11,468 ぜんぶ 一致・**ちがい 0件（最大差 0）** |
| body shift | 11,468 / 11,468 が **同じ 座標**で 一致 ＝ **0px** |
| 決定性 | 2回 焼いて **SHA-256 が 同じ** |

##### 代案 O16 は **のこして いません**

O16（4枚・みじかめ・かさなり つよめ）は 扇に 寄る危険が さらに 低い かわりに、
36px の 尾羽側が 88 → 77 と **信号が 弱く**なります。O14 が 3つの しばりを
みたした ままで もっと 読めるので、**そちらを 採りました**。

#### `lg_berryqueen_e1`（いちごのじょおう）——**裾の フリルが もう 1段 重なる**

> **2026-09-09 に 正式採用（EVO_LOCK 28枚目）。**`c_ostrich_e1` に つづく
> **「元の 輪郭を 重なり境界に 読みかえる」型の 2件目**です。ただし
> ostrich が **片がわ・後方・長い 房**なのに 対して、こちらは
> **左右対称・下端・水平の フリル段**なので、予約言語としては 独立です。
>
> **装飾を 足したのでは なく、元の 衣装構造そのものが 一段 育った** ——
> これから の「衣装系」進化の 成功パターンに つかえます。

##### 固定した もの（**変えない**）

| | |
|---|---|
| 方式 | **A方式（deco）**・base 1:1・**本体の 画素の ちがい 0**・**body shift 0** |
| SHA-256 | **`674f0ef94e95f97a5a491fb91837ec3c540dfdc70de672c176a1653354071c5d`** |
| 変えない base | **ピンクの スカート面・元の フリル・王冠・顔・髪・しゃく・上半身** ——19,820画素 すべて 無変更 |
| base bbox | x49..207 / y25..230（159x206）・面積 19,820 |
| **e1 bbox** | **x49..207 / y25..240（159x216）**・面積 **21,604（+9.0%）** |
| **よこはば** | **159px → 159px（±0.0%）** |
| 安全余白 | **L38 / R37 / T14 / B4** |
| `asym` | **false**（よこの 中心ずれ 0.000 ／ たて 0.018）|
| secondary | **0** ——金縁・宝石・spark・いちご・粒・花・halo を 1つも 足さない |
| primitive | `<path>` 2 ／ `<circle>` 22 ／ `<line>` 11 ／ `<linearGradient>` 1。**blur 0・新しい ヘルパ 0** |

##### M3 の 構造

| | |
|---|---|
| 外へ 出す ぶん | **中央 約9px・外側 約16px**（下は 14px しか ない）|
| 向き | 法線 0.45 ＋ 真下 0.55（「下へ、すこし 外へ」）|
| 端 | **既存の わきの クリームの うしろへ 約18px tuck** |
| こぶ | base と おなじ **半径 9.0・弧長ピッチ 15.3**（実測）|
| いち | **半ピッチ ずらす** ——手前の 段の 谷から 下の 段の こぶが のぞく |
| 色 | 既存フリルより **約6% こい 同系クリーム**（`#fbf0d2 → #f8e9c6 → #eed7ae → #e2c398`・ふち `#b8484f`・仕切り `#c8767c`）|

**裾の 輪郭は 実測して `BQ_R` に 焼いて あります**（(128,150) から 26°〜154° を
2°きざみ・65点。2°補間の 誤差 0.108px）。**目分量で 書かないこと。**

##### 小サイズの 確定実測

| | base | e1 |
|---|---|---|
| **36px bbox** | 22x29 | **22x30（幅 ±0列）** |
| 36px 面積 | 415 | **448（+8.0%）** |
| **36px 下1/3** | 164 | **197（+20.1%）** |
| **36px 新規最下行** | — | **row 33：0列 → 15列** |
| 36px row 32 | 8列 | **19列** |
| **24px bbox** | 16x20 | **16x21（幅 ±0列）** |
| 24px 面積 | 190 | **208（+9.5%）** |
| **24px 下1/3** | 74 | **92（+24.3%）** |
| **24px 新規最下行** | — | **row 22：0列 → 10列** |

> **24px では「2段」と 数え切れなくて かまいません。**正式な 読みは
> **①裾が 深く なった ②波状の 質量が ふえた ③base との 差が のこる**の 3点です。

##### 重なりの 前後関係は 数値でも 出る

base の 谷 11点で 下へ **+12.2px**、base の 山 11点で **+10.3px**。
**手前の 段が うすい ところで 下の 段が 厚く 出る**＝重なりが 成立して います。

##### 4背景と 反転

| 追加分の 明るさの さ | 白 | クリーム | ミント | ラベンダー |
|---|---|---|---|---|
| 平均 | **43.8** | **36.1** | 31.1 | 38.2 |
| 20%以上の 画素 | 88% | 63% | 79% | 90% |

クリームの 地が いちばん 弱いのは **base 自身も 同じ 性質**です
（`c_redpanda_e1` の クリームの 縞と 同じ 話）。128 / 36 / 24px × 4背景の
12とおりで 目視合格。反転しても こわれません（`berryqueen` は `EN_FACE` に
のせて いないので 盤面で 反転する ことも ない）。

##### 予約言語との 差（32px IoU）

| base | c_snowqueen_e1 | c_purin_e1 | c_hinotama_e1 | lg_chocoknight_e1 | c_witch_e1 | c_ghost_e1 | ch_fairy_e1 | ch_gumgum_e1 | c_ostrich_e1 |
|---|---|---|---|---|---|---|---|---|---|
| **0.897** | 0.776 | 0.739 | 0.736 | 0.715 | 0.689 | **0.574** | **0.518** | **0.496** | **0.400** |

**しきい 0.86 を こえるのは base だけ。**採用ずみの base IoU 帯
（ostrich 0.817 〜 tower 0.936）の まん中で、落とした `c_bee` 0.976 /
`c_horse` 0.967 とは はっきり ちがいます。

| | ちがい |
|---|---|
| `c_ghost_e1` | 半とうめい・**3層**の veil が 下へ 波打つ。こちらは **不とうめい・1段・水平** |
| `c_witch_e1` | **片がわ**の 斜め cape。こちらは **左右対称・後方成分 0** |
| `c_snowqueen_e1` | 左右へ **たてに** 垂れる 長髪。こちらは **下に 1行・左右 ±0** |
| `ch_gumgum_e1` | 雲＋雨・寒色・上の 角。こちらは **下端のみ**・暖色 |
| `c_ostrich_e1` | **片がわ 後方**の 尾羽の 房（幅 +31%）。こちらは **幅 ±0・左右対称** |
| `ch_fairy_e1` | 同心の 輪＋**放射**。こちらは **裾線に そった 帯・放射 0本** |

> **berryqueen の 読みは「左右対称・不とうめい・衣装裾・水平に 重なる フリル段」**。
> ここを 動かさないこと。

##### この 1体で 決まった こと（6つ）

1. **元の 輪郭が 繰りかえす 弧なら、うしろに 同素材の 段を 置く ことで
   「古い 終端」では なく「手前の 段の へり」に 読みかえられる。**
   `c_ostrich_e1` の 3条件（①繰りかえす 弧 ②小サイズで ピッチが のこる
   ③同じ 素材・同じ 終わりかた）が そのまま 効きます
2. **同じ 反復形でも、単純な 円の 列に すると 泡・真珠に なる。**
   union して **内部の 輪郭を 消し**、必要な 仕切りだけ のこすこと
   （全部の 円に ふちを 引いて 1回 やりました）
3. **フリルでは 正弦波より 円弧の union。**正弦の 谷は かならず 尖るので、
   7案 ぜんぶ **ノコギリ（王冠の とんがり）**に 見えました。base の こぶは
   円の union（半径 9.0・ピッチ 15.3 → 谷の ふかさ 4.0px）です
4. **端を その場で 止めると 貼りつけた 板に 見える。**既存の 同系色の 部位の
   うしろへ **tuck して 前後関係を 作る**こと（K1〜K3 で たての 継ぎめが 出ました）
5. **「衣装が 大きく なった」と「装飾段が ふえた」は、よこはば・本体面・
   変更領域を 別々に 測って 区別する。**ベル型の スカートが 大きく なるなら
   **よこにも のびる**はずで、ここは よこ ±0・ピンクの 面 0件変更・
   変わったのは y≥206 の 下 10px だけ です
6. **36px の 2run が へっても、それだけで 失敗に しない。**ここは 3→2 に
   へりますが、**新規行（0→15列）・局所質量（下1/3 +20.1%）・bbox 高さ +1行**が
   はっきり 立って いるので 総合で 採ります（`c_horse` は bbox も 面積も
   動かなかったので 落ちました ——そこが ちがいます）

##### 「斜め下外へ 広げる」は **主経路では なかった**

制作前の 設計で、左右外端から 斜め下外へ 強く 広げる 案（4案）を 出して
**128px で「泡」**に 見えて 落ちました。理由は 幾何です。

> **36px の bbox は すでに よこ 22列 いっぱいで、いちばん 広い 行が
> 裾の 終端そのもの**。よこ／斜め外へ 出しても 幅は 1列も ふえず、
> **いちばん 広い 行が もっと 太る だけ**＝「スカートが 大きく なった」に 直結します。
> いっぽう **下は いちばん下の 行が まるごと 空いて いる**ので、
> 下へ 出すと **新しい 行が 1行 生まれます**。

だから 主経路は **「裾線に そって 下へ、外側ほど すこし ゆたかに」**です。

#### `c_horse` は 設計だけで **不採用**に した（2026-09-08）——A要検証の まま 保留

> **11例目。**しかも 落ちた いちばんの 原因は これまでの どの 則とも ちがい、
> **「透明な あきは あるのに、小さく すると そこは もう 別の 部位の 場所だった」**でした。

仮の 言語は「元からある 尾が、重力方向へ 長く 育つ」。
**`c_horse_e1.png` は 1枚も 作って いません**（10案の 下絵を くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| 全体 bbox | **x31..224 / y25..230（194x206）**・面積 21,402・あき L20 / R20 / T14 / B14 |
| **尾の bbox** | **x182..224 / y119..189**・2,631px² |
| 根もと | 尻の 上（y119〜128）。y120 で `194..207` と **胴とは 別の run** |
| 胴との 境界 | 自分の こい 輪郭（`#6c3320`）＋ そのすぐ内がわの **白い すきま**（y150 の x195 が `#fbfcfa`）|
| 色 | 胴 `#e4a673`（明るい tan）↔ 尾 `#b88373` → `#a3695c`（**はっきり こい 茶**）|
| 太さ | 根 14 → **35px** ／ 中央 **28〜29px** ／ 先 14 → **9px** |
| 36px / 24px 換算 | **約3.9列 / 約2.6列** |

##### ①独立した 部位か → **合格**。②〜⑤も 数字は そろって いた

`c_bee` とは ちがい、**尾は 視覚的に 独立した 部位**でした ——自分の こい輪郭・
別の 茶色・尻との 前後関係・根から 先まで 1本の 帯として 追えます。

**実軸余白も これまでで いちばん よい**（主終端 (211,190) から。**自分に 当たる 方向 0**）。

| 0° | ±15° | ±30° |
|---|---|---|
| **65px** | 67 / 67 | 75 / 75 |

##### ②終端器官 ——**閉じた コンマ形**

8倍・12倍で 拡大して 分かりました。底（y183〜189）は **輪郭が 一周した
まるい キャップ**で、内部の つやも **そこで 完結**して います。
右がわの とがりは 房では なく **毛先の フリック 2つ**（y167..173 と y179..187。
y172 で run が `194..215 | 217..223` に 割れる）。

##### ⑤ ——**幾何の 余白と、小サイズの シルエット空間は 別**

ここが 今回の 新しい ところです。**4つを 分けて 測ること。**

| | `c_horse` の 値 |
|---|---|
| **幾何学的な 透明余白** | **65〜75px**（自分に 当たらない）|
| **実用上の 余白** | **約39px** ——ひづめの 接地線が **y230**。それより 下は 地面に めりこむ |
| **成長軸上に ある 別部位** | **後ろあし**（こい茶・たてに 走る・接地）|
| **その別部位との にかよい** | **方向＝どちらも たて下** ／ **色＝どちらも こい茶** ／ **太さ＝尾28〜29px・あし 22〜30px** |

##### 36px は **1pxも 変わらなかった**

| 36px | bbox | 面積 | **2run行** | 尾側の 質量 |
|---|---|---|---|---|
| base | [4,4,31,32] 28x29 | 436 | **9** | 151 |
| **H4（最良案）** | **[4,4,31,32] 28x29** | 457（+4.8%）| **7** | 172 |
| H9（最大案）| [4,4,32,32] 29x29 | 469（+7.6%）| 7 | 184 |

- **bbox 変化なし**（24px でも 18x20 の まま）
- **2run行が 9 → 7 に へる** ——足した 毛が 尾と 後ろあしの すきまを うめるので、
  シルエットは **かえって のっぺりする**
- **32px IoU vs base ＝ 0.967**（`c_bee` の 0.976 と 同類。しきい値 0.86 を 大きく こえる）

理由は 明快です。**base の bbox は すでに「尾の 右はし x224」と「ひづめの 下 y230」で
決まって いて、尾を 下へ のばしても その わくの 内がわに ぜんぶ 入ります。**

##### ⑤の 補強 ——**「透明な あき」を そのまま 成長先に しない**

> **終端から 実成長軸へ 透明な 余白が あっても、その 方向が
> 小サイズで 既存の 別部位に 占有されて いる 場合、
> A方式の 成長先としては つかえません。**
>
> 判定の ときは つぎの 4つを **分けて** 記録する こと。
>
> 1. **幾何学的な 透明余白**（画素で 測った あき）
> 2. **36px / 24px での 実際の シルエット空間**（bbox と 2run行）
> 3. **成長軸上に 存在する 別部位**
> 4. その 別部位との **色・太さ・方向の 類似**
>
> とくに、足した 形が 既存の 別部位と **同じ 方向・近い 色・近い 太さ**の 場合は、
> **bbox が 変わらなくても「成長した」とは 見なしません。**

`c_pharaoh` の **完全内包**（部位 じたいが 17px 内がわに ある）とは **別の 問題**です
——horse の 尾は **外シルエットに ちゃんと 接して います**。接して いても、
成長軸の 先が すでに 別部位の 場所なら 外形は 変わりません。

##### ⑥ ——`c_redpanda_e1` の 逃げ道が つかえない

`c_redpanda_e1` は **16pxピッチの 縞**が あったので、元キャップの 弧を
「縞の 途中の 境め」として 吸収できました。**`c_horse` の 尾は 内部境界が 0本**です
——明るい すじは **つや**で 輪郭に とどかず、白い 帯だと 思った x193..195 は
**尾の 中では なく 胴と 尾の あいだの すきま**でした。

##### 10案 ——**128px で 通ったのは 0件**

| 案 | 中み | 128px |
|---|---|---|
| H1 | そのまま 下へ | **5本目の あし／ただの 太い 棒** |
| H2 | 下＋後方 | 棒。かたむいた 板 |
| H3 | 先端を 2房に | **あしが 2本 ふえた** |
| H4 | 中腹から 波打つ | 棒（10案で いちばん まし）|
| H5 | 長さ＋房先だけ | **ほうき／はしご** |
| H6 | **同色つなぎ**＋3房 | **ズボンの すそ**。元キャップの 弧が よこ一直線に のこる |
| H7 | 同色・後方へ 細く | **くつを はいた あし** |
| H8 | フリックからだけ 出す | **別の 板が 横に 浮く**（2本目の 尾）|
| H9 | 元の 尾より 大きく 広げる | **スカート**。元の コンマ尾が まるごと 上に のこる |
| H10 | 細い 毛を 5本 | **モップ／フリンジ**（`c_mummy_e1` に 寄る）|

**H6・H7・H9 で 同色つなぎを 4回 試して、4回とも 落ちました。**

##### 分類と 逃がさない もの

`c_horse` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**B確定に しません。**たてがみ・耳・ひづめ・胴へ **自動で 進化言語を
差しかえない**こと。`c_horse_e1.png` は 作りません。

#### `c_mermaid` の 第一仮説は 設計だけで **不採用**に した（2026-09-09）

> **12例目。**未予約の 言語「ひれ」を もつ ので 再順位付けの 1番手に なった
> 個体ですが、**「未予約の 部位で ある」だけでは A有力に ならない**ことが
> 分かりました。

仮の 言語は「元からある 二葉の 尾びれが、同じ 尾びれの まま 後方へ
長く・豊かに 育つ」。**`c_mermaid_e1.png` は 1枚も 作って いません**
（5系統 15案の 下絵で 落としました）。

##### ① 上葉の 成長軸が 髪で 完全に 占有されて いる

上葉の 先（181,174）から 360°を 見ると、**18方向の うち 16方向が 0px**。
その 上には **髪が 71〜126px の 厚み**で かぶさって います。

上向きに 1,052px の deco を 置いて 実測すると、**見えたのは 54px（5.1%）だけ**
——**94.9% が 不可視**でした（bbox x188..191 / y179..195）。
`c_pharaoh` の **完全内包**と 同じ 形です。

##### ② なめらかな 閉じた 膜なので 元輪郭を 読みかえられない

フルークは **縞も 房も 段も 別素材も 持たない、なめらかな 閉じた 膜**です。
だから 4つの 逃げ道が ぜんぶ つかえません ——`c_redpanda_e1`（縞）・
`c_ostrich_e1`（繰りかえす 弧）・`c_tower_e1`（離散した 反復ユニット）・
`lg_chocoknight_e1`（別素材の rim）。5系統 15案は 128px で
**二重ひれ／3枚目の ひれ／棘**に しか なりませんでした。

##### ③ 伸ばせるのは 下葉だけ で、局所面積が 小さい

| | |
|---|---|
| フルーク ぜんたい | スプライトの **4.45%** |
| **伸ばせる 下葉** | **約 2.5%** |
| 32px IoU vs base（K系・B系）| **0.974〜0.977** ＝ 却下帯（`c_bee` 0.976・`c_horse` 0.967 と 同類）|
| 合格帯（0.936）に 届かせるには | フルーク ぜんたいの **+1.5倍**（Q案・IoU 0.938）が 要る |

その 大きさに すると **「巨大な 別の ひれ」**に 見えます。
**小さい 信号では 進化に ならず、足りる 信号に すると 別物に なる**
——両立しません。

##### 通った ところ

⑧ 予約言語との 分離（`c_firebird_e1` 0.553・`c_ostrich_e1` 0.482・
いちばん 近い `c_snowqueen_e1` でも 0.794）・本体の 画素の ちがい 0・
4背景での 見えかたは base と 同じ 性質。**落ちたのは ①②③です。**

##### 分類と 逃がさない もの

`c_mermaid` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**B確定に しません。鱗の 尾・髪・泡・水しぶき・第3の ひれへ
自動で 差しかえない**こと。

##### **「未予約の 部位で ある」だけでは A有力に しない**

> **A方式で 既存部位を 伸ばす ときは、成長軸の 余白だけでは 足りません。
> 元の 閉じた 外輪郭を「途中の 境界」へ 読みかえられる 構造が 要ります。**
>
> なめらかな 閉じた 膜・面は、**縞／反復する 弧／段／別素材**の ような
> 境界化の 手だてが 無い かぎり、A方式では **二重面に なりやすい**です。

#### `c_tutankhamun` の 第一仮説は 設計だけで **不採用**に した（2026-09-09）

> **13例目。**そして **数値が いちばん よかったのに 落ちた**例です。
> 余白・面積・IoU・本体差 ——採用条件の 数値は ぜんぶ 通って いました。

仮の 言語は「ネメスを 外へ 広げて 縞を 増やす」。
**`c_tutankhamun_e1.png` は 1枚も 作って いません**（ジオメトリ 6案・
縞の つなぎかた 6案・うしろ層 2案の 計 14案で 落としました）。

##### 数値は ぜんぶ 良かった

| | |
|---|---|
| ネメスの 可視面積 | **スプライトの 約 22.8%**（`c_mermaid` の フルークの 5倍）|
| 左右の 実軸余白 | **66〜74px** |
| 斜め下外 | **93〜105px** |
| 36px bbox | 14列 → **18列** |
| 36px 面積 | **+11.0%** ／ 上1/3 **+34.8%** |
| **32px IoU vs base** | **0.910**（採用帯 0.817〜0.936 の まん中）|
| 予約言語との IoU | **0.47〜0.64**（`c_cobra_e1` は 0.498）|
| 本体の 画素 ／ body shift | **ちがい 0 ／ 0px** |

##### ① 元輪郭と 縞の 向きが 合わない

ネメスの 外輪郭に そって、**接線と その場の 縞の 向きの なす角**を 測りました。

| 区間 | 角度 |
|---|---|
| クラウン上部 y26-34 | 72〜76° |
| クラウン側面 y46-64 | 44〜57° |
| たれ y66-100 | **76〜90°** |
| **全周平均** | **66.3°** |
| **45°未満（＝境界に 読める）** | **全周の 3% しか ない** |

**元輪郭は ほぼ 全長で 縞と 直交して います。**だから
「増えた 縞の 途中の 境界」には 読みかえられず、外へ 足すと
**かならず 二重頭巾**に なります。縞の つなぎかたを 6通り
（同色／反対色／ピッチ base 一致／ピッチ 1.3倍／全部よこ帯／ななめ下）
ためして 全滅、**反対色は かえって 元輪郭を 強調して 悪化**しました。

##### ② 縞を 消すと `c_cobra` 型

無地の 対照（T4 / S0）は **金の 無地の 盾状フード**に 見えました。

> **「縞あり＝二重頭巾」「縞なし＝盾」で、逃げ道が 両がわから 塞がって います。**

##### ③ 小サイズで 識別記号が 増えない

| 36px | base → +14px |
|---|---|
| bbox | 14列 → **18列** |
| 面積 | **+11.0%** |
| 上1/3 | **+34.8%** |
| **金／青緑の 交番（最大）** | **8 → 8**（+18px でも 9）|

面積は 増えても **「縞が 増えた」とは 読めません。**追加ぶんが
外周の 細い 帯に 押しこまれる ためです。あわせて 36px で
**頭 18列 対 肩 14列**＝頭だけ 29% 幅広に なり、
「頭だけ 異常に 大きい」にも 触れます。

##### 分類と 逃がさない もの

`c_tutankhamun` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**B確定に しません。杖・襟・ウラエウス・外側の 棺・付け髭へ
自動で 差しかえない**こと。

将来 **B方式で ネメスその ものを 描きなおせる**なら、
「たれが ほんとうに 胸まで 伸びる」（いまは 肩が 塞いで いて A では 不可能）を
再検討できます。

### Batch 4 ——**まず 2体だけ**（2026-09-07 に 選定・制作は まだ）

**6体を ならべてから 落とす**のでは なく、**いちばん 通りやすい GEN を
1体 → つぎに LEGEND を 1体**。新しい 事前スクリーニングの ものさし
そのものを たしかめる ための ならびです。

| # | species | 区分 | 進化言語（**仮**）| |
|---|---|---|---|---|
| ~~—~~ | ~~`c_rabbit`~~ | GEN | ~~みみが 上外へ 育つ~~ | **A要検証へ 降格**（②終端器官 ＋ ⑤余白 16px）|
| ~~—~~ | ~~`lg_candytree`~~ | LEGEND | ~~樹冠から 枝が のびる~~ | **A要検証へ 降格**（**枝が base に 無い** ＋ ⑤余白 14/33px）|
| ~~—~~ | ~~`c_meerkat`~~ | GEN | ~~細い 尾が 太く 長く 育つ~~ | **A要検証へ 降格**（**細い limb**・128px で 二重しっぽ）|

> **Batch 4 は 3体 とも 制作前に 落ちました。**候補は いま **0体**です。
> `c_meerkat` の いちばんの リスクは 予想どおり 余白では なく
> **尾の 太さ**でした（下記）。**第二候補を 繰り上げないこと。**

#### 5体の 軸方向の 実余白（2026-09-07 に 再測定）

| 部位 | 実測した 先端 | 主軸 | **0°** | ±15° | ±30° | 帯 |
|---|---|---|---|---|---|---|
| **`c_meerkat` しっぽ** | (191,105) | 85° | **94** | 36\* / 100 | 31\* / 92 | **94px**（帯16px）|
| `lg_candytree` たれるあめ | (62,163) | −90° | 81 | 48\* / 84 | 45\* / 94 | 81px（帯14px）|
| `lg_chocoknight` たての右 | (196,177) | 0° | **48** | 50 / 50 | 56 / 56 | 48px（帯24px）|
| `c_turtle` こうらの上 | (111,57) | 107° | **48** | 54 / 46 | 67 / 47 | —（面の 成長）|
| `c_kangaroo` しっぽ | (42,220) | −166° | 32 | 36 / 31 | 34 / 32 | 30px（帯16px）|
| `lg_candytree` 樹冠の 左 | (44,97) | 180° | **33** | 34 / 34 | 38 / 38 | 33px |
| `c_kangaroo` みみ | (207,25) | 72° | **15** | 14 / 17 | 14 / 21 | 0 |
| `lg_candytree` 樹冠の 上 | (127,25) | 90° | **14** | 15 / 15 | 16 / 16 | 14px |

`*` ＝ base に ぶつかって 止まった（わくでは ない）。
**36px で 2列＝原画 14.2px ／ 24px で 2列＝原画 21.3px。**

#### `lg_candytree` を A有力 → **A要検証**へ 降格（2026-09-07）

**A. むかしの 余白値が 実成長余白では なかった**

| 測りかた | 出る 数字 |
|---|---|
| bbox の 中心から SE / SW | **133px**（幹と たれるあめの あいだの すきま）|
| 樹冠の 上 → 上 | **14px** |
| 樹冠の 左 → 左 | **33px** |
| たれるあめの 先 → 下 | 81px |

**旧分類の 根拠は 撤回します。**

**B. そもそも 既存の 枝が ありません**

拡大で 確かめました ——**樹冠（あめ玉の かたまり）は 幹の 上に 直に のって いて、
枝は 1本も 描かれて いません。**仮言語「既存の 枝が 外へ のびる」は
**のばす べき 枝が base に 無い**ので 成立しません
（**既存枝の 延長型 A では ない**）。

**C. たれるあめへ 逃がさない**

たれるあめには 81px の 余白が ありますが、**先が 閉じた あめ玉**で
**強い 終端器官**です。その先へ のばすと ①元の あめ玉が 途中に のこる
②2本目の あめ・別の かざりに 見える ——`c_scorpion` `c_rabbit` と 同じ 型。

**いま 別の 言語へ 自動で 変更しないこと。**B確定にも しません。
将来「新要素型 A として 木らしい 新しい 枝を 出す」「B方式で 樹冠を
作りなおす」は 再検討できますが、いまは 進めません。

#### 第二候補（**まだ 繰り上げない**）

| species | 候補の 言語 | のこる リスク |
|---|---|---|
| ~~`c_turtle`~~ | ~~甲羅が 段を なして 育つ~~ | **A方式 不採用**（**完成した 閉じた 面**・同心では 15.3px が 上限）|
| `lg_chocoknight` | にぎって いる 盾が 育つ（右 48px）| **円の 輪郭が 閉じ、格子もようが そこで 終わる** ——二重円盤の 危険 |
| `c_kangaroo` | しっぽが 後方へ 育つ（32px）| **細い limb（5 → 31px）**。`c_meerkat` と 同じ 高リスク帯。**自動で 不採用には しない** |

#### `c_meerkat` は 設計だけで **不採用**に した（2026-09-07）——A有力 → A要検証

> **5例目。**しかも 落ちた 原因は これまでの 6則の どれとも ちがう
> **新しい もの**でした。

仮の 言語は「元からある 細い 尾が、同じ 一本の 尾の まま 太く 長く
立ちあがる」。**`c_meerkat_e1.png` は 1枚も 作って いません**
（5案の 下絵を くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| bbox | **x58..197 / y25..230（140x206）**・面積 20,408px²・安全余白 L47 / R47 / T14 / B14 |
| **しっぽ** | **x175..197 / y105..179（23x75）**・面積 **1,146px²** |
| 先端 | **(193.5, 105)**・先の はば **6px** |
| 根 | **y180 で 胴と 1本の run に なる**（y176 で すきま 4px・y108 で 22px）|
| **太さ** | **6px（先）→ 14px（y113〜137）→ 21px（根 y177）** |
| 中心線 | x193.5(y105) → x184.5(y129) → x189(y161) → x186(y177) の ゆるい S字 |
| **主軸** | **約85°**・軸方向の 余白 **94px**（−15° 100px / −30° 92px）|
| 終端器官 | **なし**（なめらかな 先ぼそり。もようも 裏地も 終わらない）|
| 色 | `#f7d8aa → #f2d1a7 → #dfb98a`・ふち `#c59a78` |
| 36px（上62%・あしを のぞく）| 別run **7行** / 2列以上 **7行** / 最大 **3列** |
| **24px**（同）| 別run **5行** / 2列以上 **2行** / 最大 **2列** |

##### 5案の 小サイズ ——**数値は ぜんぶ 通った**

| 案 | 36px 別run / 2列以上 / 最大 | 24px 別run / 2列以上 / 最大 |
|---|---|---|
| base | 7 / 7 / 3 | 5 / **2** / 2 |
| M1 長さだけ | 16 / 13 / 3 | 10 / 7 / 2 |
| M2 太らせて のばす | 15 / 13 / 5 | 9 / 6 / 3 |
| **M3 外へ＋曲率** | **16 / 16 / 5** | **10 / 9 / 3** |
| M4 先に ふさ | 17 / 17 / 5 | 8 / 4 / 3 |
| M5 先から 一定16px | 16 / 16 / 4 | 11 / 7 / 3 |

**24px の 合格条件（2列以上が のこる）を 5案 とも みたして います。**
それでも 落としました。

##### 128px で 5案 とも 意味が 成立しなかった

| 案 | どう 見えたか |
|---|---|
| M1 / M2 / M3 | **二重しっぽ**。太い 新しい 尾の 上に、base の 尾の 輪郭と 先が まるごと のこる |
| M4 / M5 | 付け根に **段**が 見えて、**別の 葉・板を 継いだ**ように なる |

##### **細い limb を A方式で 太く／長く する ときの 注意則**

> **みみ・尾・棒の ような 細長い 部位では、base の 外輪郭 そのものが
> その 部位の identity に なって います。**だから base の うしろへ
> 太い 延長面を 足すと、**元の 輪郭が「内がわの もよう」では なく
> 「別の 細い 部位」として のこる**ことが あります。

**固定の 太さの しきい値には しないこと。**
「20px 未満なら A不可」の ような きまりに しては いけません
——base の 質感・輪郭の こさ・もようの 有無・成長の 向きで 変わります。
位置づけは **「細い limb は 高リスク」**です。制作前に つぎを 見ます。

1. 延長面を 足したとき、**base の 輪郭が 内がわの 折れ・段として 読めるか**
2. それとも **2本目の 細い 部位に 見えるか**
3. 付け根に **段が 出ないか**

##### `c_octopus` との 対照 ——**同じ「既存部位を のばす」でも 結果が 逆**

| | 部位の 太さ | base の 輪郭は |
|---|---|---|
| **`c_octopus`（成立）** | **30〜54px** | 太い 曲線の 中に とけて、**内がわの すじ**に 読めた |
| `c_mummy`（成立・別の 型）| 12〜14px | **のばさず、ほどけて 垂れる**（延長では ない）|
| `c_rabbit`（不成立）| 4 → 42px・自由な 高さ 26px | **2本目の みみ**に 見えた（②終端器官 ＋ ⑤余白16px も かさなる）|
| **`c_meerkat`（不成立）** | **6 → 21px** | **2本目の 尾**に 見えた |

**`c_cobra` が 通ったのは「面」だから**です ——広い 面では base の 輪郭が
**フード内部の 折れ・内縁**として 自然に 読めます（正式に 許容ずみ）。
細い limb では 同じ ことが **別部位**に 読めます。

##### いまは 保留。弱い 代替案へ 逃がさない

**B確定にも しません。**将来 B方式で 尾そのものを 描きなおせる ときに、
「同じ 一本の 尾が ほんとうに 太く 長く なる」を 再検討して ください。
**いまは B制作に 進まないこと。**

| 逃がさない もの | なぜ |
|---|---|
| 尾に ふさ・かざりを 足す | 「尾が 育つ」では なく **別の ものを 継いだ**に なる（M4 で 実測）|
| 立ちあがる ポーズに 変える | base の 画素を 変える ＝ **A では ない** |
| `c_kangaroo` へ 自動で 差しかえる | 尾の 太さが **5 → 31px** で **同じ 高リスク帯**。下記 |

##### `c_kangaroo` は **高リスクの 第二候補**として 保留

尾の 直角方向の 太さを 測ると **5 → 31px** で、`c_meerkat`（6 → 21px）と
**同じ 細い limb の 帯**です。軸方向の 余白も **32px** しか ありません。

**この 時点で 自動的に 不採用には しません。**A要検証の **第二候補**として
のこします ——尾の 根もとが 太い ぶん、`c_meerkat` より 有利な 可能性が
あります。進める ときは **上の 3つの 見どころ**を 先に たしかめること。

#### `c_pharaoh` は 設計だけで **不採用**に した（2026-09-07）——A方式 不採用・A要検証で 保留

> **7例目。**これまでは「何を どう のばすか」を 見て いましたが、
> この 1体で **その 前に「その 部位は そもそも A方式で 外から 見える いちに あるか」**を
> 見る 必要が ある ことが 分かりました。制作前に いちばん 多くの むだを 弾ける 基準です。

仮の 言語は「元からある 金＋青緑の 胸元装飾が、幅広い 多層の 首かざりへ 育つ」。
**意味としては 自然**でしたが、A方式では 成立しません。
**`c_pharaoh_e1.png` は 1枚も 作って いません**（7案の 下絵を くらべて 落としました）。

##### 胸元の かざりは **ほんとうに 別パーツ**だった

まず 画素と 前後関係で たしかめました ——**新要素では ありません。**

| | |
|---|---|
| 頭巾（ネメス）の 上 | y26〜63・**たて じま**（金／青緑）|
| ひたいの 帯 | y66〜75・**べた金** |
| 頭巾の **たれ（lappet）** | 顔の 左右 x51〜77 / x150〜206・**よこ じま** |
| **まん中の 首かざり** | **x100..160 / y126..162（61x37）**・面積 **1,711px²** |
| その 中み | **青緑の 段（y138〜147）＋ 金の 帯（y147〜158）**。y159 から 素はだ |

**別パーツです** ——まん中の かざりと 左右の たれの あいだに **輪郭線が 通って います**
（y150 で x108〜112 と x148〜152）。ただし **段の 高さは 3つとも そろって います**。

##### base の 実測

| | |
|---|---|
| 全体 bbox | **x50..206 / y26..230（157x205）**・面積 23,251 |
| 安全余白 | L39 / R38 / T15 / B14 |
| 頭巾（金＋青緑・y≤125）| x51..205 / y26..125（155x100）|
| 前後関係 | たれは **顔の 前**／首かざりは **たれの うしろ**／腕は 体の 中（y162〜178 は 素はだ）／**crook は 左 x50..74 / y138..178** |
| 胸の 高さの 実余白 | 右 **59〜67px** ／ 左 **39〜57px**（左は crook が つかう）|
| 下方向の 実余白 | y178 で たれが 終わり、**y180 から こし布 ＝ 出せる すき間 0** |
| 36px の 首かざり | よこ **8.6列** × たて **5.2行** |
| 24px の 首かざり | よこ **5.7列** × たて **3.5行** |

##### 決定的な 理由 ——**外シルエットに 接して いない**

> **首かざりから 外シルエットの ふちまで、いちばん 近い ところで 17px。**
> **首かざりは 身体・頭巾の 内がわに 完全に 内包されて います。**

A方式は base を **前へ 1:1 で のこす**ので、追加した 面は
**その 17px ぶんが 完全に かくれます。**外から 見える ほど ひろげた
時点では、見えるのは **左右の たれの さらに 外がわ**だけ で、
元の 胸元装飾との 視覚的な 連続性が 失われます。

##### P1〜P7 ——**素材・段数・かざりかたを 変えても 原因は 消えなかった**

| 案 | 中み | 128px で どう 見えたか |
|---|---|---|
| P1 | 首を 中心に 半径 36→64 の 帯 | **ぜんぶ base の うしろへ 沈み、進化差が 見えない** |
| P2 | 36→78・3段 | **左右 2面＋直線的な 上端**に なり、翼／マント化 |
| P3 | 36→78・内 青緑＋外 金の ふち | **別素材の 境界を つかっても**、出現位置が 左右外なので 翼化 |
| P4 | 36→86・下ふちに 玉 | 玉を 足すと **金の 翼**に 近づく |
| P5 | たれの 外がわに 金の ふち 10px | **細すぎて ほぼ 見えない** |
| P6 | 同 12px ＋ 玉 7つ | **よこ方向の チェーン化**し、crook とも 干渉 |
| P7 | 青緑＋金の 二重の ふち 7px | 二重縁でも **可視差 不足** |

##### **シルエット内部に 完全に 内包された かざりを A方式で 育てる ときの 注意則**

> **A方式では base が 前面に 1:1 で のこる ため、成長対象が 外シルエットに
> 接して いない 場合、追加部分は 外縁まで 到達するまで **完全に かくれます**。
> 外へ 出る ほど ひろげた 時点では、元の 部位との 視覚的な 連続性が 失われ、
> 別の 翼・ふち・板・チェーンに 見える 危険が 高い です。**

##### **「内包された かざりは A不可」とは しないこと**

正式な 位置づけは **「完全に 内包された かざりは A高リスク」**です。
判断の ときは つぎを たしかめます。

1. 対象の 部位から **外シルエットまでの 距離**
2. 外へ 出るまでに **必要な 拡張量**
3. 外へ 出た ところが **元の 部位と 連続して 読めるか**
4. **base 前景による 遮蔽**
5. **128px で 別部位化しないか**

`c_pharaoh` は **最短 17px の 完全内包 ＋ 外へ 出ると 左右が 別面化**で 不成立です。

##### 成功 23体との 対照 ——**主役は かならず 外形に 接して いた**

| | 育てる 部位から 外シルエットまで |
|---|---|
| `c_octopus_e1` | **0px**（触手が 外形 そのもの）|
| `c_cobra_e1` | **0px**（フードが 外形 そのもの）|
| `lg_chocoknight_e1` | **0px**（盾が 外がわの 局所物体）|
| `c_witch_e1` | **0px**（cape という 新要素が 背後の 外形へ 直に 出る）|
| **`c_pharaoh`** | **17px（身体の 内部に 閉じこめられて いる）** |

##### 別素材の 成功則も **万能では ない**

`lg_chocoknight_e1` は **別素材で 元輪郭を 素材境界へ 読みかえて** 成立しました。
けれど `c_pharaoh` の P3 は **金＋青緑という 別素材を つかっても** 落ちました。

> 理由は **素材境界 以前**です ——追加した 面が 元の かざりの まわりに 見えず、
> **外シルエットまで 飛び出した ところだけ**が 見える ため。

**「別素材なら 閉じた 面や 内包された 部位でも A成立」とは しないこと。**

##### crook へ 逃がさない

crook は にぎりが はっきり して いますが、**今回は 進化の 主役に しません。**
`c_pharaoh` は ここで 保留です。**頭巾の 下ばし・額帯・lappet への
自動の 差しかえも しないこと。**別の 言語を 見る ときは、将来 あらためて
独立した 候補設計として あつかいます。

#### `lg_moonrabbit` の 第一仮説は 設計だけで **不採用**に した（2026-09-07）

> **8例目。**キャラ ぜんたいは **A要検証の まま 保留**で、
> **第一仮説だけ**を 落とします。B確定にも しません。

仮の 言語は「元からある うすに、金の 補強たが／ふちが つく」。
**`lg_moonrabbit_e1.png` は 1枚も 作って いません**（8案の 下絵を くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| 全体 bbox | **x43..212 / y26..230（170x205）**・面積 21,564 |
| 安全余白 | L32 / R32 / T15 / **B14** |
| **うす** | **x43..134 / y175..230（92x56）**・面積 **2,485px²**（胴は およそ x43..118）|
| **うすの 楕円あてはめ** | **中心 (79, 193)・rx 37 / ry 38**（92点・rms 0.0659）|
| 上縁 | y175〜190。**見えて いるのは 左はしだけ**（y176 で x54・y180 で x45）|
| 側面 | y184〜215。**左はしが x43 ＝ sprite の 左はし** |
| 底（高台）| y216〜230・x52..102。**y230 が sprite の 下はし** |
| きね（木の 色）| x60..189 / y130..219 |
| 前後関係 | **うすが 兎の 前**／**きねが うすの 前**／**前足が きねの 前** |
| うすの 左の 実余白 | **32〜44px** |
| **うすの 下の 実余白** | **14px** |
| **36px の うす** | **14列 × 9行**（112画素）|
| **24px の うす** | **10列 × 6行**（56画素）|

##### ⑥使用関係は **はっきり 合格**

前足が きねを にぎり、きねが うすに 入り、もちが 中に あります。
**今回 落ちたのは 使用関係では ありません。**

##### M1〜M8 ——**128px で 通ったのは 0件**

| 案 | 中み | 128px で どう 見えたか |
|---|---|---|
| M1 | 上縁だけ 金 10px | **ほぼ 見えない** |
| M2 | 左＋下の U字 11px | **金の 板が はみ出す** |
| M3 | 金＋クリームの 二層 | 同じく **板** |
| M4 | 側面の 金帯 15px | **平たい 台**に 見える |
| M5 | 同心 +10px | **背後の 金の 皿** |
| M6 | 同心 +14px | **コイン／第二の 月** |
| M7 | 金＋クリームの 外周 | **背後の 輪** |
| M8 | 下半分だけ +14px | **金の 三日月** |

##### 落ちた 理由は **露出量では ない**

疑った「見えて いる 割合」は、**むしろ 盾より 大きい**のです。

| | 全周の うち 見える |
|---|---|
| `lg_chocoknight` の 盾（t=13）| **56.9%** |
| `lg_moonrabbit` の うす（t=13）| **66.1%**（うち **48.1%** が うすに 接する）|

> **本来 補強したい 場所が「うすの 外輪郭」では なく「うすの 面の 上」**でした。
> 金の たがは 陶器の 側面に **よこ方向へ 巻かれる 帯**です。
> A方式では base が 前面に 1:1 で のこる ため、**面の 上に 描くべき 帯は
> 完全に かくれます。**外へ はみ出した ぶんだけが 見えるので、
> 金の 皿・輪・三日月・背後の 板に なりました。

### **rim と face decoration を 分ける**

> **rim / edge reinforcement** ——元の 物体の **外輪郭 そのものの 外がわ**へ
> 足す 補強。**A方式でも 成立の 余地が あります。**
> 成功例：**`lg_chocoknight_e1`**
>
> **band / decoration on a face** ——元の 物体の **表面の 上**に 描かれる
> 帯・たが・もよう・襟。**A方式では base の 前景に かくれやすく、高リスク。**
> 失敗例：**`lg_moonrabbit` の うすの たが**／**`c_pharaoh` の 胸元装飾**

#### **「面の 上の 帯は A不可」とは 記録しないこと**

正式には **「base の 面上に 本来 描くべき かざりは A高リスク」**です。
判断は つぎで 行います。

1. かざりの 予定いちが **base の 表面の 内がわ**か
2. その いちが **外シルエットに 接して いるか**
3. base の うしろへ 置いた とき **何% かくれるか**
4. 外へ 出た ぶんだけでも **元の 物体との 関係が 読めるか**
5. **128px で 別の 輪・皿・翼に 変わらないか**

#### `lg_chocoknight` の 成功則は「別素材なら 成立」では ない

| | |
|---|---|
| **`lg_chocoknight_e1`** | 追加素材を **盾の 外輪郭の さらに 外がわ**へ 置いた → 元輪郭を「ビスケット面と チョコ rim の 素材境界」に 読みかえられた |
| **`lg_moonrabbit`** | 金の たがを **うすの 側面の 上**へ 置きたい → その 場所は base に おおわれ、A方式では 描けない |

> **成功したのは「別素材だから」では なく、
> 「別素材を、元の 物体の 外輪郭に 接する rim として 自然に 足せた」から**です。

##### 分類と 逃がさない もの

`lg_moonrabbit` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**耳・尾・きね・月・もちへ 自動で 進化言語を 変更しないこと。**

#### `c_king` の 第一仮説は 設計だけで **不採用**に した（2026-09-07）

> **9例目。**キャラ ぜんたいは **A要検証の まま 保留**で、
> **第一仮説だけ**を 落とします。B確定にも しません。

仮の 言語は「元からある 王冠の 外周に、別素材の 補強 rim／宝石帯が つき、
より 高位の 王冠に なる」。**`c_king_e1.png` は 1枚も 作って いません**
（13案の 下絵を くらべて 落としました）。

##### base の 実測 ——**白い 毛皮は 王冠の 一部では ない**

| | |
|---|---|
| 全体 bbox | **x53..203 / y26..230（151x205）**・面積 22,116・安全余白 L42 / R41 / T15 / B14 |
| **王冠ぜんたい** | **x60..181 / y26..89（122x64）**・面積 **5,541px²** |
| 金の 部分 | x62..179 / y28..88（118x61）・**3,124px²** |
| こい 宝石＋内がわの かげ | x66..175 / y37..76（110x40）・238px² |
| 形 | **とがり 5つ**、先に 金の 玉。下に **金の 帯**（y70〜88）|
| 色 | 金 **`#fde7ae`**／外の とがりは **`#fbccb9` の 桃がね**／宝石 `#847d84`（最こい `#565154`）／輪郭 **`#b7988a`** |
| **白い 毛皮** | **マントの えり（y128〜160）と すそ**。**王冠には 1画素も ない** |
| 前後関係 | 王冠が **頭の 前**（y88 から 下が 髪）。ひげは さらに 下 |
| 外シルエットに なる ところ | **y26〜88 の 上・左・右 ぜんぶ** |

##### 実軸余白（王冠の 外輪郭の 各点から）

| | |
|---|---|
| てっぺんの 玉（y26）→ 上 | **15px** |
| 左の とがりの 先（y44〜52・x60）→ 左 | **49px** |
| 右の とがりの 先（y44〜52・x181）→ 右 | **63px** |
| 帯（y76〜88）→ 左 / 右 | 57〜60px / 71〜74px |

##### 13案 ——**128px で 通ったのは 0件**

| 案 | 中み | 128px |
|---|---|---|
| K1〜K5 | 法線オフセット系（金／毛皮／宝石／二重／非等方）| **とがりが つぶれて 枠・看板化** |
| K6 / K7 | **とがりに そった 膨張**（+10 / +14px）| **二重王冠**（K7 は 上ばし y12 で 安全枠 ぎりぎり）|
| K8 / K10 | 下の 帯を よこへ +13px（＋宝石）| **平たい スラブ** |
| K9 | K8 ＋ 毛皮の こぶ | **粒状化**（ふちに 粒が 散る）|
| K11〜K13 | 左右へ 強く +22 / +34px | **帽子の ひさし／板** |

##### 数値は 出た。それでも 不採用

| | 36px 画素/列/行 | 24px | Δ36 | Δ24 |
|---|---|---|---|---|
| base の 王冠 | **144 / 18 / 11** | **77 / 13 / 8** | — | — |
| K6 | 204 / 20 / 12 | 100 / 14 / 9 | +42% | +30% |
| K8 | 175 / 20 / 11 | 90 / 14 / 8 | +22% | +17% |
| K13 | 176 / 26 / 11 | 93 / 18 / 8 | +22% | +21% |

**1列の 細線にも ならず、頭本体とも 融合して いません。**
それでも **「同じ 王冠が 格上げされた」と 読める 案は 0件**でした。

上ばしの ふれはば（36px）は base **20** → K8 **15** → K13 **8** と 平らに なります。

### **外周の でこぼこが identity の 物体に rim を 足す ときの 注意則**

> **外周の 凹凸・切れこみ・とがりが identity の 主要部分で ある 物体は、
> rim を 足すと その 特徴を うめたり 二重化しやすく、A方式では 高リスクです。**

補足：

> **外周を 一周できない／途中が 本体に かくれる 形は、
> 自然な rim として 閉じにくい です。**

#### **「なめらかで 一周できない 物体は ぜんぶ 不可」とは しないこと**

固定の きまりに しては いけません。可否は 1体ずつ、
**元の 輪郭を 進化後に どう 読みなおせるか**で 判断します。

#### `lg_chocoknight` との 対照

| | |
|---|---|
| **`lg_chocoknight_e1`** | **なめらかな 閉じた 楕円の 外周** → 外がわの rim が **自然に 一周できる** → 元輪郭を **素材境界**へ 読みかえられた |
| **`c_king`** | **とがりと 谷の 連続が identity** → rim で 谷を うめるか、同じ 形を 外に 作ると **二重王冠**に なる |

##### `lg_berryqueen` へ 自動移植しない

`c_king` が 成立しなかったので、**王冠 rim 言語は そもそも 予約されて いません**。
しかも berryqueen の 王冠は **45〜73px と さらに 小さく**、左右の あきが
**80〜95px** ある ため **rim の 比率が 相対的に 大きく なります**。
**別に 検証が いります。**

##### 分類と 逃がさない もの

`c_king` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**マント・ひげ・しゃく・毛皮へ 自動で 進化言語を 変更しないこと。**

#### `c_purinala` の 第一仮説は 設計だけで **不採用**に した（2026-09-07）

> **10例目。**`c_king` と おなじ rim型の 失敗ですが、**原因は 正反対**です
> ——king は 外周が でこぼこ だから、purinala は **外周が なめらかなのに
> rim が もう 完成して いる**から。

仮の 言語は「元からある 皿の 外周に rim／縁どりが 加わって 高級な 皿に なる」。
**`c_purinala_e1.png` は 1枚も 作って いません**（5案の 下絵を くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| 全体 bbox | **x25..230 / y48..207（206x160）** |
| 皿の 外輪郭 | 楕円 **中心 (128,162)・rx102 / ry45**・最小二乗の **rms 0.0198**（229点）——盾の 0.0315 より **なめらか** |
| **口縁** | **すでに ある。**y180 で **外線 x34 → 白い 帯 x36..59 → 内側の 線 x62**。rim の はば **約26px** |
| 底／高台 | **無い**（y207 で 楕円が そのまま 終わる）|
| 皿が シルエットに なる ところ | **y144..207** |
| **左右の 実余白** | **14px**（y162）——**18px では ない**（あれは y150 の 値）|
| 下の 実余白 | **37px** |
| 前後関係 | プリン・果物・アイス・クリームは **ぜんぶ 皿の 前** |
| 36px | 30列 × 23行（415px²）／ 24px | 20列 × 16行（193px²）|

##### 5案 ——**128px で 通ったのは 0件**

| 案 | 中み | 面積 | 128px |
|---|---|---|---|
| P1 | 同素材の 細rim 8px | +15.5% | **二重皿**（同心線が **3本**）|
| P2 | カラメル色 rim 10px | +19.6% | **木の トレー／別の 皿** |
| P3 | クリーム＋カラメル 二層 12px | +24.1% | 同心線が **4本**。別の 金縁の 皿 |
| P4 | よこだけ 強め（6→13px）| +21.0% | よこ長に なっても **二重皿** |
| P5 | 底がわだけ（6→22px）| +25.5% | **台座／背後の 板** |

##### 数値は 出て いた

36px の 面積 **415 → 475〜514**、下1/3 の 行 **7 → 8〜10行**。
`lg_chocoknight_e1`（右横の 円盤・36px で 22x30）との 言語の 分かれも
**問題 なし**でした（purinala は **32x24 の よこ長**で 下1/3 が 122 → 168）。

> **落ちたのは 小サイズでは ありません。**`c_scorpion` `c_deer` `c_turtle` と
> おなじ ——**「小サイズの さが 出る こと」と「進化として 自然に 読める こと」は
> 別の 問題**です。

### **すでに rim を もつ 完成物体に、さらに rim を 足さない**

> **すでに 口縁・段・帯などの rim構造を もつ 完成物体は、その 外がわへ
> さらに rim を 足すと、元の rim との あいだに 同心線が ふえ、
> 二重物体・別トレー・別わく として 読まれやすい です。A方式では 高リスク。**

**「rim を もつ 物体は A不可」とは しないこと。**
**元の rim を 新しい 素材境界・段差・構造の 一部として 自然に
読みかえられる ときだけ、成立の 余地が あります。**

##### `lg_chocoknight_e1` との 対照

| | |
|---|---|
| **`lg_chocoknight_e1`（成立）** | 元の 外輪郭は **1本**。外がわに チョコ rim を 足した → 元輪郭を **ビスケット／チョコの 素材境界**へ 読みかえられた |
| **`c_purinala`（不成立）** | 元から **外線＋口縁の 内線**を もつ → そこへ さらに 外周を 足すと 線が 1本 ふえて **二重皿／別トレー**に 見える |

##### `c_king` との ちがい ——**なめらかで 一周できれば rim 成功、では ない**

| | |
|---|---|
| `c_king` | **外周の とがり・谷が identity** → rim で 谷を うめるか、**二重王冠**に なる |
| `c_purinala` | **外周は なめらかで 一周できる** → それでも **既存の rim が 完成ずみ**なので 二重化する |

> **この 2例から言えること ——「なめらかで 一周できれば rim は 成功する」
> わけでは ありません。**外周の 形（king）と、rim が すでに あるか
> （purinala）は **別の 条件**で、どちらか 片方でも 致命です。

##### 分類と 逃がさない もの

`c_purinala` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**果物・プリン本体・クリーム・皿の 台座へ 自動で 進化言語を 変更しないこと。**

### 「太い 既存部位の 自然な 延長」で 133体を 再スクリーニング（2026-09-07）

rim型が `c_king`（とがりが identity）と `c_purinala`（rim が 完成ずみ）で
つづけて 落ちた ので、rim を さらに 掘るのを やめて、
**`c_snowqueen_e1` で 通った 成功系統**を 横に ひろげました。

| 段 | のこり |
|---|---|
| のこり ぜんぶ | **133** |
| 太い 延長できる 部位を もつ（髪・たてがみ・太い尾・長毛・鼻・ひれ・羽・布・触手・太い腕）| 約40 |
| **予約24言語で 部位型ごと 落ちる** ——羽／ひれ（`c_firebird`）・布／マント（`c_witch`）・触手（`c_octopus`）・もこもこ（`ch_gumgum`）・放射たてがみ（`c_lion`）| 約20 |
| 細い limb（`c_meerkat` 則）・すでに rim（`c_purinala` 則）・強い 終端（`c_scorpion` 則）| **10** |

| # | species | 成長対象 | 部位の 太さ | 実軸余白（**当時の 見つもり**）| 24言語との 衝突 |
|---|---|---|---|---|---|
| 1 | `c_squirrel` | 太い ふさ尾 | 45px（のち 66〜76px と 判明）| 上 44〜53px ← **誤り。下記** | 無し |
| 2 | `lg_mammoth` | 長毛の 層 | 毛の 層 ぜんたい | 下 20〜29px | 無し |
| 3 | `c_mermaid` | 長髪 | 髪の 束 | 左下 70 / 右下 52px | **`c_snowqueen_e1` と 同言語** |
| 4 | `c_queen` | 長髪 | 髪の 束 | L48 / R48（のこりで 最大）| **同上** |
| 5 | `c_redpanda` | 縞の 太い尾 | 53〜57px | 下 16〜32px | `ch_prince_e1`（同形の くりかえし）|
| 6 | `c_princess` | 長髪 | 髪の 束 | L37 / R37 | **同上** |
| 7 | `c_wolf` | ふさ尾 | 37〜60px | 右 14px | 無し（**クリーム色の 尾先＝強い 終端**）|
| 8 | `lg_elephant` | 太い 鼻 | 約40px | 右 14px | 無し（**巻いた 鼻先＝強い 終端**）|
| 9 | `c_kangaroo` | 尾 | 5→31px | 32px | `c_squirrel` と 同言語 |
| 10 | `c_koala` | ふわふわの 耳 | 約45px | 右下 71 / 左下 51 | **耳が すでに rim 構造**（`c_purinala` 型）|

> **成功系統は 横に ひろがりますが、条件の いちばん よい 4体
> （`c_queen` `c_mermaid` `c_princess` `c_yukionna`）は ぜんぶ 髪で、
> `c_snowqueen_e1`（予約 #24）と 同じ 言語に なります。**
> 余白・太さ・終端・再解釈の 4つで いちばん よいのに、**⑥で 落ちます。**

#### `c_squirrel` は 設計だけで **不採用**に した（2026-09-07）——A要検証の まま 保留

仮の 言語は「元からある 太い ふさ尾が、上へ 立ちあがりながら、同じ 尾の まま
さらに 大きく 豊かに 育つ」。**`c_squirrel_e1.png` は 1枚も 作って いません**
（9案の 下絵を くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| 全体 bbox | **x38..217 / y26..230（180x205）**・面積 25,634px²・あき L27 / R27 / T15 / B14 |
| 尾が 外シルエットに なる 行 | **y55..y222**（右がわ ぜんぶ）|
| 尾の 外輪郭 | (146,63)→(170,**55**)→(194,60)→(**217**,95..100)→(211,118)→(205,124)→(196,129)|
| **尾の 太さ** | 上の ふくらみ **66〜76px** ／ 下の 柱 **44〜53px** |
| 房の 層 | 内がわの 面が **クリーム**（`#fcedcf`）、外が オレンジ（`#fab888`）、輪郭 `#cd7a5b` |
| 胴との 前後 | 尾は **胴の うしろ** |
| 負の 空間 | 頭と 尾の あいだ（y58..66・y66 で 1px）／フックの ポケット（y130..150）。**とじた 穴は 0画素** |
| 36px | 面積 532・2run行 5・尾runの 厚み 7 ／ 24px | 面積 244・2run行 4・厚み 3 |

##### **尾先は 強い 終端器官**（3つの 検査 ぜんぶ ⭕️）

尾の 先は **時計まわりに 内へ 巻いた フック**で 終わって います。

| 検査 | 結果 |
|---|---|
| 内がわの もようが そこで 終わるか | **終わる。**クリームの 面が (195,107) の うずで 閉じる |
| 色帯が そこで 閉じるか | **閉じる。**同じ うず |
| 強い 輪郭閉鎖が あるか | **ある。**輪郭が 巻きこんで **y130 の x184..185 で 2px まで つまむ**（y129 は x176..196 が ひとつづき、y130 で 割れる）|

##### **終端から の 実軸余白は ±30° ぜんぶ 0px**

先端は **(189,130)**、接線は 下〜下左（うずの つづき）。

| 方向 | 実余白 |
|---|---|
| 接線（159°）| **0px**（4px 先で 尾じしんに ぶつかる）|
| ±15°（144° / 174°）| **0 / 0px** |
| ±30°（129° / 189°）| **0 / 0px** |

先端は **尾じしんの くぼみの 中**に あります。

##### 9案 ——**128px で 通ったのは 0件**

| 案 | 中み | 128px |
|---|---|---|
| S1 | 長さだけ 上へ | **二重尾**（上に のった 2つめの 塊）|
| S2 | 上へ＋うしろ | **背後の 大きな 板** |
| S3 | 同心に 厚み | **二重面**（`c_turtle` と 同じ）|
| S4 / S4b / S4c | 外の 房を 1枚（波・半分・テーパー）| **背後の 板・別の 塊**。base の 輪郭が きれいな 三日月で のこる |
| S5 | 2つの 大きな 房を 上へ | **角・耳** |
| S7 | 3枚の 独立した 層 | **もこもこ**（`ch_gumgum` に 寄る）|
| S6 | フックの 先から のばす | **もう1本の 尾**。元の うずが 途中に のこる |

##### 数値は 出て いた ——11例目

| | 面積 | 2run行 | 尾runの 最大厚み | 上1/3の 占有 |
|---|---|---|---|---|
| base（36px）| 532 | 5 | 7 | 118 |
| S4b | 595（+11.8%）| 4 | **16** | **166（+40.7%）** |
| S3 | 641（+20.5%）| 6 | **18** | **194（+64.4%）** |
| base（24px）| 244 | 4 | 3 | 56 |
| S4b | 272（+11.5%）| 4 | **9** | 78（+39.3%）|

**24px でも 1列線に ならず、7〜11列の 太い 房が 複数行 のこって います。**
小サイズの 合格条件は 満たして いました。**それでも 128px で 落ちます** ——
**「小サイズの さが 強い こと」と「進化として 意味が 成立する こと」は 別**の
11例目です。

##### 分類と 逃がさない もの

`c_squirrel` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**B確定に しません。**S1〜S7 の 別案へ 進まず、**別の 部位へ 自動で
差しかえない**こと。

#### `c_turtle` は 設計だけで **不採用**に した（2026-09-07）——A方式 不採用・A要検証で 保留

> **6例目。**`c_meerkat` と **ちょうど 逆向き**の 例です ——meerkat は
> **細い limb だから** 元輪郭を 内縁に できず、turtle は
> **広い 面なのに、完成した 閉じた 輪郭だから** 内縁に できませんでした。
>
> **A方式の 適性は「細い／広い」では 決まりません。**
> **元の 輪郭を 進化後に どう 読みなおせるか**で 決まります。

仮の 言語は「甲羅が 段を なして 育つ」。**`c_turtle_e1.png` は 1枚も
作って いません**（12案の 下絵を くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| 全体 bbox | **x26..230 / y51..204（205x154）**・面積 23,471px²・fill .743 |
| 安全余白 | L15 / R14 / T40 / B40 |
| **甲羅（cream の 連結成分）** | **x26..185 / y57..179（160x123）**・面積 **7,084px²** |
| 甲羅の 頂点 | **(112, 57)**（x106..118 が y57）|
| 甲羅の 左はし | **x26**（y134..140）|
| ドームの 芯 | 中心 **(95,120)**・半径 **56〜72px**（ほぼ 円）|
| 輪郭の 色 | `#d9ba8e`（tan）／面 `#fdf5ec → #f0d4b6` |
| 内部の 緑の もよう | **x38..147 / y62..149**（輪郭の **1px 内がわ**まで 来て いる）|

##### 前後関係と 外縁の 閉じかた

| | |
|---|---|
| **頭は 甲羅の 前** | y88〜124 で 甲羅の 右がわを おおう |
| 脚 4本は 甲羅の 前 | y150〜204 |
| 尾 | **描かれて いない** |
| 甲羅の 右 rim | 頭の うしろに **x195..207 / y138..155** だけ のぞく |

外縁は **左〜上が 完全に 閉じて いて、右で 頭に ぶつかって 終わります。**

| y | 甲羅の 右はし | 頭の 左はし | すき間 |
|---|---|---|---|
| 58 | 127 | 158 | 30px |
| 62 | 141 | 153 | 11px |
| 64 | 145 | 151 | **5px** |
| 66 | — | — | **接触（以後 融合）** |

終端は **(147, 66)**。

##### 軸方向の 実余白 ——**上 48px は 正しい。ただし 全周では 15.3px**

ドーム中心 (95,120) からの 放射で 測りました（安全わく `x11..244`）。

| 向き | base の 先 | 余白 |
|---|---|---|
| **上（−90°）** | (95,59) | **47.8px** |
| 上（−80°・頂点） | (106,57) | **46.5px** |
| 上うしろ（−125°） | (62,74) | 76.5px |
| うしろ（−180°） | (35,120) | 23.8px |
| **うしろ下（−195°）** | **(26,139)** | **15.3px** ←最小 |
| 前（−50°） | (143,63) | 67.5（**すぐ 頭**）|

> **むかしの「上方向 48px」は 正しい 値でした**（実測 46.5〜48.5px）。
> けれど **全周（同心）に ふくらませる ときの 上限は 15.3px**です。
>
> **特定の 向きの 余白が 大きくても、同心拡張では いちばん せまい 向きが
> しばりに なります。**⑤の 軸方向測定の、もう1つの 実例です。

##### 12案 ——T1〜T3 に しばらず 4系統

| | 中み | 128px で どう 見えたか |
|---|---|---|
| T1 | 上へ +26px | **かごの とって** |
| T2 | 上＋ななめ後方 +30px | かごの とって（後ろ寄り）|
| T3 | 同心 +14px | **ハロー・皿** |
| T4 | 非対称の 成長輪（上26 / 後13）| 背後の 大きな 皿 |
| T5 | へりを 波に | **やぶれた シート** |
| T6 | 両はし 0 へ tapered・上30 | **ボンネット（帽子）** |
| T7 | もっと 広く・後方寄り 34 | 背後の 板 |
| T8 | せまく 高く 36 | **フード** |
| T9 | うすく 18 | **二重の 輪郭線** |
| T10 | へりの scute の 輪（9区切り）| **ホタテ貝・扇** |
| T11 | 上後だけ 6区切り・28 | 髪かざり |
| T12 | 外ふちを 弱く・区切りを こく | ホタテ貝 |

36px でも 同じ ——足した 帯は「大きく なった 甲羅」では なく
**甲羅の 上に 浮いた 細い 弧**に 見えます。

##### 数値は 全案 合格。それでも 不採用

| | 面積 | 36px 上部質量 | 24px 上部質量 |
|---|---|---|---|
| T9 | **+7.9%** | **+21.6%** | **+20.2%** |
| T6 | **+13.3%** | **+34.7%** | **+34.5%** |
| T7 | **+21.2%** | **+58.0%** | **+54.8%** |

採用ずみで いちばん ひかえめな `c_lavasnail_e1`（+7.8%）を 全案が 上まわり、
24px の のこりも じゅうぶんです。**`c_scorpion` `c_deer` と 同じ 形**で、
**数値では 1件も 落ちません。**

##### **完成感の 強い 閉じた 面を A方式で 同心拡張する ときの 注意則**

> **閉じた 外輪郭を もち、その 輪郭に そって ハイライト・内部の もよう・
> rim などが 終了して いる 面は、base の うしろへ 同心的に 面を 足すと、
> 元の 輪郭が 内縁では なく **「元の 面の 外縁」**として のこりやすい です。**
>
> その 結果、**一枚の 面が 成長したのでは なく、二重の 面が 重なった**ように
> 見える 危険が あります。

`c_turtle` の 甲羅には「面が ここで 終わる」しるしが **4つ**そろって います。

1. 外輪郭が **閉じて いる**
2. 白い **ハイライトが 外輪郭に そう**
3. 緑の **内部の もようが 輪郭で 終わる**（1px 内がわまで）
4. 下がわに **独立した rim の 帯**が ある（y128〜160）

##### **「閉じた 面は A不可」とは しないこと**

固定の きまりに しては いけません。可否は つぎで 判断します。

| | |
|---|---|
| ① | 外輪郭の **閉じかた** |
| ② | **ハイライトの 終端** |
| ③ | **内部の もようの 終端** |
| ④ | **rim の 有無** |
| ⑤ | **拡張の 向き** |
| ⑥ | base の 輪郭が **内縁として 読めるか** |

**閉じた 面その ものでは なく、完成感の 強さと 拡張の 向きが 問題**です。

##### `c_cobra_e1` との 対照 ——**「面なら A で 成立する」わけでは ない**

| | `c_cobra_e1`（成立）| `c_turtle`（不成立）|
|---|---|---|
| 元の 面 | **開いた 面**（輪郭が 本体へ 走りこんで 終わる）| **完成した 閉じた 面** |
| つや・もよう | 輪郭で 終わって いない | **つやも もようも 輪郭で 終わる** |
| 拡張の 向き | **横へ 強い 一方向**（156 → 234px・**+50%**）| **同心（全周）** |
| 元輪郭の 読み | フード内部の **折れ・内縁** | **元の 甲羅の 外縁の まま** |
| 結果 | 一枚の 盾に 見えた | **二重甲羅** |

##### 同心拡張と 一方向拡張を 分ける

> **同心拡張では、元の 輪郭と 新しい 輪郭が「似た 形で 平行に」のこるので、
> 二重面化が 起きやすい です。**一方向拡張なら、元の 輪郭は 新しい 面の
> 流れに そった **折れ**に 読めます。

##### 予約言語との 重複は **不採用の 理由では ありません**

| | ちがい |
|---|---|
| `c_salamander_e1` | あちらは **稜線・突起**。こちらは なめらかな 面 |
| `c_cobra_e1` | あちらは **横長の 盾・開いた 面**。こちらは **同心・閉じた 面** |
| `c_lavasnail_e1` | あちらは **テーパーした 一巻きの 帯**。こちらは 面 |

区別は できて います。落ちたのは **base との 意味的な 連続性が
成立しなかった**ことです。

##### いまは 保留。弱い 代替案へ 逃がさない

**B確定にも しません。**将来 B方式で 甲羅そのものを 描きなおせる ときに、
「同じ 甲羅が ほんとうに 大きく 育つ」を 再検討して ください。
**いまは B制作に 進まないこと。**

| 逃がさない もの | なぜ |
|---|---|
| 甲羅へ 棘 | **`c_salamander_e1` の 稜線と 衝突** |
| 背後に 光の 輪 | **`ch_fairy_e1` の 同心の 輪と 衝突** |
| 単純な 大型化 | **「進化 ≠ ただの 大型化」**（`c_deer` と 同じ）|
| 波打つ へり | 12案で **ホタテ貝・やぶれた シート**に なった |

#### `c_rabbit` は 設計だけで **不採用**に した（2026-09-07）——A有力 → A要検証

仮の 言語は「元からある 細長い みみが、上外へ さらに 長く 大きく 育つ」。
**`c_rabbit_e1.png` は 1枚も 作って いません**（7案の 下絵を くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| bbox | **x67..188 / y25..230（122x206）**・面積 17,326px²・fill .689 |
| 安全余白（bbox から）| L56 / R56 / **T14** / B14 |
| 左のみみ | **x67..114 / y25..73**・先 **(81.5, 25)**・根 **(103.5, 73)** |
| 右のみみ | x141..188 / y25..73（x=127.5 に ついて ほぼ 対称）|
| 頭と つながる いち | **y76**（y25〜y73 は 別の run）|
| **みみの 軸** | **115°**・長さ **52.8px** |
| 太さ | 先 4px → **最大 42px**（y52〜58）→ 根 38px |
| 内がわの 裏地 | y34〜y70 の こいピンク（**先で 終わる**）|
| 36px | 上ばし y3・みみは 6行・左右 **4〜5列ずつ** |
| 24px | 上ばし y2・みみは 4行・左右 **3列ずつ** |
| 色 | `#fcd7e0` → `#f8b4c6` → `#e79ab6`・ふち `#d8879b` |

##### **NE/NW 110px は まちがいでした**

| 測りかた | 出る 数字 |
|---|---|
| bbox の 中心から NE / NW | **110px**（みみの わきの 空き）|
| **みみの 先から 軸に そって** | **16px** |

##### 7案 とも 落ちた

| 案 | どう 見えたか |
|---|---|
| R1 軸の まま 上へ +11px | いちばん まし。だが 7倍で **二重みみ**が はっきり |
| R4 外へ ゆるく | **4本の みみ／角**。しかも わく はみ出し |
| R5 外へ そりかえる | **リボン／角**。はみ出し |
| R6 / R7 包む（はば 58 / 66）| **頭の 上の 大きな リボン** |
| R3 太らせる だけ | **リボン**。進化に 見えない |

**外へ ふる ほど「2本目の みみ・角」に、太く する ほど「リボン」に**なります。
軸の まま しか 逃げ道が なく、そこには 16px しか ありません。

##### 小サイズの 差が これまでで いちばん 弱い

| | 24px の 最大厚み | base との 差 |
|---|---|---|
| `c_witch_e1`（W5b・採用）| 6列 | 29px |
| `c_witch` W5（「控えめすぎる」と 判断）| 5列 | 22px |
| **`c_rabbit` R1** | **1列** | **6px** |

**採用ずみの いちばん 弱い ものの さらに 4〜5分の1**です。

##### 二重の 理由で 撤回

**②終端器官**（みみの 先が 輪郭で 閉じ、内耳の 裏地が そこで 終わる）と
**⑤余白不足**（軸に そって 16px）——**どちらか 片方でも 致命**でした。

##### いまは 保留。弱い 代替案へ 逃がさない

**B確定にも しません。**将来 B方式で みみそのものを 描きなおせる ときに、
「同じ みみが ほんとうに 長く なる」を 再検討して ください。

| 逃がさない もの | なぜ |
|---|---|
| みみを ただ 太らせる／横へ 開く | **リボン・角に なる**（実測）|
| 頭上に かざりを 足す | **`c_purin_e1` の 頭上トッピングと 衝突** |
| しっぽへ 自動で 変更 | **`c_meerkat` と 言語が かぶる** |

#### 第二候補（**A要検証**・まだ 制作枠に 入れない）

| species | 候補の 言語 | のこる リスク |
|---|---|---|
| ~~`c_meerkat`~~ | ~~しっぽが 育つ~~ | **A方式 不採用**（2026-09-07・細い limb）|
| ~~`c_turtle`~~ | ~~甲羅が 段を なして 育つ~~ | **A方式 不採用**（2026-09-07・完成した 閉じた 面）|
| `lg_chocoknight` | にぎって いる 盾が 育つ | にぎりは 確認ずみ。**円盤・ハローに 見えないか** |
| `c_kangaroo` | しっぽ＋あしが 後方へ 育つ | **`c_meerkat` と 言語が 近い**。同時に 採らない |

### LEGEND の e1 は いまの 仕様では **干渉しません**（2026-09-07 に 確認）

Batch 3 で LEGEND を 外したのは「aura・silhouette・flyer と 干渉するので」
でしたが、調べたら **干渉しません**でした。

| | evo を つかうか |
|---|---|
| 図鑑の 詳細画面 | **つかう**（7-7-3-6）——ここだけ e1 が 出る |
| `duBake()`（たいけつの 金の オーラ）| **つかわない**（`drawPortrait(…, L.id, 192)` に evo を わたして いない）|
| `gdFly`（まもり神が よこぎる）| **つかわない**（`gdPortrait(pid)` の 128px キャッシュ）|
| `flyer.sil`（予告の 黒い影）| **スプライトを 使わず** 放射グラデを 描くだけ |

> **将来 この 3つを evo-aware に 変える ときは、もう一度 検証する こと。**

### `c_sheep` は 設計だけで **不採用**に した（2026-09-08）——A要検証の まま 保留

`c_ostrich_e1` / `lg_berryqueen_e1` の「反復外周を 重なり境界へ 読みかえる」型を
ねらった 個体ですが、**tuck 先が ない**ことで 落ちました。
仮の 言語は「体の 羊毛の 房が 同じ 波形の まま 外へ もう1段 重なる」。
**`c_sheep_e1.png` は 1枚も 作って いません**（5系統の 下絵で 落としました）。

#### **ピッチの 数値は 撤回します**（2026-09-09）

はじめ「体側の 羊毛の scallop ピッチ **9.4〜11.5px（36pxで 1.3〜1.6列）**」と 書きましたが、
**まちがいでした。**

| | |
|---|---|
| 撤回する 値 | 9.4〜11.5px（36pxで 1.3〜1.6列）|
| **正しい 値** | **約 40.8px（36pxで 5.7列）** |
| 原因 | **曲線の 画素階段**を 反復として 拾った |

輪郭を 色で 区間確定してから 法線偏差で 測りなおし、4倍拡大の 目視と
底面プロファイル（脚の 2つの 台地の ほかは なめらか）でも 確かめました。
**耳を 羊毛と まちがえた のと 同じ たぐいの 誤りです。**

> **ただし A不成立という 結論は 変わりません。**あれは
> ①全周の 閉ループ ②tuck 先が ない ③S1（負例）が 予想どおり 二重殻に なった
> ④36px の 可読な こぶが ふえない、で 決まって いて、**ピッチは 根拠に 入って いません。**

#### base の 区分けは 色・内部輪郭・前後関係で 先に 確定する

胴の 中心 (128,150) から 角度で 切ると こうです。

| | |
|---|---|
| 342°〜48° | 右の 体の 羊毛（半径 67〜82）|
| 54°〜120° | あしと すそ |
| 126°〜198° | 左の 体の 羊毛（半径 67〜80）|
| 204°〜216° / 324°〜336° | みみ（半径 103〜108）|
| 222°〜240° / 294°〜312° | つの |
| 246°〜288° | 頭の 上の 羊毛（半径 117〜124）|

**bbox の 四すみを 作って いるのは 羊毛では ありません** ——左右は **みみ**（x32 / x223）、
上は 頭の 上の 羊毛（y26）、下は **あし**（y230）。
つの・みみ・あしは **ぜんぶ 羊毛の 前**に あります。

> **重心からの radial tip で 部位を 決めては いけない、の 実例です。**
> いちばん 外へ 出て いるのは みみ なので、自動検出は **みみを 羊毛と まちがえます。**

#### 5系統 ——**S1 を いちばん 先に「負例」として 作る**

| | 中み | 128px |
|---|---|---|
| **S1** | **全周に もう1段**（負例の 対照）| **予想どおり 二重殻** |
| S2 | 下半分に ずらして 1段 | あしが うまる |
| S3 | 左右の 側面に 1段 | 横に 板を 貼った |
| S4 | 既存の 大きな 房だけ 1つ ふやす | 5倍で 見ると **元の 閉じた scallop 線が 中に のこる** |
| S5 | S3 を 粗く | 同じ |

36px の 可読な こぶ：base 6/6 → **S4 5/5・S3 4/6・S1 5/7**。
**ふえたのは S2（7/7）だけ**で、それは あしを こわして います。

#### 落ちた 理由 ——**tuck 先が ない**

羊毛の 輪郭は **体の 輪郭 そのもの**で、ぐるりと 閉じて います。
その ループを 切って いるのは つの・みみ・あし だけ で、**どれも 羊毛の 前**に あります。
だから うしろに 1段 足すと、**元の 閉じた 線が まるごと 中に のこって 二重殻**に なります
（`c_turtle` の「完成した 閉じた 面」と 同じ 型）。

`c_ostrich_e1` / `lg_berryqueen_e1` が 成立したのは、**弧の 端が 別の 部位の うしろへ
消えて いた**からです。羊には その 端が ありません。

#### 分類と 逃がさない もの

`c_sheep` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**B確定に しません。**つの・みみ・しっぽ・草・光へ **自動で 差しかえない**こと。

### 「反復外周を 重なり境界へ 読みかえる」型で 128体を 再スクリーニング（2026-09-09）

`c_sheep` が 落ちた あと、**この型の 候補が まだ いるか**を のこり 128体で 見なおしました。
**新しい e1 画像は 0枚**です。

やりかたは **色で 部位区間を 先に 確定する**こと ——外輪郭を たどり、各点の
**内がわ 5/7/9/11px の 中央値の 色**を Lab で クラスタして、同じ色の 区間（run）に 分けます。
そのうえで run ごとに こぶの 数・ピッチ・**band 深さ**（内向きに 色が 変わるまでの px。
70px 以上なら「層では なく 本体そのもの」）・実成長軸の 余白を 測ります。

既知の 7件で 先に 較正して、ぜんぶ 正しく 再現する ことを 確かめました。

| | こぶ | ピッチ | band | |
|---|---|---|---|---|
| `lg_berryqueen` 裾フリル | 10 | **17.5px** | **12px / 99%** | 合格（実測 15.3px と よく 合う）|
| `c_ostrich` 尾羽 | 3 / 2 | 30.0 / 20.7 | 26/92%・13/95% | 合格 |
| `c_sheep` 羊毛 | 4 | 40.8 | **26px / 76%** | 不合格 |
| `c_turtle` 甲羅 | 1以下 | — | 33〜66px | 不合格 |
| `c_mermaid` フルーク | 2 | 37 | — | 不合格 |
| `c_tutankhamun` ネメス | 2 | 81 | — | 不合格 |
| `c_redpanda` 尾 | 2 | 50 | 11/100% | 不合格（別の 方式で 成立した子）|

#### 条件「36pxで 2列」は **この roster では だれも 落とさない**

こぶが 3つ以上の 反復弧は **92 run** ありましたが、
**ピッチ 14px（＝36pxで 2列）未満は 0件**でした。
**「`c_sheep` 型＝ピッチ 2列未満」を 早期除外の 条件に つかっても、何も 除外できません。**
効くのは **「全周の 閉ループ／tuck 先が ない」**の ほうです。

#### 上位8体

| | 部位 | こぶ | ピッチ | band | 判定 |
|---|---|---|---|---|---|
| `c_swan` | 折りたたみ翼の 羽の段 | 4 | 36.0 | 19/82% | **A要検証（第一候補）** |
| `c_king` | マントの 白い 毛皮の ふち | 8 | 20.9 | **9/99%** | A要検証（**#28 と 同じ言語**）|
| `c_eagle` | 翼の 風切羽の 段 | 4 | 39.0 | 24/64% | 除外（#16 直撃）|
| `c_tart` | 上面の いちごの 環 | 4 | 38.8 | 14/100% | 除外（#26 ＋ 余白 14〜16px）|
| `lg_whale` | 扇の 羽の 段 | 3 | 26.1 | 8/100% | 除外（#10 放射・扇が すでに 支配的）|
| `lg_kraken` | 足もとの 泡 | 5 / 7 | 31.4 / 23.7 | 12/96% | 除外（#19・#6）|
| `lg_phoenix` | 翼の 後縁 | 7 | 16.1 | 11/96% | 除外（#16・#17・#12 が 全部 乗って いる）|
| `c_taffy` | ねじれ飴の 稜 | 3 | 31.9 | 11/100% | 除外（#14・閉じた 本体・band は **縞の 誤検出**）|

**A有力は 0体**でした。

`c_king` は 構造が いちばん きれいですが、32px IoU が
`c_snowqueen_e1` **0.804** / `lg_berryqueen_e1` **0.745** と 採用帯の 上限 0.86 に せまり、
**言語も #28 と 同じ**なので 上げません。

#### `c_zebra` は 除外 ——#25 との 衝突は よけられない

こぶが 3つ以上の run は 尾の 房 1つだけ で、**band 70px / 16% ＝ 層では ありません**。
縞は `c_bee` と 同型の **本体 一枚面の 上の 表面模様**（内部輪郭で 部位が 切れて いない）、
たてがみは みじかい 稜、尾先は **閉じた こい房＝強い 終端器官**（`c_wolf` / `c_squirrel` 型）。
言語も 尾＝**#25 `c_redpanda_e1`** と 正面衝突、たてがみ＝#12 `c_salamander_e1`。

#### この型は **ほぼ 枯れて います**

反復する 外周弧を もつ 部位は 実質 **6カテゴリ**しか なく、**5つが 予約ずみ**でした。

| 部位の カテゴリ | 予約先 |
|---|---|
| 翼・ひれ | **#16 `c_firebird_e1`** |
| 放射する 扇 | **#10 `ch_fairy_e1`** |
| 衣装の 裾・毛皮の ふち | **#28 `lg_berryqueen_e1`** |
| 段・積み重ね | **#26 `c_tower_e1`** |
| 後方の 房 | **#27 `c_ostrich_e1`** |
| 全身の もこもこ | **`c_sheep` 型で 不成立** |

**のこって いる 自由な 言語（みみ・首・きば・甲羅・枝角）には、
そもそも 反復外周弧が ありません。**

あと 2つ 記録して おきます。

- **band 深さが いちばん 効く 一次指標。**「本体そのもの」で 落ちるのが 最多でした
  （`c_cactus` 70/42%・`c_hedgehog` 70/37%・`c_fennec` 70/21%・`ch_kagero` 70/19%・
  `c_shark` 70/41%・`c_whale` 70/48%・`c_kangaroo` 70/44%・`lg_moonrabbit` 70/49%・`c_zebra` 70/16%）
- **band は 縞・格子を 層と まちがえます。**`c_taffy` の band 11px/100% は 層では なく
  **虹色の 縞 1本の はば**でした。**目視は 省けません。**

### `c_swan` の 第一仮説は 設計だけで **不採用**に した（2026-09-09）

仮の 言語は「折りたたんだ 翼の 羽の 段が 1枚 ふえる」。
**`c_swan_e1.png` は 1枚も 作って いません**（12案の 下絵で 落としました）。

#### base の 実測

| | |
|---|---|
| bbox | x29..226 / y25..230（198x206）・不透明 24,890px |
| 余白 | L18 / R18 / T14 / B14 |
| 離散ユニット | **4〜5枚**（＋うしろの 大きな 翼塊）。先は (106,92) (78,100) (63,104) (49,94) (36,118) (31,152) |
| 各羽の 輪郭 | 1枚ずつ **自前の 閉じた 輪郭**（`rgb(176,122,140)`）を もつ ＝ 部位として 独立 |
| 羽の はば（y108）| **16 / 8 / 18 / 26 px** |
| 羽どうしの すきま | **2 / 4 / 9 px** |
| **分離帯の たかさ** | **y94..118 の 24px ＝ スプライトの 11.6% だけ** |
| 根もと | ぜんぶ y116 で 翼塊に 合流（tuck ずみ）|
| 36px の 羽先 run | row12:2 / **row13:4 / row14:4** / row15:3 / row16:2（**6行だけ**）|
| 24px | row8:2 / **row9:4** / row10:2（**3行だけ**）|
| 空いて いる ところ | ①左の くさび **x11..41（31px）**・y96..116 ②中の すきま **x125..147（23px）** ③羽列より 上（y<92。**列方向では ない**）|

#### 12案 ——**意味を 守ると 小さすぎ、差を 出すと 翼が 開く**

全12案とも A方式（base 1:1・装飾は 背面）で、**本体の 画素差 0 / body shift 0px**。

| 案 | 中み | 面積 | 32px IoU vs base | 36px の こわれかた | 128px |
|---|---|---|---|---|---|
| W1 | 外端に 1枚 | +1.3% | **0.988** | 変化なし | 迷子の 羽 |
| W6 | 扇の中に 1枚（細）| +1.4% | **0.983** | 4run 2→**3** ○ | **白い 棘** |
| W10 | 扇の中に 1枚（太短・base幅）| +1.4% | **0.988** | 4run 2→**3** ○ | **平たく 削げた 羽** |
| W2 | 外端に 2枚 | +1.6% | 0.985 | 変化なし | 迷子の 羽×2 |
| W7 | 扇の中に 2枚 | +3.1% | 0.971 | 変化なし | 棘×2・左の 余白 2px |
| W12 | 扇の中＋中の すきま | +3.0% | 0.968 | 2run 5→**4** | くさびを 差した |
| W11 | 中の すきまに 1枚 | +1.6% | 0.980 | 2run 5→**4** | すきまを うめた |
| W8 | 両はしに 1枚ずつ | +4.5% | 0.950 | 4run 2→**0** | うしろに 立つ **羽かざり** |
| W9 | 二重 scallop | +4.2% | 0.959 | 2run 5→**4** | **のこぎり・とさか** |
| W3 | 段を 1列 深く | +6.2% | 0.945 | 2run 5→**3** | **翼が 高くなった／2枚目の 翼** |
| **W4** | **後方へ 長く（負例）** | +6.4% | 0.939 | 変化なし | **尾羽**（＝#27）|
| **W5** | **上下へ 開く（負例）** | **+21.5%** | **0.821** | 2run 5→**12** | **翼が 開いた**（＝#16）|

**12案で 数値が いちばん よいのは W5**（IoU 0.821・2run 5→12）で、
それが **まさに 禁止した「翼が 開いた」**です。

> **数値が よくなる 向きが、そのまま 失敗の 向きでした。**

#### 落ちた 理由は 幾何

扇の 分離帯は **24px ＝ スプライトの 11.6%** しか なく、列方向の 空きは
**31px** で 安全枠に 接して います。見えて いる 羽は はば 14〜26px・
可視の たかさ 約20px の 小さな かたまりなので、**1枚 ふやしても 面積は +1.4〜1.6%**。
採用ずみで いちばん ひかえめな `c_tower_e1` **+6.7%** /
`c_lavasnail_e1` **+7.8%** にも 届きません。

| | 反復区間が スプライトに 占める わりあい | 1ユニット ふやした ときの 効果 |
|---|---|---|
| `c_tower_e1` | 積み重ねが **キャラ そのもの** | +6.7% |
| `lg_berryqueen_e1` | 裾が **下辺 ぜんたい** | +9.0% |
| `c_ostrich_e1` | 尾羽が 局所 **+115%** | +24.9% |
| **`c_swan`** | 扇は **11.6% の 局所ディテール** | **+1.4%** |

背景コントラスト（W10・追加分の 明るさの さ）は 白 55.8 / クリーム 49.0 /
ミント 48.0 / ラベンダー 48.7 で じゅうぶん、左右反転（`swan:1`）も こわれません。
**それでも 不採用です** ——36px / 24px で base と 見分けが つかない（IoU 0.988）ため。

#### 分類と 逃がさない もの

`c_swan` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**B確定に しません。**首・くちばし・尾・水しぶきへ **自動で 差しかえない**こと。

「羽の うしろに 光の 羽根紋」「翼の ふちに 羽毛かざり」などは
**第一仮説の 救済では なく 別案**です。やる ときは 予約28言語との 衝突を
はじめから 見なおす 別フェーズに します。

### `lg_mammoth` の 第一仮説は 設計だけで **不採用**に した（2026-09-10）

> **14例目。**しかも **左右一対の 部位**で 落ちた はじめての 例です
> ——片がわが `c_pharaoh` の 完全内包 だった ため、**一対で 成りたつ
> 進化言語**そのものが こわれました。

仮の 言語は「左右一対の 牙が、同じ 曲率の まま 大きく 巻き上がる」。
**`lg_mammoth_e1.png` は 1枚も 作って いません**（5系統の 下絵で 落としました）。

#### 部位は 色・内部輪郭・前後関係で 先に 確定する

牙は **象牙色**（R−B ＝ 0〜+9）、体は どこも 青寄り（R−B ＝ −20〜−50）。
牙は **自前の 青灰の 輪郭線**（最暗 `rgb(123,160,192)`）を もつので、
`c_bee` の ものさしでは **視覚的に 独立した 部位**です。
象牙の コアを たねに して、輪郭線と 透明で せき止めた flood fill で 2本に 分けました。

#### base の 実測

| | |
|---|---|
| sprite 全体 | bbox **x25..230 / y31..224（206x194）**・不透明 27,356px・あき L14 / R14 / T20 / B20 |
| **左（遠側）牙** | bbox **x26..76 / y131..180（51x50）**・1,317px |
| 形 | ほぼ 完全な **円環の 弧**。中心 **(50.7, 150.0)**・背骨半径 **20.2px**（角度ごとの 実測 18.0〜22.3 と とても 一定）|
| 太さ | 平均 **13.3px**（最大 20.3・両はしで 0.5 まで 先ぼそり）|
| **角度スパン** | **273°**（−30° の 根元 → 0 → 90 → 180 → 240° の 先端）。空いて いるのは **87° の 口だけ** |
| 根元 | (68, 140) あたり。y141 で 自分の 輪郭線に ふたを され、その上 y137〜140 は 不透明の 体色 ＝ **体の うしろへ 潜って いる** |
| 実際の 先端 | **(38, 131)**・幅 3px。真上 y127〜130 は **alpha 0**（＝外シルエット上）|
| 先端の 向き | 接線 dx/dy ＝ −0.470 ＝ ワールド **−65°**（右ななめ上）|
| 局所曲率 | 上るほど **右へ** 強く 曲がる（中心線 x：y150 で 31.5 → y140 で 34 → y131 で 38）|
| 36px 換算 | 牙ぜんたい 7.2 x 7.0 セル／太さ 1.9 セル／**先端は 0.1〜0.9 セル** |
| 24px 換算 | 4.8 x 4.5 セル／太さ 1.25 セル／先端 0.6 セル未満 |
| **右（近側）牙** | bbox **x84..132 / y140..187（49x48）**・1,540px |
| **外シルエット接触** | **0画素**・最短距離 **9.2px**（(85,169) → (76,171)）＝ **完全内包** |

#### 落ちた 理由は 4つ（どれか 1つでも 致命）

**① 左右一対の うち 近側の 牙が 完全内包**

右の 牙は 外シルエットに **1画素も 接して いません**（最短 9.2px）。
A方式は base を 前へ 1:1 で のこすので、**背面の deco では その 牙の
成長差分を 見せられません**。左の 牙だけを のばすと、
「顔を かこむ **一対**の 牙が 育った」には ぜったいに 読めません。

> **一対で 成りたつ 進化言語は、片がわしか 見せられない 時点で 破綻します。**

**② 遠側の 牙の 先端が 強い 終端器官**

- 輪郭線が 先端で **閉じて いる**（y131 に 自前の インク、その上は alpha 0）
- **象牙の コアも 先端の 手前で 閉じる**（輪郭は y131 まで 届くのに 象牙は y133 で 終わる）
- 強く **先ぼそる**（太さ 0.8 → 4.3 → 6.5 → 8.0px を 15° で 通過）

うしろから 継ぐと **古い 先端が 途中の くびれとして のこり、二重牙**に なります
（`c_scorpion` `c_rabbit` `c_squirrel` と 同型）。

**③ 自然曲率方向に 実成長空間が ない**

先端 (38,131) からの 扇（0° ＝ 自然接線 −65°）。**bbox 中心からは 測って いません。**

| off | 実余白 | | off | 実余白 |
|---|---|---|---|---|
| −60° | 47.0px（枠）| | −5° | 27.0px（体）|
| −45° | 79.5px（枠）| | **0°** | **24.5px（体）** |
| −30° | 120.0px（枠）| | +5° | 24.5px（体）|
| −20° | 120.0px（枠）| | +10° | 23.5px（体）|
| −15° | 121.5px（枠）| | +15° | 22.0px（体）|
| | | | +20° | 20.5px（体）|
| | | | +30° | 20.0px（体）|
| | | | +45° | 0.5px（体）|

**自然接線で 24.5px、自然曲率のがわ（+10〜+30）は 20.0〜23.5px しか ありません。**
120px 空いて いるのは −15〜−30（ワールド −80〜−95° ＝ ほぼ 真上〜上左）で、
**既存の 曲率と 逆向き**です。**曲率を 不自然に 変えて 回避しないこと。**

**④ 既存の 牙が すでに 約273° 巻いて いる**

のこりの 口は **約87°**。同じ 曲率で のばすと **頭・鼻のがわへ もどります**。

r=20.2 の まま 240°→330° を たどると、−117°〜−75°（45°分）は 透明ですが、
その 自由な 45° は **y128〜137 の ほぼ 水平な 帯**で、**既存 bbox の 内がわ**。
つまり 外形を ふやすのでは なく、**牙先と 頭の あいだの 切り欠きを 埋める**だけ です。

半径を 外へ 広げる（螺旋）版も 自由な 角度は **24〜42°**しか ありません
（+8px → 42° で (57,122) に 衝突／+14px → 36°／+20px → 33°／+28px → 24°／+36px → 27°）。

#### M1〜M5 ——128px で 通ったのは 0件

5案 とも A方式（base 1:1・**本体の 画素の ちがい 0**・body shift **0px**）。

| | 可視追加 | 36px 面積 | 36px bbox | **36px 2run** | 32px IoU(base) | 128px の 読み |
|---|---|---|---|---|---|---|
| base | — | 573 | 30x28 | 8 | — | — |
| **M1** 短い延長（同半径 +35°）| 117px(+0.4%) | 576(+0.5%) | 30x28 | **8 → 8** | **0.996** | 輪／釣り針。**base と ほとんど 同じ** |
| **M2** 曲率保持の 中程度の 巻き(+60°) | 225px(+0.8%) | 577(+0.7%) | 30x28 | **8 → 7** | **0.991** | ほぼ 完全な **リング／ドーナツ** |
| **M3** 内向きカール強め（負例）| 262px(+1.0%) | 579(+1.0%) | 30x28 | **8 → 6** | 0.991 | **うず**。#14 `c_lavasnail_e1` と 正面衝突 |
| **M4** 外向きに 大きく 開く（負例）| 822px(+3.0%) | 593(+3.5%) | 30x28 | 8 → 18 | 0.959 | **角／棒**。異物・しかも 曲率と 逆向き |
| **M5** 長さだけ 増やす（負例）| 256px(+0.9%) | 577(+0.7%) | 30x28 | 8 → 8 | 0.991 | **別の 巨大牙／くちばし** |

24px：base 262px / 20x18 / 2run 4 ／ M1 263・2run 3 ／ M2 264・2run 3 ／
M3 264・2run 3 ／ M4 273(+4.2%)・2run 9 ／ M5 265・2run 4。

**M1〜M3 は 36px の 2run が ふえず、M2 / M3 では 減ります。**
`c_swan` `c_horse` と 同じ「外形を 広げずに 負の 空間を 埋める」失敗です。

#### #14 との 分離 以前の 問題だった

M3 は ねらいどおり「うず」に 見えて #14 と 分けられません。
ところが **うずに 見えない M1 / M2 は 32px IoU が 0.991〜0.996** で、
不採用帯（`c_bee` 0.976 ／ `c_horse` 0.967 ／ `c_swan` 0.983〜0.988）よりも
さらに base 寄りです。

> **うずに 見えない案は、そもそも 進化に 見えません。**
> 予約言語（`c_lavasnail_e1` 0.715 ／ `c_devil_e1` 0.751 ／ `c_cobra_e1` 0.705 ／
> `ch_fairy_e1` 0.647 ／ `tw_ice_e1` 0.775）とは 離れて いますが、
> **base と 離れて いない**ので 意味が ありません。

#### 分類と 逃がさない もの

`lg_mammoth` は **A要検証の まま 保留**。**第一仮説だけ**を 不採用に します。
**B確定に しません。長毛・鼻・耳・氷・雪・オーラ・地面装飾へ
自動で 差しかえない**こと。

> 長毛は 30-7 の 実測で **band 70px / 36% ＝ `c_bee` 型の 表面模様**で、
> 部位を 区切る 内部輪郭を 持たない ことが すでに 分かって います。

### 制作前スクリーニングに 使った ものさし

#### 一次フィルタ「あき 30px 以上」——**絶対の きまりに しない**

のこりを しぼる ときに「いちばん 広い 安全余白が 30px 以上」を つかい、
**59体**（GEN 54 / CHARS 1 / LEGEND 4 / TOWER 0）に なりました。

> **「30px 未満 ＝ 自動的に A不可」とは しないこと。**
> 30px は **量産の 候補を しぼる ための 一次フィルタ**です。
> ほんとうの 可否は **成長の 向き／元の 部位の 大きさ／
> 36px・24px での のこりかた／意味的な 連続性**で 決めます。

#### いま 優先順位を 下げた もの ——**「永久に A不可」では ありません**

いま **強い A言語が 思いつかない**か、**既存の 言語と ぶつかる**ので
順番を うしろに した、という だけ です。**禁止リストに しないこと。**

| species | 理由 |
|---|---|
| `c_churro` `c_eclair` `c_daifuku` `c_youkan` `c_roll` `c_pretzel` | のばす 部位が 無い（fill .80〜.92 の のっぺりした 一体）＝単なる 大型化に なる |
| `c_seahorse` `c_soft` | うず ＝ `c_lavasnail_e1` と 衝突 |
| `c_parfait` `c_bigparfait` | 頭上の トッピング ＝ `c_purin_e1` と 衝突 |
| `c_dango` | 同形の くりかえし ＝ `ch_prince_e1` と 衝突 |
| `c_tutankhamun` | 頭まわりの 対称な 一枚盾 ＝ `c_cobra_e1` と 衝突 |
| `c_wizard` `c_prince` | マント ＝ `c_witch_e1` と 衝突 |
| `lg_pegasus` `c_butterfly` `c_manta` | 左右 2枚の 翼・ひれ ＝ `c_firebird_e1` と 衝突 |
| `lg_whale`（kujaku）| 放射状の 扇 ＝ `ch_fairy_e1` に 近い ＋ 左右 14px |
| `lg_kyubi` `lg_kraken` | ⑤余白不足 |
| `tw_candy` `tw_choco` `tw_star` | ⑤余白不足（上の 表）|
| `c_giraffe` | **首は「つなぎ」**。長く すると 頭が 動く ＝ A では 不可（`c_crab` の 関節と 同じ ④）|

#### まだ 割りあてて いない 言語

| 言語 | 持って いそうな base |
|---|---|
| **みみが 長く 育つ** | `c_rabbit`（Batch 4）・`c_fennec` `c_koala` `lg_elephant` |
| **首が 長く のびる** | `c_giraffe` `c_ostrich` `c_seahorse` ——ただし ④に 当たりやすい |
| **髪が 長く 育つ** | `c_queen` `c_princess`（`c_snowqueen` は #24 で 採用ずみ・`lg_berryqueen` は #28 で 別の 軸を つかった）|
| **しっぽが 育つ** | `c_meerkat` `c_squirrel` `c_kangaroo` |
| **きばが のびる** | `lg_mammoth` `lg_elephant` |
| **甲羅・殻が 育つ** | `c_turtle` `c_armadillo` `c_hermit` |
| **段が つみあがる** | `c_cake` `c_wedding` `c_mille` |
| **ひれが 育つ** | `c_shark` `c_orca` `c_clownfish` |
| **枝・角が 育つ** | `lg_candytree`（Batch 4）——`c_deer` は 保留だが **言語は 未割りあて** |

### A方式が 落ちる 原因は **6種類** ——**制作前スクリーニング（正式）**

Batch 3 の いちばんの 成果です。**絵を 作る 前に かならず この 6つを
たしかめる こと。**通らない キャラは **画像を 作る 前に A要検証へ
落として かまいません。**

| | 何を 見るか | 見つかった 個体 |
|---|---|---|
| ① **遮蔽** | 成長の 接続点が 隠れて **意味が 切れないか** | `c_deer` |
| ② **終端器官** | **強い 先端器官を こえて** 延長しようと して いないか。**派手な 形だけが 終端では ありません**（下記）| `c_scorpion` `c_rabbit` |
| ③ **負の 空間** | 切れこみ・穴・すきまを **追加面が うめないか** | `c_crab` |
| ④ **比率変更不能** | ほんとうは **細く／長く／位置を 変える** べき 部位を、base 固定の A で むりに あつかって いないか | `c_crab` `c_oni` `c_giraffe` |
| ⑤ **余白不足** | 安全わくの 中で **36px / 24px に のこるだけの 成長量**が とれるか。**測るのは 部位の 先から 軸に そって**（下記）| `c_crab` `c_oni` `c_rabbit` |
| ⑥ **使用関係** | 持ちものなら **にぎり・手・姿勢・前後関係**が はっきり して いるか | `c_oni` |

**どれも 単独で 致命です。**そして **どれも「数値では 出ません」**
——`c_scorpion` の S5 は 面積 +11.0%・36px の 上ばし y6→y2 まで 変わって
なお 不採用でした。

> **128px の 意味的な 連続性 ＞ 小サイズの 数値差。**
> この 順を 入れかえない こと。

さらに、**完成ずみ 24言語との 重複は 36px / 24px の 最終シルエットで
判定する**こと（primitive の 重複では ない）。

これに くわえて、**細い limb（みみ・尾・棒）を 太く／長く する 案は
高リスク**です（`c_meerkat` の 節）。⑦番めの スクリーニングに 数えるほど
きまった ものさしは まだ ありませんが、**制作前に かならず
「base の 輪郭が 内がわの 折れに 読めるか／2本目の 部位に 見えるか」を
たしかめる こと。**

**広い 面でも 同じ 問いが 要ります。**完成感の 強い 閉じた 面
（甲羅・殻・盾）を **同心に** ひろげると、元の 輪郭は 内縁では なく
**元の 面の 外縁**の まま のこります（`c_turtle` の 節）。

**そして その 前に、いちばん 早く 弾ける 問いが あります。**

> **その 部位は そもそも A方式で 外から 見える いちに あるか。**
> **外シルエットに 接して いない かざりは 高リスク**です（`c_pharaoh` の 節）
> ——A方式は base を 前へ 1:1 で のこすので、内包された ぶんは
> **完全に かくれます**。成功 24体は **すべて 主役が 外形に 接して いました**。

**接して いても、まだ 足りません。**足す ものが
**外輪郭の 外に つく rim**なのか、**面の 上に のる 帯**なのかで 分かれます
（`lg_moonrabbit` の 節）——うすの たがは 露出が **66.1%** も あったのに、
描くべき 場所が 面の 上だった ので 落ちました。

> **A方式の 適性は「細い／広い」では 決まりません。**
> **元の 輪郭を 進化後に どう 読みなおせるか**で 決まります。
> ——`c_meerkat` は 細い limb だから、`c_turtle` は 広い 面でも
> 完成した 閉じた 輪郭だから、どちらも 内縁に できませんでした。

#### ②の 補強 ——**終端器官は「派手な 先端」だけでは ありません**

> **見るのは 形の 派手さでは なく「そこで 終わったと 目に 伝わるか」**です。

つぎの どれかが あれば、形が 素朴でも **強い 終端**です。

- **外の 輪郭が 先で 閉じて いる**
- **内がわの もよう・裏地・つやが そこで 終わって いる**
- **先ぼそり／まるみで 形が 完結して いる**

`c_scorpion` の 針は **形が 個性的**でしたが、
**`c_rabbit` の みみは 形は ただの まるい 先なのに 強い 終端**でした
——輪郭が 閉じ、内がわの こいピンクの 裏地が そこで 終わる ためです。
うしろへ 足すと **「元の みみ ＋ うしろの 2本目の みみ」**に 見えます。

#### ⑤の 訂正 ——**bbox の 中心から 測っては いけません**

> **記録（2026-09-07・`c_rabbit`）**：bbox の 中心から 8方向で 測ると
> NE/NW **110px**。ところが **みみの 先から みみの 軸に そって 測ると
> 16px**しか ありませんでした。中心から 測ると、
> **部位の わきの 空きを 数えて しまいます。**

**部位を 育てる A方式では、つぎを 本判定に する こと（正式ルール）。**

1. 成長させる 部位の **付け根**
2. 部位の **中心線／主軸**
3. **実際の 先端**
4. **先端から 主軸方向の 安全余白**
5. 要れば 主軸 **±15〜30°** の 候補方向

**「キャラ全体の どこかに 空きが ある」だけでは 合格に しません。**

> **`c_rabbit` だけの 例外では ありません。**`lg_candytree` でも
> bbox 中心から SE/SW **133px** に 対して、実際の 部位成長余白は
> **14 / 33 / 81px** と 大きく ちがいました（2026-09-07 に 確認）。
> **bbox 中心の 8方向 clearance は 粗い 候補抽出用に かぎります。**

**「あき 30px 以上」の 一次フィルタも 同じ**です ——あれは
「キャンバスの どこかに 成長の 候補空間が あるか」を 見る
**粗い 一次フィルタ**でしか ありません。部位ごとの 可否は
**部位の 先端 → 成長軸方向の 実余白**で 決めます。

> **`c_squirrel` で 同じ まちがいを くりかえしました**（2026-09-07）。
> 尾の いちばん 上の **ドームの 中ほど**から 測って「上 44〜53px」と
> 報告しましたが、**ほんとうの 尾先（巻いた フックの 先）から
> 接線方向へ 測ると ±30° ぜんぶ 0px**でした。
> `c_rabbit` の「中心から NE/NW 110px → みみの 軸では 16px」と
> **まったく 同じ 誤差**です。

##### **④ 終端から の 実軸余白**（`c_squirrel` の 修正・2026-09-07）

**太い 既存部位の 自然な 延長**を ねらう ときは、候補に する 前に
**この 順**で 見る こと。

| | |
|---|---|
| ① | その 部位は **外シルエットに 接して いる**か |
| ② | **細すぎる limb** では ないか |
| ③ | **終端器官が 弱い**か |
| ④ | **終端そのものから、実際の 成長軸へ 余白が ある**か |
| ⑤ | base の 輪郭を 進化後に **内がわの 房・層・折れ**として 読みなおせるか |
| ⑥ | **予約言語と 衝突しない**か |

> **④が 今回の 修正です。中ほどや bbox 中心から 見た 大きな 空きは、
> 終端が そこへ 伸びられなければ 成長余白では ありません。**

**固定の しきい値には しないこと。**「終端余白 0px なら つねに A不可」と
一般化はしません ——**既存部位そのものを 延長する 仮説では、非常に 強い
失敗信号**として あつかいます。

##### **⑤の 補強 ——「反復もようが ある」だけでは 元輪郭を 境界化できない**

> **記録（2026-09-09・`c_tutankhamun` と `c_mermaid`）**

`c_redpanda_e1`（縞）・`c_ostrich_e1`（繰りかえす 弧）・`lg_berryqueen_e1`
（フリルの 段）は、**元の 輪郭を もようの 途中の 境界へ 読みかえられた**ので
通りました。ところが `c_tutankhamun` は **もようが あるのに 通りません**。
向きが ちがう ためです。

**制作前に この 4つを 測る こと。**

| | |
|---|---|
| ① | 元の **外輪郭の 向き**（接線）|
| ② | 内部の **反復もようの 向き** |
| ③ | **①と ②の なす角** |
| ④ | 小サイズで その もようの **交番数が 実際に 増えるか** |

> **元輪郭と もようの 向きが ほぼ 直交して いる 場合、外がわへ
> 同じ もようを 足しても、元輪郭は「もようの 境界」では なく
> 「古い 外縁」として のこります。**

だから **余白あり ＋ 独立した 部位 ＋ 反復もようあり だけでは 足りません。**
さらに **「元輪郭を 途中の 境界へ 変換できる 向きの 整合性」**が 要ります。

| | 元輪郭 ↔ もようの 角 | 結果 |
|---|---|---|
| `c_redpanda_e1` | 縞と ほぼ 平行 | **成立** |
| `c_ostrich_e1` | 繰りかえす 弧が 外縁 その もの | **成立** |
| `lg_berryqueen_e1` | 裾線に そった 水平の 段 | **成立** |
| **`c_tutankhamun`** | **平均 66.3°・45°未満は 3%** | **不成立（二重頭巾）** |
| `c_mermaid` | **もようが そもそも 無い**（なめらかな 閉じた 膜）| 不成立（二重ひれ）|

##### **閉じかけた 大角度の 弧を A方式で のばす ときの 注意則**

> **記録（2026-09-10・`lg_mammoth` で 発見）**

既存部位が **すでに 大きな 角度を 巻き終えた 閉じかけの 弧**の とき、
「同じ 曲率の まま のばす」A方式は 成立しにくい です。
続き先が **自分の 弧の 内がわ（口）**に なり、外形を 広げずに
**負の 空間を 埋めて 2run を 減らす**ためです。

制作前に **3つ**を たしかめる こと。

| | |
|---|---|
| ① | 弧の **角度スパン** |
| ② | **先端から 自然曲率方向の 実余白**（bbox 中心から 測らない）|
| ③ | 延長が「**外シルエットの 増加**」に なるか「**既存の 負の 空間の 充填**」に なるか |

**270°前後 以上は 強い 危険信号**ですが、**角度だけで 自動除外する
正式な しきい値には しないこと** ——弧の 太さ・先端の 開きかた・
まわりに 何が あるかで 変わります。

##### **左右一対の 部位は、片がわの 完全内包で 言語ぜんたいが こわれる**

`c_pharaoh` の 完全内包則の **補強例**です。

> **左右一対の 部位では、一方だけが 完全内包でも、
> 一対で 成りたつ 進化言語ぜんたいを こわす ことが あります。**

`lg_mammoth` は 左の 牙が 外シルエットに **65画素 接して いる**のに、
右の 牙は **接触 0画素・最短 9.2px の 完全内包**でした。
片がわだけ のばしても「顔を かこむ **一対**の 牙が 育った」には 読めません。

**「片がわが 接して いるから A候補」と しないこと。**
一対・左右対称・複数個で 1つの 意味に なる 部位は、
**その 全部が 外シルエットに 接して いるか**を 見ます。

#### `c_crab` は 設計だけで **不採用**に した（2026-09-07）——A要検証の まま 保留

> **`c_deer` `c_scorpion` に つづく 3例目。**原因は **1つでは なく 4つ**
> かさなって います。**まとめて 1つの 則に しないこと。**

仮の 言語は「左右の 細い 関節が 上へ のび、その 先端の 大きな ハサミ 2つが
高い 位置へ 持ち上がる」。**`c_crab_e1.png` は 1枚も 作って いません**
（8案の 下絵を くらべて 落としました）。

##### base の 実測

| | |
|---|---|
| bbox | **x25..230 / y54..201（206x148）**・面積 18,273px²・fill .599 |
| 安全余白 | **L14 / R14 / T43 / B43** |
| 左の ハサミ＋腕 | **x25..90 / y54..126（66x73）**・3,316px² |
| 右の ハサミ＋腕 | **x165..230 / y54..126（66x73）**・3,328px² |
| ハサミだけ | 左右とも 66x59・約 2,690px² |
| 甲羅 | x95..160 / y96..180（66x85）・5,480px² |
| **関節（腕）** | **x41..61・y116..124 ＝ はば 20px**（右も 19〜20px）|
| 胴との 接続 | **y126** で 甲羅と 1本の run（`43..212`）に なる |
| **切れこみ** | 左 x62..69 / 右 x186..192・**y68..76**（内がわ・上むき）|
| 左右対称 | IoU **0.9886**（`255-x`）——**厳密対称では ない**（手がきの ゆれ）|
| 36px | 上ばし y7・2run行 **13**・ハサミは y7〜y16 で **左右 9列ずつ**・上45% 左70/右69 |
| 24px | 上ばし y5・2run行 **6**・ハサミは y5〜y9 で **左右 6列ずつ**・上45% 左35/右35 |
| 関節 20px | 36px で **2.8列** ／ 24px で **1.9列**（これ以上 細いと 24px で 消える）|

##### 8案 とも 128px で 落ちた

| 案 | どう 見えたか |
|---|---|
| C2 等倍の ハサミを 26px 上に | **ハサミが 2つ かさなる**（scorpion と 同系統）|
| C3 1.35倍（付け根で）| **切れこみが うまって ミトン**。関節も 太る |
| C11 1.35倍（ハサミの 中心で）| **小さい ハサミが 大きい ハサミの 上に のる**。関節が 消える |
| C12 1.50倍（同上）| 同じ、さらに 強い。あき L2 |
| C13〜15 外がわに 三日月 amp18/26/34 | **わくから はみ出す**（L −5 / −11 / −11）|
| C16〜18 同 amp8/11/14 | **平らな 板が うしろに 貼って ある**。関節も 太る |

##### 落ちた 原因は 4つ（どれか 1つでも 致命）

**A. ハサミは 強い 終端器官**（`c_scorpion` と 同系統）——先へ 足すと
もとの ハサミが 途中に のこり、2つに 見える。

**B. identity が 負の 空間に 依存**（下の 注意則）——切れこみの ある 行が
**base 10行 → C3 で 3行**。ハサミ → ミトンに なった。

**C. 関節を 細く できない。**base の 画素なので、うしろに 足せば
**かならず 太く なります**。

| | 関節の はば |
|---|---|
| base | **20px** |
| C3 | **27px**（太った）|
| C11 / C12 | **0**（甲羅と くっついて 消えた）|
| C16〜18 | 25〜38px（太った）|

しかも **ハサミを 動かせない**ので「持ち上がる」が 作れません
——lift 量 ＝ もとの ハサミの 露出量 で、逃げ場が ありません。

**D. よこの 成長余地が 小さい。**ハサミが すでに **bbox の 全幅**
（x25..230）を 占めて います。安全余白 4px を のこすと 外へ 出せるのは
**約 10px**＝66px の ハサミに **+15%**。128px では 成長に 見えず、
うしろの 板に 見えました。上は T43 ありますが、ハサミの 天面は
**x52..60 の 9px しか ない 尾根**なので、上へ のばすと **細い とげ**に なります。

##### 「ハサミを 変えず 腕だけ のばす」も できない

関節は base の 画素で、**上は ハサミ・下は 甲羅が すきま なく 接して います**
（y124 で ハサミ、y126 で 甲羅）。外がわ（x25..50 / y126..175）に 別の 腕を
通すと、base の 腕と あわせて **輪（とって）**に なります
——`c_lavasnail` で 3回 やって 落ちた 形です。

##### **固有の 負の 空間を もつ 形を A方式で ひろげる ときの 注意則**

> **キャラクターの identity が 切れこみ・穴・すきま などの
> 負の 空間に 強く 依存して いる 場合、base の うしろへ 面を 足す
> A方式では、その 空間を うめて 別の 形に 変えて しまう 危険が
> 高い です。**
>
> だから A案では つぎの 3つを たしかめる こと。
>
> 1. 大事な 負の 空間が たもたれるか
> 2. 128px で **同じ 器官**として 読めるか
> 3. 36px / 24px でも その identity が のこるか

**「負の 空間を もつ キャラは A不可」とは しないこと。**
足す 面が その 空間へ 入りこまない 作りなら A で 成立しえます
（じっさい 外がわだけに 足した C16〜18 では 切れこみは 10行の まま
のこりました ——落ちたのは C と D の ほう です）。

##### `c_cobra` との 対照

| | |
|---|---|
| **`c_cobra`** | フードの **外縁が なめらかで、identity を 決める 大きな 穴・切れこみが ない** → うしろから 外へ 面を ひろげても **元の フードとして 読めた** |
| **`c_crab`** | **ハサミの 切れこみ**という 負の 空間が identity を 決める → うしろへ 面を 足すと 切れこみが うまって **別物に なった** |

##### A方式が 落ちる 原因は **4種類**（まとめて 1つに しない）

| | 原因 |
|---|---|
| `c_deer` | **遮蔽**により 成長の 接続点が 見えない |
| `c_scorpion` | **強い 終端器官**を こえて 延長できない |
| **`c_crab`** | **終端器官 ＋ 負の 空間の 消失 ＋ 関節比率を 変えられない ＋ 成長余地不足** |

A / B の 分類精度を 上げる ための **失敗パターン**として つかいます。

##### いまは 保留。弱い 代替案へ 逃がさない

**B確定にも しません。**将来 B方式で

- 元の ハサミを ほんとうに 大きく 描きなおす
- **切れこみを たもったまま** 大型化する
- 関節を ほんとうに 長く 細く する
- ハサミぜんたいを 高い 位置へ 移す

が できる ときに、「細い 関節＋高い 位置の 大きな 2つの ハサミ」を
再検討して ください。**いまは B制作に 進まないこと。**

**つぎの ものへ 自動で 差しかえない こと**（衝突するか、弱い 大型化に なる）。

| | なぜ |
|---|---|
| 甲羅へ 棘 | **`c_salamander` の 稜線と 衝突** |
| 泡・水しぶき | **`ch_donut_e1` の 水流と 衝突** |
| 脚を ふやす | 意味が 弱い |
| 背後に 輪 | `ch_fairy_e1` の 同心の 輪と 衝突 |
| ハサミの まわりを ただ 太らせる | **`c_lion`「たてがみを 大きく する」と 同じ 弱さ** |

#### `c_cobra_e1`（コブラ）——**フードが 左右へ 開いて 一枚の 盾に なる**

Batch 3 の 3体目。`c_deer` `c_scorpion` の あとで、**A方式が
いちばん きれいに 成立した**例です。

| | |
|---|---|
| 本体 | `cobra` を 1:1・画素の ちがい **0**・body shift **0px** |
| 変更しない | 顔・首・とぐろ・**元フード**の base 画素 **0** |
| 主役 | **元からある 首まわりの フードが、中央の 首から 左右へ 大きく 開き、外縁の なめらかな 一枚の 盾形に なる** |
| 枚数 | **1枚**（左右で 2枚に しない）|
| 対称 | **完全対称**（`asym` は つけない）|
| secondary | **なし**（spark・aura・dust・粒・光輪 0）|
| primitive | **`catmull()` だけ**。新しい primitive 0 |
| 色 | ふち `#bc904e` ／ 面 `#f7e8c6` → `#ebca82` → `#c49a57`（ぜんぶ base 実測）|
| **きんし** | 暗紫・黒・**半とうめい**（`fill-opacity` を つかわない）|

> **主役は よこはばの 増加では ありません。**
> **元フードと つながった 同素材の 一枚面として 開く**ことです。

##### 確定の 数値

| | base | e1 |
|---|---|---|
| bbox | x43..212 / y25..230 | **x15..240 / y24..230** |
| 安全余白 | — | **L4 / R4 / T13 / B14** |
| **よこ** | 170 | **226（+32.9%）** |
| **たて** | 206 | 207（**+0.5%**）|
| 面積 | 26,049 | **35,165px²（+35.0%）** |
| visible deco | — | **9,116px²**（bbox x15..240 / **y24..y169**）|
| 重心 | — | **(131.1, 103.4)** |

**この 強い 異方性（よこ +32.9% / たて +0.5%）が「大型化」では なく
「フードを 開く」という 意味に そのまま つながって います。**

##### 小サイズの 固定実測 ——**翼とは run の 向きが 逆**

| | base | e1 |
|---|---|---|
| **36px 最大はば** | 24 | **32** |
| 36px 拡張行数 | — | **20行** |
| **36px 2run行** | **6** | **1** |
| 36px 上45% 占有 | 230 | **336（+46.1%）** |
| **24px 最大はば** | 16 | **22** |
| 24px 拡張行数 | — | **14行** |
| 24px 2run行 | 3 | **0** |
| 24px 上45% 占有 | 107 | **156（+45.8%）** |

> **翼系は run を ふやし、コブラの フードは すきまを うめて run を へらします。**
> `c_devil_e1` 6行・`c_firebird_e1` 13行 に 対して **cobra e1 は 1行**。
> 予約言語の 独立性を たしかめる **強い ものさし**なので のこすこと。

##### 元フードの 内縁は **のこして よい**

base の 元フードの 輪郭が e1 の 中に 線として のこります。**正式に
許容します** ——128px では **フード内部の 折れ・内縁**として 自然に 読め、
36px / 24px では 一枚の 面に とけます。
**これを 消す ために base の 画素を 変えないこと**（変えたら B方式）。

##### 下端は とぐろの うしろへ 消す

追加面は **y169 まで 見えて、y170 から 完全に とぐろの うしろ**に
すべりこみます。**見える ところで 切らないこと** ——切ると
「別の 板」に 見えます。上の 継ぎめは、ふくらみが **頂点（y25）で 0**
なので そもそも 出ません。

##### 対称軸は **base の 実画素**から 決める

> **記録（2026-09-07）**：`255 - x`（＝連続座標 X=127.5）で うつしたら
> 最終画素の bbox が **x15..239** に なり、左だけ 1px 広く なりました。
> `256 - x`（＝X=128.0）に 直して **完全対称**（IoU 1.0000・
> アルファの 左右の さ 最大 8/255・非対称な 行 0・bbox の 中心 127.5）。

**これを 全assetの 共通の 式に しないこと。**知見は こうです。

> **対称の キャラでは、理論上の キャンバス中心だけで 決めず、
> base の 実画素の 左右の 対応を 測って、それに 合わせて
> 生成がわの 反転軸を 決める こと。**

コブラは base の フードが 画素で **`L50 ↔ R205`（たすと 255）**＝
連続座標 **X=128.0** が 軸でした。

##### H6 からの 確定変更（2つ）

| | なぜ |
|---|---|
| ふくらみの 最大 36 → **34px** | 安全余白を L2R3 → **L4R4** に しても、36px/24px の 差が **弱く ならなかった**（最大はば 32 / 22 の まま）。**わくいっぱいに する ことじたいを 進化の 強さに しない** |
| 反転 `255-x` → **`256-x`** | 画素の 対称性を base に 合わせる ため |

##### 既存言語との ちがい（**primitive では なく 最終の 読みで**）

| | |
|---|---|
| **`c_cobra_e1`** | **頭・首を つつむ 一枚の なめらかな よこ長の 盾** |
| `c_devil_e1` | 肩〜胴の うしろの 暗い **切れこみつき bat shadow** |
| `c_firebird_e1` | 下外へ のびる **左右 別々の 実体の 翼** |
| `c_witch`（予定）| 肩から **斜め後方**へ 流れる 一枚の cape |
| `c_crab`（予定）| **細い 関節＋先端の 大きな 2質量** |

#### `c_scorpion` は 設計だけで **不採用**に した（2026-09-07）——A確実 → A要検証

> **`c_deer` に つづく 2例目**ですが、**落ちた 原因は 別**です。
> `c_deer` は **接続点が 見えなかった**、`c_scorpion` は
> **接続は 見えるのに 終端器官を こえられなかった**。

仮の 言語は「既存の 節のある 尾が、片側から 高く 大きな 弧を 描いて
反り返る」。**`c_scorpion_e1.png` は 1枚も 作って いません**
（設計と 下絵の 比較だけ で 落としました）。

##### base の 実測

| | |
|---|---|
| bbox | **x25..230 / y44..211（206x168）**・面積 18,035px²・fill .521 |
| 安全余白 | L14 / R14 / **T33** / B33 |
| 尾の 付け根 | 右下 **(195,112)** あたり |
| **胴と 1本に なる 行** | **y116**（run が `118..210` に なる）。y112〜114 は まだ 別 run |
| 節数 | 中心線 **107.9px**、こい 切れめが 6 / 30 / 55 / 106px ＝ **主要な 節 4つ ＋ 先の 針** |
| 太さ | 帯で **32〜48px**（y88..112 で 34〜37px）。節の ピッチ **約25px** |
| 曲率 | 根(195,112) → (196,84) は ほぼ 垂直 → (176,68) で 左へ → 針(136,74) が 下向き。**約120°の 弧** |
| **針（かぎ）** | **x125..167 / y64..86**。開いた かぎで、輪郭が とても 強い |
| 36px の 現状 | 上ばし **y6**・24行・2run以上 **10行**（尾は y6〜y14 で 読めて いる）|
| 24px の 現状 | 上ばし **y4**・16行・2run以上 **5行** |
| 空きの ふところ | 上の帯(y<44) 7,722px² ／ 弧の左(y44..115・x<125) 8,197px² |
| すきま | NW150 / NNW112 / WNW107 / SW79 / NE66 / SE61 / N59 / S42 / NNE36 / ENE33 / E21 / W17 |

**接続は 問題では ありませんでした。**アンカーは 針の ふくらみ
（y78 の run `127..209`）の 中＝**完全に 見えて いる 連続領域**です。

##### 6案 とも 数値は 通った

| 案 | bbox | 面積 | 弧の 面積 | 36px 上ばし | 2run行 | 24px 上ばし |
|---|---|---|---|---|---|---|
| base | y44..211 | — | 3,832 | y6 | 10 | y4 |
| S1 先から 巻く | y44..211 | +6.7% | 5,042（+32%）| y6 | 12 | y4 |
| S2 外に もう1本 | y20..211 | +15.6% | 6,617（+73%）| y2 | 6 | y1 |
| S3 左へ のばす | y44..211 | +6.9% | 5,070 | y6 | 9 | y4 |
| S4 反り返る | y27..211 | +9.5% | 5,542（+45%）| y3 | 11 | y2 |
| **S5 高く 反り返る** | y17..211 | **+11.0%** | **5,811（+52%）** | **y2** | **13** | **y1** |
| S6 ゆるく 左上 | y44..211 | +7.0% | 5,093 | y6 | 11 | y4 |

弧を **原画で y27 まで**上げると 36px の 上ばしが y6 → y3 に なり、
はっきり 変わります。追加できる 節は **4〜5節**まで
（ピッチ25px・空きの 弧の左 8,197px²）。

##### **128px で 5案 すべて 成長の 連続性が 成立しなかった**

| 案 | 128px で どう 見えたか |
|---|---|
| なめらかな 帯 | **別素材の 角を 貼った**ように 見える（節が ない）|
| 節を 同じ みちで 足す | 尾は のびるが、**base の 針が くさりの 途中に のこる** |
| 節を base 並みに 大きく＋つや＋新しい 針 | **数珠・いも虫**。base の 節は 細長い しずく形で、円の くさりとは 別部位 |
| 外がわに もう1本の 弧 | **2本目の 尾**（先が 2つ）|
| 下へ たれる | 左に **数珠が ぶらさがった**だけ |

##### 終端器官を こえる A延長の 注意則

> **A方式で 既存部位を 延長する とき、その 部位の 先端に
> 強い 固有の 終端器官が ある 場合、その 終端を こえて かざりを
> 足すと「同じ 部位の 成長」として 読めなく なる ことが あります。**
>
> 起きる 失敗は この 3つ。
>
> - もとの 終端器官が **途中に のこる**
> - 追加した ところが **別部位に 見える**
> - 終端を 迂回すると **2本目に 見える**

**「終端器官が ある子は ぜんぶ A不可」では ありません。**
終端の 形・前後関係・成長の 向きに よっては A で 成立します
（`c_firebird` `c_devil` は **根もとから 面を 育てた**ので 終端を こえて いない）。
**判定は かならず「128px で 元の 部位との 成長の 連続性が 読めるか」**です。

##### `c_deer` との ちがい

| | 落ちた 原因 |
|---|---|
| `c_deer` | **みみに よる 遮蔽・前後関係**で、Y字の 接続点が 見えなく なる |
| `c_scorpion` | **接続は 見える**が、既存の **針という 終端器官**を こえられない |

同じ A の 失敗でも 原因は 別です。**まとめて 1つの 則に しないこと。**

##### 数値の 成功を 採用理由に しない

S5 は 面積 **+11.0%**・弧 **+52%**・36px の 上ばし **y6 → y2**・
2run行 **10 → 13** まで 変わって、なお 不採用です。

`c_mummy` の C2 と **ちょうど 逆向き**の 例に なりました。

| | |
|---|---|
| `c_mummy` C2 | **面積を ふやしても 小サイズの 輪郭差が 出ない** |
| **`c_scorpion` S5** | **小サイズの 輪郭差は 十分 出るのに、意味的な 成長の 連続性が 成立しない** |

だから これからは **3つを 別々に 判定する**こと。
**どれか 1つ 通っただけでは 採用しません。**

1. 意味的な 成長の 連続性（128px）
2. 36px / 24px の 識別性
3. 予約ずみ 言語との 独立性

##### いまは 保留。ほかの A案へ 逃がさない

**B確定にも しません。**将来 B方式で 尾ぜんたいを 描きなおせる ときに、
「節のある 尾そのものが 長くなり、高く 大きな 弧を 描いて 反り返る」を
再検討できます。その ときは **既存の 針を 途中に のこさず、
成長後の 尾の ほんとうの 終端へ 針を 移す**こと
——base の 画素を 変えるので **A では なく B** です。

**つぎの ものへ 自動で 差しかえない こと**（衝突するか、弱い 大型化に なる）。

| | なぜ |
|---|---|
| 鋏を 大型化 | **`c_crab` と 正面衝突**（同じ Batch・ルール⑥）|
| 背中へ 棘を 足す | **`c_salamander` の 稜線と 衝突** |
| 尾の まわりを 太く かこむ | A方式では ふちの ハロー＝**影**に しか 見えない |

#### `c_octopus_e1`（タコ）——**元からある 触手が 長く うねって 育つ**

Batch 3 の 1体目。`c_firebird_e1` の「既存部位の 延長」を **複数本**に
おしすすめた はじめての 例です。

| | |
|---|---|
| 本体 | `octopus` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | **元からある 太い 触手が、長く うねって 育つ** |
| 数 | **4本**（T1/T1' 主役・T3/T3' 補助）。T2/T2'・T4/T4' は **さわらない** |
| きんし | **新しい 触手を 足さない**（「数が ふえる」では ない）|
| きんし | 泡・水しぶき・spark・halo・王冠・吸盤の 追加表現。**secondary なし** |
| 変更しない | 頭・目・口・ほっぺ。**進化差は 下半分だけ** |
| 色 | `#f7b6cd` → `#f187a8` → `#ea96ae`・ふち `#cb5f7e`・つや `#fdd9e6`（ぜんぶ base の 触手から 実測）|
| bbox | **x16..239 / y34..239（224x206）**。安全わくに 左右・下 **5px** のこす |
| 面積 | 27,161 → 31,584（**+16.3%**）|

#### 進化言語は「数が ふえる」では ない

**既存部位の 長さ・曲率・外形が 育つ**進化です。

| | |
|---|---|
| `sp_rpurin_e1` | **1本**の 太い 帯が 一方向へ。谷なし |
| `ch_donut_e1` | 連続した **水流＋同心の 波紋** |
| `c_mummy`（予定）| **細い 帯**が ばらばら。外形線が 破れて「ふさ」 |
| **`c_octopus_e1`** | **丸い 頭＋下に 太い 有機的な 曲線が 4本、それぞれ 独立に 育つ** |

分ける 決め手は 本数では なく **太さ**（mummy 12〜14px ↔ octopus 30〜54px）、
**外形線の 連続性**（破れる ↔ 破れない）、**谷の 数**（rpurin は 0）。

#### **36px の 合格基準（固定）**

> **base ＝ 大きな 下ローブ 2 ／ e1 ＝ 大きな 下ローブ 4。**
> 左の 外がわ T1 の 山・左の 前面 T3 の 山・**中央の 谷**・
> 右の 前面 T3' の 山・右の 外がわ T1' の 山 が **分離して 読める**こと。

```
base:  26 27 28 28 28 28 30 30 31 31 31 30 30 29 28 28 29 30 30 31 31 31 30 30 28 28 28 28 27 26   山=2
e1  :  32 33 33 33 33 33 33 30 31 32 33 33 33 33 32 28 28 32 33 33 33 33 32 31 30 33 33 33 33 33 33 32   山=4
```

いまの **山 33 / 33 / 33 / 33**と **谷 30 / 28 / 30**の 組みたてを たもつこと。
**数字そのものを 合わせるのが 目的では ありません** ——
**4つの うねりが つぶれて 2つの 塊に もどらない**ことが 必須です。

**24px** は 4本を 数えられなくて かまいません。ただし
**「丸い頭＋ほぼ 平らな 底」→「下半分に 複数の 大きな 波打ち」**は たもつこと
（いま 山4・行はば 20列 → 22列）。

#### いちばん のばせるのは「36px で 読めて いない」触手

実測すると **直感と 逆**でした。

| 先端 | 真下 | ななめ |
|---|---|---|
| **T1 外がわ (28,187)** | **57** | **81** ←いちばん あいて いる。**36px で 1本も 読めて いない** |
| T2 (52,204) | 40 | 57 |
| **T3 前 (82,221)** | **23** | 33 ←**すでに 読めて いて、余地が ない** |

だから 主役は **T1 / T1'**、前の T3 / T3' は **base の 先 y221 → y237（+16px）
だけ**。前だけ のばす A1 は **山が 2の まま・幅も 変わらず**、不採用です。

#### 谷を うめない ための しばり

- **T1 の 先を x59 より 右へ 出さない**（x60..67 は T3 との 谷）
- **T3 を x111 より 内がわへ 出さない**（x113..127 は base で ゆいいつ 深い 中央の 谷）

太くして 4本とも 大きく したら、T1 と T3 が **融合して 谷が 1つ**に なりました。

#### 先を きつく 巻かない

`flowPath` の 内がわの オフセットが **自己交差**して、腕の 中ほどに
**黒い 矢印状の 跡**が 出ます。巻きを ゆるめて 消しました。

#### primitive は 増やして いない

**`catmull()` ＋ `flowPath()` だけで 成立**しました。中心線その ものに
S字と 先の 巻きを 入れる 方式です。`armPath()` は 要りませんでした。

`glossPath()` は **進化言語では なく、base の 素材感に そろえる ための
陰影の 補助**です（`c_firebird_e1` の `wingVeins` と 同じ あつかい）。
**「新しい かざり」として 数えないこと。**つやを 入れる 前は、
つやのある base の足の となりで **別素材の 靴下**に 見えました。

#### `c_mummy_e1`（ミイラ）——**外形が「ふさ」に ほぐれる**

Batch 3 の 2体目。`c_octopus_e1` が「太い 曲線が 育つ」なら、
こちらは **小サイズで 輪郭の 連続性を こわす**のが 主役です。

| | |
|---|---|
| 本体 | `mummy` を 1:1・画素の ちがい **0**・body shift **0px** |
| 変更しない | 顔・目・包帯本体・腕・脚の base 画素 **0** |
| 主役 | **ほどけた 包帯が 不規則な 方向へ 垂れ、外形が「ふさ」に ほぐれる** |
| 帯 | **6本**（左3 / 右3）・太さ **19〜21px** |
| 方向 | **斜め上外・横外・下外の まぜあわせ** |
| 対称 | **完全な 鏡像に しない**。PLAN に `asym:true` |
| secondary | **なし**（spark・dust・halo・粒・魔法エフェクト 0）|
| 連結成分 | **e1 ぜんたいで 1つ** |
| bbox | **x18..239 / y26..235** |
| 面積 | 23,962 → **29,564px²（+23.4%）** |
| 左右対称度 | IoU **0.926 → 0.807** |
| 色 | `#f7f6fb` → `#e6e3f0` → `#d1cbe1`・ふち **`#d3bca2`**（ぜんぶ base の 包帯から 実測）|

#### 進化言語は「包帯が ふえる」「大きく なる」では ない

**小サイズで 輪郭の 連続性が くずれ、複数の すき間を もつ 外形へ 変わる**
ことが 本質です。

#### 小サイズの 確定実測

| | base | e1 |
|---|---|---|
| **36px の 破れ** | **2 / 30** | **14 / 31** |
| **24px の 破れ** | **1 / 20** | **6 / 21** |

> **数字そのものを 合わせるのが 目的では ありません。**
> 最終条件は **36px でも 24px でも「外形が ほぐれた 包帯の ふさ」と
> 読める**こと。あわせて、36px で 帯が **1pxの 細線ばかりに ならず、
> いまのように 2〜5列の run が 複数 のこる**ことも 大事です
> （1列ばかりなら 粒・細線に 寄りすぎ）。

#### 本数は **6本**（5 / 6 / 7 を 実測して 決めた）

| | 36px | 24px | 面積 |
|---|---|---|---|
| 5本 | 13/31 | 5/21 | +17.7% |
| **6本（採用）** | **14/31** | **6/21** | **+23.4%** |
| 7本 | 14/31 | 6/21 | +25.0% |

**7本に しても 小サイズの 読みは 良く ならず、面積だけ ふえました。**
「本数を ふやせば 進化が 強く なる わけでは ない」——`c_salamander` の 棘、
`c_devil` の 切れ込み、`c_onibi` の 子と 同じ 原則です。

#### アンカーは **run が 1つに つながって いる 行**へ

`c_octopus_e1` で 足した きまりが ここでも 効きました。base は
**y94..102 / y126 / y162..190 / y210..230** で run が 割れて います。
6本 すべてを **run が 1つの 行**（y86 の x66..202／y146 の x52..204／
y150 の x57..199／y110 の x70..186／y198 の x80..174／y202 の x78..176）へ
つないだので、**宙に 浮いた 継ぎはぎは 1つも 出ませんでした**。

#### ほかの 言語との ちがい（36px / 24px の 最終シルエットで）

| | |
|---|---|
| `ch_queen_e1` | 小さい **粒**が 多数・**独立成分が 多い** |
| `sp_rpurin_e1` | **1本**の 太い 連続帯。**24px では 1枚に もどる**（破れ 1/20）|
| `c_ghost_e1` | 胴の 下から **下へ 広がる 半透明の 3層** |
| `c_octopus_e1` | **下方向**に 複数の 太い 有機曲線 |
| **`c_mummy_e1`** | **頭・腕・すそから 不規則な 方向へ のびる 包帯で 外形が ほぐれた ふさ** |

**primitive では なく、36px / 24px の 最終シルエットで 別言語**である
ことを たもつこと。

#### 帯の 太さ ——18〜20px が 上限でも 下限でもある

| | |
|---|---|
| w=10〜14 | 36px は 破れるが **24px で 4〜5/21 に 落ちる** |
| **w=18〜20** | **24px で 6/21。いちばん よい** |
| w≥24 | 帯どうし・帯と 本体が **融合して かえって 減る** |

base の 包帯の 間かくは **12.9〜13.9px**（3列で 実測）。それより
すこし 太いのが ちょうど よい。

#### 直線的な 帯に しない

1回目は 制御点 3〜4点の ほぼ まっすぐな 帯で、128px で
**小枝・アンテナ**に 見えました。制御点を 5点に して
**「外へ 出る → 重力で 垂れる」の 弧**に し、幅に
`1 + 0.13·sin(3πt)` の さざなみを 入れて 布の 質感を 出して います。
先は まるく ——三角・角・炎・羽根の ような 先端は きんしです。

#### primitive は 増えて いない

**`catmull()` ＋ `flowPath()` だけ**で 成立しました。
**包帯 1本 ＝ 1つの 閉じた パス**、6本は それぞれ 独立した パス
（別々の 付属物なので）。1本を 小片に 分割して いません。

### A方式の 可視差分上限 —— 意味を 壊さず 36px の 採用信号へ 届くか

A方式（base 1:1・装飾は **背面だけ**）で 足せるのは、**いま 空いて 見えて いる ところ だけ**です。
既存の 部位の いち・長さ・重なりは 変えられません。だから
**追加できる 可視差分には 上限が あります。**

> **その 上限が、意味を 壊さない はんいで 36px の 採用信号へ 届かなければ A不成立。**
> 逆に、信号を 出す ために 部位を 大きくした 結果、**別の シルエット言語へ 移るなら、
> その 時点で 不成立。**

**反復の 有無・ピッチ・tuck の 可否・成長軸の 余白が ぜんぶ 合格でも、
この 上限判定で 落ちる ことが あります。**判定は「反復が あるか」では なく、
**「その 反復が 36px の 採用信号を 出せる だけの 可視面積を もつか」**で 行います。

上限が 足りない サイン（どれかに 当たったら 下絵に 進まない）——

- 反復する 部位の **可視面積が 小さい**
- **反復区間が みじかい**
- 既存の 外形の **内がわに 収まって いる**
- 1ユニット ふやしても **run の 数・bbox・局所の 質量・外周の 識別記号が ほとんど 変わらない**

#### **IoU は 補助**。見るのは「傾き」

1点の IoU より、**差分を 強めた ときに 進化の 意味が 保てるか**という
**傾き**を 見ること。`c_swan` は この 傾きが 悪く、差分を 強めると
早い 段階で「翼が 開く」へ 移りました（12案で 数値が いちばん よい W5 が、
まさに 禁止した「翼が 開いた」）。**この形は A不成立の 説明として いちばん 強い**です。

#### つぎに しらべる 4つの ものさし（**まだ 正式ルールでは ありません**）

| | 何を 測るか |
|---|---|
| **VGC** | A方式で 安全枠の 中に 足せて、しかも base に 隠れずに **見える** 最大面積 ÷ base の 全面積 |
| **SCR** | その 部位が base の 外周に 占める 長さ ÷ 外周の 全長 |
| **SDC** | 36px で、理想的に 1ユニット ふやした ときに 新しく ふやせる 外周セル・run・bbox列 の 理論上限 |
| **SBP** | どこまで 大きくすると 別の 言語へ 移るか。その **直前**の 差分量 |

#### 仮ゲート ——**まだ 正式ルールでは ありません**

> 36px で「新規の 外周 2列以上」「局所の 質量 +10%以上」「run の 増加 1以上」の
> どれも 満たせる 見こみが SBP の 手前で 立たないなら、下絵に 進まない。

`c_swan` には きれいに 効きます（W10 は 局所質量 +3.2% で 早く 落ちる／
W3 は run が 減って 落ちる／W5 は 数値は 通るが SBP で 落ちる）。

**けれど 採用ずみ 28体へ 遡って 検証して いません。**
数字（2列 / +10% / run 1以上）は、**成功例・失敗例に VGC / SCR / SDC を 当てて
較正して から** 正式な しきい値に 上げること。**いま 固定しないこと** ——
`c_swan` だけに 最適化した 足切りに なります。

### 可視差分ゲートの 較正（2026-09-10）——**base の 幾何だけでは 足切りできない**

> **記録（2026-09-10）**：上の 仮ゲートを 正式な しきい値に 上げる ため、
> **VGC / SCR / SDC を 実装して 採用ずみ 28体と 代表的な 不採用 11体に 当てました。**
> **結果は 不合格です。**指標は 消さずに のこしますが、
> **残り129体の 順位づけ・自動除外には つかいません。**

代表的な 不採用例は `c_sheep` / `c_swan` / `c_turtle` / `c_bee` / `c_horse` /
`c_mermaid` / `c_tutankhamun` / `c_pharaoh` / `lg_moonrabbit` / `c_king` / `c_purinala` の 11体。

#### 測った もの

| | 定義 |
|---|---|
| 部位 | 採用ずみは **e1 の 追加画素から 4px 以内の base 外輪郭点**（仮説を つかわない 客観的な 定義）。不採用は 記録ずみの 仮説が ねらった 部位を bbox で 指定 |
| 到達域 R | **安全枠 ∧ 透明** の 中を、部位の 輪郭から **測地距離** D 以内に 広げた 集合。測地なので 体を 回りこめず、**base に かくれる 画素は 定義上 入りません** |
| **VGC** | R の 画素数 ÷ base の 不透明画素数 |
| **VGC36** | R を 足した とき **36px で 新しく ON に なる cell 数** ÷ base の 36px の ON cell 数 |
| **OCC** | R が ふれた 36px cell の うち、**base で すでに ON** の わりあい（＝成長軸上の 他部位占有）|
| **SCR256 / SCR36** | 部位の 外輪郭長 ÷ 全外輪郭長 ／ 36px で 部位の 輪郭が 乗る cell 数 ÷ 全輪郭が 乗る cell 数 |
| **SDC** | R を 36px へ 投影し、新規外周列/行・局所質量・run 数・bbox・識別記号を **別々に** 測る（合成しない）|
| **SBP** | 自動化しない。概念だけ |

#### ① 到達距離 D が 較正できない

採用ずみ 28体が **実際に どこまで 伸びたか**（部位の 輪郭からの 測地距離）を 測りました。

| | 最小 | 中央 | 最大 |
|---|---|---|---|
| 実成長 p90 | **9px**（`lg_berryqueen`）| 36px | **252px**（`c_lavagolem`）|
| 実成長 max | 15px | 56px | 280px |

部位の 層の 厚み（色の band 深さ）との 比 **band ÷ 実成長p90 は 0.05〜3.50**。
**関係が ありません。**

- `c_lavagolem` 252px / `c_onibi` 165px …**離れた 浮遊ユニット型**は 部位から 遠くに 置く
- `lg_berryqueen` 9px / `lg_chocoknight` 11px …**rim 型**は ごく 近く

> **1つの 距離モデルで 採用ずみ 28体を おおえません。**
> D を 大きくすると VGC は ただの「空きキャンバス量」に なり、
> 小さくすると 浮遊ユニット型の 正例を 落とします。

#### ② base だけの 指標は 負例を **1体も** 落とせない

**「採用ずみ 28体を 1体も 落とさない」閾値**に して、代表的な 不採用 10体
（`c_pharaoh` は 輪郭に 接して いないので 別枠）を 何体 落とせるか。

| 指標 | 正例の 限界値 | 落とせた 負例 |
|---|---|---|
| VGC（無制限）| 58.3% | **0 / 10** |
| VGC36（無制限）| 59.9% | **0 / 10** |
| SCR256 | 4.8% | **0 / 10** |
| SCR36 | 6.1% | **0 / 10** |
| OCC（無制限）| 12.4% | **0 / 10** |
| newC @ D=14 | 23 | **0 / 10** |
| **newC @ D=20** | **36** | **0 / 10** |
| newC @ D=28 | 57 | **0 / 10** |
| OCC @ D=14 / 20 / 28 | 29% / 27% / 21% | **0 / 10** |

**全滅です。**しかも 重なりかたが いちばん わるい 形でした。

| newC @ D=20 | |
|---|---|
| **正例の 最小** | **`c_tower` 36**・`c_onibi` 38・`ch_apple` 40・`c_redpanda` 51 |
| **負例の 最小** | **`lg_moonrabbit` 71**・`c_horse` 73・`c_mermaid` 73・`c_bee` 76 |

> **正例の 下端が 負例の 下端より 下**に あります。
> **どんな 閾値を 置いても、負例を 1体 落とした しゅんかんに `c_tower_e1` が 死にます**
> （71 で 切ると 正例が 8体 巻きこまれる）。

#### ③ 成長方向を 与える「円すい版」も 正例を こわす

方向を 宣言して ±35° の 円すいに しぼると、**採用ずみ 28体の うち 8体が newC＝0**
に なりました（`ch_donut` `ch_prince` `c_lavagolem` `c_onibi` `c_lavasnail`
`c_octopus` `c_witch` `c_snowqueen`）。

**巻きつく・散らばる・多方向へ 出る 成長は、1つの 円すいでは 表せません。**
負例は 0/9 しか 落とせませんでした。

#### ④ 仮ゲート「新規外周2列 / 局所質量+10% / run+1」の 結果

採用ずみ 28体が **実際に 出した** 36px の 信号に 当てました。

| 条件 | false negative |
|---|---|
| 新規外周 2列以上 | **3** ——`c_salamander`(0,0)・`c_ghost`(0,1)・`c_witch`(1,0) |
| 局所質量 +10%以上 | **1** ——`c_purin`（−6.4%。warp 方式で 本体が 縮む）|
| run 増加 1以上 | **3** ——`c_cobra`(0)・`c_witch`(0)・`lg_chocoknight`(0) |
| **AND（3つ とも）** | **6 / 28 ＝ 21%。破棄** |
| **OR（どれか 1つ）** | **0 / 28** |

> **AND 版は 破棄します。**`c_purin` `c_salamander` `c_ghost` `c_cobra` `c_witch`
> `lg_chocoknight` の 6体を 誤除外します。
>
> **OR 版は 28体 ぜんぶ 通せますが、下絵が できた あとにしか 計算できません。**
> だから **事前の 足切りでは なく、事後の サニティチェック**として つかいます
> （「作った 下絵が 弱すぎないか」の 確認）。

#### 成功と 不成功の 対照

| | なぜ 成立した／落とせなかった か |
|---|---|
| `c_tower_e1` | 面積は **+6.6%** と ひかえめ だが、**局所質量が +180%**。dH 4・ident 4。**全体量では なく 局所量で しか 拾えない** |
| `lg_berryqueen_e1` | **dW 0（全幅 不変）**でも **dH 2**・newC 38・局所 +21.1%。**幅と 高さを 別々に 見れば 拾える** |
| `c_ostrich_e1` | **SCR256 は 11% しか ない**のに 成立。拾えたのは **局所質量 +125.6%** |
| `c_redpanda_e1` | 32px IoU が 高め（base と 0.889）でも 成立。**局所質量 +89.3%・ident 8** で 拾える |
| `c_swan` | **落とせない。**newC@D20 ＝ 96 で `c_tower` の 36 より 大きい |
| `c_horse` | **落とせない**（73）。全方向の 到達だと **尾の よこの 空きを 拾って しまう** |
| `c_mermaid` | **落とせない**（73）。かくれる ぶんは 除外できるが、**回りこんで 別の 空きに 出る** |
| `c_tutankhamun` | newC@D20 ＝ 136 と 大きい。**数値では 落とせないし 救えない**（想定どおり）|

#### **SCR は「局所の シルエット寄与」を 表して いない**

`c_ostrich_e1` が 代表例です ——**SCR256 は 11%** しか ないのに、
36px では 尾羽の 局所質量が **+125.6%** で はっきり 読めます。
**外輪郭長の わりあいでは、局所の 寄与を 測れません。**

#### **全体量ベースの ゲートは 作れない**

`c_tower_e1` の ような「キャラ ぜんたいに 対しては 小さいが、局所では +180%」の
成功例が ある かぎり、**面積比・全体 IoU で 足切りする ゲートは 原理的に 作れません。**
局所量で 見る しか なく、局所量は「どこが 局所か ＝ 仮説」を 必要と します。

#### 正式に のこす 機械判定は **1つだけ**

> **対象の 部位が base の 外シルエットへ 1点も 接して いない 場合、
> その 部位を 背面の deco で 育てる A方式は 原理的に 不可視に なる。**

`c_pharaoh`（胸元の かざり・最短 17px の 完全内包）を 自動で つかまえます。
**採用ずみ 28体は 全員が 輪郭に 接して いる**ので、false negative は 構造的に 0 です。

**ただし いまは 実装しません。**`check-chars.mjs` にも 新しい tools にも 入れず、
**設計知見として 記録するだけ**に します。

#### これから の やりかた

- **VGC / SCR / SDC は「失敗した 指標」として 記録に のこす。**削除しない。
  ただし **残り129体の 順位づけ・自動除外には つかわない**
- 制作前の 判定は これまでどおり、**対象部位 ＋ 成長方向 ＋ 進化言語**を
  先に 仮説として 定義し、**その 仮説ごとに 構造を 確かめる**
- 機械化を 考えなおす ときも、**全species 共通の 単一モデルには しないこと。**
  少なくとも つぎの 成長モードごとに 別の モデルが 要ります。

  | | |
  |---|---|
  | 輪郭連続型 | `c_ostrich` `lg_berryqueen` `c_redpanda` `c_cobra` `lg_chocoknight` |
  | 離散／浮遊ユニット型 | `c_lavagolem` `c_onibi` `c_tower` |
  | 巻きつき／多方向型 | `ch_donut` `c_snowqueen` `c_witch` `c_octopus` |

- **いまは その 再設計に 進みません。**

#### 機械の 役わりを 変える

> **「自動ゲートで 候補を 落とす」のでは なく、
> 「人が 立てた 仮説の 矛盾を 自動で 見つける」ほうへ 機械化の 役わりを 変えること。**

今回 分かったのは、機械が 向いて いるのは **候補えらび**では なく
**はっきりした 違反の 検出**だという ことです。

| 機械が 向いて いる | 機械が 向いて いない |
|---|---|
| 完全内包（外シルエットに 接して いない）| どの 部位を 育てるか |
| 本体の 画素差・body shift | どの 方向へ 育てるか |
| 安全枠の はみ出し・ふちの 切れ | 進化の 意味が 通るか |
| 小サイズの 差分（36px / 24px）| 予約言語と ぶつかるか |

### Batch 2 の 設計 ——火・霊の 8体（**制作前に 言語を 分ける**）

火・霊は **ほうって おくと 全員「炎を 大きく」「オーラを 足す」に なります。**
だから 描く 前に 8つの **進化言語**を 決めます。Batch 1 の 7言語とも
かぶらせない こと。

| species | 進化言語 | main | secondary | 置き場 | 36px の しるし | 方式 |
|---|---|---|---|---|---|---|
| `c_hinotama` | **上へ のびる 炎の 舌** | 舌 3枚（中央みじかい・左右ながい）| 上へ 舞う 火の粉 | 上＋NE/NW（100）| たてに 伸びた 炎 | A（**body shift 12px**）|
| `c_onibi` | **小さな 分身の 群れ** | 子の 鬼火 3〜4（尾つき）| ゆらぎの 粒 | NW100 / NE87 / SW・SE70 | まわりに 青い 小さな 火 | A |
| `c_ghost` | **重なる 薄い ヴェール** | 波打つ すそ 3枚 | ほのかな 燐光 | SW80 / SE83 / S39 | すそが 広がって 波打つ | A |
| `c_salamander` | **背の とげが 育つ** | 背びれの 稜線 | 尾の 先の 炎 | 背中（N99 / NW139）| 背が ぎざぎざに 高い | A |
| `c_lavagolem` | **ひび割れの 発光＋浮遊する 破片** | 浮く 岩 4〜5 | 割れ目から もれる 赤い 光 | NE・NW125 / SW・SE105 | まわりに 角ばった 岩 | A |
| `c_lavasnail` | **殻の うずが 外へ のびる** | 溶岩の 渦 1本 | 火の粉 | 殻の うしろ（N72 / NE57）| 渦を 巻いた 殻 | A |
| `c_firebird` | **翼が 大きく ひらく** | 炎の 翼 2枚 | 尾羽の 火の粉 | ななめ上（NW73 / NE59 / SE105）| 翼が 開いた かたち | A |
| `c_devil` | **うしろに 大きな かげ** | 半透明の こうもりの かげ 1つ | 足もとの 影 | SW93 / NE・NW80 | うしろの 黒い 大きな 影 | A |

**あぶない 組み合わせ**（ここを 見はる）。

| 組 | どう 分けるか |
|---|---|
| `c_hinotama` ↔ `c_onibi` | **1つの かたまりが 上へ** ↔ **小さい 分身が 散る**。暖色 ↔ 寒色 |
| `c_onibi` ↔ `ch_prince` | 小さくて **尾が ある・弧に 散る** ↔ 大きくて **止まって いる 四つ葉** |
| `c_lavagolem` ↔ `tw_ice` | **不透明・不規則な 岩＋赤い 光** ↔ すきとおる 対称の 結晶 |
| `c_ghost` ↔ `sp_rpurin` | **左右へ 広がる 波打つ すそ** ↔ **1方向の 帯** |
| `c_firebird` ↔ `c_devil` | 明るい **オレンジの 翼** ↔ 暗い **半透明の かげ** |
| `c_lavasnail` ↔ ほか | **渦**は この子の 専用に する |
| `c_salamander` ↔ `tw_ice` | 体に **そって 生える** ↔ 外に **浮かぶ** |

**よこ向きの 4体**（`c_ghost` / `c_salamander` / `c_lavasnail` / `c_firebird`）は
`EN_FACE` で 進む向きに 反転します。体ごと 反転するので 破綻は しませんが、
**向きの ある モチーフ（三日月・かたむいた 文字状の もの）は さけること。**

#### `c_lavagolem_e1`（ようがんゴーレム）——**割れて 浮いた 岩**

| | |
|---|---|
| 本体 | `lavagolem` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | **本体の 外へ 浮きあがった 岩の かけら 5つ** |
| きんし | **炎・オーラで つつまない** |
| secondary | かけらの **割れ目の 発光**（`#fc907e` ＋ 芯 `#ffdcc0`）。**本体の 亀裂は 描き変えない**（A方式なので）|
| 色 | 岩 `#ede3e5 → #b99a9d → #9f575f`・ふち `#8f5058`（base の 実測）／溶岩 `#fc907e`・`#ffdcc0`（base の 亀裂の 実測 `#fcae8a` / `#fc907e`）|
| 36px | **まわりに 角ばった 岩が 浮いた ゴーレム** |

**`tw_ice_e1` と 形で 分ける**のが いちばん 大事です。

| | 形 | 数と ならび | 色 |
|---|---|---|---|
| `tw_ice_e1` | すきとおる **対称の 六角** | 左右に 大きく 2つ | 水色・明るい |
| `c_lavagolem_e1` | **不とうめいで 不規則な 多角形**（辺の 数も 半径も たね ごとに ばらつく）| **5つ・大小 14〜26・角度も ばらばら** | 岩の むらさき茶・**暗い** |

**白黒に して 36px / 24px で ならべても 分かれます**（色を 見なくても
「小さい かけらが 散る」↔「大きな 板が 2つ」）。

**かけらは 5つ まで。**7つ（＋小さな かけら 2つ）に したら
**`ch_queen` の「粒の 連なり」に 近づきました**（1回 やりました）。

**四すみに 等間かくで 置かないこと。**左上に 大小 2つ かためて
「板が 割れて はがれた」ように 見せ、**右上は わざと 空けて** います。

> **本体の 亀裂を 光らせるのは B方式に とっておく。**A方式では
> 本体の 画素を 変えられないので、いまは **浮いた かけらの がわ**に
> 光る 割れ目を 持たせます。将来 B に 上げた ときに
> 「本体の 亀裂まで 光る」という 一段 上の 表現が のこせます。

**B へ 送りかえす かもしれない 3体**（作って みて 決める）——
`c_hinotama`（上の 余白が 13pxしか なく body shift が いる）／
`c_firebird`（よこの 余白が 23〜25pxしか ない）／
`c_devil`（上の 余白が 18pxしか なく、かげの 頭が 出せない）。

#### `c_salamander_e1`（サラマンダー）——**背中に 沿って 育つ 稜線**

| | |
|---|---|
| 本体 | `salamander` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | **背中の 線に つながって 生える 1つづきの 稜線**（棘 6つ）|
| きんし | **体から はなれた 大きな かざりを 浮かせない**（`c_lavagolem` と 正反対）|
| きんし | **1本ずつ 別の パスに しない**（下記）|
| secondary | **なし。**尾の 先の 炎は 場所が 無くて 落としました（下記）|
| 色 | `#fdcc91 → #fcb078 → #f19673`・ふち `#d5775a`（ぜんぶ base の 板の 実測）|
| 36px | **なめらかな 背中 → はっきり ギザギザ**に 育ったと 読める |
| 24px | 歯の 1つ1つは 消えるが、**背が 高く 金いろに ざらついた**ことは のこる |

#### **1つの パスに union する**（1本ずつ 描かない）

棘を 1本ずつ 別の `<path>` に すると、かさなった ところに 線が のこって
**「三角を ならべただけ」**に 見えます（ぱふそでの ふちと 同じ 失敗）。
`crestPath()` が 0.5px きざみで **union（各棘の 高さの min）**を とり、
**1つの パス**にします。下の へりは 背中より 12px 下＝体に かくれます。

#### 根の 線は 実測する（`SBACK`）

base の 上の 輪郭を 1px きざみで はかり、その **谷**（＝いまの 小さな 板の
根もと）を なぞった 13点です。棘の 高さは `sRootY(x)`（**棘の 中心では なく
その場の x**）から とるので、**傾いた 背中にも 沿います**。

#### 先は「線を ならして」まるめる

`stroke-linejoin="round"` は **1px ほどしか 効きません**。とがった ままだと
この子だけ するどく なって、base の まるい 板と 別ものに 見えます。
上の 線に σ2.4 の ガウスを かけて まるめます。

#### 数と 大きさ ——**細かくしない**

8本（ピッチ 13px）で 作ったら、36px で **ぎざぎざが ぼやけて「ざらざら」**に
なりました。**本数を ふやすのでは なく、主要な 棘を 大きく する**こと。

| | ピッチ | 36px での ピッチ | |
|---|---|---|---|
| 8本 | 13px | 1.8px | ぼやける |
| **6本** | **16〜19px** | **2.3〜2.7px** | **採用** |

いまは `[132,18] [115,35] [96,46] [77,38] [58,44] [42,24]`
（[根のx, たかさ]・半はば 9〜13・かたむき −3〜−6）。
**高さを 単調な 山に しないこと** ——18→35→46→38→44→24 と
中で 一度 下げて います。

#### いちばん 高い 棘でも **頭より 下**に とどめる

てっぺんは y=70。頭の てっぺんは y=40 なので、**主役は 頭の まま**です
（「本体より 棘が 主役に ならない」）。

#### 左はしは x=31 で 止める

x=30 に 7本目を 置いたら、尾が 左へ 曲がって いく ところで
**稜線の 下の へりが 体から はみ出して、尾の 外がわに 細い すじ**が
出ました（bbox も 左へ 3px のびた）。尾の 外には base の 小さな 板が
つづいて いるので、**大きい 棘は 背中で 止めて よい**。

#### 外形は **1pxも 広がって いません**（それでよい）

| | base | e1 |
|---|---|---|
| bbox | 25..230 / 40..216 | **まったく 同じ** |
| 面積（256px）| 18,162 | 20,308（**+11.8%**）|
| 重心 | (134.4,131.3) | (128.8,128.5) |

Batch 1 の 8体は 外形が +6〜+69% のびましたが、この子は **+0%**です。
稜線が base の 頭の 高さと 体の はばの 内がわに 収まる ためで、
**それでも 36px で ギザギザは はっきり 読めます**
（量産ルールの ⑤「必要な ぶんだけ」の とおり。**そろえない**）。

#### 尾の 先の 炎は **落としました**

Batch 2 の 設計表では secondary を「尾の 先の 炎」に して いましたが、
**場所が ありません**。うしろあしが `x88..134 / y174..190` を 使って いるので、
尾の 先の まわりで 空いて いるのは **`y193..216` の ひくい よこ長の くさび**
だけ です（実測）。

そこに 置いた 炎は **横に ねた 三日月（かぎ）**にしか 見えず、
尾に 生えた 別の ひれの ように なりました（1回 やりました）。
「後ろに 置いた 炎に 見えない」という 採用条件と 正面から ぶつかるので、
**稜線 1本に しぼりました。**

> **足しては いけない もの**（この 子の 進化記号を 弱めます）——
> 尾の 炎／体の まわりの オーラ／浮かぶ 岩や 結晶／棘の 本数を ふやす。

#### 左右反転（`EN_FACE`）でも こわれない

`EN_FACE.salamander = 1`（右向き）なので、左へ 進む あいだは 反転します。
稜線は **背中に 沿った 曲線**だけで、文字も 記号も 向きの ある モチーフも
入って いません。128 / 36 / 24px で 反転させて 確認ずみ。

#### `c_onibi_e1`（おにび）——**小さな 分身が 漂う**

Batch 2 の **3つめの 進化言語**です。

| | シルエット言語 |
|---|---|
| `c_lavagolem` | 体から **はなれた 大きな** かたまり |
| `c_salamander` | 体に **つながって 育つ** 輪郭 |
| **`c_onibi`** | **小さくて じりつした 火が 何個も 漂う** |

| | |
|---|---|
| 本体 | `onibi` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | **子の 鬼火 3体**（頭＋細い 尾。**顔は 描かない**）|
| 数 | **3体。**36px で じゅうぶん 読めた ので 4体目は 足さない（5体以上は きんし）|
| 大きさ | ふくらみの はば **44 / 34 / 28px**（base の つぶは 最大 15x24 なので 2〜3倍）|
| いち | NW(48,50) ／ SW(40,212) ／ SE(214,212)。中心から **137° / 220° / 313°** |
| 色 | `#f7fcfd → #c7d0fb → #9fabe4`・ふち `#8995c7`（ぜんぶ base の 実測。**暖色を つかわない**）|
| 36px | **本体の まわりに 小さな 青い 炎が ふえた**と 読める |

#### **頭と 尾を 1つの パスで** えがく

頭を 描いて 別に 細い 尾を つないだら、**おたまじゃくし**に 見えました
（1回 やりました）。`c_salamander` の 稜線と 同じ 話で、
**1つの パスに して しまう**のが 直しかたです。上は base と おなじ
「まん中の 舌＋左右の 小さな 舌」、下は そのまま 細く のびて 尾に なります。

#### 縦長に しない ——**base と おなじ「ずんぐり」**に そろえる

尾を 長く すると 比が 0.5 前後に なり、**木の葉／羽根／ナイフ**に
見えました（2回 やりました）。base の 比は 179:206＝**0.87** です。
いまの 子は 幅1.30 / 高さ1.90＝**0.68** ——尾が ある ぶん 縦長ですが、
ふくらみが かたまりの 大半を しめるので 炎に 見えます。

**尾を のばすより 舌を 深く する**こと。「炎」を 決めるのは
**上の ぎざぎざ**で、下の 尾では ありません。

#### **確定仕様（2026-09-06）——4つの 群れの 言語を まぜない**

> 正式採用。**この 4つの ちがいは これから 先も たもつこと。**

| | 何が 何個 |
|---|---|
| `ch_queen_e1` | **細かな 粒の 連なり**（1つ1つは 読めない・数十）|
| `ch_prince_e1` | **大きな 同形モチーフの 反復**（四つ葉が 4か所）|
| `c_lavagolem_e1` | **大型で 角ばった 浮遊岩**（5つ・不とうめい・暗い）|
| **`c_onibi_e1`** | **3つの 小さな 独立した 炎**（頭＋尾・ふちあり・うすい）|

固定した もの ——本体 base 1:1／本体画素差 **0**／body shift **0**／
子鬼火 **3体**（4体目を 足さない）／配置は NW・SW・SE の **非対称**／
主役は **小さな 独立した 炎の 群れ**／色は base 実測の 青〜藤系／
**暖色 0**。

#### 円にも 左右対称にも 等間かくにも しない

`313° → 137°` の **184°を まるごと 空けて** います。そこには base 自身の
つぶが 4つ（(162,53) (172,37) (211,99) (214,114)）あるので、
**子は 左と 下、base の つぶは 右と 上**で つりあいます。

#### 子は base の つぶと **ふちで** 見わける

base の つぶ（7個・最大 15x24）は **ふちが ありません**
（実測：ふちの 画素も `#c5c7f1` で 中みと ほぼ 同じ）。
子には base の いちばん こい ふち `#8995c7` を 3.2px で 引いて、
**大きさと ふちの 2つ**で 別ものに します。

**色を こくして 目立たせようと しないこと。**この子は 世界ぜんたいが
うすい 青〜藤なので、こくすると 別の キャラに なります。24px で
うすく 見えるのは 正しい ——それでも base の つぶより 大きいので、
**星くずでは なく 火**に 読めます。

#### 外形（`c_salamander` と ならべて 見る）

| | 面積 | はば | たかさ |
|---|---|---|---|
| `c_salamander_e1` | +11.8% | **+0%** | +0% |
| `c_onibi_e1` | +17.6% | +16.8% | +6.3% |

**そろえません**（量産ルール ⑤）。稜線は 体の 内がわに 収まり、
漂う 群れは 外へ 出る ——のびかたが ちがうのが 正しい。

#### `c_lavasnail_e1`（ようがんカタツムリ）——**殻の うずが 外へ のびる**

Batch 2 の **4つめの 進化言語＝うず（回転・巻きこみ）**です。

| | シルエット言語 |
|---|---|
| `c_lavagolem` | はなれた **大きな 岩** |
| `c_salamander` | 体に つながって 育つ **輪郭** |
| `c_onibi` | **3つの 小さな 独立した 炎** |
| **`c_lavasnail`** | **1本の 帯が 巻いて もどる** |

| | |
|---|---|
| 本体 | `lavasnail` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | 殻の 上の 肩から 出て、**ちょうど 1周 巻く 帯** |
| きんし | 炎・オーラ・粒・浮遊物に しない（帯は **ひとつづき**）|
| 色 | 殻 `#c78d94 → #be8088 → #a46b72`・ふち `#986369`／溶岩の すじ `#f7ad7f`・芯 `#fcc78e`（ぜんぶ base の 実測）|
| secondary | 火の粉 **3つ だけ** |
| 36px | **殻から 外へ 大きく 巻く うず**と 読める |

#### **確定仕様（2026-09-06）——Batch 2 の 4つの 言語**

> 正式採用。**この 4つの ちがいは これから 先も たもつこと。**

| | シルエット言語 |
|---|---|
| `ch_fairy_e1` | **同心円＋放射** |
| `sp_rpurin_e1` | **1方向へ のびる 帯** |
| `c_lavagolem_e1` | **散った 浮遊岩** |
| **`c_lavasnail_e1`** | **巻いて 中心へ もどる 1本の うず** |

固定した もの ——本体 base 1:1／本体画素差 **0**／body shift **0**／
主役は **殻から 外へ 大きく 巻く 1本の うず**／巻き数 **1周**／
帯の はば **22 → 1.5px の 強い テーパー**／
配置は **殻の 肩から 上へ・非対称**／secondary は **火の粉 3つ だけ**。
**halo・とって・1方向の 尾に ならない** いまの 形を たもつこと。

#### まわる 向きは base の みぞに そろえる

base の うずは **外へ 行くほど 右まわり**（画面で 時計まわり）です。
だから 新しい うずも **内へ 行くほど 左まわり**にします。
逆に すると、同じ 絵の 中で 2つの うずが 逆に まわって 見えます。

#### **ちょうど 1周**。1.25周・1.8周は「とじた わっか」に なる

1周ぶんの 半けいの へりは `34 → 13` の **21px**で、帯の はば（6px）より
広いので **みぞが 36px でも 見えます**。周を ふやすと 間かくが
せまく なって 帯どうしが くっつき、**handle（とっての 輪）**に 見えました。

| まき数 | 1周の 間かく | |
|---|---|---|
| 1.8周 | 6.9px | 帯が くっついて ただの かたまり |
| 1.25周 | 15.2px | まだ とじた わっかに 見える |
| **1.0周** | **21px** | **採用** |

#### はばを 一定に しない ——**halo は はばが 一定**

`22 → 1.5px`。ここを ゆるめると、どんなに 曲げても わっかに 見えます。

#### うずの 中心は **頭と 殻の あいだの くさび**

`(132,50)`。base の あきは **右に 30〜46px・上に 32px** しか なく、
殻の まわりを ぐるりと 回る うずは **入りません**
（(167,142) を 中心に すると、上を こえる 半けい 90px は
 右で わくから 出る）。いっぽう y58 で **x99..151 が あいて いる**ので、
そこに 中心を 置きます。ch_fairy の 同心円とは
**中心が キャラの まん中に ない**ことで 分かれます。

#### 3つ 作って 2つ 捨てた

| 案 | どう 見えたか |
|---|---|
| 殻の 右を まわって 上へ ぬける 大きな 帯 | **やかんの とって**。はばが ほぼ 一定で、右が まっすぐ だった |
| すきまを 広げて もう一度 | やはり **とって**。殻の まわりを 一定の すきまで 回ると halo に なる |
| **殻の 肩から 出て 1周 巻く**（採用）| うずに 見える |

#### 外形（ここまでの Batch 2）

| | 面積 | はば | たかさ |
|---|---|---|---|
| `c_salamander_e1` | +11.8% | +0% | +0% |
| `c_onibi_e1` | +17.6% | +16.8% | +6.3% |
| `c_lavasnail_e1` | **+7.8%** | **+0%** | **+17.4%** |

**そろえません**（量産ルール ⑤）。よこは 1pxも 広げず、**上へ だけ** のびます。

#### `c_ghost_e1`（おばけ）——**重なった うすい ヴェールが 下へ 広がる**

Batch 2 の **5つめの 進化言語＝半とうめいの 層**です。

| | シルエット言語 |
|---|---|
| `sp_rpurin` | **1方向へ のびる 帯** |
| `ch_donut` | 連続した **水流**（水滴・波紋・水色）|
| `c_onibi` | 小さな **分身の 群れ** |
| **`c_ghost`** | **左右へ 広がりながら 下へ 波打つ 層** |

| | |
|---|---|
| 本体 | `ghost` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | **下へ 広がる 波打つ すそ**（3層）|
| きんし | 粒を まわりに 散らさない（`c_onibi` と 重なる）／長い 1方向の 尾に しない（`sp_rpurin`）／水滴・波紋・水色（`ch_donut`）／発光・halo（`ch_fairy`）|
| 色 | `#fdf7f4 → #f6e6f1 → #dcd2ef`・ふち `#d1acc0`（ぜんぶ base の 実測）|

#### **確定仕様（2026-09-06）——Batch 2 の 5つの 言語**

> 正式採用。**この 5つの ちがいは これから 先も たもつこと。**

| | シルエット言語 |
|---|---|
| `sp_rpurin_e1` | **1方向へ のびる 帯** |
| `ch_donut_e1` | 連続した **水流** |
| `c_onibi_e1` | **3つの 小さな 独立した 炎** |
| **`c_ghost_e1`** | **本体の 下から 左右へ 広がる、重なった うすい ヴェール** |

固定した もの ——A方式／base 1:1／本体画素差 **0**／body shift **0**／
**3層**（奥ほど 広く・深く・うすい）／
**粒・水滴・波紋・halo・発光を 足さない**／
いまの 下半分の シルエットを たもつ。

> **bbox ぜんたいの はばだけを 進化判定に つかわないこと。**
> この子の 進化の さは **36px / 24px で 下ばしに のこる よこはばと
> 波打つ すそ**です（bbox の はばは 36px で ±0）。

#### **下（奥）ほど 広く・深く・うすく**

逆に して 手前の 1まいを いちばん 広く すると、そこが うしろを
ぜんぶ おおって **すそが 1本しか 見えません**（1回 やりました）。

| 層 | よこ | すその 高さ | 山 | 深さ | ぬり | ふち |
|---|---|---|---|---|---|---|
| 奥 | x21..235 | y218 | 4 | 17 | .30 | .62 |
| 中 | x31..224 | y205 | 5 | 15 | .46 | .74 |
| 手前 | x44..212 | y194 | 3 | 13 | .66 | .86 |

はば・すその 高さ・山の 数・深さ・とうめい度を **1層ずつ ずらす**こと。
同じ 波線を 等間かくで ならべると 布に 見えません。

#### **ぬりと ふちの とうめい度を 分ける**

`opacity` を まとめて かけると **ふちまで うすく なって、白い 地では
すそが 消えます**。`fill-opacity` と `stroke-opacity` を 別に して、
ぬりは うすいまま ふちだけ こく のこします。

#### すその 高さは **本体の 下ばし（y209）より 下**に する

はじめ すそを y186〜214 に 置いたら、まん中が 本体に かくれて
**左右だけ 見える「ひれ」**に なりました（1回 やりました）。
いまは y194 / 205 / 218 で、3本の すそが ぜんぶ 見えます。

#### 36px で 効いて いるのは **下半分の はば**

bbox の はばは 36px で +0%（base も e1 も 30px）ですが、
**下半分は まったく ちがいます**。

| 36px | base | e1 |
|---|---|---|
| y28 | 12px | **30px** |
| y29 | 4px | **30px** |
| y30〜32 | 0px | **30 / 29 / 17px** |

bbox だけ 見て「変わって いない」と 早とちり しないこと。

#### 外形（ここまでの Batch 2・**そろえない**）

| | 面積 | はば | たかさ |
|---|---|---|---|
| `c_salamander_e1` | +11.8% | +0% | +0% |
| `c_onibi_e1` | +17.6% | +16.8% | +6.3% |
| `c_lavasnail_e1` | +7.8% | +0% | +17.4% |
| `c_ghost_e1` | **+23.3%** | +4.9% | +14.0% |

#### `c_firebird_e1`（ひのとり）——**翼を 下外へ 大きく 開く**

Batch 2 の 6つめ。**打ち下ろしの 二重翼**です。

| | |
|---|---|
| 本体 | `firebird` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | **左右2枚の 追加翼**（近い側＝大／遠い側＝小）|
| きんし | 上へ 炎を 足さない（`c_hinotama`）／暗い 影に しない（`c_devil`）／水平の 半とうめい層に しない（`c_ghost`）|
| 色 | `#fcbc95 → #fc9b80 → #fc937e`・ふち `#c4685b`（base の **手前翼**の 実測。尾羽の クリーム `#f9dbb0` は つかわない）|
| secondary | **なし**（尾羽・火の粉・spark は 足さない）|

#### **確定仕様（2026-09-06）——Batch 2 の 6つの 言語**

> 正式採用。**この 4つの ちがいは これから 先も たもつこと。**

| | シルエット言語 |
|---|---|
| `c_hinotama`（予定）| **縦へ のびる 炎** |
| `c_ghost_e1` | **下へ 広がる ヴェール** |
| `c_lavagolem_e1` | **浮遊岩** |
| **`c_firebird_e1`** | **本体に つながった 2枚の 打ち下ろし翼** |

固定した もの ——A方式／base 1:1／本体画素差 **0**／body shift **0**／
主役は **打ち下ろしで 下外へ 大きく 開いた 二重翼**／
**近い翼＝大・遠い翼＝小**（3/4 の 見えかたとして 正しい 左右非対称）／
羽根は **union された ひとつづきの シルエット**／
**尾羽は 変更しない**／secondary なし／火の粉・spark なし／
**上方向へ 炎を ふやさない**。

> **この子は A方式の 大事な 成功例**です。外がわに 独立した かざりを
> 置くのでは なく、**すでに ある 部位（翼）の 延長**として 新しい
> 大きな シルエットを 作る 型が 成りたちました。
> つばさ・みみ・つの・しっぽの 進化に そのまま つかえます。

#### **上へ 開く 姿勢は この わくでは 作れない**

36px に すると base は まん中の 高さで すでに よこ幅の **97%**
（29 / 30px）を つかって います。だから 肩の 高さで 横へ 開いても
**1pxも 増えません**。差が 出せるのは **下1/4（y24..y31）だけ**。
さらに 右上は **尾羽**（x150..232 / y37..175）が ふさいで いるので、
上向きの V字も 作れません。よって 翼は **下外**へ 開きます。

| 36px | base | e1 |
|---|---|---|
| y25 | 15px | **28px** |
| y27 | 10px | **29px** |
| y29 | 8px | **30px** |
| y30 / y31 | 1 / 0px | **32 / 31px** |

#### **放射の 扇に しない**（4回 やって 4回とも 落ちた）

| 作りかた | どう 見えたか |
|---|---|
| 肩から 羽根を 放射させる 扇（くぼみ 深い）| **3本の 長い 帯**。翼に 見えない |
| 同じ 扇で くぼみを 浅く | **破れた 旗** |
| 扇の 羽根を 太く まるく | **手・肉球** |
| 扇を なめらかな こぶに | **雲・クッション** |

正解は **そった 1本の 三日月**（`wingBlade`）でした ——前縁は
なめらかな 弧、後縁だけに こぶ。しかも **こぶは 外がわ 半分だけ**に
かけること（ぜんたいに かけると また 手や 葉に 見えます）。
羽根らしさは **輪郭では なく 中の すじ**（`wingVeins`）で 出します
——base の 手前翼と 同じ 見せかたです。

#### 付け根の へりだけ 別あつかい

`featherWing` の ときに、根の 角（±92°）と 羽根の 先の 角度を
そのまま ならしたら、制御点が 反対がわへ 飛んで **bbox が x0**に
なりました。付け根の 2辺は 制御点を 自分で 置くこと。

#### つながり ——**base の 翼の すぐ 外から 出す**

近い翼は base の 手前翼の 先（**156,170**）の すぐ 外を、
遠い翼は 左の 三日月（**39,180**）の うしろを 通します。
中心線を 肩（116,126 / 46,140）から 始めるので、
**新しい翼だけが うしろに 浮いて 見えません**。

| | 見えて いる ぶん |
|---|---|
| 近い翼（右・大） | **3,388px²**・x131..231 / y102..230 |
| 遠い翼（左・小） | **1,919px²**・x13..52 / y133..227 |

3/4 の 見えかたなので **左右非対称が 正しい**（近い翼が 1.8倍）。

#### 外形（ここまでの Batch 2・**そろえない**）

| | 面積 | はば | たかさ |
|---|---|---|---|
| `c_salamander_e1` | +11.8% | +0% | +0% |
| `c_onibi_e1` | +17.6% | +16.8% | +6.3% |
| `c_lavasnail_e1` | +7.8% | +0% | +17.4% |
| `c_ghost_e1` | +23.3% | +4.9% | +14.0% |
| `c_firebird_e1` | **+23.5%** | +6.3% | +9.2% |

#### `c_hinotama_e1`（ひのたま）——**頭の 炎冠が 太く 高く 育つ**

Batch 2 の 7つめ。`c_firebird` が **横・ななめ下**へ 開く 翼なので、
こちらは **上だけ**へ 育ちます。

| | |
|---|---|
| 本体 | `hinotama` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | **頭の 炎冠**（舌 3本）|
| きんし | 上へ「新しい 炎を 3本 足した」ように 見せない／独立炎を 1つも 置かない（`c_onibi`）／よこに 太らせない（`c_firebird`）|
| 色 | `#fdb98e → #fe9f83 → #f4a081`・ふち `#cd7060`（ぜんぶ base の 上の 舌の 実測）|
| secondary | **なし**（火の粉・spark・halo・追加の 独立炎は 足さない）|

#### **確定仕様（2026-09-06）——Batch 2 の 3つの 炎の 言語**

> 正式採用。**この 3つの ちがいは これから 先も たもつこと。**

| | シルエット言語 |
|---|---|
| `c_onibi_e1` | **離れた 3つの 独立炎** |
| `c_firebird_e1` | **下外へ 開く 二重翼** |
| **`c_hinotama_e1`** | **上へ 成長した 連続した 炎冠** |

固定した もの ——A方式／base 1:1／本体画素差 **0**／body shift **0**／
**よこ幅 ±0.0%**／たかさ +6.8%／主役は **頭頂の 炎冠が 太く 高く 成長**／
新しい 炎舌は **3本**／3本は 独立せず **1つの 連続した 炎冠**に 見える／
**base の 分離炎 2つは そのまま**／新しい 独立炎は **0**／
secondary なし／火の粉・spark・halo なし。

> **高さの 増加率だけで 評価しないこと。**36px の 進化差の 本体は
> **上端 +2行 ＋ 上1/3の 炎の 質量**です。よこ幅を ふやさず
> 上だけが 育って いる いまの 形を たもつこと。

#### **body shift は 0 が 最適**（上へ ずらすと かえって 低く なる）

上へ S px ずらすと、追加炎が つかえる 上の あきも 同じだけ 減るので、
総丈は `221-S`。**ずらすほど 低く なります**（設計フェーズで 実測）。

#### 舌は base の 冠の **山の 上**に 立てる

base の 冠は 山が x82(y68) / x144(y37) / x166(y65)、谷が x96(y78) / x152(y72)。
舌を その 山の 上に 立てると「新しい 炎を 3本 足した」では なく
**「もとの 舌が 育った」**に 見えます。くぼみ（108,46）（152,40）は
**本体の 中**なので かくれ、見えるのは 冠から 立ちあがる ところ だけ です。

#### パラメトリックな union は 4回とも 落ちた

| 作りかた | どう 見えたか |
|---|---|
| 上の 線＋根まで べた塗り | 細い 頭の よこに **たての まっすぐな へり**＝カーテン・板 |
| 根を 太い ままの レンズ | **耳・リボン** |
| くびれつきの レンズ union | **王冠**（たいらな 頂点と 角ばった くぼみ）|
| 舌を せまく 高く | やはり **王冠** |

正解は **ベジエで 直に 書いた 1つの パス**でした。base の 舌と 同じ
「**低い ところで 外へ ふくらむ → くびれる → 先が 内へ そる**」S字を
1本ずつ 書きます。まっすぐな 三角に すると **トゲ・角**に なります。

#### base の 分離炎（2つ）は そのまま

base は もともと 分離した 小さな 炎を 2つ 持って います
（左上 x88..115 / y25..67・右上 x153..168 / y38..65）。
**そのまま のこし、進化の 主役にも しません。**
新しい 分離炎は **1つも 足しません**（`c_onibi` の「独立炎の 群れ」と 分ける）。

#### 36px は「上端 +2行」より **上1/3の 質量**

| 36px | base | e1 |
|---|---|---|
| y2 | 0px | **13px** |
| y3 | 1px | **14px** |
| y4 | 2px | **15px** |
| y5 | 10px | **15px** |
| y6〜y10 | 12〜14px | **17px** |

高さは +6.8%（+14px）しか ふえませんが、**上1/3が 1.2〜13倍**に なります。
**高さの 増加率だけで 見ないこと。**

#### 外形（ここまでの Batch 2・**そろえない**）

| | 面積 | はば | たかさ |
|---|---|---|---|
| `c_salamander_e1` | +11.8% | +0% | +0% |
| `c_onibi_e1` | +17.6% | +16.8% | +6.3% |
| `c_lavasnail_e1` | +7.8% | +0% | +17.4% |
| `c_ghost_e1` | +23.3% | +4.9% | +14.0% |
| `c_firebird_e1` | +23.5% | +6.3% | +9.2% |
| `c_hinotama_e1` | **+15.2%** | **+0.0%** | +6.8% |

#### `c_devil_e1`（あくま）——**背後に 開く 1つの 大きな コウモリ影**

Batch 2 の 8つめ（最後）。**8 / 8 で Batch 2 は 完了**です。

| | |
|---|---|
| 本体 | `devil` を 1:1・画素の ちがい **0**・body shift **0px** |
| 主役 | 本体の 背後に **左右へ 大きく 開く 1つの こい コウモリ影** |
| きんし | 小コウモリ・粒・火の粉・spark・halo・足もとの 影・下へ のばす すそや 霧 |
| きんし | **羽根の 内部線を 入れない**（`c_firebird_e1` と 分ける）|
| 色 | `#63264d → #5c2243 → #522040`（ぜんぶ base の 実測。**真っ黒は つかわない**）|
| 不とうめい | **fill .62 ／ stroke .88**（別に 決める）|
| secondary | **なし** |
| 36px | **背後から 左右へ 開く 大きな 暗い 影** |

#### base は すでに 小さな コウモリ翼を 持って いる

これが A1 を えらんだ 決め手です。**左 x43..88 / 右 x168..213・y135..173**。
だから 影は「別の ものを 貼った」では なく **その 翼の 延長**として 置けます
——`c_firebird_e1` で 確立した 型（外がわに 独立装飾を 置かず、既存部位の
延長として 大きな シルエットを 作る）が そのまま つかえます。

付け根は **上 (86,140) → 下 (104,206)**（左）と、それを x=128 で うつした
**(170,140) → (152,206)**（右）。どちらも **本体の 中**なので かくれます。
base の 小翼は x=128 に ついて 対称（43+213 も 88+168 も 256）なので、
**うつすだけで そろいます**。

#### **確定仕様（2026-09-06）——Batch 2 の「大きな もの」3つの 言語**

> 正式採用。**この 3つの ちがいは これから 先も たもつこと。**

| | シルエット言語 | 不とうめい | 色 | 向き |
|---|---|---|---|---|
| `c_firebird_e1` | **実体の 翼**（羽根の すじ あり）| 不とうめい | 暖色 `#fcbc95`〜`#f4a081` | **下外**へ 打ち下ろす |
| `c_ghost_e1` | **重なった 薄い 層**（3層）| .30 / .46 / .66 | 淡色 `#fdf7f4`〜`#dcd2ef` | **下**へ 波打つ |
| **`c_devil_e1`** | **1枚の 大きな 影**（すじ なし）| **.62** | **暗紫** `#522040`〜`#63264d` | **上外〜横**へ 開く |

`c_lavagolem_e1`（離れた 岩 5つ）とも 分かれます ——影は **本体と つながった
1つの かたまり**で、連結成分は **e1 ぜんたいで 1つ**（左右の 翼は どちらも
本体に くっついて いる）。**独立した パーツに 見えては いけません。**

#### 切れ込みは **2つ**。そして **本体の 外に 置く**

`batShadow` の まど `[lo,hi]` は **0＝翼さき / 1＝付け根**の 位置です。
はじめ `from` 1つで「さきから 72%」に かけて いましたが、谷が
**y132 あたり＝本体の うしろ**に 入って **36px で 1pxも 出ませんでした**。

base の 左右の あきは 行で まるで ちがいます（実測）。

| y | 本体の 左はし | あき |
|---|---|---|
| 64〜102 | 49〜52 | ひろい |
| **110〜158** | **33〜44** | **いちばん せまい** |
| 174〜206 | 63〜83 | ひろい |

いまの まど `lo .30 / hi .78` の 谷は **(26,174) と (48,200)**。
どちらも 本体の 外なので 36px に 出ます。

#### 深さは **22 が 上限**。24 で 翼が ちぎれる

せまい ところ（y110〜158）で 内へ 切ると、**翼が 本体の うしろに
入りきって 見える ぶんが 分断されます**。実測（見える 影の 連結成分）——

| notch | 見える 影の かたまり | |
|---|---|---|
| 18 / 20 / 22 | **2つ**（左翼・右翼）| OK |
| **24** | **4つ** | **ちぎれた** ——`c_lavagolem` の 独立パーツに 見える |

**深さを 上げても 36px の 読みは 良く なりません**（18/20/22 で
プロファイルが ほぼ 同じ）。谷の 場所の ほうが 効きます。

#### 36px で 読める 谷は **1つだけ**。それでよい

設計フェーズで 2 / 3 / 4個を 測って、**外がわの 自由な へりに 出るのは
1〜2個まで**と 分かって います（4個は 平ら）。いまは 谷を 2つ 刻んで、
36px では 大きい ほうが はっきり 1つ 読めます。
**本数を ふやさないこと** ——`c_salamander` の「本数を ふやすのでは なく
主要な 棘を 大きく する」と まったく 同じ 話です。

#### 翼さきを とがらせすぎない

`T` を (15,98) から (14,84) へ 上げて とがらせたら、せまい ところを
通る 材料が へって **また 4つに ちぎれました**（1回 やりました）。
いまの まるい さきの ままに すること。

#### ぼかしは かけて いない

「36px で 翼形が 明確なら sharp を 優先」の とおり、**`filter` を
1つも つかって いません**。だから わくの ふちで 切れる 心配も ありません
（bbox x13..242 / y25..230 ——安全わく x11..244 の **2px 内がわ**）。

#### 実測

| | |
|---|---|
| base bbox | x42..213 / y25..230（172x206）|
| e1 bbox | **x13..242 / y25..230（230x206）** |
| 影の bbox | **x13..242 / y62..209（230x148）** |
| 影ぜんたい | 16,594px² |
| **本体から 見える 影** | **9,792px²（59.0%）** ——左 5,039（51.5%）/ 右 4,753（48.5%）|
| 面積 | 24,853 → 34,645（**+39.4%**）|
| 連結成分 | e1 ぜんたいで **1つ**／見える 影は 左右の **2つ** |

#### 36px / 24px

| 36px 行 | base | e1 |
|---|---|---|
| y8 | 18 | **28**（+10）|
| y10〜12 | 20 | **32〜33**（+12〜13）|
| **y13〜14** | 20 | **34（+14）** |
| y15〜18 | 22〜23 | **34**（+11〜12）|
| y20〜22 | 24〜25 | **32**（+7〜8）|
| y25 | 16 | **26**（+10）|
| y27〜29 | 17 | **21**（+4）|

#### 合格条件は **最大幅では なく 行の 数**（固定）

> **最大幅だけで 評価しないこと。**この子の 進化差の 本体は
> **36px で y8〜y29 の 22行が base より 広がって いる**ことです。
> ここを 指標として のこします。
>
> **24px は 切れ込みの 数が 読めなくて かまいません。**
> 「**左右へ 大きく 開いた 暗い 影**」が のこって いれば 合格です
> （いまは y5〜y19 が +1〜+8・最大 22/24列）。

#### 左右反転

影は x=128 に ついて 対称なので、うつしても こわれません
（`EN_FACE` に `devil` は のせて いない ——正面の子）。128 / 36 / 24px で 確認ずみ。

#### 外形（Batch 2 ぜんぶ・**そろえない**）

| | 面積 | はば | たかさ |
|---|---|---|---|
| `c_salamander_e1` | +11.8% | +0% | +0% |
| `c_onibi_e1` | +17.6% | +16.8% | +6.3% |
| `c_lavasnail_e1` | +7.8% | +0% | +17.4% |
| `c_ghost_e1` | +23.3% | +4.9% | +14.0% |
| `c_firebird_e1` | +23.5% | +6.3% | +9.2% |
| `c_hinotama_e1` | +15.2% | +0.0% | +6.8% |
| **`c_devil_e1`** | **+39.4%** | **+33.7%** | **+0.0%** |

`c_devil` は Batch 2 で **いちばん よこに 広がり、たては 1pxも 伸びません**。
`c_hinotama`（たてだけ）と ちょうど 逆で、8体の のびかたが ばらけて います。

#### Batch 2 は **8 / 8 完了**（2026-09-06）

8体とも「同じように 豪華に する」のでは なく、**進化の 意味そのもの**が
別々に なって います。ここが Batch 2 の いちばん よい 成果です。

| | シルエット言語 |
|---|---|
| `c_lavagolem_e1` | **離れた 浮遊岩** |
| `c_salamander_e1` | **体に つながって 育つ 稜線** |
| `c_onibi_e1` | **3つの 小さな 独立した 炎** |
| `c_lavasnail_e1` | **巻いて 中心へ もどる うず** |
| `c_ghost_e1` | **重なった 薄い ヴェール** |
| `c_firebird_e1` | **本体に つながった 実体の 翼** |
| `c_hinotama_e1` | **上へ 育つ 炎冠** |
| `c_devil_e1` | **1枚の 大きな 魔影** |

### Phase 7-7-3-8-3 ——DEX_ART系と TOWER系に 本物の e1（2枚）

**コードの 変更は `ART_KEYS` に 2語だけ**です。
`evoArtKey` / `artKeyFor` / `drawPortrait` / `charThumb` /
`buildCharDetail` / `evolveInst` / `tryEvolve` は **1文字も さわって
いません** ——`1aad036` の resolver が そのまま 拾う ことを
証明する フェーズ です。

| 種の ID | base の キー | ゲームが 読む もの |
|---|---|---|
| `ch_choco`（かみなりのこ・DEX_ART系）| `kaminari` | `art/sprites/ch_choco_e1.png` |
| `tw_ice`（アイス・TOWER系）| `icecream` | `art/sprites/tw_ice_e1.png` |

これで **GEN（`c_purin`）・DEX_ART（`ch_choco`）・TOWER（`tw_ice`）の
3経路 ぜんぶ**が 本物の asset で 通りました。

#### 種の ID で 分かれる ことを 本物で 見る

`tw_ice` の base は `c_icecream`（GEN の アイス★3）と **共有**です。
`ch_choco` は `c_choco` / `tw_choco` と 名前が 似て います。

| | base | e1 |
|---|---|---|
| `ch_choco` | `kaminari` | **`ch_choco_e1`** |
| `c_choco` | `choco` | `choco`（専用assetが ないので base）|
| `tw_choco` | `choco` | `choco`（同上）|
| `tw_ice` | `icecream` | **`tw_ice_e1`** |
| `c_icecream` | `icecream` | `icecream`（**`tw_ice_e1` を 拾わない**）|

```
node tools/check-evo-key.mjs      # 終了コード 0 で 合格
```

#### 絵の 中み

どちらも **base を 1:1 で おいて、すきとおる 余白に かざりを 足した**
ものです（本体の 画素の ちがい **0件**）。

| | かざり | つかった 余白 |
|---|---|---|
| `ch_choco_e1` | 頭上に 電光の 冠（5本）＋左右に 大きめの いなずま 2つ | 上（本体を 16px 下へ ずらして 58px 作った）|
| `tw_ice_e1` | 左右に 大きめの 氷の けっしょう＋小さい かけら | よこ（58px ずつ）|

- 色は **base から 拾う**こと。冠の 金は base が 持って いる
  いなずまの 金（`#e0af53`〜`#fffdf0`）、けっしょうの 水色は
  アイスの すくいの 色（`#93cfe6`〜`#ffffff`）
- **小さい 点を たくさん 散らさない。**36px で 読めるのは
  **シルエットの ちがい**だけ です（丸い 雲 → ぎざぎざの 冠／
  細い コーン → 左右に ひろがる 氷）
- 背景・地面・文字・数字・記号は **足しません**（「絵に していい ものの
  ものさし ——文字が 入るなら だめ」）

#### 実測

| | 本体 | ぜんたい | 中心ずれ | 本体の 画素 |
|---|---|---|---|---|
| `ch_choco_e1` | 206x172（base の 100% / 100%）| 222x220 | 0.002 | 24,006件 … ちがい **0** |
| `tw_ice_e1` | 140x206（100% / 100%）| 231x218 | -0.004 | 21,050件 … ちがい **0** |

どちらも 256x256・8bit・**カラータイプ6（RGBA）**・透過。
**2回 走らせても SHA-256 が 同じ**（`ce145f8c…` / `15fc3822…`）。
`c_purin_e1` は `7f31c0b0…` の まま 1バイトも 変わって いません。

#### 「キーが 合って いる」だけでは 足りない

ゲームが 読んだ 絵を **リポジトリの PNG と 画素で** つきあわせます。

| | 不とうめいな 画素 | RGBの ちがい | アルファの ちがい |
|---|---|---|---|
| `ch_choco_e1` | 28,690 | **0** | 0 |
| `tw_ice_e1` | 25,829 | **0** | 0 |
| `c_purin_e1` | 32,044 | 598（最大差 **1**）| 0 |

プリンだけ ±1 出るのは canvas の かけざんの まるめです
（8-1 で 見た とおり。**この絵は アルファの 最大が 254** なので、
`alpha === 255` で 数えると **0件に なって 検査が 空ぶりします**
——`>= 250` で 見ること）。

#### 詳細画面で 端から 端まで

| | |
|---|---|
| 未進化（4体）| `1aad036` と **画素 完全一致** |
| `evo=e1` | かみなりのこ・アイスだけ 絵が 変わる。プリン・クマは そのまま |
| **ほんとうに 進化させる** | ボタン → 「はい」で 秘薬 −1・ボタンが 消える・「すがた＝進化ずみ」|
| **読みなおし** | 同じ e1 の 絵（`sweetTD.inst` に `evo:'e1'`）|

#### check-chars は 特例を 1つも 足して いない

8-2 で **すじで 見る**形に して あるので、2枚 足しても
`check-chars.mjs` は **1文字も 直さずに 通りました**
（しっぽが `EVO_IDS` ／ あたまが ほんとうに ある 種の ID ／
`ART_KEYS` に ある ／ ファイルが ある）。

4とおりで 落ちる ことを 確認ずみ ——`kaminari_e1`（古い 名前）／
`ch_choco_e9`（EVO_IDS に ない）／ ファイルを 隠す ／
`ART_KEYS` から 外す。

### Phase 7-7-3-8-1 ——はじめての 進化の 絵（`purin_e1`）

**コードの 変更は `ART_KEYS` に `'purin_e1'` の 1語だけ**です。
`evoArtKey` / `artKeyFor` / `drawPortrait` / `buildCharDetail` /
`evolveInst` / `tryEvolve` は 1文字も さわって いません
——**既存の resolver が そのまま 拾う**ことを 証明する フェーズ です。

#### 絵の 置き場は 2つ（既存の きまりの まま）

| | |
|---|---|
| 原画 | `art/purin_e1.png`（1254x1254・**さわらない**）|
| ゲームが 読む もの | `art/sprites/c_purin_e1.png`（**256x256**・RGBA・78KB）|

`art/` 直下は `tools/_files.mjs` の KEEP に 入って いないので、
**原画は アプリにも オフラインの キャッシュにも 入りません**
（`www/art/purin_e1.png` は 無い ことを 確認ずみ）。
`art/sprites` は ディレクトリで 拾うので、**新しい png は 自動で 入ります**。

#### 1枚 置くだけでは 拾えない ——`ART_KEYS` が ゆいいつの もと

読みこみは `for (const k of ART_KEYS){ … im.src = 'art/sprites/' + k + '.png'; }`
の **1ループだけ**です。だから

- ファイルを 置いて `ART_KEYS` に 足しわすれる → `check-chars` の
  **「絵の とりこぼし」**が 落とす
- `ART_KEYS` に あって ファイルが 無い → 404 で `ART_SPRITE` に 入らず、
  `evoArtKey` が base へ fallback する（**画面は こわれない**）

#### 進化の 絵の 検査は **名前の ならびに しない**

特例リストは ふえる たびに ふくらんで、いつか だれも 直さなく なります。
だから `check-chars` は **すじが 通って いるか**を 見ます。

| | |
|---|---|
| ① | しっぽが `EVO_IDS`（いま `e1`）に ある 進化IDか |
| ② | base の キーが `ART_KEYS` に いるか（元の子が いない 進化は ありえない）|
| ③ | ほんとうに ファイルが あるか（`ART_KEYS` にだけ ある ＝ 404）|
| ④ | base が **共有キー**（`candy` / `star` / `icecream` / `choco`）で ないか |

④が 大事です ——この 4つは GEN と お菓子タワーで base を 分けあって いるので、
`candy_e1` を 1枚 置くと **両方が 同時に 進化して 見えます**（8-0 の 調査）。

5とおりで 落ちる ことを 確認ずみ ——共有キーに evo を 足す ／ ファイルの ない
evo を 足す ／ `EVO_IDS` に ない しっぽ ／ base が `ART_KEYS` に ない ／
`purin_e1` を `ART_KEYS` から 外す。

#### 架空assetの 検査は `try` / `finally` で 完全復元

`check-chdetail.mjs` は `ART_SPRITE[base+'_e1']` に その場で むらさきの
canvas を 入れて ためしますが、**`delete` では 本物を こわします**。
もとの 値を おぼえて **`finally` で かならず もどす** 形に しました
——いまの 対象（`bear` / `candytree` / `candy`）が 本物の `_e1` を
持って いなくても、**将来 足された ときに こわれません**。

#### 実測

| | |
|---|---|
| `artKeyFor(プリン, null)` | `purin` |
| `artKeyFor(プリン, 'e1')` | **`c_purin_e1`**（fallback では なく 実 asset）|
| ほかの子（クマ・ケーキ・ウサギ・タワー）に `'e1'` | ぜんぶ base の まま |
| `ART_KEYS` | 159 → **160**件。`_e*` は `c_purin_e1` の 1件だけ |
| キャラ数 | **128 のまま** |

**「キーが 合って いる」だけでは 足りません。**詳細画面が えがいた 画素を
リポジトリの `art/sprites/c_purin_e1.png` と つきあわせて、
**不とうめいの 32,044画素が ぜんぶ ±1 以内**である ことまで 見ました
（±1 は canvas が アルファを かけた 形で 持つ ための まるめ。
 半とうめいの ふち 1,021画素は のぞく）。

詳細画面の 絵を `toDataURL` で くらべて

- **変更前 ↔ 変更後（evo なし）**：7件とも **画素 完全一致**
- **evo なし ↔ `e1`**：**プリンだけ 変わり、ほかの 6件は 完全一致**
- 進化 → 保存 → 読みなおし：`evo=e1`・秘薬 2→1・**絵も 画素 完全一致**
- 秘薬を 2こ 持って いても **未進化なら base**

### Phase 7-7-3-8-4-C1 ——進化の 絵を **非戦闘UI**へ ひろげた（2026-09-09）

> **記録（2026-09-09）**：C の 通し確認で「**27枚 ぜんぶが 詳細画面 1か所でしか
> 見えて いない**」ことが 分かったので、**新しい 絵を 1枚も 作らずに**
> 表示先の ほうを ひろげました。

#### それまでは 詳細画面 だけだった

`instEvoOf()` を 絵に わたす 本番の ところは **`buildCharDetail` の 1行だけ**でした。
resolver（`artKeyFor` / `evoArtKey`）は 3つの 道 ぜんぶに 入って いたので、
**足りなかったのは 引数 1つ**です ——「`artKeyFor` を 通って いない」のでは なく
「**evo値を わたして いない**」。カナリア（base をマゼンタ・e1 をシアンに 差しかえ）で
2引数の 呼びかたは ぜんぶ base、evo を わたした ものだけ e1 に なる ことを 実測しました。

#### ひろげた のは **非戦闘UI の 5系統だけ**

| | 呼びかた | 大きさ |
|---|---|---|
| ① 図鑑一覧カード（`lazyThumb`）| `genThumb(o.gen, size, evo)` ／ `charThumb(o, size, evo, o.id)` | 64px |
| ② 仲間を選ぶ | `genThumb(o, 56, evoOfSpecies(o.id))` | 56px |
| ③ ショップバーの 札（`buildShop`）| `charThumb({pid:'tw_'+k}, 46, evoOfSpecies('tw_'+k), 'tw_'+k)` | 46px |
| ④ 選択中の 札（`syncShop`）| `genThumb(TOWERS[selTool].gen, 42, evoOfSpecies(...gen.id))` | 42px |
| ⑤ 情報カード（`openInfo`）| `genThumb(T.gen, 46, ...)` ／ `charThumb({pid:'tw_'+t.k}, 31, ...)` | 46 / 31px |

**盤面（`drawTower` / `drawEnemy`）は base の まま**です ——C2 の しごと。

#### 種から 進化を 読む 口は **1つ・読むだけ**（`evoOfSpecies`）

```
function evoOfSpecies(sp){
  if (!sp) return null;
  const iid = instOfSpecies(sp);
  return iid ? instEvoOf(iid) : null;
}
```

**`ensureInst()` を 呼びません。**見るだけで 個体が でき、セーブまで 変わります
（`detailIid` と まったく 同じ 理由）。`dex` も 書きかえず、`saveInst()` も しません。
**個体が 無ければ `null` ＝ これまでどおり base。**
実測：何回 呼んでも 個体数は 変わらず（6 → 6）、`sweetTD.inst` の 中みも 同じ。

#### **いまは 1種＝1個体 だから 一意に 引ける**

`MINT_MULTI` が から の あいだは、`instOfSpecies()` が かえす 個体は **その種で
ただ 1つ**です。だから「この種の すがた」が 曖昧に なりません。
**複数所持を 開けたら ここは 再設計が いります** ——一覧に「どの 個体の すがたを
出すか」を 決める 場所が 要る ためで、`evoOfSpecies` を そこへ 差しかえます
（呼ぶ 側は 1行も 直りません。`detailIid` と 同じ 逃げ道）。

> **詳細画面は これまでどおり 個体（`detailIid` → `instEvoOf`）から 読みます。**
> 一覧は 種から、詳細は 個体から ——**入れかえないこと。**複数所持を 開けた とき
> どちらも まちがえます（`check-evo-surfaces` が 見はって います）。

#### **てきには 自分の 進化を 出さない**

盤面の てき（`e.gi`）は **世界の てき**で、プレイヤーの 個体では ありません。
27枚の うち **14枚は てき側の 種**（`side:'en'`）で、`TOWERS` にも 登録されません。
だから `drawEnemy` へ evo を 流しては いけません
——てき側の 種を 進化ずみに しても 盤面の 絵が 1pxも 変わらない ことを 実測ずみ。

#### キャッシュ

`genThumb` / `charThumb` は **呼ぶたびに 新しい canvas** を 作るので、
5系統に キャッシュは ありません（**キーの 衝突が そもそも 起きない**）。
`genSpriteCache` / `rbCache` は もともと `id@px@evo`。
**`pcache` / `gdPcache`（`pid` だけの キー）は C1 の 対象外**なので、
**先回りして 改修しません** ——つなぐ ときに キーを 直すこと。

#### 小サイズは 9体で 重点確認

C で 全身IoU が 高かった 9体（`c_tower` `c_lavasnail` `lg_chocoknight`
`c_salamander` `c_redpanda` `c_snowqueen` `ch_apple` `c_hinotama` `c_octopus`）を
**64 / 56 / 46 / 42 / 31px** で 見て、**5つの サイズ すべてで 画素が 変わる**
（31px でも 20〜131画素）ことを 確認。**差が 小さいからと いって 絵を
描きなおしては いけません** ——採用は 部位ごとの 質量で 決めて います。

#### 検査

```
node tools/check-evo-surfaces.mjs      # 終了コード 0 で 合格
```

5つ 見ます ——①5系統が evo を 受けとる ②詳細画面を こわして いない
③てきへ 流して いない ④表示の 処理が `ensureInst()` を 呼ばない
⑤**EVO_LOCK が 1件も 変わって いない**。6とおりで 落ちる ことを 確認ずみ。

> **`check-chars` の「進化の 絵の caller」は 前提を 書きかえました。**
> 7-7-3-6 では「わたすのは 1か所だけ」でしたが、いまは **ゆるした 窓
> （詳細画面＋5系統）の 中だけ**か を 見ます。**回帰では なく 前提の 変更**です
> （7-4・7-6 で 2回 やった のと 同じ 形）。**盤面と てきへ 流すと 落ちます。**

### Phase 7-7-3-8-4-C2 ——進化の 絵を **盤面の 自分がわの タワー**へ（2026-09-09）

C1 で 非戦闘UI が e1 に なりましたが、**いちばん 長く 見て いる 盤面が
base の まま**でした。C2 で なかまと お菓子タワーを つなぎます。
**新しい e1 画像は 1枚も 作って いません。**

#### 変えたのは `drawTower` の 中だけ（4か所）

```
const tSp  = T.gen ? t.k : 'tw_' + t.k;                       // 種の ID
const tEvo = evoOfSpecies(tSp);                               // 進化した すがた（無ければ null）
const tArt = T.gen ? T.art : evoArtKey(T.art, tEvo, tSp);     // お菓子タワーの 絵の キー
```

| どこ | 前 | 後 |
|---|---|---|
| なかまの 絵 | `genSprite(T.gen, px)` | `genSprite(T.gen, px, tEvo)` |
| なかまの にじいろ | `rbSprite(sp, px, 'g_'+id)` | `rbSprite(sp, px, 'g_'+id, tEvo)` |
| お菓子タワーの 絵 | **`ART_SPRITE[T.art]`（生読み）** | `ART_SPRITE[tArt]` |
| お菓子タワーの にじいろ | `rbSprite(sp, px, 'a_'+T.art)` | `rbSprite(sp, px, 'a_'+tArt)` |

#### `ART_SPRITE[T.art]` の 生読みは **resolver を 通らない ゆいいつの 穴**だった

Phase C の 調査で 見つけた ものです。7-7-3-6B で `drawPortrait` は
`evoArtKey` を とおる ように なりましたが、**盤面は そのまま**でした。

生読みは 4か所 ——絵を はる ところ 1つと、**はね・だい**（`ようせいのはね`）と
**むねのリボン**を 出すかの **ガード 2つ**、それに `drawPortrait` の ぶん。
**片方だけ 直しては いけません** ——絵と かざりで 見る キーが ずれます。
だから **1か所で 解決して（`tArt`）、3つとも それを 見ます**。

> `evoArtKey` は「絵が ある ときだけ 進化の キー、無ければ base」を かえすので、
> **ガードの 真偽は 1つも 変わりません**（`ART_SPRITE[tArt]` が 真なのは
> `ART_SPRITE[T.art]` が 真な ときだけ）。

#### 種の ID は **かならず 明示して わたす**

`candy` / `star` / `icecream` / `choco` の 4枚は GEN の 子と お菓子タワーで
base を 分けあって います。だから **`base + '_e1'` の fallback を
復活させては いけません**（Phase 7-7-3-8-2）。

| 盤面の タワー | 種の ID | 出る 絵 |
|---|---|---|
| `ice`（お菓子タワー・進化ずみ）| `tw_ice` | **`tw_ice_e1`** |
| `c_icecream`（なかま・未進化）| `c_icecream` | `icecream` |

**同じ 画面に 2体 ならべて 実測**しました ——お菓子タワーだけ e1、
なかまは base の まま。共有base の 取りちがえ **0件**です。

#### てきには ぜったいに 流さない

盤面の てき（`e.gi`）は **世界の てき**で、プレイヤーの 個体では ありません。
`drawEnemy` は `genSprite(e.gi, px)` の **2引数の まま**です。

27枚の うち **14枚は てきがわの 種**なので、ここを つないだら
「自分が 進化させた 子が てきとして 出てくる」ことに なります。

検査は **14体 ぜんぶを 進化ずみに して、base を あか・e1 を みどりに
差しかえ**、W60 / 150 / 190 / 230 を 走らせて 見ます
（あか 47,708画素 ＝ カナリアは 効いて いる／**みどり 0画素**／
`@e1` の キャッシュキー **0件**）。

> **タワーが 1体でも のこって いる 画面で てきを 測っては いけません。**
> タワーの 仮色（みどり）を てきの ぶんと 数えて しまいます
> （じっさい 18,444画素 出ました）。**まっさらな ページ**で 見ること。

#### キャッシュキーは base と e1 で 分かれる

| | base | e1 |
|---|---|---|
| なかま | `c_witch@48` | **`c_purin@48@e1`** |
| なかまの にじいろ | `g_c_witch@48` | **`g_c_purin@48@e1`** |
| お菓子タワーの にじいろ | `a_candy@48` | **`a_tw_ice_e1@48`** |

お菓子タワーは **tag に 解決ずみの 絵の キーが 入る**ので、evo を
わたさなくても 分かれます。**`pcache` / `gdPcache` は 1文字も さわって いません。**

#### 進化した その場で **札・情報カード・図鑑の カードも 作りなおす**

盤面は 毎フレーム えがきなおすので すぐ e1 に なりますが、
**ショップバーの 札・情報カード・図鑑の カードは ひらいた ときに 1回**
しか 作られません。C2 の 前は 盤面も base だったので そろって いましたが、
**C2 で 盤面だけ 先に e1 に なると、同じ子が
「盤面＝e1 ／ 札＝base」に 見えます**（実測）。

だから `tryEvolve` が `'ok'` を かえした ときだけ
`buildShop()` / `syncShop()` / `openInfo(selTower)` / `buildColGrid()` を
呼びなおします。**`tryEvolve` の 中では やりません** ——writer と UI の
責任を 分けた ままに する ため（7-7-3-4）。

| | 盤面 | ショップバーの 札 |
|---|---|---|
| 進化まえ | base | base |
| **進化した ちょくご** | **e1** | **e1** |
| 読みなおし | e1 | e1（絵が 画素まで 一致）|

#### セーブの かたちは 1バイトも 変えて いない

- `towers.push({ k, c, r, lv, cd, ang, pulse, ph })` … **`iid` を 足さない**
- `saveRun` の `tw` … `{ k, c, r, lv }`（にじいろは `+ rb, rbx`）。
  **`evo` を 複製保存しない**

進化は `sweetTD.inst` が 正本です。run の セーブに 写すと、
**同じ ことを 2か所に 書く**ことに なって かならず ずれます。

#### **1種＝1個体だから 成りたつ 暫定設計**

```
evoOfSpecies(t.k)   ←  いまは instOfSpecies() が 一意に 引ける
```

`MINT_MULTI` が から の あいだは、その種の 個体は ただ 1つ なので
「この タワーの すがた」が 曖昧に なりません。

> **複数所持を 開けた ときは ここを 置きかえます。**
> `towers[i].iid` と `saveRun.tw[].iid` が 要るように なり、
> **`evoOfSpecies(t.k)` に たよる 盤面の 表示は つかえなく なります**
> （同じ 種の 個体A と 個体B が ちがう すがたを 持てる ため）。
> **C2 では 実装しません。**

#### 検査

```
node tools/check-evo-board.mjs      # 終了コード 0 で 合格
```

8つ 見ます ——①なかまが evo を 受ける ②お菓子タワーが generic な
resolver を とおる ③てきは evo を 見ない ④`ART_SPRITE[T.art]` の
生読みが 消えて いる ⑤`base + '_e1'` の fallback を 作って いない
⑥盤面から `ensureInst()` を 呼ばない ⑦tower と run の セーブに
`iid` / `evo` が ない ⑧EVO_LOCK 27件 SHA 変化 0。

ソースだけでは 足りないので、**盤面の 画素**でも 見ます
（`c_purin`→e1 ／ `c_witch`→base ／ `c_icecream`→base ／
`ice`→e1 ／ `candy`→base）。9とおりで 落ちる ことを 確認ずみ。

> **`file://` では 見られません。**キャラの 絵で canvas が よごれて
> `getImageData` が 例外に なるので、検査が その場で HTTP に 出します。

> **`check-chars` の 2つの 行は 前提を 書きかえました。**
> 「絵の resolver」は 7-7-2 の「ゲームからは evo を わたさない」を
> **`drawTower` の 中だけ ゆるす**形に、「進化の 絵の caller」は
> 盤面を 合法の 窓に しました。**回帰では なく 前提の 変更**です
> （7-4・7-6・C1 と 同じ 形）。**`drawEnemy` は これまでどおり 落ちます。**

#### 大きさと 速さ

| 画面 | CELL | なかま（ちがう画素）| お菓子タワー |
|---|---|---|---|
| 320x568 | 24 | 24px で **54.7%** ／ 40px で 50.3% | 24px で **24.3%** ／ 40px で 19.6% |
| 393x852 | 41 | 40px で 50.3% ／ 64px で 48.3% | 40px で 19.6% ／ 64px で 17.6% |
| 430x932 | 45 | 48px で 50.5% ／ 72px で 48.2% | 48px で 19.7% ／ 72px で 17.3% |

**差が 小さいからと いって 絵を 描きなおさないこと。**C2 は 配線だけ です。

高さは 6画面すべて 変わって いません（`#hud` 59.17 ／ `#panel` 83 ／
`#skillBar` 70.44 ／ `#ctrl` 46 ／ 盤面 288・492・540 ／ よこはみ出し 0）。
fps は タワー20体で **60**、78体（盤面ぜんぶ）で 31 ——
**変更前も 31〜32** なので 変わって いません。

### Phase 7-7-3-8-4-C3 ——進化の 絵を 出す 場所を **3つに 分けて 固定**（2026-09-09）

C1（非戦闘UI）・C2（盤面の 自分がわ）で「進化の 絵が ゲーム体験に 届かない」
いちばん 大きな 問題は 解けました。ここから **表示先を むやみに ふやさず**、
**いまの 仕様で 正しく 見せられる ところ**と **個体システムが できてから の ところ**を
分けて 固定します。**この 回は 調査と 設計だけ。コードも 絵も 1つも 変えて いません。**

#### いま つないで ある 8か所

| 表示先 | 何から evo を 引くか | resolver | キャッシュ | 大きさ | 複数所持で 作りなおすか |
|---|---|---|---|---|---|
| キャラ詳細 | **個体**（`detailIid` → `instEvoOf`）| `artKeyFor` ／ `evoArtKey` | なし（毎回 焼く）| 128px | **どの 個体を 見るか えらばせる**（読み口は そのまま）|
| 図鑑一覧カード | 種（`evoOfSpecies`）| 同上 | なし | 64px | **未決**（下の 表）|
| 仲間を選ぶ | 種 | `artKeyFor` | `genSpriteCache`（`id@px@evo`）| 56px | **iid 必須** |
| ショップバーの 札 | 種（`'tw_'+k`）| `evoArtKey`（TOWERS.art の 道）| なし | 46px | 選択中の 個体 |
| 選択中の 札 | 種（`T.gen.id`）| `artKeyFor` | `genSpriteCache` | 42px | 選択中の 個体 |
| 情報カード | 種（`T.gen.id` ／ `'tw_'+t.k`）| 両方 | `genSpriteCache` | 46 / 31px | **盤面の tower の iid** |
| 盤面の なかま | 種（`t.k`）| `artKeyFor` | `genSpriteCache` ／ `rbCache`（`g_id@px@evo`）| `CELL*0.99`〜（24〜72px）| **`towers[i].iid` 必須** |
| 盤面の お菓子タワー | 種（`'tw_'+t.k`）| `evoArtKey` | `rbCache`（`a_<解決ずみキー>@px`）| 同上 | 同上 |

**詳細だけが 個体から、のこり 7つは 種から**です。C1 で 決めた とおり
**ここを 入れかえないこと**（`check-evo-surfaces` が 見はって います）。

#### のこりの 表示先 ——A / B / C

**A ＝ いまの 1種＝1個体でも e1 を 出すべき**（プレイヤーが 持って いて、
いま 装備・所有して いる ものを 見せる 画面）。

| | いまの 呼びかた | 何が 出る | 足すときの 注意 |
|---|---|---|---|
| ワザバー | `charThumb({pid:ch}, 38)` | `ch_*` **7枚** | キャッシュを 通らないので いちばん 軽い |
| まもり神を えらぶ | `charThumb({pid}, 38)` | `lg_chocoknight` | 同上 |
| 応援パレード | `portraitOf(pid)` 44px | `ch_*` | **`pcache` が pid だけ**（下の §）|
| そらを とぶ 伝説（**持って いる ほう**）| `portraitOf(pid)` 44px | `lg_*` | 同上 |
| まもり神の 演出（`gdFly`）| `gdPortrait(pid)` 128px | `lg_*` | **`gdPcache` が pid だけ** |
| おかしの国 | `charThumb({pid}, s)` 40〜84px | `ch_*` / `lg_*` | **pid→種の 変換が すでに 中に ある**（`pid==='rpurin' ? 'sp_rpurin' : 'ch_'+pid`）|
| 伝説対決の **味方** | `drawGen(g, S, duS.ally)` | `side:'ch'` の GEN | 自分が えらんだ 子。3引数の まま |
| 記録シェア画像 | `drawPortrait(pid, 150)` | `ch_*` | canvas。**SNSに 出る ので 価値は 高い** |

**B ＝ 仕様を 決めて から**（下の §「取得の しゅんかん」）。

- たまごの 結果（`showEggResult`）
- ステージクリアの **新しい なかま**と **ごほうびの たまご**
- たいけつの ごほうびの たまご（`duEggs`）
- 伝説との 出会い（`legendArrival`）

**C ＝ わざと base の まま**。

| | なぜ |
|---|---|
| 盤面の てき（`drawEnemy`）| **世界の てき**で、プレイヤーの 個体では ない（C1・C2 で 固定）|
| **伝説対決の 相手**（`duBake` の `drawPortrait(duS.L.id,192)`）| `checkLegends` が `!hasDex` で しぼるので、**たたかう 相手は つねに 未所持**。構造的に base |
| でんせつの **黒い影**（`flyer.sil`）| 形を 見せない ことが 目的 |
| 図鑑の 未所持カード | 個体が 無いので `evoOfSpecies` が `null` を かえす ——**自動で 正しい** |
| アバター（`avThumb`）| 進化の 対象では ない |
| 表紙・ようこそ・OGP・`invite.html` | **焼きこみ画像**。そもそも キャラ描画を 通らない |

#### 取得の しゅんかんは **自動で e1 に しない**

たまご・ステージクリア・ごほうびは「**いま 手に 入れた**」画面です。
ここを ひとまとめに `evoOfSpecies` へ つなぐと **まちがえます**。
3つを 分けて 決めること。

| | いま おきて いること | どう 見せるのが 正しいか |
|---|---|---|
| **新規取得の 個体** | `gainSpecies` → `mintInst` で **evo なしの 個体**が できる | **base。**手に 入れた ばかりの 子が 進化ずみで 出るのは うそ |
| **既所持 species の 再取得**（たまごの ダブり）| 個体は ふえず **かけらに なる**（`eggDraw` の `dup:true`）| **e1 でよい。**「すでに いる子だった」と 言う 画面なので、いま 持って いる すがたを 見せる ほうが 正しい |
| **進化ずみの 既存個体を ごほうび演出で 見せる** | （いまは 無い）| e1 |

> **`evoOfSpecies` を 通せば base に なる、に たよっては いけません。**
> いまは 新規個体に evo が 無いので たまたま base に なりますが、
> `instOfSpecies()` は「その種の **どれか 1つ**」を かえします。
> 複数所持を 開けた とたん、**新しく 引いた 子の 演出に
> 前から 持って いる 進化ずみの 個体の 絵**が 出ます。
>
> だから 取得演出は **引いた 個体の `iid` を そのまま 受けとる**形に します
> （`eggDraw` / `gainSpecies` が `iid` を かえす）。**C3 では 実装しません。**

#### `pcache` / `gdPcache` ——**`pid` は 種の ID では ない**

| | キー | 大きさ | 影響する 表示先 |
|---|---|---|---|
| `pcache` | `pid` | 44px | 応援パレード ／ そらを とぶ 伝説 |
| `gdPcache` | `pid` | 128px | まもり神の 演出（`gdFly`）|

**`pid` から 種を 逆引きしないこと**（7-7-3-8-2 の きまり）。
`pid` は **DEX_ART の キー**で、種の ID とは 別の 名前空間です。

| pid | ほんとうの 種 |
|---|---|
| `purin` | **`ch_purin`**（精霊の こおりのつぶ）。GEN の プリンは `c_purin` で **別もの** |
| `rpurin` | **`sp_rpurin`**（`ch_rpurin` では ない）|
| `candytree` | `lg_candytree` |
| `tw_ice` | `tw_ice`（そのまま）|

だから **呼ぶ 側が 種を わたす**こと。`drawKingdom` は すでに
`pid === 'rpurin' ? 'sp_rpurin' : 'ch_' + pid` を 中に 持って いるので、
そこが そのまま 足がかりに なります。

##### 将来の キーは **`pid@evo`**。`iid@evo` に しないこと

```
いま        pcache[pid]
つなぐとき  pcache[pid + '@' + evo]      ← evo が null なら これまでと 同じ キー
```

**キャッシュが 知りたいのは「絵が どう 変わるか」だけ**です。
`iid` を キーに すると **同じ 見た目の 個体が べつべつに 焼かれて** むだに なります
（`genSpriteCache` が `id@px@evo` で `iid` を 入れて いないのと 同じ 理由）。

> **キーは `pid@evo`、引くのは `iid → evo`。**この 2つを 分けること。
> **C3 では 変更しません。**

#### 複数所持を 開けた ときの 引っこし表

C1 / C2 で **種から** 引いて いる 読み口を ぜんぶ ならべます。

| 読み口 | いま | 複数所持に なったら |
|---|---|---|
| **図鑑一覧カード** | 種 | **未決。**「その種の 代表 1体を 出す」のか「個体ごとに ならべる」のかを 先に 決めること |
| **仲間を選ぶ** | 種 | **iid 必須。**「どの 個体を 置くか」を えらぶ 画面に なる |
| ショップバーの 札 | 種（`tw_*` だけ）| 選択中の 個体 |
| 選択中の 札 | 種 | 選択中の 個体（`selIid`）|
| 情報カード | 種 | **盤面の tower の iid**（いま 見て いる 1体の すがた）|
| **盤面の tower** | 種（`t.k`）| **`towers[i].iid` ＋ `saveRun.tw[].iid` が 要る。**`evoOfSpecies(t.k)` は つかえなく なる |
| 詳細画面 | **個体**（`detailIid`）| そのまま。ただし 個体を えらぶ 口を 足す |
| （まだ base の A群）| — | 装備・選択の iid |

> **図鑑だけは 未決の まま のこします。**「種の 集合」として 見せる 画面なので、
> 個体を ならべると 図鑑の 意味が 変わります（「7-7 将来の 前提」の
> **`dex` は 種の 集合**の 話と 直結）。ここは 交換機能と 同時に 決めること。

#### 27枚の 実利用（2026-09-09 実測）

| 表示先 | 27枚の うち 出る 枚数 |
|---|---|
| キャラ詳細 128px | **27** |
| 図鑑一覧カード 64px | **27** |
| 仲間を選ぶ 56px | 3 |
| ショップバーの 札 46px | 1 |
| 選択中の 札 42px | 3 |
| 情報カード | 4 |
| 盤面の なかま | 3 |
| 盤面の お菓子タワー | 1 |
| **盤面に 出る（合計）** | **4** |

内わけ ——GEN 17（`side:'ch'` **3** ／ `side:'en'` **14**）・`ch_*` **7**・
`sp_*` 1・`lg_*` 1・`tw_*` 1。

| | |
|---|---|
| **ふつうに あそんで いて 見る 可能性が ある** | **27枚 ぜんぶ**（図鑑と 詳細で 全部 見える）|
| **盤面で 見える** | **4枚**（`c_purin` `c_tower` `c_witch` `tw_ice`）|
| **てき専用 ——自分の 盤面には ぜったいに 出ない** | **14枚** |

> **これは 欠点では ありません。**図鑑は この ゲームの 主役の 画面で、
> 27枚 ぜんぶが そこに 出ます。けれど **「盤面で 見える e1 は 4枚」**は、
> つぎに どの e1 を 作るかを 決める ときの **いちばん 効く 数字**です。
>
> - `side:'ch'` の GEN（60種）と `tw_*`（4種）は **盤面に 出る**
> - `ch_*`（12種）は **ワザバー**（A候補）を つなげば 出る
> - `side:'en'` の GEN（68種）は **図鑑と 詳細だけ**

#### これから 守る こと

- **てきに プレイヤーの 進化を 出さない**（`drawEnemy` は 2引数の まま）
- **取得の 演出を 自動で e1 に しない**。新規個体は base、ダブりは e1
- **複数所持を 開ける ときは、種から 引いて いる 7つの 読み口を ぜんぶ 見なおす**
- **`pcache` / `gdPcache` は `pid@evo`**。`iid` を キーに しない
- **表紙・ようこそ・OGP は 対象外**（焼きこみ画像）

### Phase 7-7-3-7 ——進化の 操作を つなぐ（**はじめて 遊ぶ人が 進化できる**）

キャラクターの 詳細画面 → たしかめ → `evolveInst` → **成功した ときだけ**
進化の秘薬を 1こ 消費。ここで はじめて「1個体が 永久に `e1` に なった」が
ゲームの 中で 成りたちます。**能力値には 1つも さわって いません。**

#### じゅんばんが すべて（`tryEvolve` 1か所）

```
function tryEvolve(o){
  if (!o || o.item || !hasDex(o.id)) return 'no';        // ① 所持キャラクターか
  if (gear[EVOLVE_ITEM] < 1) return 'poor';              // ② 秘薬（**個体を 作る 前**）
  let iid = instOfSpecies(o.id);                         // ③ ある 個体を つかう
  if (iid && instEvoOf(iid)) return 'done';              // ④ もう 進化ずみ
  if (!iid) iid = (ensureInst(o.id) || {}).id || null;   // ⑤ legacy の ときだけ 補う
  if (!iid) return 'no';
  if (!evolveInst(iid, EVO_FIRST)) return 'ng';          // ⑥ 書きこみ
  gear[EVOLVE_ITEM]--; saveGear();                       // ⑦⑧ 成功した ときだけ
  return 'ok';
}
```

**①②③④を すませてから ⑤**に 入るのが 大事です ——秘薬 0 で ボタンに
さわっただけ・やめた・未所持、の どれでも **legacy の 個体を 作っては
いけません**（見るだけで セーブが 変わるのと 同じ たぐいの 事故）。

**`evolveInst()` が false なのに 秘薬が へる 道を ぜったいに 作らない。**
だから へらすのは ⑥の あと **1か所だけ**です。`evolveInst()` には
`gear` も `ensureInst` も UI も 足しません（7-7-3-4 で 決めた writer の 責任）。

#### 出し分けは **`gear` の 在庫**。`visible` を 流用しない

`evolve` は `visible:false` で 通常の 入手経路も 0件 なので、
**ふつうに あそんで いる 人には 何も 出ません。**出るのは
`gear.evolve > 0` の 人だけ です。

> **公開の 軸（`visible`）を「進化できる」の 判定に つかっては いけません。**
> 公開と 所持は 別の 軸です（7-7-3-2 で 分けた ばかり）。

進化の 操作を 出す 条件は **所持ずみ ＋ 秘薬1こ以上 ＋ まだ 進化して いない**。
アイテム・未所持・まだ 会って いない 伝説には 出しません。
**絵の 系統（GEN / DEX_ART / TOWERS.art）は 条件に しません**
——6B で 3つ とも 受け口が そろったので、画像は 進化の 条件では ありません。

#### たしかめは **この画面の 中の 2段**。新しい モーダルは 作らない

既存の しくみを 調べた ところ、この ゲームに 確認ダイアログの 共通方式は
**ありません**（ショップは たしかめ なしで 買う）。ゆいいつ 近いのが
図鑑の カードの `buyWait`＝「もう一度タップで決定」です。

だから 同じ 考えかたで、詳細画面の 中に 2段で 出します。

| | |
|---|---|
| ふだん | `🌱 進化させる` ＋「進化の秘薬 ×N」|
| 1回 おすと | 「進化の秘薬を 1こ つかって、この仲間を 進化させますか？」＋ `はい` `やめる` |

**やめた ときは 何も 変わりません**（個体・evo・在庫・セーブ ぜんぶ）。
画面を ひらきなおすと たしかめは やりなおしです（`chConfirm` を 消す）。

#### 二重実行

進化は 不可逆なので、**連打しても 秘薬は 1こしか へりません**。守りは 2つ。

- 成功すると `buildCharDetail()` が 出しなおし、`evo` が 立って いるので
  **ボタンが そもそも 出なく なります**
- 直に 呼んでも `tryEvolve` の ④が `'done'` を かえします

非同期の 演出が まだ 無いので、**大きな ロックは 足して いません**。

#### 検査

```
node tools/check-evo-op.mjs      # 終了コード 0 で 合格（実行）
node tools/check-chars.mjs       # 「進化の 手つづき」の 行（ソース）
```

`check-evo-op.mjs` は **毎回 まっさらな ページから** 始めます
（進化は 不可逆なので 使い回せない）。見るのは 7つ。

| | 何を |
|---|---|
| legacy | ひらいた だけ→個体0／やめた→個体0・秘薬そのまま／確定→個体1・evo=e1・秘薬1→0・読みなおしても のこる |
| individual | 1回目 成功（秘薬2→1）／2回目 `'done'`（秘薬 そのまま）|
| 連打 | 「はい」を 3回 つづけて 押しても 秘薬は 1こだけ |
| 秘薬0 | ボタン なし。**直に 呼んでも 個体を 作らない**（`'poor'`）|
| 未所持 | ボタン なし・`'no'`・個体も 在庫も 動かない |
| アイテム | 詳細の 入口も 出ない・`'no'` |
| **canary** | `EVO_IDS` から `e1` を 外して `evolveInst` を かならず false に する →`'ng'` で **秘薬が 1つも へらない** |

canary は **本番の コードを 1文字も 変えません** ——集合から 1つ 抜くだけ です。

ソース検査は **じゅんばん**を 見ます ——在庫の 確認が `ensureInst` より 先か・
進化ずみの 確認が `ensureInst` より 先か・`gear--` が `evolveInst` より **あと**か・
へらす ところが 1か所か・戻り値で 守って いるか・`evolveInst` に `gear` や
`ensureInst` が 入って いないか・`tryEvolve` の 外から `evolveInst` を
呼んで いないか・出し分けに `visible` を つかって いないか。

5とおりで 落ちる ことを 確認ずみ ——`evolveInst` より 先に へらす／在庫の
確認より 先に `ensureInst`／`evolveInst` を 外から 呼ぶ／出し分けに `visible`／
`evolveInst` に `gear` を 入れる。

#### 「本番の caller は 0か所」を あてにした 検査は ここで 意味を うしなう

`check-inst-evo.mjs` には 7-7-3-4 の「本番から 呼ぶ ところは 0か所」が
入って いました。**7-7-3-7 が わざと それを 変えます。**落ちたら 回帰では なく、
検査の ほうを 新しい 前提（「`tryEvolve` の 中の 1か所だけ」）に 書きかえること。

#### 秘薬 0 の ふつうの セーブでは 前と 同じ

詳細画面の DOM を 6件 くらべて、**から の `<div id="chAct">` が 1つ ふえた
ほかは 完全一致**（`.cDet` を 足した ときと 同じ 形）。絵も 画素 完全一致。
6画面で よこはみ出し 0・「戻る」は 動かない・「はい」の タップ判定も 正しい。

### Phase 7-7-3-6B ——似顔絵（`charThumb`）の 系統にも 受け口を 作る

7-7-3-6 で つないだのは `drawGen` の 道（`GEN_LIST` の 127体）だけ でした。
6B で **似顔絵の 道**（精霊・ながれぼし・伝説・お菓子タワー）にも
受け口が できて、詳細画面は こう なりました。

```
art.append(o.gen ? genThumb(o.gen, 128, evo) : charThumb(o, 128, evo));
```

#### 絵の 系統は **3つ**。しっぽは **1つ**

調べた ところ、base の 絵の キーが 出る ところは 3つ ありました。

| | base の 出どころ | だれ | えがく 関数 |
|---|---|---|---|
| ① | `artKeyOf(rc)` | `GEN_LIST` の 127体 | `drawGen` |
| ② | `DEX_ART[id]` | 精霊・ながれぼし・**伝説** | `drawPortrait` |
| ③ | `TOWERS[k].art` | **お菓子タワー** | `drawPortrait` |

> **②と③は 同じ `drawPortrait` の 中の 別の 道**です。
> 精霊と 伝説は ②で **同じ 道**、お菓子タワーだけ ③の **別の 道**
> （`DEX_ART` に `tw_*` は 1つも 無く、`TOWERS[k].art` を ひく）。
> だから 検査は ②から 1件（伝説）・③から 1件（お菓子タワー）を ためします。

**`artKeyFor` を むりに つかい回しては いけません** ——あれは `rc`
（`GEN_LIST` の 記録）を とる ①専用で、②③は `rc` を 持ちません。

かわりに **しっぽ（「あれば variant、無ければ base」）だけ 共有**します。

```
function evoArtKey(base, evo){
  if (!base || !evo) return base;
  const k = base + '_' + evo;
  return ART_SPRITE[k] ? k : base;        // ← ある かは 実物で 見る
}
function artKeyFor(rc, evo){ return evoArtKey(artKeyOf(rc), evo); }   // ①
const ak = evoArtKey(DEX_ART[id], evo);                               // ②
const ta = evoArtKey(T.art, evo);                                     // ③
```

**3か所に 書かないこと。**いつか どれかだけ ふるまいが ずれます。
検査は `artKeyFor` が しっぽを 自分で 書いて いないか（`ART_SPRITE` を
直に 見て いないか）まで 見ます。

#### 生の `ART_SPRITE[T.art]` の のこりに 気をつける

お菓子タワーの 道は **2か所**で `T.art && ART_SPRITE[T.art]` を 見て
いました（はね・だいを 出すか の 判定と、絵を はる ところ）。
**片方だけ 直すと 見た目が こわれます**（はねが 出たり 消えたり する）。

検査は **`drawPortrait` の 中だけ**を 見ます。盤面（`drawTower`）にも
同じ 式が 4か所 ありますが、あちらは evo を わたさない 別の 関数なので
**そのままが 正しい**（ぜんぶ 禁止に すると 盤面まで 巻きこみます）。

#### キャッシュは 1つも 足して いません

似顔絵の キャッシュは `pcache`（44px・`portraitOf`）と
`gdPcache`（128px・まもり神）の 2つで、どちらも **呼び出し元が
evo を わたしません**。だから キーが かち合う ことは なく、
**従来の キーの まま**です。検査が「この 2つに evo が 入って いないか」を
見はります（evo の ために 新しい cache は 作らない）。

詳細画面の `charThumb` は **その場で 1枚 作る**だけで キャッシュを
通りません。`genSprite` / `rbSprite` は 7-7-2 の とおり
base と evo で キーが 分かれた ままです。

#### 本番は 見た目が 変わらない

`_e1` の 絵は **まだ 0件**なので、②③も base に fallback します。
詳細画面の 絵を `toDataURL` で くらべて、**gen・精霊・伝説・
お菓子タワー・未所持・legacy の 6件が 画素 完全一致**
（`e1` を 持たせた 場合も 同じ）。

検査の 中だけで 架空の `_e1` を 登録すると こう なります。

| | base | 架空asset あり | 消した あと |
|---|---|---|---|
| 伝説（②）| `candytree` | **`candytree_e1`** | `candytree` |
| お菓子タワー（③）| `candy` | **`candy_e1`** | `candy` |
| クマ（①）| `bear` | **`bear_e1`** | `bear` |

```
node tools/check-chdetail.mjs      # 3つの 道を 架空assetで
node tools/check-chars.mjs         # 「絵の resolver」「進化の 絵の caller」
```

7とおりで 落ちる ことを 確認ずみ ——resolver が `inst` を 直に 見る／
`charThumb` が `detailIid` を 呼ぶ／図鑑の カードから evo を わたす／
`e1` でも `_e1` を えらばない／base へ fallback しない／
base と evo の キャッシュが 衝突する／`artKeyFor` が しっぽを 自分で 書く。

### Phase 7-7-3-6 ——進化の 絵を **詳細画面だけ**に つなぐ

保存されて いる 個体の `evo` → 絵の resolver → 詳細画面の 表示。
**経路が ここで はじめて 一周 つながりました。**

つないだのは **詳細画面の 絵 1か所だけ**です。盤面・図鑑の カード・
ワザバー・ショップ・そのほかの キャラ描画には **evo を わたして いません**。

```
buildCharDetail(){
  const iid = detailIid(o);              // 個体を 決めるのは 画面がわ（7-7-3-5）
  const evo = iid ? instEvoOf(iid) : null;
  art.append(o.gen ? genThumb(o.gen, 128, evo) : charThumb(o, 128));
  …
  row('すがた', evo ? '進化ずみ' : 'まだ 進化して いません');   // ← 同じ 値
}
```

#### `iid` は **1回だけ** 引く

絵と「すがた」の 行が **おなじ `iid` から** 出て いる ことが 大事です。
2回 引くと、複数所持を 開けた とき **絵は 個体A・文は 個体B**に なりえます。
検査は `detailIid(` と `instEvoOf(` の 数が **どちらも 1回**である ことを 見ます。

#### わたすのは caller。**えがく ほうは のぞかない**

`artKeyFor` / `drawGen` / `genSprite` / `genThumb` / `drawPortrait` / `charThumb` の
中から `inst` / `instOfSpecies` / `instEvoOf` / **`detailIid`** を 呼びません
（7-7-2 の 責任分離）。`genThumb` は 3つめを `drawGen` の 4つめへ
**そのまま 流すだけ**です。

**わたさなければ base。**だから 図鑑の カード（`lazyThumb`）は
**2引数の まま＝1文字も 変わりません。**

#### `charThumb` の 系統は **まだ**（別フェーズ）

`o.gen` の カード（`GEN_LIST` の 127体）は `drawGen` → `artKeyFor` を 通ります。
いっぽう CHARS・お菓子タワー・伝説は `charThumb` → `drawPortrait` → **`DEX_ART[id]`**
の **別系統**で、`rc` を 持たないので `artKeyFor` が つかえません。
あちら用の receiver は べつの フェーズです ——**ここで 特別な えだを 作らない**
（7-7-2 の 申し送り どおり）。

#### 本番は 見た目が 変わらない ——`_e1` の 絵が 0件 だから

`ART_SPRITE` に `_e1` は 1件も ありません（`art/sprites/` にも ファイル 0件）。
だから `artKeyFor(rc,'e1')` は **base に fallback** します。

**証明は 盤面の 画素では なく「詳細画面の 絵 そのもの」で とりました。**
詳細画面の 絵は 粒子が 無く **完全に 決まった 絵**なので（同じ版を 2回 撮って
`toDataURL` が 1バイトも ちがわない）、盤面の ゆれの ような 統計が 要りません。

| | 変更前 ↔ 変更後 | 変更前 ↔ 変更後（個体に `e1` を 持たせて）|
|---|---|---|
| クマ（gen・個体あり）| **完全一致** | **完全一致** |
| こおりのつぶ（`{ sp }` だけ）| 完全一致 | 完全一致 |
| プリン（個体なし legacy）| 完全一致 | 完全一致 |
| クジラ（未所持）| 完全一致 | 完全一致 |
| キャンディ（お菓子タワー）| 完全一致 | 完全一致 |
| あめのきのせい（伝説）| 完全一致 | 完全一致 |

#### 架空の `_e1` で 切りかわる ことを たしかめる

本番に 絵が 0件 なので、**検査の 中だけで** `ART_SPRITE[base + '_e1']` に
その場で むらさきの canvas を 入れます（**ファイルは 1つも 作りません**）。

| | 絵の キー |
|---|---|
| evo なし | `bear` |
| `e1` ＋ 架空asset あり | **`bear_e1`** |
| 架空assetを 消す | `bear`（fallback）|

**まん中の 色は ぴったり `255,0,255` に なりません**（実測 `240,60,237`）
——`portraitShade` の つやと `drawGenFx` が あとから かかる ため。
**色あいで 見る**こと。おわったら `delete` して、のこって いない ことも 見ます。

キャッシュキーも `c_bear@64` と `c_bear@64@e1` に **分かれて いる**ことを
見ます（7-7-2 の receiver を そのまま つかって いる 証拠。
**新しい キャッシュも 詳細画面専用の えだも 作って いません**）。

#### 検査

```
node tools/check-chdetail.mjs      # 実行（架空assetで base ↔ evo）
node tools/check-chars.mjs         # 「進化の 絵の caller」を 足した
```

`check-chars` の 新しい 行が 見るのは 2つ ——**えがく 関数の 中から 個体を
のぞいて いないか**と、**`genThumb` に evo を わたす 本番の ところが
`buildCharDetail` 1か所だけ**か（図鑑の カードは 2引数の まま・
`ART_KEYS` に `_e1` が 入って いない ことも）。

> **窓の しっぽを `'}'` で さがさないこと。**途中の かっこに 当たって
> 窓が 短くなり、呼び出しを 窓の 外と 見て **「詳細画面の 外から
> わたして いる」と うそを 言いました。**中の さいごの 文
> （`row('すがた'`）で とめること。

7とおりで 落ちる ことを 確認ずみ ——`artKeyFor` が `instEvoOf` を 直接 呼ぶ／
`genThumb` が `detailIid` を 呼ぶ／図鑑の カードから evo を わたす／
`e1` でも `_e1` を えらばない／base へ fallback しない／
キャッシュキーが base と evo で 同じ／詳細で `ensureInst` を 呼ぶ。

#### まだ つないで いない もの

`evolveInst` の 本番 caller・`gear['evolve']` の 消費・進化ボタン・
たしかめの 画面・進化の 演出・ほんものの `_e1` の 絵。
**遊ぶ人は まだ 進化できません。**次の フェーズで
「進化ボタン ＋ 秘薬 1こ 消費 ＋ **成功した ときだけ** 消費」を つなぎます。

### Phase 7-7-3-5 ——キャラクターの 詳細画面（`#chOv`）**見せるだけ**

将来 **個体の origin・進化・基本形・複数個体・交換**の 受け皿に なる 画面です。
いまは 1種＝1個体 なので、**いまある ものを 安全に 見せる だけ**。

**進化ボタン・秘薬の 消費・進化の 絵は つないで いません。**
`evolveInst()` を 呼ぶ ところは 0か所、絵も base の まま。
`artKeyFor(..., instEvoOf(iid))` を 本番に つなぐのは **次の フェーズ**です
——「画面を 足した」と「進化で 絵が 変わる」を 別に 検証できるように する ため。

#### 入口は カードの 小さな ボタン。**既存の タップを 置きかえない**

図鑑の カードには すでに タップの はたらきが あります ——ワザを もつ
**13体**は「ワザバーに 貼る／外す」、在庫 0 の ときは
**2回タップで 買う**（`buyWait`）。ここを 詳細画面に すりかえると、
遊ぶ人が 貼れなく なります。

だから カードの すみに 小さな 入口（`.cDet`・23x23px）を 置き、
`stopPropagation()` で **カードの タップには 1つも さわりません**。

- 置くのは **左上**。右上は 📌（`.cPin`）が つかって います
  （実測：かさなり なし。`elementFromPoint` が `cDet` を かえす）
- `position:absolute` なので **カードの 高さを 1pxも 変えません**
  （`#colGrid` は `minmax(92px,1fr)` で、中みが 太ると 列が へる）
- 出すのは **キャラのカードだけ**。アイテムは キャラでは ないので 出さず、
  **まだ 会って いない 伝説にも 出しません**（わざと ？？？ で 見せて いるので、
  名前が わかると 台なし）

#### スクロールするのは 中みだけ

`#chOv` ぜんたいを スクロールわくに すると、下まで スクロールした とき
**「戻る」が 画面の 外へ 出て 押せなく なります**（まもり神の `#gdOv` と、
図鑑・ガイドの 見出しを sticky に した のと まったく 同じ 話）。
`#chBody` に `flex:1 1 auto; min-height:0; overflow-y:auto`。
**`min-height:0` を わすれないこと** ——ないと 中みの 高さで つっぱって
スクロールせず、下が 切れた ままに なります。

6画面で 実測：ページ幅の はみ出し 0・`#chOv` と `#chBody` の よこはみ出し 0・
いちばん下まで スクロールしても「戻る」は **1pxも 動かない**。

#### 個体を 決めるのは **画面がわ 1か所**（`detailIid`）

```
function detailIid(o){
  if (!o || o.item || !hasDex(o.id)) return null;
  return instOfSpecies(o.id);          // ← いまは 1種＝1個体
}
```

- **描画の 中で `inst` を 総あたりしない**こと。個体を 知って いるのは
  この画面（individual-aware な がわ）で、`artKeyFor` の ような resolver や
  共通の えがき関数は **わたされた ものだけ**を 見ます（7-7-2 の ならび）
- 将来 `MINT_MULTI` を 開けたら、**ここだけを「個体の 一覧・えらぶ」に
  差しかえます**（呼ぶ 側は 1行も 直らない）
- **`ensureInst()` を 呼んでは いけません。**見るだけで 個体が でき、
  セーブまで 変わります。未所持は `null`＝個体なし
- 読むのは かならず reader を とおす（`instOriginOf` / `instEvoOf`）。
  `inst[id].origin` を 画面から 直に 読まないこと

> `check-chars` の「instの 参照」を **2段に 分けました。**
> 生の 箱と 作る／書く もの（PLUMB）は 定義ブロック・`gainSpecies`・
> `detailIid`・検査どうぐ の 4つ だけ。読み口（READER）は それに くわえて
> **`buildCharDetail` からも 読めます。**分けるのが 大事なのは、
> **画面が 読める ように なっても「作る・書く」は 入って こない**ことを
> 見はりつづける ため です。

#### 出す もの

| 行 | 中み |
|---|---|
| 状態 | 仲間に います ／ まだ いません |
| とくちょう | `o.fx`（あれば）|
| 手に 入れた日 | `dexRec` の `w` と `d`（あれば）|
| あつめかた | 未所持の ときだけ（`o.hint`）|
| **入手元** | **個体が ある ときだけ**（`instOriginOf`）|
| **すがた** | **個体が ある ときだけ**（`instEvoOf`）|

#### `origin` は **この画面だけの 最小の 変換**（`ORIGIN_TX`）

`ORIGIN` は 言語に よらない 安定IDなので、`egg` `stage` を そのまま 見せると
読めません。かといって **大きな i18n の 辞書は まだ 作りません** ——
2言語に するのは ずっと あとの 話（⑩）なので、ここで 全部を まとめる 場所を
作ると かならず 中途半端な まま のこります。

**知らない 値は `null` を かえして 行ごと 出しません。**だから
`unknown`（古い セーブ・`{ sp }` だけの 個体）の ときは
「入手元」の 行が **そもそも 出ません** ——`不明` と 書くより 静かです。

#### `evo` は **内部IDを 見せない。恒久の 名前も まだ 決めない**

出すのは「進化ずみ」／「まだ 進化して いません」だけ。
**`e1 = 第1進化` の ような 日本語名を データに 焼かないこと** ——
名称の 体系が 決まって いないので、決める 前に 焼くと 直せなく なります。

#### 検査

```
node tools/check-chdetail.mjs      # 終了コード 0 で 合格
node tools/check-chars.mjs         # 「instの 参照」を 2段に した
```

7つの ケースを 見ます ——所持＋個体あり（origin あり）／所持＋`{ sp }` だけ
（origin なし＝行が 出ない）／所持だが 個体が ない（legacy＝個体の 行が 両方 出ない）／
未所持（絵が うすい・あつめかた が 出る）／お菓子タワー／検査用に `e1` を
入れた とき「進化ずみ」に なる／**内部IDが 画面に 出て いない**。

**見るだけで 何も 変わらない**ことも 見ます ——`inst` の 数・`instSeq`・
`sweetTD.inst`・`dex`・`gear`（セーブと 現物）。

既存の カード操作も **ほんとうに 押して** たしかめます ——在庫 0 の
1回目タップで「買いますか」／2回目で 買えて `×1 所持` に なる／
タップで 貼れて 📌 が 1つ ふえる／もう一度で 外れて 0 に もどる／
**詳細ボタンを 押しても 📌 も 在庫も 変わらない**。

> **どの子で ためすかを 決めうちに しないこと。**旗が 無いと `pins` に
> 4体 自動で ならび、引っこし処理が 在庫を 1こ 保証するので、
> 「まだ 貼って いなくて 在庫 0」の カードが 1つも 無く なります
> （じっさい 別の子を 押して しまいました）。`sweetTD.pins` `sweetTD.items`
> `sweetTD.itemsV2` を 先に 決めうちに して、**DOM から えらぶ**こと。
>
> **どの タブに 出るかも 決めうちに しないこと**（クマは 敵、ぷりんは 仲間）。
> 5つ 順に さがします。
>
> **貼った ときの 文では くらべられません。**`togglePin` が 'on' を かえす
> ときは `buildColGrid()` が ふだんの 案内に もどすので、文は 変わりません。
> **📌 の 数**で 見ること。
>
> **`elementFromPoint` は 見えて いない 点で `null` を かえします。**
> スクロールして 画面の 中へ 入れてから 測ること（測らないと
> 「タップできない」と 見まちがえます）。

6とおりで 落ちる ことを 確認ずみ ——`detailIid` が `ensureInst` を 呼ぶ／
画面が `inst[iid].origin` を 直に 読む／内部の evo ID を そのまま 見せる／
`stopPropagation()` を わすれる／カードの タップを 詳細に すりかえる／
詳細画面の 絵に evo を わたす。

#### 図鑑の DOM は `.cDet` ぶん だけ ふえた

前↔後で `.cDet` を **のぞいた** 図鑑の HTML は **完全に 一致**します
（足した ボタンぶんだけ 差が 出るのが 正しい）。
`colCount`・タブ5つの `N / M`・ガイド・ショップ・完成判定・`gear`・
drop pool・6画面の 寸法も ぜんぶ 同じ。fps 60・JSエラー 0。

### Phase 7-7-3-4 ——個体の 進化を 書く **ゆいいつの 口**

`instEvoOf()`（7-7-1 の 読み口）に 対する 書き口を 足しました。
**UI・アイテムの 消費・進化の 絵には まだ つないで いません。**

```
const EVO_IDS = new Set(['e1']);        // ゆるされた 進化IDの ゆいいつの あつまり

function evolveInst(id, evo){
  const r = inst[id];
  if (!r) return false;                 // ① その個体が ない
  if (!EVO_IDS.has(evo)) return false;  // ② ゆるされて いない 進化ID
  if (instEvoOf(id)) return false;      // ③ もう 進化して いる（不可逆）
  r.evo = evo;
  saveInst();
  return true;
}
```

#### する ことは 5つ だけ

① 個体が ある ② `evo` が ゆるされた 値 ③ まだ 進化して いない
④ `inst[id].evo` に 入れる ⑤ `saveInst()`

**つぎは 1つも 入れません。**呼ぶ 側の しごと です
——`gear` の 消費／「進化の秘薬」を 持って いるかの 確認／画面の 更新／演出／
`towers` さがし／種さがし（`instOfSpecies`）／`dex` への 書きこみ／
`t.rb` `t.rbx`（**run-local の 秘薬**。7-7-0 で 別システムと 決めた）／能力値。

> **混ぜると「成功した ときだけ 消費する」が 書けなく なります。**
> 「進化できたか」と「アイテムが あったか」が 1つの 関数の 中で からまるためです。
> だから かえすのは **真偽だけ**（`unlock` と 同じ 形）。

#### ゆるす 値は 集合で 持つ ——文字列なら 何でも 通さない

`evo` は **セーブに 直に 入ります**。打ちまちがいや よその 軸の 言葉が
まざると **あとから 取りのぞけません。**だから `EVO_IDS` 1か所で 決めます。

入れて よいのは **進化の 段階と 分かれみち だけ**（いずれ `e2` / `e2a` / `e2b`）。
`halloween` / `bday` / `event` / `limited` は **別の 軸**なので 入れません
——「★は 強さ。限定性を ★の 本数に 埋めない」と まったく 同じ 話です。
ふやすのは `EVO_IDS` の 1行だけ。

#### いまは `null → e1` だけ。2回目は ことわる

すでに 何か 入って いたら ことわります（**おなじ `e1` でも**）。
`e1 → e2` は **まだ 設計しません** ——もとに もどせるか・ねだん・分かれみちが
決まって いないので、ここに 通り道を 作ると **決める 前に つかわれます。**

#### 古い 個体も そのまま 進化できる

`{ sp, origin }`（7-6）も `{ sp }` だけ（7-3〜7-5）も、`evo` が 無いので
**base（`null`）として 読めて、そのまま `e1` へ 進めます**（実測）。
読みこみの 一括書きかえも `evo:'base'` の 書きこみも **1つも して いません。**
`origin` も こわしません（`{sp,origin,evo}` に **のばすだけ**）。

#### **`dex` を 見て いない ことは、この 形でしか 証明できない**

`instOriginOf`（7-6）・`instEvoOf`（7-7-1）と まったく 同じ 話です。
1種1個体の あいだは、`dex` を のぞいても 同じ こたえに 見えます。
見わけが つくのは **`dex` には 進化らしい 値が あって、個体には 無い**とき だけ。

```
dex['c_bear'] = { origin:'egg', evo:'e1' }      個体 = { sp:'c_bear', origin:'egg' }（evo 無し）
  正しい … instEvoOf(iid) → null ／ evolveInst(iid,'e1') → true
  のぞく … instEvoOf(iid) → 'e1' ／ evolveInst(iid,'e1') → **false**（もう 進化した 扱い）
```

だから canary は **読み口と 書き口の 両方**が 落ちます。
`dex` は 1文字も 書きかえて いない ことも 読みなおして 確かめます。

#### 検査

```
node tools/check-inst-evo.mjs      # 終了コード 0 で 合格
node tools/check-chars.mjs         # 「instの 参照」に evolveInst / EVO_IDS を 足した
```

見るのは ——存在する 個体＋`e1` → 成功／`instEvoOf` が `e1`／保存→読みなおしで
`e1`／2回目の `e1` は 失敗で 値も 不変／存在しない iid・空文字・`null`・引数なし・
`e2`・`birthday` は 失敗（しかも **値を 1つも 動かさない**）／`{ sp }` だけの
古い 個体も `e1` へ／`dex` 側に `evo` が あっても 見ない／
**本番から 呼ぶ ところが 0か所**。

8とおりで 落ちる ことを 確認ずみ ——`instEvoOf` が `dex` を のぞく／
writer が `gear` を 見る／`ITEM_BY_ID['evolve']` を 見る／`instOfSpecies` を
つかう／`rb` `rbx` を 書きかえる／本番（`buildSkillBar`）から 呼ぶ／
`EVO_IDS` に `halloween` を 入れる／`EVO_IDS` を 通さず 文字列なら 何でも 通す。

> **失敗の ときに「値が 動かない」ことまで 見ること。**`return false` を
> 書きわすれて 先に `r.evo = evo` を していても、**戻り値だけ 見て いると
> 気づけません**（画面には 何も 出ません）。だから `e2` や `birthday` を
> ためした あとに `instEvoOf` を もう一度 読んで います。

#### 本番の ふるまいは 1つも 変わって いない

呼ぶ ところが 0か所 なので、`evolve` の 在庫・ふつうの 画面・キャラの 見た目・
戦闘能力・`saveRun`・`towers` の 形は **ぜんぶ これまでどおり**です
（前↔後で 図鑑DOM・ガイド・ショップ・`gear`・drop pool・6画面の 寸法まで 完全一致）。

### Phase 7-7-3-3 ——育成アイテムを 1件 足す（軸は 4つに なった）

進化の秘薬（`evolve`）を **データとして だけ** 足しました。
**進化処理・UI・入手経路は 1つも つないで いません。**

```
{ id:'evolve', name:'進化の秘薬', sn:'しんか', ic:'🌱', col:'#a8e6a0', need:false,
  battle:false, visible:false, drop:false,
  desc:'仲間の すがたを 変える', note:'（じゅんび中）' },
```

#### 軸は 4つ。それぞれ 別の 関数が 決める

| 関数 | 属性 | 決めるもの | いつ 入れた |
|---|---|---|---|
| `isBattleItem` | `battle !== false` | 戦闘中に つかえるか | 7-7-3-1 |
| `isPublicItem` | `visible !== false` | 存在を 公開するか | 7-7-3-2 |
| **`isDropItem`** | **`drop !== false`** | **通常ドロップに 入るか** | **7-7-3-3** |
| （属性のみ）| `late` | 何ウェーブから 出るか | — |

3つとも **未指定＝これまでどおり**。既存7件には `battle:true` も
`visible:true` も `drop:true` も **1つも 足して いません**。

#### **`late` を「落とさせない」の 代用に しない**

`late` は「何ウェーブから 通常ドロップの 候補に 入るか」で、
**通常ドロップ そのものを 禁じる 軸では ありません。**大きい 数
（`late:999999`）で 代用すると、**いつか その ウェーブに 届いた とたん
出ます**。禁じたい ときは `drop:false` です。

`check-chars` は `late` が **5000 を こえたら 落とします**
——`tools/sim.mjs` が n=1..5000 を まわすので、そこを こえる `late` は
モデルからも 届かない ＝「ぜったい 出さない」の つもり だからです。

#### 写しの 配列は 作らない

`BATTLE_ITEMS` `PUBLIC_ITEMS` は 読みこみの ときの 写しですが、
**ドロップに `DROP_ITEMS` は 作りません。**pool は ウェーブごとに 変わるので、
`dropPoolAt(w)` が すでに その 役目を 果たして います。
`dropOK` の 中に `isDropItem` を 1つ 通すだけ です（もとを 2つに しない）。

#### `id` は 将来 変えない 英数字。`elixir` の ならびに しない

`sweetTD.gear` の **キーに なる**ので、あとから 変えると 在庫が 消えます。
既存の 秘薬（`elixir` / `elixir3`）は **戦闘の 消耗品**で、進化は 別システム
（7-7-0）なので、名まえを ならべません。

絵は **まだ 作りません。**`icKey` も `art` も 持たせないので、`dataIcon()` は
④の 絵文字に 落ちます。🌱 は **仮**です ——ほかの アイテムの 絵を さすと、
5D-2-0 の「🧪 と 同じ絵が 2つ ならぶ」を くりかえします。

#### 足した 結果（実測）

| | 前 | 後 |
|---|---|---|
| `ITEMS` | 7 | **8** |
| `ITEM_BY_ID` から 引ける／`gear` の 在庫 | 7 | **8** |
| `BATTLE_ITEMS`・ワザバー・💎の 2セット・`setKinds()` | 7 | **7** |
| `PUBLIC_ITEMS`・図鑑・ガイド | 7 | **7** |
| stage / battle の drop pool（W1〜W5000）| 7 | **7** |
| `colCount`・完成判定・国の にじ | | **不変** |

`__dbg.dropPool` だけは **前からの もの（`late` だけ）**なので、
`drop` の 軸を 見ず `evolve` を ふくみます。**意味を 変えない** ために
そのままに して ありますが、**ほんとうの pool は `stageDropPool` /
`battleDropPool`** です。

#### セーブは 引っこし不要

古い セーブには `evolve` の キーが ありません。`gear` は
**`for (const it of ITEMS) gear[it.id] = 0;` で 先に 0 を 敷いてから**
セーブを 重ねるので、無い キーは **そのまま 0** です。
一括の 書きかえは 1つも 要りません（実測ずみ）。

#### 検査

```
node tools/check-chars.mjs      # 「通常dropの 軸」の 行（ソース）
node tools/check-evolve.mjs     # 終了コード 0 で 合格（実行）
```

`check-evolve.mjs` は 8件目が **データ基盤には いて、3軸ぜんぶで 外れて いる**
ことを 見ます ——`ITEM_BY_ID`／`gear`／`BATTLE_ITEMS`／`PUBLIC_ITEMS`／
ドロップの しきり／図鑑／ガイド／ワザバー／`useItem`／ショップの「Nしゅるい」／
W1・200・250・**5000** の 両 pool／在庫の 保存と 読みなおし／
古い セーブでの 0 あつかい。

> **在庫 0 の まま ワザバーを 見ては いけません。**`gear[it.id] < 1` で
> どのみち 出ないので、**から の 配列を 見て 合格**に なります
> （じっさい なりかけました）。`__dbg.give(null, 3)` で **8件ぜんぶ
> 持たせてから** 見ること。

6とおりで 落ちる ことを 確認ずみ ——`battle:false` を 外す／`visible:false` を
外す／`drop:false` を 外す／`drop` 可否を `battle` で 代用／`visible` で 代用／
`late:999999` で 代用。

#### 「N件の まま」を あてにした 検査は 7-7-3-3 で 意味を うしなう

`check-public.mjs` には「`PUBLIC_ITEMS` ＝ `ITEMS`（7件・同順）」、
`check-drop.mjs` には「pool は `late` だけで 決まる」が 入って いました。
**7-7-3-3 が わざと それを 変えます。**落ちたら 回帰では なく、
検査の ほうを 新しい 前提に 書きかえること
——くらべる 相手を **生の `ITEMS` では なく しきりで 数えなおした もの**に、
pool の 期待値に **`drop` の 軸**を 足しました。

### ドロップの `late` は 1か所（`dropPoolAt`）

> **記録（2026-09-03）**：7-7-3-2 の 調査で 見つけた 既存不具合を
> 独立 bugfix で 直しました。

`late` を 見て いたのは **ステージクリアだけ**で、戦いの ドロップ
（`spawnGift`）は 見て いませんでした。だから CLAUDE.md に
「こだいの秘薬は `late:200`。ウェーブ200からの ドロップでしか 出ません」と
書いて あるのに、じっさいは **ウェーブ1から 敵が 落として いました。**
「坂では なく 階段に する」という かべの 設計と 正面から ぶつかります。

```
const dropOK     = (it, w) => gear[it.id] < ITEM_CAP && (!it.late || w >= it.late);
const dropPoolAt = w => ITEMS.filter(it => dropOK(it, w));
```

**新しい 属性は 1つも 作って いません。**`drop:false` も `battleDrop` も
`stageDrop` も 別の pool も 要りません ——**`late` の 適用もれ**だっただけ です。
2か所の 呼び出し口は どちらも `const pool = dropPoolAt(wave);` の 1行に なりました。

#### 目でも 実機でも 気づけない

絵にも 文にも 出ません。落ちる ものが 1つ 多いだけ なので、
**遊んで いても「そういう ものだ」と 思う**だけ です。
しかも `__dbg.dropPool` が **ステージクリアの 側だけ**を 写して いたので、
モデルも 検査も この もれを 見て いませんでした。

#### 検査の 口は 3つに 分ける

| | 何を 見るか |
|---|---|
| `__dbg.dropPool(w)` | **前からの もの。意味を 変えない**（`late` だけ・`gear` を 見ない）|
| `__dbg.stageDropPool(w)` | ステージクリアが つかう 共有の pool |
| `__dbg.battleDropPool(w)` | **ほんとうに `spawnGift` を 走らせて** 出た pool |

**戦いの ほうは 式を 写しては いけません。**写すと「検査だけ 直って 本体は
古いまま」に なります（`check-btn` が ボタンの 文言で さがして いて
空ぶりした のと 同じ 形）。

**でたらめに たよるのも だめ**です ——「たまたま 出なかった」で 落ちる
検査に なります。`Math.random` を 差しかえて、1回目＝落とすかの さいころ、
2回目＝pool の どれを えらぶか、として `i/N`（N＝`ITEMS` の 数）を
N回 まわすと、pool が それより 短くても **0〜pool.length-1 を もれなく、
しかも 順に** 通ります。だから **ならびまで くらべられます。**

```
node tools/check-chars.mjs      # 「ドロップの late」の 行（ソース）
node tools/check-drop.mjs       # 終了コード 0 で 合格（実行）
```

`check-drop.mjs` は W1 / 122 / 123 / 199 / 200 / 201 / 250 で
**ステージと 戦いが 同じ pool（中みも ならびも）**か、
**さかいめが その ウェーブ ちょうどから（`>=`）**かを 見ます。
さかいめは 検査の 中でだけ `late:123` の 架空アイテムを さしこんで、
W122 で 入らない・W123 で 入る ことで たしかめます
（本物の `index.html` は 1文字も 変えません）。

| | W1 | W199 | W200 | W201 | W250 |
|---|---|---|---|---|---|
| こだいの秘薬（`late:200`）| — | — | ✅ | ✅ | ✅ |

4とおりで 落ちる ことを 確認ずみ ——`spawnGift` から `late` を 外す
（＝元の バグに もどす）／`>=` を `>` に する／ステージ側だけ 直して
戦い側を もどす／戦い側だけ 別の しきい。**元の バグに もどした ときは、
W1 の 戦いだけ `elixir3` が まざる**ことが そのまま 出ます。

ソース検査は **`late` を 生で 見て いる 行が ふえて いないか**も 見ます。
ゆるすのは しきり じしんと `late:` の 定義と 検査どうぐだけ です。

## 遊びかたガイドは 数字を 書かない

ガイド（`buildHelp`）は **古くなりやすい 画面**です。じっさい「200ウェーブごとに
道が変わる」「げんきのバーは 盤面の上」「合言葉は メイン画面のボタン」と、
どれも 直したあとの 実物と ちがっていました。

**数字と 場所は 変数から 出すこと。**

| 書かない | かわりに |
|---|---|
| 200ウェーブ | `LAP_WAVES` |
| 20ウェーブ | `STAGE_WAVES` |
| 12の国 | `THEMES.length` |
| 127体 | `GEN_LIST.length` |
| 5% | `LEG_RATE*100` |
| ウェーブ60 | `LEGENDS[0].wave` |

**敵は タイプ名で 書かないこと。**盤面に 出るのは 国ごとの すがた
（パンダ・ライオン…）で、`ENEMIES` の 名前（どすんケーキ・あいすぼう）は
遊ぶ人には 見えません。「かたい子」「はやい子」のように **うごきで** 書きます。
`TOWER_TIPS` と `SKILLS` の `desc` も ガイドに そのまま 出るので 同じです。

## 日本語について

コード内のコメントは日本語で。「何をしているか」ではなく「なぜそうしているか」を書きます。

UIの日本語は**出す場所で書き分けます**。全部ひらがなだと大人には読みにくく、
幼児向けに見えてしまうためです（このゲームは子どもから大人までが対象）。

**原則：かな以外はすべて漢字・カタカナ。かなで残すのは名前と、キャラのセリフだけ。**

| どこ | どう書くか | 例 |
|---|---|---|
| メニュー・ボタン・数字・ガイド・ショップ・説明文 | **漢字／カタカナ** | 閉じる・強化・売る・攻撃・範囲・図鑑・仲間・敵・伝説・合言葉・着せかえ・コレクション |
| **アイテム名・キャラ名・世界の名前** | **かなのまま**（かわいさの核） | おほしさま・かけら・たまご・ぷりんぼう・ほしのクジラ・シュガーガーデン |
| ワザ名・キャラのセリフ・かけ声・お祝い | **かなのまま** | まもりのたて！・やったね！・おかしの国が…！ |

**さわってはいけない文字列**：`CODES` の合言葉（さくらまんかい など）は
データのキーです。表示を変えると、いままでの合言葉が通らなくなります。
